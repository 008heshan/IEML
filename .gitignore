# ============================ 不入库的东西 ============================
# 判据只有一条：**能不能由仓库里的东西重新生成**。能，就不入库。

# --- Rust / Tauri 构建产物（2026-09-15 实测：光 target/debug 就有 47 GB）---
/src-tauri/target/

# --- 前端依赖与产物 ---
/node_modules/
/dist/

# --- 运行期产物 ---
*.log
/tmp/
.vite/

# --- 编辑器 / 系统 ---
.vscode/
.idea/
Thumbs.db
Desktop.ini

# --- ★ 本地数据与密钥：**永远不许进仓库** ---
#   ms_client_id.txt / prefs.json / instances.json 都住在数据目录里
#   （默认 `D:\IEML`，不在仓库内）。这里再挡一道，防止有人把数据目录
#   拷进仓库后误提交。
ms_client_id.txt
prefs.json
instances.json
*.part
*.part.*

# --- 打包出来的安装程序不进仓库（体积大、且随时能重建）---
/src-tauri/target/release/bundle/

# --- ★ 更新签名私钥：绝不能进仓库（丢了就再也发不了更新）---
*.key
*.key.pub

# --- 图标瘦身前的原图备份（2.2 MB，只在本地留底，不入库）---
/src/assets/_original/

# --- ★ 开发记忆与外部参照：**不入库**（2026-09-25 公开化清理）---
#   判据有两条，都不是"体积"：
#   ① `.workbuddy/memory/` 是**本机开发过程**的记录，里面全是本机路径
#      （`C:\Users\Administrator`、`E:\IEML`、`D:\IEML`）与实例数据 —— 属于工作台，不是产品；
#   ② `.workbuddy/refs/` 里是 **PCL2 的反编译片段**（6 个 .xaml/.vb）。
#      PCL2 **不开源** —— 把别人的闭源源码放进 GPL-3.0 仓库是法律风险，不只是难看。
#   本地保留（移出跟踪用 `git rm -r --cached .workbuddy`，文件不会丢）。
/.workbuddy/
