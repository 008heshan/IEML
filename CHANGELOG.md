# 变更记录

> 版本号规则见 [`docs/VERSIONING.md`](docs/VERSIONING.md)。
> **每完成一轮就加一节**，不要事后补 —— 事后补的那次一定会漏掉最关键的细节。
> ★ **已经发布过的记录不许改写** —— 错了就新写一节更正它（见 dev.11 对 dev.10 的更正）。

---

## 0.1.0-rc.10 — 2026-09-26（第七十九轮：更新说明实时获取 + 游戏目录列表只留玩家选过的）

### 新增了

* 新增了「更新日志」页实时获取更新说明：进页面就刷新，右上角也能手动刷新。
* 新增了资源列表的显示方式切换：矩阵（一行多个）与条形（一行一个），在排序右边。

### 修复了

* 修复了「更新日志」页老是落后一版，看不到当前这一版改了什么。
* 修复了下载页整合包列表：换数据来源时，上一批结果还留在屏幕上，分不清哪个是哪个。
* 修复了 CurseForge 的整合包翻不动页 —— 上游给的总数没被读进来，界面以为只有一页。
* 修复了版本清单的分组：同一个版本号会出现两次，一列里看着很乱。
* 修复了游戏目录列表里会留下一些已经没有用的旧目录 ——
  以前每次启动都会把它"顺手记一下"，其中还包含跑测试时留下的临时目录。
* 修复了搜索框与下面那排结果贴得太近。
### 优化了

* 优化了更新说明的读取：能连上就用线上的，连不上就用启动器自带的那份 ——
  以前这一页只认自带的那份，所以"已经是最新版了，这一版改了什么"看不到。
* 优化了筛选与排序那一排的字号（11px → 13px，与下拉一致）与图标尺寸，
  并给翻页条上下留出余量 —— 小字与细图标在高分屏上看着毛躁。
* 优化了版本清单里「快照」那一档：试玩版现在跟着它自己的版本走，不再单独堆成一堆。
* 优化了 CurseForge 这一路的走法：**全部走国内镜像，key 从启动器里彻底移除** ——
  不需要填任何东西，也不会再去等官方接口（官方那条路在国内本来就时通时断）。
* 优化了 Modrinth 与其它在线接口的走法：**官方优先，慢或连不上就换镜像** ——
  以前只有"失败"才换，网络一卡就只能干等。
* 优化了发给上游的标识：按社区规范写成 `IEML-Launcher/版本号 + 仓库地址`，
  并修正了里面一个打不开的网址。

### 删除了

* 删除了游戏目录列表里"扫盘扫到的同款目录"这个来源：只显示玩家自己选过的那些。
* 删除了资源下载页那句"数据来自 …"，以及素材列表底下那行小字说明。

### 修改了

* 修改了「切换游戏目录」那张表：**只显示玩家自己选过的目录**（加上正在用的那个）。
  以前它还会扫一遍盘，把碰巧存在的 `<盘>\IEML` 也列出来 ——
  那些目录玩家从没选过，却和"选过的"长得一模一样。
* 修改了「关于」页里的源码地址：原来写的 `github.com/Heshan001/IEML` **是个不存在的地址**
  （账号名是 `008heshan`），点过去是 404；现在 CNB 与 GitHub 两个仓都写出来。

---

## 0.1.0-rc.9 — 2026-09-26（第七十八轮：代码仓与发布仓合并 + 公开化收尾）

用户：「**仓库强推送，github库转为公开，CNB 同，合并代码仓与发布仓，CNB 的代码仓直接作为分发仓**」。

### 修改了

* 修改了更新端点：`IEML-releases` → **代码仓 `IEML_Official/IEML`** ——
  两仓合并后，release 直接发在代码仓上（`publish-cnb.mjs` 的 `REPO` 常量是唯一改动点）。
* 修改了 CurseForge 的路由：**官方返回 401/403 时也换国内镜像，且换镜像时不带 key**。
  以前"有 key 就走官方"，于是一把"能过鉴权但读不到 mod 数据"的 key 会把**搜索整条路打死**
  （实测同一把 key：`categories` 200、`mods/search` 403）。
* 修改了几处把内置 `client_id` 说成「Prism Launcher 公开 id」的注释 —— 它是用户自己的
  Azure 应用注册（授权页显示那个注册的应用名）。

### 删除了

* 删除了仓库里的运行期产物：`debug.log`、`instances.json`、`prefs.json`（含账号 UUID）、
  探针留下的空目录、`tmp/`（26.3 MB，含一个重写前的仓库快照）。
* 删除了 `src-tauri/gen/`（Tauri 按 capabilities 生成的 ACL 清单与 schema，952 kB）
  —— 移出跟踪、改由构建生成（实测：动一下 `capabilities/` 就会重建，且与入库那份逐字节相同）。
* 删除了 iOS / Android 图标与 macOS 的 `.icns`（4.2 MB；只发 Windows）。
* 删除了 `.workbuddy/` 的跟踪（开发记忆 + 6 个 PCL2 反编译片段），本地保留。

### 修复了

* 修复了内置的 CurseForge key：它从初始提交起就在 git 历史里 —— 现已清空，
  并重写了**本地与两个远端**的历史（含 7 个带注解的标签）。
* 修复了「数据根目录要重启才生效」「437 项 Rust 测试」等一批与事实不符的说法（README / VERSIONING / DECISIONS）。
* 修复了 `DECISIONS.md` 里 7 个指向不存在标题的死锚点。

### 新增了

* 新增了 4 条防复发门禁（都进了 `verify`，项数 25 → 29）：
  敏感串扫描、文档锚点自检、README 与事实一致、仓库根布局白名单。
* 新增了探针 `tools/probe/probe-cf-mirror-keyless.mjs`：证明"不带任何 key 也能查 CurseForge"
  （5/5：搜索 / 文件 / 详情 / 分类 / 指纹反查）。

> ★ **桥接说明（重要）**：`latest.json` 的地址是**编在 exe 里**的，所以已经装了旧版的用户
> 只会去问**旧发布仓**。因此这一版必须**同时发到旧发布仓**（`publish-cnb.mjs --bridge`），
> 老客户端更新到 rc.9 之后才会开始读代码仓。顺序反了 = 老用户永远收不到更新。

### 补记（2026-09-26，同一天）：**旧发布仓退场**（没重新构建 exe，所以写在这里而不新开一节）

用户指出「**cnb 的两个库没有合并（发布库没有废除）**」—— 核对属实：端点虽然已经指向
代码仓，但旧仓还留着**完整的第二套发布面**（10 个 release + 22 个 tag）。

* 删除了旧仓里重复的 **10 个 release** 与 **20 个失效 tag**，只留 `latest` 与 `v0.1.0-rc.9`。
* 修改了桥清单：包地址从**代码仓**改成**旧仓自己的副本**，让桥**自足**；
  并把 rc.9 的包放回旧仓，作为"代码仓那个包坏了"时的退路。
* 修改了两个仓的简介与旧仓的 README（旧仓写明"这里剩下什么 / 什么时候能删"）。
* 归档了旧仓（只读），**并实测归档后匿名下载仍然全通** —— 那批老客户端不会因此断更新。
* 新增了探针 `tools/probe/probe-legacy-endpoint-bridge.mjs`（7 条判据）：
  桥断掉是**静默**的（老客户端只显示"检查更新失败"），所以这条判据必须能一条命令重跑。
* 新增了发布脚本的"**桥冻结**"闸门：桥清单是手改过的自足形态，拿脚本再刷一次就会把它
  改回"依赖代码仓"的单点 ⇒ 现在同版或更新版本再跑 `--bridge` 会直接报错，
  要重刷必须显式加 `--bridge-republish`。
* 新增了 `compareVersions`（跨阶段比较：`0.1.0-rc.9 < 0.1.0`、`rc.10 > rc.9`）供上面那道闸门判定。
* 修复了本地 git 残留：重写后**本机 `.git` 里还能取到重写前的历史**（一个不可达的旧 tag
  能拉出旧 48 个提交，含初始提交，全带那把已作废的 key）。已 `reflog expire` + `prune`
  ⇒ 旧对象取不到、全对象扫 key 前缀 0 命中、`.git` 34.5 → 17.5 MB（204 提交/7 标签照旧）。
* 删除了构建垃圾：`bundle/nsis` 里 rc.4~rc.8 六个历史安装包与 `%TEMP%` 测试残留，回收 186.6 MB。

> ★ 本轮**没有改代码、没有重建 exe**，所以不新开 `## ` 节 —— 按 `MEMORY.md` 里那条规矩
> （`--docs` 要求 CHANGELOG 最新一节标题 == 当前版本），补记写成 `### ` 挂在 rc.9 这一节里。

---

## 0.1.0-rc.8 — 2026-09-25（第七十七轮：版本列表**读当前文件夹**）

用户（两张 PCL2 截图，「版本选择」那张左边是文件夹列表、右边是分组后的版本）：

> 你看PCL，就是像换了个文件夹去读游戏版本，可以无缝切换

### ① 上一轮（rc.7）我理解错在哪

rc.7 改的是**呈现**：把缺文件的版本折进「错误的版本」。那是 PCL 列表的*形状*，
不是它的*数据来源*。PCL 的关键是：**列表就是"这个文件夹里的版本"**，
换文件夹 = 换一份游戏数据。而 IEML 列的是**账本里的实例** —— 账本住在启动器自己的家里
（ADR-七十二），与文件夹无关，所以用户"换到 E 盘却还看见 D 盘那几条"，看着就像没刷新。

★ 顺带记一条查法：这次是靠 `diag-what-root-is-used.mjs` 把**每个会暴露路径的面**都抓一遍
才定位准的 —— 结论是"界面每一页文字里 `D:\IEML` 出现 0 次、`machine_info` 也已经是 E:"，
于是问题不在"没刷新"，而在"列表的数据源本来就不是文件夹"。

### ② 改法

* 后端新增 **`folder_versions`**（`commands_real::scan_folder_versions`）：真读盘扫
  `<root>/.minecraft/versions/*`，每个目录给出 `dir / id / inherits / mc_version /
  loader_name / has_json` —— **不看账本、不看网络清单**。加载器只按目录名认
  （`fabric-loader-…` / `…-forge-…` / `neoforge` / `quilt` / …）：要精确到"哪个加载器的哪个版本"
  得再读一遍 JSON，而列表行本来就会显示盘上事实，不值得多做一次读盘；
* **「版本列表」的数据源换成它**：文件夹里的版本 = 列表里的行；账本实例只负责给某个版本
  补上"你起的名字 / 设置 / 存档目录"（按「MC 版本相同 + 是不是加载器版本也相同」一对一认领）；
  **认领不到的既不删、也不显示**（这一页就是"这个文件夹里有什么"，不属于它的东西不占位置 ——
  账本原样保留，目录换回去它们就又出现了）；文件夹里有、账本里没有的版本 →
  「还没建实例」+「用它建一个」就地建实例；页头常驻「N 个版本 · 文件夹 <路径>」
  （PCL 左侧那份"当前文件夹"）；
  ★ 顺带把 2026-09-23 那条"哪些实例的版本文件不在磁盘上"的体检也从这一页去掉了：
  在不在由**文件夹扫描**直接回答，再单独查一遍就是第二份判据（`instanceHealth` 命令还留着，
  后端与探针在用）；两条整幅的提示（rc.7 的「当前游戏目录里还没有版本」与
  「有版本的磁盘文件已经不在了」）也都去掉 —— 同一件事现在由列表的空状态说一次；
* **认 `.minecraft` 本身**（PCL 的「添加已有文件夹」就是这么选的）：新增纯函数
  `normalize_root_target` —— 选到 `.minecraft` 时自动取上一级（否则会在它下面再建一层
  `.minecraft`，版本永远是空的），并在返回里带 `normalized_from`，界面照实说
  "你选的是 .minecraft，已按它的上级目录当根目录"。

### ③ 真机验证（`tools/live/probe-versions-read-folder.mjs`，五条全绿）

造一个"PCL 文件夹"，三份版本，其中 `1.21.4` **账本里没有**：

```
① 换过去后列表 = ["Minecraft 26.2", "Minecraft 26.2 + Fabric 0.19.5", "1.21.4"]
   （第三行只存在于这个文件夹里 ⇒ 证明列表来自文件夹，不是账本）
② 账本里对不上的 1.12.2 → 「错误的版本（1）」折叠组，一条不丢
③ set_data_root(<沙盒>\.minecraft) → path=<沙盒>，normalized_from=<沙盒>\.minecraft
④ 从界面换回 D:\IEML → 列表当场变成 21 行（那个文件夹的版本），折叠组消失
⑤ 记账文件收尾 = D:\IEML
```

新增两条后端单测：`folder_versions_reads_the_folder_not_the_ledger`、
`dot_minecraft_target_is_lifted_one_level`。门禁 `node tools/verify.mjs` **25/25**、tsc 0。

### ③ 收尾：用户看了折叠组那行之后

「既然不在这个文件夹就不用显示了」⇒ 折叠组整个去掉，**不属于这个文件夹的条目直接不显示**
（账本一条都没删：把目录换回去，它们立刻又出现在列表里）。列表里只剩"这个文件夹里有什么"，
页头一直写着是哪个文件夹 —— 想找旧目录的东西，就切回旧目录。

### ④ ★ 同一个坑踩了两次

`folder_versions` 的返回是 **snake_case**（`has_json`），而前端类型承诺 camelCase（`hasJson`），
桥接层偷懒直接透传 ⇒ `v.hasJson` 是 `undefined` ⇒ 每份版本都被当成"没有 JSON" ⇒
**后端明明返回 3 个版本、界面上 0 行**（"读不到 ≠ 没有"这条老规矩的又一个变种）。
第二处是探针里把 `normalized_from` 写成了 `normalizedFrom`（红了一次才发现）。
教训：**跨 IPC 的字段名必须显式映射** —— 同一个文件里 `machineInfo` 一直是这么写的；
而"后端对、界面空"这种症状，第一件事就该去比对两边的字段名。

---

## 0.1.0-rc.7 — 2026-09-25（第七十六轮：版本列表按**当前游戏目录**说话）

用户（截图：换目录之后的「版本列表」页）：

> 换目录后不会自动刷新，我的建议是，点击版本列表就刷新一下，重新读取当前选择的游戏目录

### ① 现场是什么（先纠正一句"没刷新"）

截图那一页：3 个条目全在报错（「有版本的磁盘文件已经不在了 · 3 个」+「这些版本还没有游戏文件」），
而用户刚在设置页把游戏根目录换走 —— 看着像"页面没刷新"。

★ 事实是**那一页已经刷新过了**：那两句提示就是按**新根目录**当场体检出来的。
真正的问题是**这一页没有一句话说清"你选的这个目录里什么都没有"**：

* 版本列表列的是**账本里的实例**，而账本住在启动器自己的家里（ADR-七十二），
  **与游戏根目录无关** ⇒ 换到空目录后条目一个不少，只是全部缺文件；
* 于是"换目录前"与"换目录后"看到的是同一张列表 + 同一堆红字，谁也看不出目录换了。

### ② 改法（三条，都在用户指的那条路上）

* 后端 `machine_info` 新增 `version_count`：`<root>/.minecraft/versions` 下的目录数
  （读不到 = 0，不编数）。`reloadAfterRootChange()` 本来就会重读 `machine_info`，
  所以换目录那一刻这个数就跟着变；
* `VersionsPage` **挂载时 + 当前根目录一变**都调 `refreshInstances()` ——
  与"窗口重获焦点时刷新"**同一个函数**（2026-09-23 建的），不新开第二份；
  那条"版本文件还在不在"的体检也把 `currentRoot` 加进依赖。一句话：这一页的数据源只有一个；
* 最上面（排在"文件不在了"之前）把话说明白：
  「当前游戏目录里还没有版本 —— 现在用的是 `<路径>`，里面一个版本文件都没有；
  下面的条目引用的版本在别的目录里。想换回去：设置 → 存储 →「新建/切换…」」
  + 一个「去设置里换目录」按钮。

★ 为什么判据用**磁盘事实**（`version_count === 0`）而不是"实例是不是都缺文件"：
后者分不清"刚换到空目录"与"用户自己在资源管理器里把版本删了" —— 这两种要说不同的话
（后者已有专门那条 Note，且 2026-09-23 明确要求**不替用户删条目**）。

### ③ 真机 A/B（`probe-live-root-switch-ui.mjs` 新增第 ④ 条）

```
刚发布的 rc.6（没这个修复）：④ ✗（页面上没有那条提示）　⑤ ✗（machine_info 里没有 version_count）
rc.7：                       ④ ✓（提示在，且里面写着新路径）　⑤ ✓（版本数 22 > 0，回到 D:\IEML）
```

后端那条探针仍然 ①–⑤ 全 ✓（"换目录即时生效"没被这次改动碰坏）；
顺手给界面探针加了截图（`tmp/versions-empty-root.png`），给人眼核对那一页的样子。

### ④ 顺手查清的一件事：用户这次为什么把根目录指到了 `E:\IEML`

`E:\IEML` 正是**本仓库目录** —— 而「换一个游戏根目录」弹窗里按盘符给的候选就是
`<盘符>\IEML`，E 盘上恰好就是它。不是 bug，但很容易看岔。
已清掉那次运行在仓库里建的两个**空**目录（`E:\IEML\{.minecraft,instances}`），
并把记账文件恢复成 `D:\IEML`。
★ 教训：以后看到"仓库里冒出 `.minecraft`/`instances`"，先查 `%APPDATA%\IEML\datadir.txt`，
别先怀疑代码。

### ⑤ 门禁

```
node tools/verify.mjs        25 / 25
cargo test --lib             462 passed
cargo check --tests          0
tsc --noEmit                 0
```

---

## 0.1.0-rc.6 — 2026-09-25（第七十五轮：换游戏根目录**即时生效**）

用户：

> 我不想要重启才生效，切换游戏数据应该是实时的

### ① 以前为什么非得重启

`AppPaths` 是**启动时解析一次**的值，`AppState` 里存的就是它（每个命令写 `state.paths.xxx`）。
于是 `set_data_root` 只能"校验 + 把选择写进 `datadir.txt`"，然后告诉用户**重启后生效** ——
设置页那个「重启后生效」角标、弹窗底那句 `★ 换完重启才生效`，都不是文案保守，是**事实如此**。

### ② 改法：后端换句柄 + 前端刷派生数据

* `AppState.paths`：`AppPaths` → **`RwLock<Arc<AppPaths>>`**，配 `AppState::new` / `paths()` / `set_paths()`。
  `paths()` **克隆 Arc 出来用**（不跨 `await` 持锁 —— 这条是从以前那次"持锁等子进程锁"的死等里学来的）；
* `set_data_root`：校验 → `remember_root` → `AppPaths::from_root(target)` → `ensure()` / `ensure_own()`
  → **当场 `set_paths`** → 返回 `restart_required: false`；
* `own_root` **不变**（账本 / Java / 缓存 / 日志仍住在启动器自己的家里），所以换根目录**不搬任何启动器数据**；
  旧根一个字节都不动（语义仍是"只换地方，不搬东西"）；
* 前端新增 `reloadAfterRootChange()`：重读 `machine_info` + 实例清单（`machine/set` 新 reducer）+ `rescanJava`，
  成功后派一条 toast。**不重启、也不刷页面** —— 刷页面会把当前页 / 滚动位置 / 弹窗全丢掉，
  而这次切换只是一次数据源替换；
* 界面上"重启后生效"那一套整块删掉：`pendingRoot` 状态、设置页的角标、它的 CSS（`.droot-pending`），
  以及弹窗底那行 → `★ 换完立刻生效，不用重启`。
  ★ 最后这行是**复查时抓到的**：后端与状态都改了，脚注还留着老话 —— 用户点完会照旧以为要重启。

### ③ 真机 A/B（两条探针，都先证明能红）

| 探针 | 老构建 rc.5 | 新构建 |
|---|---|---|
| `tools/live/probe-live-root-switch.mjs`（后端 ①–⑤） | ②✗ ③✗（同一进程仍读旧根）④✓（只写记录）⑤空绿 | ①–⑤ 全 ✓ |
| `tools/live/probe-live-root-switch-ui.mjs`（界面 ①–⑤） | ②✗ ③✗ ④✗ ⑤✗ | ①–⑤ 全 ✓ |

* 后端 ③ 是"句柄真换了"的硬证据：新根下没有 `.minecraft/versions`，
  `instance_health` 立刻把 **3/3** 个实例标成"版本缺失"（老根下 0/3）；
* 界面 ③④ 是用户看得见的那一半：点「用这个」→ toast 说"即时生效"、弹窗自动关、
  设置页那行**当场**变新目录、切到版本列表就看到「有版本的磁盘文件已经不在了 · 3 个」；
  "没刷页面"用 `window` 标记证（事后还在 = 同一个文档）；
* ★ 界面判据**第一版写错了**（期望"实例没了"）：账本住在启动器自己的家里（ADR-七十二），
  换根目录不动它 —— 那 3 个条目还在，只是目录换成新根下的。红了一次才发现，已改正。

### ④ 顺手修的

* **门禁抓出真问题**：`cargo check --tests` 报 `tests/live_forge_processor.rs` 编译不过 ——
  `AppState.paths` 换类型之后，那个集成测试还在手写字面量（上次 `running` 改表时它也红过一次）。
  改走唯一构造口 `AppState::new(paths)`：以后字段再加，这里也不会漏。
  ★ 这条正是门禁里"Rust 全部测试目标可编译"存在的理由（`--lib` 看不见 `tests/*.rs`）；
* **探针两头擦记账文件**：老构建会把"换回来"当成「这就是当前的数据目录，没有变化」而拒绝落盘
  ⇒ 记录停在临时目录，**下次启动就真的按那个目录起来**（第一次红证就踩了）；
  现在开跑前摆正、结束后擦干净，并打印它擦了什么；
* **清掉开发期一次误选址留下的 `E:\IEML\{.minecraft,instances}`**：一次开发运行把记账文件指到了
  仓库目录，应用忠实地照着建了目录、还在账本被接管后抽了 61.7 MB natives 进去 —— 没有报错、没有提示。
  清理前逐个核对：那 63 个文件在 `D:\IEML` 里都有同名同大小的原件，`D:\IEML\instances.json` 里 0 个 `E:` 引用；
* **清掉搬家前留在游戏根目录的三坨重复件**（`D:\IEML\{cache,logs,java}`，合计 9.57 MB）：
  文件名与 `D:\IEML-launcher` 下那份逐个对上、各自最新时间都是 9/24 14:02（都在 A-4 搬家之前）。

### ⑤ 门禁

```
node tools/verify.mjs        25 / 25
cargo test --lib             462 passed
cargo check --tests          0
tsc --noEmit                 0
```

★ 如实记：`src/styles/pages.css` 本来就是**混着行尾符**的（HEAD 里 2969 CRLF + 190 LF + 5 处多余 CR）。
这一版只动 `droot-pending` 那一块（4+/10-），其余行尾符**原样保留** —— 没有顺手全文件统一
（先按"顺手统一"改过一版，190 行无关 diff 全冒出来了，退回去了）。

---

## 0.1.0-rc.5 — 2026-09-25（第七十四轮：最小化不再烧 CPU + 低性能开关不再改你的档位）

两件事都是用户报的，而且第二件是查第一件时顺手挖出来的。

### ① 最小化之后还在烧 CPU（用户截图：任务管理器里 IEML 与 WebView2 分成两摊）

用户的原话是「当 IEML 运行时，会分开显示，一个本体，一个渲染」。
"分成两摊"本身是 WebView2 的固有结构（一个浏览器进程 + 渲染器 + GPU + 工具进程，
身份由运行时定，宿主改不了；官方旗标清单里没有可用的旗标，Tauri 的 issue 也还开着）——
**但量它的时候发现了一个真缺陷**：

```
前台空闲（什么都不做）  单核 77%   562 MB
最小化之后              单核 37%   561 MB   ← 用户根本没在看，还在全速重绘
```

根因：前端本来有这套机制 —— `ui/glass.ts` 的 `.tab-hidden`（暂停所有 CSS 动画、
停掉 GL 的 rAF 循环、挡掉取色定时器），CSS 里也写着 `animation-play-state: paused !important`。
**但它只认 `document.hidden`，而 WebView2 在窗口最小化时并不翻这个标志**：

```
最小化之后前端看到的：{ hidden: false, state: "visible", hasFocus: false, rootClass: "" }
```

于是那套从来没被触发过（它只对"页面真的被隐藏"生效）。

修法：信号从**窗口**这一侧取，复用它原来那三件事，不另写一套。

* Rust（`lib.rs` 的 `setup`）：线程每 800 ms 看一次 `window.is_minimized()`，
  **状态变了才**发 `ieml:window-minimized`（平时零开销、不占主线程）；
* 前端（`ui/glass.ts`）：订阅它并进 `applyVisibility`（`hidden = document.hidden || minimized`），
  `dispose()` 里摘掉监听；浏览器开发模式没有这个事件时静默跳过。

真机判据：

```
修之前：最小化 37%（单核）
修之后：最小化  2%，且 rootClass = "tab-hidden"（证明暂停真的生效）
```

★ 如实记：前台空闲那 33%–77% 是"灵韵视效"本身的开销（用户自选档位），**没有动它**；
两个数之间差这么多是不同次采样的波动（期间还夹着应用的自动降档），不作为结论。

### ② 低性能损耗模式会**永久改掉**用户的档位（查内存时挖出来的）

【用户报：「先把这个内存查了」→ 我发现无论怎么把视效设成「灵动」，重启后 `prefs.json` 里都是 `mid`】

根因（`SettingsPage` 的那个开关）：打开它时会调 `vfx.choose('weak')` + `setMotion('lite')` ——
**两个都写盘**，把用户选的「灵动 / 灵韵」直接覆盖成降档值；
而"开之前的原值"只存在一个**内存 ref**（`lowPerfPrev`）里 ⇒

* 同一次会话里关掉还能还原；
* **重启之后 ref 是 null，关掉开关时什么都不还** —— 档位就永远停在「弱化 / 减少」。

这与那个开关自己的承诺相反（第五轮用户要求的是「关掉时**还回用户原来的选择**」）。

修法：**一个字节都不写盘**。开关只翻自己（`<html class="low-perf">` + `localStorage.ieml.lowPerf`），
降档完全走**派生**：

* 视效：`AppContext` 的 `vfxDowngradedByLowPerf`（顺手改正一处不一致 ——
  它原来只把 aura 压到 `mid`，而被删掉的 `choose('weak')` 是压到 `weak` 的；
  用户第五轮的原话是「弱化视效」，所以现在统一压到 `weak`）；
* 动效：同一个 effect 里 `applyMotion(lowPerf ? 'lite' : readMotion())` ——
  用户选什么就一直存在 `localStorage` 里，关掉开关自然回到它，重启也一样。

真机判据（`tools/live/probe-lowperf-keeps-user-level.mjs`，**先证明能红**）：

```
坏构建（rc.4 那份）：
  ② 开低性能模式：生效 weak/lite，**盘上也变成了 weak**   ✗（用户的选择被覆盖）
  ④ 重启后：开关还开着，但"用户选的"已经是 weak/lite；关掉它 → 仍是 weak/lite ✗（回不来）
好构建：
  ② 生效 weak/lite，**盘上仍是 aura/aura** ✓
  ④ 重启后"用户选的"仍是 aura/aura；关掉 → 回到 aura/aura ✓
```

### ③ 顺便查了内存（用户要求）——结论是**没有可省的空间**

| 项 | 实测 |
|---|---|
| 页面自己的 JS 堆 | **3 MB**（DOM 110–180 节点） |
| 我们的数据（3 个实例 + 缓存）带来的差 | **~14 MB** |
| 一个实例的全部进程 | 13 个 / 工作集约 **560 MB**（私有 ~520 MB） |
| 其中 GPU 进程 | 119–130 MB |
| 其中渲染器 ×2 | 112–122 MB |

* 换视效档（aura ↔ weak）：556 ↔ 557 MB —— **没差**（"镜头/GL"在 aura 与 mid 下都启用）；
* 开低性能损耗模式：内存 562 → 563 MB（**没差**），但**空闲 CPU 35% → 1%** ✓
  —— 这个开关省的是 CPU，不是内存；
* 结论：那 560 MB 是 Chromium/WebView2 的结构（我们这边只有 3 MB 堆 + 14 MB 数据），
  **没有可优化的空间**，也没有为了"看着小"去做无意义改造。

### 验证与产物

* `tsc` 干净；`cargo check` 干净；`node tools/verify.mjs` 25 项全过；
* 探针：`probe-lowperf-keeps-user-level` 4/4（红绿对照见上），rc.4 之后那批仍全绿；
* 本轮同时做了内部整理（探针公共库合并、CSS 死规则 29 条、构建废品 15.4 GB → 0.02 GB），
  细节见 ADR 七十五 / 七十六。

---

## 0.1.0-rc.4 — 2026-09-24（第七十三轮：实例目录回到游戏盘 + 启动器自己的家搬出系统盘）

rc.3 之后你连着报了四件事，另外点了两次头（"ABC 全做"、"发布 rc4"）。这一节按**你报的顺序**记，
每一条都写了根因与真机证据 —— 本轮没有一条是我自己找的问题。

### ① 「我在用 D 盘的目录，为什么版本列表给我定位到 C 盘的 instances 了」

判断是对的：**实例真的被读写到系统盘上去了**。真机事实（改之前）：

```
D:\IEML\instances          130 文件 / 122.8 MB（6 个版本目录，清单里三个实例都在这边）
%APPDATA%\IEML\instances    33 文件 /  30.9 MB（只有一个 fabric-262 空壳）
```

清单里的 `vanilla-262` / `vanilla-1122` 在 C 盘那份里**根本不存在**；
而当天 14:01 玩出来的 `options.txt` 落在 C 盘。

根因是 2026-09-23 那次「启动器自己的目录搬出游戏根目录」搬多了：

```rust
// src-tauri/src/platform.rs（改之前）
instances: own_root.join("instances"),   // ← 跟着 java/cache/logs 一起搬走了
```

`instances/{slug}/game/` 就是游戏的工作目录（`--gameDir`），里面是存档、Mod、配置 ——
那是**游戏数据**，必须跟着用户挑的游戏盘走（与 `.minecraft` 同级）；
搬走 `java/cache/logs` 才是对的。现在按**数据归属**分家：

| 东西 | 住哪 |
|---|---|
| `.minecraft`、`instances/` | **游戏根目录** `<root>` |
| `instances.json` / `prefs.json` / `ms_client_id.txt` / `cf_api_key.txt` | 启动器自己的家 |
| `java/`、`cache/`、`logs/` | 启动器自己的家 |

**同族的两处也一并修了**（都属于"读的不是那份活的"）：

* `instance_health`（「版本失联」提示）与 `instance_usage`（下载页"这个版本还有没有人在用"）
  仍在读 `paths.root.join("instances.json")` —— 也就是 **A-4 之前**的老位置。
  真机上那份从 09-24 01:25 起没再被写过，于是这两处一直拿陈旧清单算，读不到时还静默返回空。
  现在走 `own_file_for_read`（优先启动器自己的家、回退老位置）。
* `migrate_data_root` 每次启动都把 `java/cache/logs` 从老位置复制进**游戏根目录**：
  真机上 `D:\IEML\cache` 与 `%APPDATA%\IEML\cache` 是各 1839 个文件、9.53 MB 的**双份**。
  现在跨根搬家只剩 `instances`，反方向新增 `adopt_own_dirs`（游戏根目录里的启动器目录收养回家）。

★ **实例目录的补齐按 mtime 判胜负**（不是"目标优先"）—— 这条是量出来的：
`fabric-262/game/options.txt` 老位置那份是当天 14:01（`lang:zh_cn`、`renderDistance:12`），
游戏根那份是 09-12（`lang:en_us`、16）。按"目标优先"，用户换回游戏盘后
**今天玩出来的设置会被 12 天前那份盖掉**，而他只会以为是自己记错了。
现在源更新就赢，覆盖前把目标备份成 `<文件名>.ieml-bak`；`.minecraft` **不**按 mtime。

真机判据（`tools/live/probe-instance-root.mjs`，**故意用真实数据**、旧发布版 vs 新构建对照同一批实例）：

```
① 旧发布版 3/3 落在 C:\…\AppData\Roaming\IEML\instances（复现用户的报告，判据能红）✓
② 新构建   3/3 落在 D:\IEML\instances ✓
③ 旧版给出的路径里 vanilla-262 / vanilla-1122 磁盘上不存在；新构建三个都存在 ✓
④ 新构建这次启动没往游戏根目录写启动器文件（D:\IEML\cache 与 logs 无任何新文件）✓
⑤ 那两天写在老位置的设置跟着走了：新家 options.txt 的 lang=zh_cn，
   被盖掉的那份在 options.txt.ieml-bak（lang=en_us）✓
```

### ② 「版本列表读的还是其他盘的目录」—— 那次是**修复版没到他机器上**

用户回这一句时，机器上正在跑的是 `Desktop\IEML.exe`（20:50:44 启动，sha256 `A8238F38…191B`
= 19:15 的 rc.3 构建，**不含**本次修复）。他机器上有**两份**启动器，而且两份都是旧的：
双击桌面那份、以及桌面/开始菜单快捷方式指向的安装版（`%LOCALAPPDATA%\IEML\ieml.exe`）。

处置：两处都换成修复版（sha256 逐个核对），然后用**部署后那份 exe**、按用户的原动作
（版本列表 → 某一行 ⋯ →「打开目录」）再验一遍（`tools/live/probe-versions-page-dir.mjs`）：

```
版本列表 3 行 → 三个游戏目录全部落在 D:\IEML\instances\… 且都存在 ✓
「打开目录」的 toast = D:\IEML\instances\vanilla-262 ✓
资源管理器窗口的 LocationURL = file:///D:/IEML/instances/vanilla-262 ✓（命令真的把 D 盘目录交给了系统）
```

★★ **中间踩到一次探针自己造成的假红**（教训记在这里）：我事后又跑了一遍"旧版 vs 部署版"对照，
结果自相矛盾 —— 部署版 exe 的哈希明明是修复版，却报出 C 盘路径。根因在探针：
两个阶段**共用同一个 CDP 端口**，而启动器是单实例的 —— 旧进程没被彻底清干净时，
第二阶段起的那个新进程被单实例挡掉、悄悄退出，探针照样连得上那个端口（那是旧进程在服务）。
修法：每个阶段各用一个端口、起之前等进程真的消失、连不上就报「本次测量无效」并**非零退出**。
修完立刻复现了真相（那个新进程根本没起来）。

### ③ 「为什么还会显示 C 盘那个目录的游戏？」

这一问把上一轮的遗留翻了出来：C 盘上确实还有东西，但**已经不是列表的来源**。逐项量过之后
用户说「ABC 全做」，于是做了三件事（**每一项删之前都先核对"目标侧已有"**）：

| 删掉的 | 大小 | 删之前的核对 |
|---|---|---|
| `%APPDATA%\IEML\instances` | 30.7 MB | 独有文件 **0** 个（D 盘那份全有） |
| `%APPDATA%\IEML\shared`（旧 `.minecraft`） | **1079 MB** | 6754 个文件里 D 盘 `.minecraft` 缺 **0** 个；里面只有 assets/libraries/versions/launcher_profiles.json —— **没有存档、没有配置、没有资源包/Mod** |
| `%APPDATA%\IEML\cache` / `logs` / `java` | 9.6 MB | 已复制到新的家（1839 / 11 / 0 个文件） |

**C：启动器自己的家也搬出了系统盘。** 选址规则与数据根目录同一套判据：

```
① IEML_OWN_DIR（测试 / 绿色版显式覆盖）
② %APPDATA%\IEML\ownroot.txt 记录        ← 新增
③ 自动选址：空闲空间最大的非系统盘上的 IEML-launcher（本机 = D:\IEML-launcher）
④ 兜底：%APPDATA%\IEML
```

* 为什么必须有 ② 记录：③ 按"当前空闲空间"挑，而空闲空间每天都在变 ——
  没有记录的话 D/E 排序一变，启动器的家就自己搬家、账本看着就"丢了"。
  记录放在 `datadir.txt` 旁边，理由一样（记录本身必须是"与所选盘无关的固定位置"）。
* **顺序是硬要求**：`migrate_own_root` 必须跑在 `adopt_records` **之前** ——
  否则新家还空着时，`adopt_records` 会把游戏根目录里那份**陈旧**的 `instances.json`
  （真机上停在 09-24 01:25）当成"新位置没有"补进来，用户会看到四小时前的版本列表。
* 搬迁**只复制、绝不删源**；腾出系统盘空间是另一件事（这次用户点了头）。

结果：`%APPDATA%\IEML` 从 1.11 GB 降到 **2516 字节 / 5 个文件** ——
`datadir.txt`(8) + `ownroot.txt`(17) + `known-roots.json`(91) + `instances.json.bak`(1980) +
`prefs.json.bak`(420)。前三个是"必须留在固定位置"的记录（自举原因写在代码注释里），
后两个是万一的退路。

真机判据（`tools/live/probe-own-root-move.mjs`，跑部署后的 exe，六条全绿）：

```
① ownroot.txt = D:\IEML-launcher ✓
② 账本 + java/cache/logs 都在新家 ✓
③ 新家账本与界面互证（实例 3 条 = 版本列表 3 行；主题 daiqing = 界面上那个）✓
④ 界面照常（data_dir = D:\IEML）✓
⑤ 把当前偏好原样存回一次 → 新家 prefs.json 的 mtime 前进、C 盘那份根本不存在 ✓
⑥ 老位置只剩记录文件 ✓
```

★ 判据写错两次的教训：⑤ 第一版用"取版本清单会写 cache"当写入证据 —— 清单还在 TTL 内时
应用**什么都不写**，红的是"没写到 D 盘"而读起来像"C 盘还在被写"；改成"原样存回偏好"
这种与网络/TTL 无关的动作才稳定。② 第一版要求"账本四件都在"，可 `cf_api_key.txt`
本机从来没设过 —— 判据本身写错了，改成"老位置**有的**那几件都要在新家"。

### ④ 「点击左侧栏切换页面时，不会回到最顶端，而是继承上一页的位置」

内容区是**同一个** DOM 节点（`<main className="content">`，`overflow-y: auto`），
换页只把它里面的组件换掉 —— 而 `scrollTop` 挂在它自己身上，浏览器没理由替我们归零。

改法：`AppShell` 里加 ref + `useLayoutEffect`，换屏时 `scrollTop = 0`。
三个决定各有理由：

* 用 `useLayoutEffect` 而不是 `useEffect`：后者在浏览器画完之后才跑，
  新页面会先以"上一页那个位置"闪一帧再跳回顶部。
* 依赖 = "这一屏的身份"（一级页面 / 打开的版本 / 二级页面 / 下载页页签 / 资源安装页目标）；
  **同一屏内的操作不进依赖** —— 版本列表换筛选、设置页拨开关、搜索框打字都不该把人拽回顶部。
* 不做"返回时恢复原位置"：用户要的是回顶部；按屏记位置属于新功能，没做。

真机判据（`tools/live/probe-page-scroll-top.mjs`），★ 判据挑**长页 → 长页**：

```
为什么挑长页：目标页比源页短时，浏览器会自己把 scrollTop 夹到新页最大值（常常正好 0）——
             那样这条判据在坏构建上也会绿（第一版就踩了：设置 906 → 版本列表 直接 0）。
坏构建：① 设置(906) → 更新日志 实际 906 ✗   ② 更新日志(1610) → 设置 实际 906 ✗
好构建：① 0 ✓   ② 0 ✓
③ 实例「设置」(396) → 「概览」= 0 ✓（烟雾判据：概览页短，会被夹到 0）
④ 同一屏内拨开关 906 → 890 ✓（点靠边缘控件会被浏览器滚进视野几像素，不是归零）
⑤ 三个页面确实滚得动（906 / 1610 / 396）—— 防止 ①②③ 变成"永远为 0"的假绿
```

### ⑤ 更新日志本身：**说人话 + 固定五段**

用户在 rc.4 开发期间定下了更新日志的格式（`src/data/release-notes.ts`，那份是**给用户看的**，
与本节这份工程记录分开维护）：

```
新增了 / 修复了 / 优化了 / 删除了 / 修改了      ← 顺序即显示顺序，没有内容的段不写
```

三条硬规矩：**每条开头重复一遍类别词**（段标题写「修复了」，条目就写「修复了 …」）；
**言简意赅**、不写括号里的解释与好处；**不写假话**（没做的、只在某一台上做过一次的、
只是"不再复现"的都不许写成"已修复"）。段名进了类型（`NoteGroupTitle`），写错会被 `tsc` 拦住。

同时按这个格式**重编了 rc.1 / rc.2 / rc.3**（以及更早的 beta.53–beta.57），并把
「关于页更新按钮」那条从 rc.3 挪到了本节 —— 它是 rc.3 发布**之后**才修的，不属于 rc.3。

★ 顺带修掉一条**偶发**红的测试：两条测试共用了同一个临时目录 tag（`"adopt"`），
并行跑会互相 `remove_dir_all` 掉对方的种子目录（第一次全量跑就红在这里、单独跑却总过）。
判据红了先怀疑量法 —— 这条与上面那次探针假红是同一个教训。

### 验证与产物

* `cargo test --lib` **462 通过**（含本轮新增的 8 条：路径规则、mtime 判胜负、
  `adopt_own_dirs`、`migrate_own_root`、`first_usable`、`remember_own_root` 幂等、
  `open_dir` 落在游戏根目录、`norm_path` 归一）；
* `tsc` 干净；`node tools/verify.mjs` **24 项全过**（含发布版 exe 内嵌前端一致性）；
* 真机探针 4 条：`probe-instance-root`（5/5）、`probe-manifest-location`（3/3）、
  `probe-versions-page-dir`（5/5）、`probe-own-root-move`（6/6）、`probe-page-scroll-top`（5/5）；
* 用户机器上的部署：桌面 `IEML.exe` 与快捷方式指向的安装版都换成新构建
  （旧的四份备份留在仓库 `tmp/ieml-rc3*.exe.bak`）。

★ 如实记一件与产品无关的事：C 盘那 1.11 GB 是**用户点头之后我在他这台机器上手动删的**
（删前逐项核对"目标侧已有"）。应用本身只做**复制**、不删源 —— 别的机器上那份旧数据还在，
想清的话得自己删；这一条没有写进用户向的更新日志（那是假承诺）。

---

## 0.1.0-rc.3 — 2026-09-24（第七十二轮：你点出来的四处界面问题）

这一版**只改了四处界面上的事，没有新功能**（rc 阶段仍然冻结）。
四条全部来自你，原话就是下面的小标题 —— 没有一条是我自己找的。

### ① 下载页的标题栏不再被封面糊住

> 你：「**只有下载页的标题栏会被糊住，应该是图层问题**」

判断是对的，确实是图层问题。真机量出来：

```
.page-head   position: sticky, z-index: 20   ← 应该在上面
.top-glass   position: fixed,  z-index: 5    ← 应该在下面
实际绘制顺序：["top-glass", "page-title", "page-head", …]   ← 玻璃压在最上面
```

根因：全仓**只有下载页**多包了一层 `.page-fill` 外壳（其它页面的页头直接挂在内容区下），
而入场动画是加在"内容区的直接子元素"上的 —— 外壳一带动画，**它自己就成了层叠上下文**，
页头的 `z-index: 20` 被关在里面，跟外面的玻璃层（5）不再可比，于是玻璃反压到页头之上，
滚动时封面从页头下过，标题连同页头一起被糊掉。

修法：**外壳不动画，改让它里面的元素各自淡入** —— 视觉完全一样，层叠关系才对。
★ 顺带修好一处一直存在的不一致：以前下载页的页头**自己没有**入场动画。

### ② 实例概览页去掉了两个重复的启动键

> 你：「**这两个启动键都不要**」

同一个动作在界面上原本有三处入口（版本列表行菜单、实例概览页头、实例侧栏底部），
现在只留**版本列表**那一处：

* 去掉：概览页头那个「启动 / 停止游戏」；
* 去掉：实例侧栏底部那个「启动这个版本 / 停止游戏」。

★ **能力没删，只是入口去重**：版本列表那一行的 `⋯` 菜单里「启动」仍在
（探针里专门留了这条对照）；停止游戏的入口也还在 —— 启动页上的「停止游戏」，
以及侧栏底部那份"正在运行的版本"列表，每个都能单独停。

### ③ 资源下载页的「时代分类」放大一档

> 你：「资源下载里的**时代分类**都大一点，目前有点小有点费眼」」

| 元素 | 改前 | 改后 |
|---|---|---|
| 版本筛选项（`全部 472 / 26.3 3 / 1.21 168 …`） | 12px | **13px**（点选区域也放大到 6×13） |
| 分组标题（`26.3`） | 13px | **14px** |
| 分组标题里的版本号（等宽字体） | 13px | **14px**（另加一点字距：等宽同字号看着更小） |
| 分组右侧「3 个版本」 | 12px | **13px** |
| 顶部「X 的全部版本（472 个）」 | 12px | **13px** |

### ④ 「安装游戏」页的版本清单也放大同一档

> 你（截图 = 安装游戏页）：「**这个也要放大**」

| 元素 | 改前 | 改后 |
|---|---|---|
| 渠道筛选（正式版 / 快照 / 愚人节 / 全部） | 11px（最该看清的一排，原来最小） | **13px**，高度 23→27 |
| 世代标题（`26.3` / `26.2` …） | 12px | **13px** |
| 标题右侧「1 个」 | 12px | **13px** |
| 版本行里的版本号 | 13px | **14px** |
| 版本行下面那行小字（`2026-09-15 · 盘上有 Fabric 0.19.5`） | 11px | **12px** |
| 「点一行就进下一页…」那句提示 | 11px | **12px** |

★ 这两处字号改动都**只作用在资源/安装列表这一套类名上**，没有动全局徽标与其它页面
（判据里带了对照片：侧栏导航项字号不变）。

### 验证与产物

```
node tools/verify.mjs   24 项全过（含「取消语义」「崩溃规则两侧一致」两条新门禁）
cargo test --lib        454 通过
真机探针（都能重跑，见 tools/live/）：
  probe-no-launch-buttons   ① 概览页头只剩「安装 Mod」 ② 侧栏无「启动这个版本」
                            ③ （对照）版本列表行菜单里「启动」仍在
  probe-header-layer        绘制顺序：页头在玻璃之上；外壳不再带动画；入场动画没丢
  probe-res-groups-size     资源页五处字号（含"版本行不该变"的对照）
  probe-install-list-size   安装页六处字号（含"侧栏不该变"的对照）
```

★ 图层那条判据**先被证明能红**：把修复临时撤掉重建后，三条判据全红，而且红线里就是
`["top-glass", "page-title", …]` —— 玻璃压在最上面，与线上现象一致。

### 这一版**没有**包含什么

* 没有新功能；rc.2 里那些修复原样都在（rc.3 是它**之上**的小修，不用重装 rc.2）。
* 仍然没做的还是 rc.2 那节列的那些（CurseForge 整合包的自动安装、更新 Mod 删旧文件的
  端到端复现、更新日志页三处未复现的低项）。
* 一处如实说明：字号与图层这类改动，我只在本机量了**计算值 + 截图**，
  没有在别的分辨率、别的主题下逐个复查 —— 你在某个主题/窄窗口下看着别扭就告诉我。

---

## 0.1.0-rc.2 — 2026-09-24（第七十一轮：把界面上的假话清干净）

用户看完缺陷报告后说「**全权交由你来做**」，随后又定了这一版的两件事：
「**重写一遍更新日志的 rc1 更新报告，然后正式发布 rc1**」——
查证后 rc.1 其实**已经发布过**（线上端点自检：`version=0.1.0-rc.1`、Ed25519 验签通过），
而更新检查是按**版本号**比较的：再发一次 rc.1，已经装了 rc.1 的人**收不到更新**。
所以按用户确认，**升到 rc.2 发布**，并把这一轮的修复写成本节（rc.1 那节按规矩保留不动）。

### 一句话

这一版**没有加任何功能**（rc 冻结），只做一件事：
**把"界面说的"和"程序做的"对齐** —— 一共修掉 16 条已确认缺陷
（严重 4 条、高 4 条、中 7 条、以及 12 条一次性列清的廉价低项），并清掉一批死代码。

★ 顺带说一句关于 rc 流程本身的话：rc.1 的判据里写着「连续 2 个 rc 没有新 P0」，
而 rc.1 发出去之后的那一次系统排查**又挖出一个 P0（A-0）**——
这正是"观察期"该有的样子：**计数器归零，重来**。所以 rc.2 是这一轮观察的重新开始，
不是"rc.1 的补丁包"。完整清单在 `docs/BUG-REPORT-2026-09-24.md`，
每条的设计取舍在 `docs/DECISIONS.md`（ADR 六十二 ~ 七十）。

### 你会直接感觉到的变化

**① 所有删除/危险操作的确认框，以前是"摆设"**

这一条最严重：**14 处确认框根本没有生效**。
点「删除实例」「删除游戏根目录」「删除 Mod」时，弹窗会出现，但你点「取消」它照样删；
点「确认」……它其实什么都没等，直接就删了。根因是 Tauri 把 `window.confirm`
换成了一个 **Promise**，而代码写的是"如果没确认就返回" —— 一个 Promise 永远是"真"，
所以那道守卫从来没拦住过任何东西；再加上权限清单里**没有** `allow-confirm`，
每次调用其实都在后台报错，而界面把错误吞了。

现在换成启动器自己的弹窗（`src/ui/confirm.tsx`），**取消真的会停下**、
**确认才真的执行**，而且删除类操作的文案里把"删什么/不删什么"说清楚。

**② 实例设置里填的东西，重启后不会再丢**

「启动后自动进入服务器」「指定 Java」「实例级 JVM / 游戏参数」「窗口标题」
这些以前**存不进磁盘**：Rust 那份实例结构体比前端少 7 个字段，
而存盘要经过 Rust —— 每次保存都会把这 7 个字段悄悄丢掉。
※ 顺带修好了「端口写坏的地址」的提示：以前界面说"地址会原样传给游戏"，
实际是**端口被丢掉、游戏连默认的 25565**（可能落到另一台服务器上）。现在提示说的是真话。

**③ 版本列表里的「启动」以前点了没反应**

三个「启动」入口（版本列表行、实例概览、安装完成后的按钮）**全都是死的**：
事件的监听器装在"启动页"里，而按钮在别的页面上 —— 事件发出去时，现场一个监听者都没有。
现在监听器搬到常驻的界面外壳上，点「启动」会切到启动页、选中那一行，并真的把游戏拉起来。

**④ 删掉游戏根目录，不会再连实例清单和设置一起删掉**

`instances.json` / `prefs.json` / 登录用的 client_id / CurseForge Key
以前就住在**游戏根目录**里 —— 你在「候选盘」那页点删除，实例和设置会一起消失
（而确认框里一个字都没提）。现在这四个文件住在启动器自己的数据目录
（Windows 是 `%APPDATA%\IEML`），删游戏根目录**不碰它们**；
老用户升级时会把旧位置那几份**复制**过来（只复制、绝不删源，覆盖前还会留 `.bak`）。

**⑤ CurseForge 的版本列表以前是"假的"**

整合包页能切到 CurseForge 搜索，但**版本列表和下载走的还是 Modrinth**：
CF 命中的数字 id 拿去问 Modrinth，结果就是那句「这个整合包没有可下载的版本 /
上游没有给它发布任何文件」—— 把"我们问错了接口"说成了"上游没有"。
现在版本列表真的按来源走（CF 的包会列出 CF 上的真实版本）。
※ 但要说清楚：**CurseForge 的整合包还不能自动安装**（它用的是 `manifest.json`，
而自动安装目前只支持 Modrinth 的 `.mrpack`）—— 界面现在会**提前写明**并拒绝，
而不是给一个假的失败原因。这一步没做，不是网络问题。

**⑥ 其它（同属"界面说的和做的不一致"）**

| 以前 | 现在 |
|---|---|
| 断网点「检查更新」→ 关于页写「已是最新版本」 | 如实写「检查更新失败：<原因>」并标红；只有真的查过才说"已是最新" |
| Quilt 会**自动装 QFAPI**（用户 2026-09-15 明确说过不要） | 不再自动装；缺前置时由 Mod 管理页如实报出来 |
| 更新 Mod 后提示「已替换为新版本」，而旧 jar 一个都没删 | 真的删掉旧文件（**默认进系统回收站**，删错还能捞回来） |
| 「'mods 目录'按钮」打开的是实例根目录 | 打开真正的 `game\mods` |
| 「这些版本还没有游戏文件」是假警报（判据是内置的 10 个版本） | 改成**读盘**判断；装 1.21.4 之类不再误报 |
| Quilt 实例带 `fabric-api-…jar` 却被报「缺 Quilted Fabric API」并让你再装一个 | 两个源的前置包判据合成一处，不再引导你装第二份 API |
| Modular：「也可以把 .jar 直接拖进窗口」（**拖放根本没实现**） | 删掉这句承诺，改指真的存在的那条路 |
| 侧栏「最近玩过」永远不出现（依赖一个永远为空的字段） | 那个死块删掉了（游玩时长是你当初明确要求删掉的功能） |
| 整合包安装页「点哪一行装哪一行」其实装的是最新那个 | 点哪行装哪行（版本号当参数传，不再依赖界面状态时序） |
| 用户自己点取消，却弹红色「安装失败：任务被取消」 | 说「已取消安装，已下载的文件保留」 |
| 「创建副本」说"游戏文件共享、不多占空间"，实际整份复制 | 文案改成与实际一致 |
| 设置页写「候选盘会列出来」（那个功能不存在） | 改成真的存在的「会列出你用过的那些目录」 |
| 数据包装完不告诉你要放进世界、光影装完不说要 Iris | 后端本来就有的说明现在真的会弹出来（「还有一步」） |
| 「读不到版本列表」那半句"磁盘上的记录没有被清掉"从来没显示过 | 显示出来了（字段名不匹配，正文被丢掉） |

### 还有一件事：崩溃分析的两份规则表

崩溃规则在 Rust 与前端各有一份，**已经漂移了**：
36 条 vs 35 条、12 条的名字都不一样（`out-of-memory-heap` / `oom-heap` …）。
现在两份读**同一份判据表**（同一段日志，两边必须判出同一个结论），谁改一边不改另一边就会红。
※ 这份判据表立刻抓出一个真 bug：显卡驱动那条规则的写法**永远不会命中**
（真实崩溃日志里驱动名在下一行，而 `.` 不跨行）—— 两侧都修好了。

### 诚实清单（这一版**没有**做到的事）

* **CurseForge 的整合包不能自动安装**（见上）；CF 的**资源**（Mod/资源包/光影/数据包）这条路是通的。
* **「更新 Mod 会删掉旧文件」没走完端到端真机**：它需要一个"同一 Mod 有两个版本、
  版本对得上、且没被作者禁止第三方分发"的样本；我试过几次都卡在最后一步。
  验到的是：判据有单测（4 条）+ 删除动作复用已在用的"进回收站"接口。
* **这台机器上 `cdn.modrinth.com` 请求超时**（`api.modrinth.com` 在应用里可用）。
  所以"真的从 Modrinth 下 Mod/整合包"这一步在我的环境里走不完 ——
  这是**网络/线路**问题，不是启动器的：如果你那边也遇到，安装会如实报"网络请求失败"。
* 缺陷报告里那条「更新日志页三处与实现不符」**没复现**（找不到对应文字，当时也没留行号），
  所以这一版没动它。按仓库规矩，已发布的记录不改写；要改应当新写一节更正。
* 其余低项（历史更新日志的措辞、少量死导出）没动。

### 验证与产物

```
node tools/verify.mjs                 → 全部 24 项通过（新增「取消语义」「崩溃规则两侧一致」）
cargo test --lib                      → 454 通过 / 0 失败
真机探针（都能重跑，见 tools/live/）：
  probe-a0-fixed     确认框：会弹窗、点取消真的拦住、点确认才真删
  probe-a1-fixed     版本列表的「启动」：切页 + 选中那一行 + 真的把游戏跑起来
  probe-a4-fixed     删除整个游戏根目录之后，实例清单与设置都还在
  probe-b2-fixed     关于页的更新状态：还没查过 / 已是最新 / 失败（含原因，标红）
  probe-c1-fixed     来源切 CurseForge：资源 5 个版本、整合包 50 个版本、并如实说明不能自动装
  probe-c5-fixed     点第一行 / 第二行 → 装的确实是各自那一行
  probe-c18-fixed    「来自 CurseForge」/「来自 Modrinth」各说各的
  probe-bug-repro-9  C-3/C-4/C-6/C-7 四条一次跑完
```

升级提示与数据位置：
* 首次启动会把你**旧位置**的实例清单/设置/密钥**复制**到 `%APPDATA%\IEML`（只复制、不删源；
  覆盖前先 `.bak`）。游戏根目录里那几份**保留不动**，随时能退回旧版启动器。
* 游戏文件（`.minecraft`）、存档、Mod 都**没有动过**。

---

## 0.1.0-rc.1 — 2026-09-21（第七十轮：进入 rc —— 功能冻结）

用户两句话：「**3 这个，没问题**」（rc 判据第 ③ 条：他亲手验了 CurseForge 界面链与正版登录）
和「**现在就进 rc.1**」。所以这一轮**没有功能改动** —— 它记的是"进 rc 的五条判据逐条走完"，
以及第一次以 `rc` 的名义走完整条发布链路（升版 → 构建 → 部署 → 发布 → 验端点）。

### rc 是什么意思（写在这里，免得以后当成"又一个 beta"）

**功能冻结**：从这一版起只修 bug、不再加功能。要加的东西得先退回 `beta`
（`docs/VERSIONING.md` §3.3：`dev < alpha < beta < rc < 正式`）。
这不是仪式 —— 它换来的是"发布动作本身也被走过一遍"这件事有记录可查。

### 五条判据的现状（判据原文见 `docs/VERSIONING.md` §3.3）

| # | 判据 | 现状 |
|---|---|---|
| 1 | 没有"用户报过、还没定位"的问题 | ✅ 三条全部落地（见下两节） |
| 2 | 文档 / 注释 / 界面不许有假承诺 | ✅ beta.53 清过一轮（README / `bridge/tauri.ts` 注释 / CF key 三处说法）；beta.57 又把 ADR-058 与实现对齐（`installMode: quiet`、状态只有一份、`ready` 语义） |
| 3 | **用户亲手验关键链路**（CF 切来源 → 搜索 → 安装、正版登录） | ✅ 2026-09-21 用户亲手验过，报「没问题」。★ 来源是**用户报告**，不是我们自己的证据 —— 照实记 |
| 4 | 交付门禁 + 真机断言全绿 | ✅ 17 项 + 本轮一次完整的发布自检 |
| 5 | 连续 `N = 2` 个 rc 没有新 P0 | ⬜ **从这一版开始观察** —— 这就是 rc.1 存在的意义 |

### 判据 ① 的三条是怎么清掉的（两条销账、一条修掉）

1. **启动崩溃**（`0xC0000409`）：根因是 `eprintln!` 在管道断开时 panic，而 release 是
   `panic="abort"` —— 复现实验（关掉 stdout/stderr 读端）修前 3/3 崩、修后 3/3 存活，
   全仓库换成不 panic 的 `say!`（beta.53）。
2. **`1.20.1-forge-47.2.0` 报 7 个库缺失**：beta.53 已复核销账；这一轮又用真机证据钉死 ——
   跑扫**真实数据根**的那条测试（`real_installed_version_has_no_missing_libraries`），
   **20 个已装版本全部「缺失 0」**：

   ```
   1.20.1:              classpath 43 项 · natives 7 个 · 缺失 0
   1.20.1-forge-47.2.0: classpath 29 项 · natives 0 个 · 缺失 0
   rd-132211:           classpath  7 项 · natives 2 个 · 缺失 0    ← 曾经"只有 jar 没有 JSON"
   ```

   ★ 这条测试**必须带 `--nocapture` 跑**：它正是 dev.5 那次"假绿"的主角
   （写死旧路径 → 每次都跳过），不带就只能看到一行 `test result: ok`，
   看不见它扫了哪些版本、缺几个。**判据要能看见"它测了什么"，不只是"它绿了"。**
3. **下载尾部卡顿**：★ **记成"不再复现"，不记成"已修复"** —— 见下一节。

### 「不再复现」这条要单独说清楚

用户报过「下载引擎下载……最后一个文件，需要等待很长时间」，并强调这是**所有版本下载的共性**。
当时的处置是**刻意不改调度**（原话："没有证据就改调度，很容易把已经对的地方改坏"），
只补了一样原本没记下来的东西 —— **每一轮的耗时**：

```
[IEML/download] 各轮耗时：第0轮 41.2s · 第1轮 16.8s（合计 58.0s，3598 个文件）
```

2026-09-21 用户报**不再复现**，选择销账。★ 两者对未来的价值完全不同：
"已修复"意味着根因知道了，"不再复现"意味着**没有根因、它可能还会冒出来**。
好在它不是无影无踪 —— 复发时日志里 `各轮耗时` / `第 N 轮补下 M 个失败文件` /
`换 mcimirror 重试` 三条就能指出"最后那段时间被谁吃掉"。

### 验证

```
node tools/verify.mjs                    → 全部 17 项通过
工具链自检（tools/env/build-signed.ps1） → 公钥与 conf 内置的一致 ✓（不一致会导致"每个玩家都验签失败"）
构建耗时                                  → 6 分 34 秒（release 全量）
产物                                      → ieml.exe 8,944,640 字节
                                            IEML_0.1.0-rc.1_x64-setup.exe 3.27 MB
                                            + .sig 420 字节          ← 没有它等于更新通道断了
部署                                      → 桌面与构建产物**逐字节相同**
                                            sha256 D6EAB63D65B54CC8C2164537E1BBF388C8D5D662C17493BEE8851E4395D9BA82
verify-manifest（上传前）                 → 16 项全绿
verify-endpoint（上传后）                 → 线上 version=0.1.0-rc.1；匿名下包 3.27 MB；
                                            key id 8B66ACD053761632 匹配；Ed25519 验签通过
check-exe-wiring                          → 端点 / 公钥 / 插件 / ACL / installMode 五样都在 exe 里
```

★ **第一次跨阶段升版，所以单独验了 semver 排序**：`0.1.0-rc.1` 比 `0.1.0-beta.57` 大，
靠的是 semver 的**预发布标识符按 ASCII 比**（`"beta" < "rc"`）。之前所有发布都是同阶段内递增
（beta.56 → beta.57），**那条路走熟了不代表这条也通**。真机实测：自报 `0.1.0-beta.56`
的客户端开机 8 秒后自己出现角标，文案「**新版本 0.1.0-rc.1 · 重启并更新**」——
beta 的用户会收到 rc。（只验到"认出来"，**没有点**：这一轮不动本机装着的客户端。）

★★ **顺带补上了 beta.57 那节记的"没验到"**：那次写「下载中那一档（`新版本 vX · N%`）
在本机宽带下几秒就跳过去了，没留下证据」。这次轮询抓全了：

```
[8s]  角标：新版本 0.1.0-rc.1 · 下载中
[9s]  角标：新版本 0.1.0-rc.1 · 23%
[10s] 角标：新版本 0.1.0-rc.1 · 重启并更新
```

三档（下载中带百分比 / 重启并更新 / 重试下载）由此**都有真机证据**。
`重试下载`那一档仍未直接观察到（它只在下载失败时出现）—— 这条如实留着。

---

## 0.1.0-beta.57 — 2026-09-21（第六十九轮：热更新 + 静默安装）

用户：「**我想要热更新和静默安装**」。

### 之前是什么样（离"热"还差三件事）

更新链路在 beta.44 就打通了，但**只有一条手动路径**：设置 → 关于 →「检查启动器更新」
→ 发现新版本 → 「下载并安装」。三处不够：

1. **不查就不知道**：不点那个按钮，永远不知道有新版本；
2. **点完才开始下**：3.27 MB 得等它下完，用户盯着进度条；
3. **安装有界面**：当时的 `installMode` 是默认的 **`passive`** —— NSIS 会显示一条
   进度条窗口（不是向导，但也不是静默）。

### 现在

| | 之前 | 现在 |
|---|---|---|
| 什么时候查 | 用户点按钮 | **开机 8 秒后自动查一次**（`AUTO_CHECK_DELAY_MS`），失败**静默** |
| 什么时候下 | 点"下载并安装"才开始 | **查到就后台下** —— 用户看到角标时通常已经下好了 |
| 怎么知道 | 得进设置页 | **顶栏「新版本 vX」角标**（三种状态：下载中 / 重启并更新 / 重试下载） |
| 怎么装 | `passive`：一条进度条 | **`quiet`：NSIS 收 `/S`（完全无界面）** |
| 装完 | —— | 插件补 `/R`：**安装程序自己把启动器拉起来** |

★ **`quiet` → `/S` + `/R` 不是猜的**，是从插件源码的 `config.rs` 里读出来的：
`nsis_args()` 给 `Quiet → ["/S"]`、`Passive → ["/P"]`；`nsis_restart_after_install_args()`
给 `BasicUi → []`、其余 `["/R"]`。也就是说"装完自动回来"是插件白送的
（`restartAfterInstall` 默认 `true`），我们只需要把档位设成 quiet。
配置写在 `tauri.conf.json` 的 `plugins.updater.windows.installMode`。

### 状态只有一份（这条决定了代码放哪）

`useLauncherUpdate` 从设置页**提到 `AppContext`**：顶栏角标与设置页那一行看的是
同一件事 —— 各挂一份 hook 就是两份状态（一边"下载中 40%"、一边"发现新版本"），
而且会**各查一次、各下一次**。现在 `useApp().update` 是唯一那份。

`UpdatePhase` 多了一个 **`ready`**：包已经躺在本地，只等用户点「重启并更新」。
把 `download` 与 `install` **拆开**是"热"的关键 —— 合并成 `downloadAndInstall`
就只有"用户点了才开始下"这一条路。装失败时状态**退回 `ready`**（不是 `error`）：
包还在手上，按钮仍然是"重启并更新"，可以直接再试（不然等于让用户重新下一次）。

### 三处"不许骗人"的地方

* **自动查失败不留痕**：开机弹一条红色"检查更新失败"是噪音 —— 用户什么都没做。
  手动点的那次才报错（`checkNow({silent:true})` 是自动那条路）。
* **下载失败不许停在"下载中"**：退回 `available` 并带上原因。
  "看起来还在动"是最难被发现的一类假状态。
* **角标不可点时给理由**：下载中那枚是 `disabled` + `title`（"正在后台下载，下完就能一键更新"），
  不是默默点不动。

### 验证

真机端到端跑通（桌面版 + CDP 驱动界面 + 轮询进程），**判据落在 NSIS 实际收到的参数上**，
不是"我没看见窗口"。

**★ 先造一个"旧客户端"**：线上已经是 beta.57、装着的也是 beta.57 —— **自己比自己永远没有更新可更**。
于是把 debug 客户端**临时**编译成 `0.1.0-beta.56`（`set-version 0.1.0-beta.56` +
`tauri build --debug --no-bundle`），让它去更线上的 beta.57；验完已改回 beta.57 并重跑全量校验。

```
① 什么都不点，只等：约 8 秒后自动查 → 查到就后台下 → 顶栏角标自己变成
   「新版本 0.1.0-beta.57 · 重启并更新」      ← 自动查 + 自动下，全程没进设置页
② 点角标：客户端自己退出（exit code 0）
③ 随后 %LOCALAPPDATA%\IEML\ieml.exe 的 FileVersion 变成 0.1.0-beta.57   ← 真装了
④ 再等几秒，进程列表里出现**装着的那一份**在跑：
   ieml.exe :: "C:\Users\Administrator\AppData\Local\IEML\ieml.exe"     ← /R 把启动器拉回来了
⑤ 轮询抓到安装器进程自己的命令行（**这才是"静默"的直接证据**）：
   IEML-0.1.0-beta.57-installer.exe ::
     "…\Temp\IEML-0.1.0-beta.57-updater-zG0nUD\IEML-0.1.0-beta.57-installer.exe"
     /S /UPDATE /R /ARGS
   → /S 在（静默，无界面）、/P **不在**（不是被动进度条）、/R 在（装完重启）
```

★ **为什么不能只靠"我没看见窗口"**：没看见可能是"装得太快"、"窗口被别的东西挡住"、
"我根本没盯住那一秒"。`/S` 是**安装器自己收到的参数**，它骗不了人。
反过来 —— `check-exe-wiring.mjs` 里只探得到 `installMode` 这个**字段名**（它作为 serde
元数据存在，值未必是明文），所以**字符串探针证明不了档位**，这个案子只能由这条命令行结掉。

★ **本轮我自己的判据也犯过一次错**（记下来）：第一版脚本用 `/setup/i` 过滤安装器，
而它的进程名是 `IEML-0.1.0-beta.57-installer.exe` —— 过滤出 0 条，
于是**证据明明躺在日志里，脚本却报了 3 项失败**。改成 `/installer|setup/i` 后全绿。
教训：**判据不能比事实更窄**；"我这条 grep 没命中"与"这件事没发生"是两回事。

★ **没验到的，说清楚**：下载中那一档角标（`新版本 vX · N%`）在本机宽带下几秒就跳过去了，
没留下证据；无更新时角标**不出现**这条只能由"平时看不到它"间接说明（角标只在有更新时可达）；
**游戏运行中**点更新的后果没测 —— 现在没有"更新前先关游戏"的判断。

---

## 0.1.0-beta.56 — 2026-09-21（第六十八轮：靠窗口底部的「⋯」菜单被切掉）

用户一张截图（版本列表靠下的某一行点开「⋯」，菜单只看得见「打开设置 / 启动」两项，
后面几项在窗口外面）+ 两个字：「**修bug**」。

### 根因：位置是**写死**的，它从来没问过"下面还剩多少地方"

```ts
setMenuPos({ top: Math.round(r.bottom + 4), right: … });   // ← 老代码
```

菜单是 `position: fixed`（这一点是对的 —— 见下面那段"被列表容器裁掉"的老账），
但它的纵坐标**永远朝下**，而 `.row-menu` 既没有第二个方向（朝上翻），
也没有 `max-height` + 滚动。于是：

* 版本列表是**整屏的长列表**，靠底部那几行正是最常点的地方；
* 那里的菜单有一半在窗口外面，**「打开目录」「删除」永远点不到**。

★ 这与 `CustomSelect` 早就踩过、也早就修好的那个坑是**同一个**：
「面板朝上还是朝下要按容器剩多少空间决定」（那次是"java 选择方式的下拉栏被遮挡了"）。
`CustomSelect` 有 `measureDrop()`，而 `.row-menu` 是另一套手写的位置计算 —— 漏了。

### 改了什么

1. **先量高度，再定方向**：`useLayoutEffect` 在**浏览器绘制之前**量出菜单真实高度，
   然后按"下面放不下、上面更宽裕"翻上去（判据与 `measureDrop` 同一条）。
   ★ 用 `useLayoutEffect` 而不是 `useEffect`：后者在绘制之后跑，用户会看到菜单
   先出现在下面、再跳到上面。
   ★ 高度**量真实值**，不估算：条目数随版本类型变（原版那一行没有「Mod 管理」）。
2. **`max-height` 兜底 + `overflow-y: auto`**：上下都不够时菜单**自己滚**，
   而不是被裁掉 —— 只给 `max-height` 不给滚动，等于把它换个方式切掉。
3. **顺带**：窗口尺寸一变就关掉菜单（坐标是按"打开那一刻"的按钮位置算的，会过期）。

### 验证

**★ 第一版修法自己也有个 bug，是真机测出来的**（值得记）：

我先把位置算成"朝下 + `max-height` = 下面剩余"，再用 `useLayoutEffect` 量高度去判方向。
可那时菜单**已经被那个 `max-height` 截短了** —— `offsetHeight` 量到的是截短后的高度
（实测 **50px**，而它本来有 206px）。翻转判据拿到 50，就认为"下面放得下"，
于是**永远翻不上去**：菜单被压成一条 50px、只能滚着看的窄条。
改成量 **`scrollHeight`**（内容高度，不受 max-height 影响）才真的对了。

★ 教训与这一轮的主题同一个：**"量出来的数"要先问清"它是不是已经被我改过了"**。

真机实测（桌面版，CDP 读真实几何）：

```
① 最上面那一行（视口 1180×760）：菜单 top=234 ≥ 按钮 bottom=230 → 朝下
   整体在视口内（234~440），「删除」可见，不可滚
② 视口压到 1180×420、并排到底部那一行（按钮 top=334 / bottom=362）：
   下面只剩 50px，放不下 206px 的菜单 → **翻到按钮上方**
   菜单 124~330（bottom 330 ≤ 按钮 top 334），max-height=326px（上面剩的空间）
   「删除」可见、不需要滚；点空白处能关掉
   ★ 压矮视口是**故意制造"下面放不下"**：本机只有 5 个版本，
     最后一行离窗口底边还有 278px，那种情况下"朝下开"才是对的
```

---

## 0.1.0-beta.55 — 2026-09-21（第六十七轮：动效三档 —— 减少 / 适中 / 灵韵）

用户：「**我想要更好的更高级的动效，把动效选项设为三档：减少动效、适中动效、灵韵动效**」。

### 三档各自是什么

| 档位 | `data-motion` | 内容 |
|---|---|---|
| 减少动效 | `lite` | **一个动画都不放**：状态直接切换、不做过渡（无障碍那一档，与系统"减少动态效果"同义） |
| 适中动效 | `mid` | **默认**。页面淡入 + 弹窗/提示条入场 + 按钮与卡片的悬停反馈（原来散在各处的那些） |
| 灵韵动效 | `aura` | 在适中之上再加：背景光晕缓慢漂移、正文抬起、卡片错峰入场、悬停辉光、启动按钮呼吸 |

**灵韵具体多出来的六件**（都写在 `app.css` 的「动效档位」一节，逐条有注释）：

1. **页面入场**：淡入 + 正文比标题晚 60ms 抬起来（`translateY(10px)`）。
   ★★ 挂的是 **`.content > *`**，不是 `.page-fill` —— 这条是**真机打脸打出来的**：
   第一版挂在 `.page-fill` 上，真机一测发现"只有下载页有入场动画"，
   因为**全仓只有下载页渲染 `.page-fill`**（其余页面返回片段
   `<><div className="page-head">…</>`，直接挂在 `.content` 下面）。
   而这种"部分生效"看起来还挺正常，不测就发现不了。
   `.content` 自己**不能挂**（切页时它不重挂，动画只会放一次）。
   ★ 另外 `:not(.page-head)`：页头是 sticky 的，抬它会牵动吸顶那次布局。
2. **弹窗 / 提示条**：末端**过冲一点点**（`cubic-bezier(.2,1.2,.4,1)`，1.2 > 1）。
   幅度刻意小 —— 过冲大了就成卡通。
3. **按钮**：悬停抬 1px、按下缩到 0.975、主按钮悬停时**同色辉光**（不是黑影）。
4. **可点卡片**（`.pack-card` / `.res-card`）：悬停抬 3px + 同色描边 + 更深的影。
5. **背景三团光晕缓慢漂移**：46 秒一轮、位移只有 1~2%。
   ★ 目标是让静态界面**不像一张图**，不是让人盯着背景看 —— 位移再大一点就会在
   文字后面晃，那是干扰不是高级。
6. **启动按钮呼吸**：3.6 秒一次的外发光强弱（悬停时让位给 hover 那套，两层发光不打架）。

★ **卡片的错峰入场**只给**前 7 张**逐个加延迟（第 8 张起统一 245ms）：
第 40 张要等 1.4 秒才出现，那就不是"高级"是"卡"。版本列表那种 900 行的长列表
**一条延迟都不加** —— 几百个元素同时做 transform 是实打实的掉帧，而它们也没有
"逐个登场"的叙事需要。

### 顺手还了一笔旧账：原来的「减少动效」**重启就失效**

老实现是 `prefs.json` 里的布尔 `reducedMotion`，设置页改它只做一次
`classList.toggle('reduce-motion')` —— **启动时从来没把它应用过**。
也就是说：开了减少动效，当次有效，重启回来动画照旧，而开关还显示"开着"。

现在：档位存 `localStorage['ieml.motion']`，`main.tsx` **在首屏之前**挂
`<html data-motion>`（灵韵有入场动画，等 React 挂载再切档会让用户先看一遍满血动效），
并在 boot 时把老字段迁移过来（只有"本机还没选过档位"时才采纳它）。

### 判据只有一份：`src/ui/motion.ts`

* **合法值只在那里定义**（三档 + 界面文案 + 每档的说明）。前端任何地方要读写档位都走它；
  读到不认识的值（老版本、手改 localStorage、坏数据）**落回 `mid`** —— 不猜、也不崩。
* **系统优先**：`prefers-reduced-motion: reduce` 是用户在操作系统里表达的"我晕动效"，
  它**盖过**这三档。设置页据此把这一行**禁用**并写明原因（"Windows 里开着
  「减少动态效果」…这里选什么都不会动"），而不是让用户改了没反应；
  系统设置变了那一行实时跟着变（监听 `matchMedia`）。
* **本机偏好，存 localStorage**（不是 `prefs.json`）：与 `ieml.lowPerf` 同一条理由 ——
  同一份配置在弱机上想要"减少"、在台机上想要"灵韵"。
* **两个开关的交叉影响要说出来**：低性能损耗模式会关掉背景光晕那一层，所以
  "灵韵 + 低性能损耗"时那一行会提示"光晕漂移看不到，其余动效照常"。

### 验证

```
pnpm exec tsc --noEmit   → 零错误
node tools/verify.mjs    → 全部 17 项通过
```

桌面真机（CDP 读真实 DOM + **计算样式**，不是"源码里有这段 CSS"）：

```
① 灵韵（localStorage → 重载后仍生效）
   <html data-motion>=aura
   背景光晕   animation=ieml-aura-drift 46s
   .content 的直接子元素  animation=ieml-rise 0.3s
   卡片（20 张）animation=ieml-card-in 0.28s，延迟 0s / 0.035s / 0.07s / 0.105s…
   启动按钮   animation=ieml-breathe 3.6s
② 适中：页面淡入 0.16s；光晕不漂、按钮不呼吸（灵韵那几件一个都没有）
③ 减少：子元素与按钮的 animation-duration 都是 1e-05s（= 0.01ms，等于不动）
④ 设置页「外观 → 动效」：三个选项都在、当前档高亮、下面写着这一档是什么；
   点「灵韵动效」→ <html data-motion> 立刻变 aura、localStorage 也写进去了
⑤ 系统 prefers-reduced-motion: reduce（CDP 模拟）：三个选项全部禁用，
   并出现「被系统设置覆盖」+ "Windows 里开着「减少动态效果」…这里选什么都不会动"
```

已发布并验端点（线上 `0.1.0-beta.55` == 本地、匿名下包 + 内置公钥验签通过）。

---

## 0.1.0-beta.54 — 2026-09-20（第六十六轮：切换列表改成 PCL 那种「文件夹列表」）

用户给了 PCL 的截图（两张卡片：「当前文件夹 / `D:\Minecraft\.minecraft\`」、
「测试目录 / `D:\测试目录\.minecraft\`」）+ 一句：「**这个切换列表我想要 PCL 这样的**」。

### 我上一版错在哪

beta.51 我做的是**盘符列表**（`D:\` / `E:\` / `C:\` + 主张建的 `<盘>\IEML`）。
它回答的是"机器上能往哪写"，而用户要的是"**回到我去过的那个地方**" ——
盘符列表每次都要人重新想一遍放哪，而且用过一次的目录它**根本不记得**。
PCL 的「文件夹列表」列的是**用过的游戏文件夹**，一行一个，点一下就回去。

### 现在是什么

`DataRootPicker` 的「切换」区改成**文件夹列表**：

| 一行里有什么 | 说明 |
|---|---|
| 名字 | 路径最后一段（`D:\测试目录` → `测试目录`），盘根目录就用盘符 |
| 角标 | 「正在用」/「找不到这个目录」/「系统盘」 |
| 路径 | mono + 截断（完整值在 `title` 上） |
| 按钮 | 「用这个」；**目录已经不在的给「移除」**（点"用这个"只会得到一句后端报错） |

列表来源（`platform::list_known_roots`）：

1. **现在正在用的那个**（排最前 —— PCL 截图里也是"当前文件夹"在最上面）；
2. **用过的**：`%APPDATA%\IEML\known-roots.json`（新→旧，最多 12 条）；
3. **盘上扫到的同款目录**：`<盘>\IEML` 且**确实存在**的那些。

★ 第 3 条是给"第一次用这个功能"的人准备的老用户升级上来时列表只有 1 条，
对着一行空白没法"切换"。只收**确实存在**的目录 —— 不编路径。

### 三个关键决定（都有理由）

* **记录文件放 `%APPDATA%\IEML\`（与 `datadir.txt` 并排），不放数据根目录里。**
  它记的是"启动器知道哪些文件夹"，而这正是**数据目录不可用/要换掉**时要读的东西 ——
  放进数据根目录就有自举问题（换到别处就看不到自己的历史了）。
* **禁用/按钮的判据是"精确到路径"**（`is_current`），不是"同一块盘" ——
  这条是 beta.52 补记四修过的 bug，这里继续沿用。
* **`set_data_root` 成功后立刻 `remember_root`**：换过去又换回来是这类功能
  最常见的用法，列表里没有它，用户就得再去系统对话框里翻一遍。
  另外启动时也记一次（当前目录 + 老位置 `%APPDATA%\IEML`，只要它还在）。

★ 上一版的**盘符列表删掉了**（`list_data_volumes` 命令 + 前端 `dataVolumes`）：
同一件事不留两套，`platform::list_volumes` 留着当"扫盘找同款目录"的内部工具。

### 附带回答：PCL 的"先下 jar、首启补全" vs 我们的"装完即完整"

用户问哪个更好。**保持我们的**，理由三条：

1. **首启不打脸**：PCL 那种做法把几 GB 的下载挪到"用户已经坐在电脑前等着玩"的那一刻，
   而我们装完就是装完 —— 用户点启动时不用再等（这是 `beta` 阶段最该守住的东西）。
2. **失败在装的时候暴露**：装到一半失败，用户看到的是一句"装失败"；
   首启补全失败，用户看到的是"点了启动没反应"，两者的可诊断性差很远。
3. **我们其实两条都有**：`prepare_spec` 的**启动前自愈**就是 PCL 那套"缺什么补什么"
   （`net/installer.rs` 的 repair 路径，缺文件先自愈、补不齐才报）。
   也就是说：装的时候走完整流程，启动时还有一张网 ——
   而**只有 jar 没有 JSON 的目录**（`versions/1.21.11` 那种）在我们的模型里
   不会被当成"已装"（判据是版本 JSON），也不会被启动，属于**明确的半成品状态**。

### 验证

```
cargo test --lib platform::  → 30 项通过（新增 4 条：去重置顶 / 上限与移除 /
                                坏文件当空表 / 当前那条排最前且名字可读）
cargo check --tests          → 0 错
pnpm exec tsc --noEmit       → 零错误
node tools/verify.mjs        → 全部 17 项通过
```

桌面真机（CDP 读真实 DOM，窗口内宽 1180，本机数据）：

```
弹窗打得开；标题区 = 「切换 · 点一下就用它（不会打开资源管理器）」
3 行，界面与后端 list_data_roots 逐条一致：
  IEML [正在用]  D:\IEML                                    [当前]（禁用）
  IEML [系统盘]  C:\Users\Administrator\AppData\Roaming\IEML [用这个]
  IEML           E:\IEML                                    [用这个]
正好一行标「正在用」；名字取路径最后一段；取消能关掉
```

★ 第 2、3 行分别来自"老位置"（`legacy_data_roots`）与"盘上扫到的同款目录" ——
两条来源都在真机上出现了，不是只写了代码。

已发布并验端点：线上 `0.1.0-beta.54` == 本地、匿名下包 + 内置公钥 Ed25519 验签通过。

---

## 0.1.0-beta.53 — 2026-09-20（第六十五轮：两次"查不出来的启动崩溃"抓到真凶了）

### 一、真凶：**日志管道断了，会直接崩掉整个启动器**

前两轮各崩过一次（beta.51 20:43:45、beta.52 21:44:28），都是
`IEML.exe` 异常码 `0xc0000409`（BEX64），而且**同一份 exe 换个启动方式又是好的** ——
两次都因为"没有任何现场"而记成"未复现"。

这一轮把它按住了，三步：

1. **先量清楚"哪种启动方式会崩"**：四组配置各起 4 次（普通启动 / 只带调试端口 /
   只带独立 WebView2 profile / 两个都带）—— **16/16 全部存活**。
   所以跟那两组环境变量无关，唯一的共同点是**启动方式**：两次崩溃都是从
   pwsh 后台任务里 `& exe` 起的（stdio 走管道），而双击 / `Start-Process` /
   `stdio:'ignore'` 这三种 20+ 次全好。
2. **照着这个形状复现**：起进程后立刻关掉 stdout/stderr 的读端 →
   **3/3 稳定崩在 `3221226505`（= `0xC0000409`）**，而且崩在**启动两秒后**。
3. 那个时刻正在打印的是 `net::probe` 后台线程的
   `[IEML/probe] 中位 TTFB = …`。

**根因**：`eprintln!` / `println!` **写失败时会 panic**（Rust 的标准行为：
`failed printing to stdout: The pipe is being closed`），而 release 用的是
**`panic = "abort"`** —— MSVC 的 `abort()` 走 `__fastfail`，退出码正好是 `0xc0000409`。
于是：**一条日志，杀死了整个启动器**。用户那边的触发场景很实在 ——
从命令行/脚本里启动、父进程不读输出、控制台被关掉，都会走到这一步。

**修法**：把诊断输出收成**一个出口** `src/logx.rs` 的 `say!`（写失败就当没说：
`let _ = writeln!(stderr, …)`），并把 `src-tauri/src` 下 **17 个文件、100 处**
`eprintln!` / `println!` 全量换成它。

★ 没有去改 `panic = "abort"`：那是体积与"崩得干净"的选择，
问题从来不是 panic 策略，而是**我们用一个会 panic 的宏来打日志**。

★★ 加了一条**防漂断言**（`logx::tests`）：非测试代码里再出现
`eprintln!` / `println!` 就直接红。这种 bug 只在"别人的机器 + 别人不读输出"时现形，
靠记性守不住。

修复后的复现实验：**同一组操作 3/3 存活**（修前 3/3 崩）。

### 二、顺带挖出来的：**集成测试已经编译不过很久了**

查上面那条时顺手跑了一次 `cargo check --tests` —— **红的**：

* 四个集成测试的 `LaunchSpec` 缺 `libraries_dir`（这个字段是 dev.12 加的）；
* `live_forge_processor` 还在用 `AppState.running = Mutex::new(None)`
  （beta.6 的"多开实例"已经把它换成**按实例 id 索引的表**）。

也就是说 `pnpm test:live` / `test:fresh` / `test:416` / `test:neoforge` …
**这些真机脚本全跑不起来**，而没人发现 —— 因为 `verify` 只跑 `cargo test --lib`。

* 四个测试文件的 initializer 补齐（值取它们各自的 `shared.join("libraries")`）；
* ★★ **门禁加到第 17 项**：`Rust 全部测试目标可编译`（`cargo check --tests`）。
  判据特意是"只编译不运行"：真机测试要联网、要装好的游戏，不能进交付门禁；
  但"它们至少能编译"是必须守住的底线。

### 三、rc 前置清单的进度（用户 2026-09-20：「其余的开始吧」）

| # | 事项 | 这一轮做到哪 |
|---|---|---|
| 1 | 两条病因未明的报障 | **其中一条（启动崩溃）已经定位并修掉**（见上）；另一条（下载尾部卡顿）仍需用户补现象 |
| 2 | 半成品版本目录 + forge 库缺失复核 | `versions/1.21.11` 只有 jar 没有 JSON —— 核对代码：`installed` 的判据是**版本 JSON**（`commands_real.rs` 的 `versions_dir.join(format!("{id}.json")).is_file()`），所以它**不会被当成"已装"**，界面上不会说谎；下次装这个版本时 JSON 会自动补上。原报告里那四个目录（1.20.1 / 1.21.1 / 26.1.1 / rd-132211）**现在都有 JSON** 了，那笔账可以销 |
| 3 | Java 无国内镜像 | README 新增**「已知限制」**一节写明来源只有 Adoptium 官方；**界面上也改了**：下载失败时明说"它没有国内镜像，网络不好会失败 —— 可以稍后再试，或用下面扫到的本机 Java"（原来只有一句"下载失败：…"，用户会以为是我们坏了） |
| 4 | `VERSIONING.md` §3.3 重写 + 平台口径 | 已重写：beta 判据改成**对得上现在**的检查单（原来引用的两节 README 早就不存在了）；**rc 判据**单列成 5 条（含"用户亲手验关键链路"）；**平台口径 = 0.1.0 只发 Windows**（依据：没有 CI、一台 mac/Linux 都没跑过、Rust 侧确有 Windows 专属代码），跨平台推到 0.2.0；`N = 2` 并写明"证明不够就改成 3" |
| 5 | 文档口径一批 | README 三个过期数字（382→**437** 项 Rust 测试 / 7.66 MB→**8.9 MB** exe、2.81→**3.4 MB** 安装包 / 442 kB→**约 760 kB** 产物）；`ADR-054` 与 `FLOW-install-and-launch.md` 里"引用已不存在的 README 表"的标注；`LAUNCHER_SOURCE_STUDY.md` 的 17 条开放问题逐条标注（1/2/3/11/13/15/16 已有结论）；`bridge/tauri.ts` 的 CF key 注释（不再写"设置页可覆盖"）；`MEMORY.md` 里几条过期事实（当前版本、体积、两张 PNG、原生下拉处数） |
| 6 | 用户亲手点：CurseForge 界面链、正版登录 | 仍待用户 |
| 7 | 发布 | 本轮做完就发（`release:verify`） |

### 验证

```
cargo check --tests            → 0 错（修前 5 个目标编译不过）
cargo test --lib               → 437 项通过（+5：两卷盘断言、same_path、日志出口防漂）
pnpm exec tsc --noEmit         → 零错误
复现实验（关掉 stdout/stderr 读端）→ 修前 3/3 崩(0xC0000409)，修后 3/3 存活
node tools/verify.mjs          → 全部 17 项通过（含新增的那一项）
```

---

## 0.1.0-beta.52 — 2026-09-20（第六十四轮：再双击一次，调起已经在跑的那个）

用户：「**当 IEML 正在运行时，如果再次双击快捷方式，我希望能调起正在运行的 IEML，
而不是打开一个新的**」。

### 为什么这不只是"体验问题"

两个启动器同时跑会真出事：

* 两个进程同时写 `instances.json` / `prefs.json`，后写的把先写的**整份覆盖**；
* 同一份游戏目录被两个界面同时读改（Mod 管理、清理缓存、装东西），看着都"成功"。

所以这一条既是用户要的体验，也是一条**数据安全**上的修补。

### 怎么做

`tauri-plugin-single-instance`（**第一个注册**的插件 —— 它要在 setup 阶段就决定
"我是第几个"），回调里把主窗口 `show` + `unminimize` + `set_focus`。

★ **"能置前台"不是白来的**：Windows 只允许前台进程抢焦点，而第二个实例刚被用户
双击（它手里有前台权利）—— 插件在退出前先 `AllowSetForegroundWindow(第一个实例的 pid)`
把这份权利交出去，第一个实例的 `set_focus()` 才真的生效。少了这一步，
用户看到的只是"任务栏闪一下"（典型的半成品）。

★ **顺序如实记**：`run()` 在建 Builder 之前还有一段数据目录选址 + 一次性搬家，
所以第二个实例会先跑完那段才退出。那些操作是幂等的（搬家是"绝不覆盖"语义），
代价几十毫秒 —— 不值得为它把整段启动逻辑挪进 setup。

### 留了一条真机断言守着（不然它坏了没人知道）

新增 `tools/live/live-single-instance-check.mjs`，四条判据：
第二个实例**自己退出** / 第一个实例**被还原** / 它的窗口**成为前台窗口** /
全程**只剩一个**进程。

★ 它起手先看这份 exe 有没有人在用，**有就拒绝跑（退出码 2）** ——
不替用户关掉他正开着的启动器。

真机实测（debug 与 release 两份都跑过）：
```
第二个实例 退出码 0（346 ~ 367 ms，不是"起完再自己关"）
第一个实例：最小化 True → 之后 iconic=False；前台窗口 == 它的窗口；进程数 = 1
```

### 补记一：beta.51 那次没复现的启动崩溃

beta.51 部署刚结束时（20:43:45）有过一次启动崩溃，系统日志里是
`IEML.exe` 异常码 `0xc0000409`（BEX64）。**之后同一份 exe 用四种不同配置各起过一次，
全部正常**，包括触发它的那组环境变量 —— 所以这一条**没能复现，也就没能定位**。
如实记在这里：若再遇到，WER 报告在
`C:\ProgramData\Microsoft\Windows\WER\ReportArchive`（按 `IEML.exe` 找），
把时间点说出来即可对上号。**不当成"已经解决"**。

### 补记二：用户的**正常操作**把一条过期的断言打红了

这一轮 `pnpm verify` 第一次跑是**红的**：

```
thread 'platform::tests::app_paths_shape' panicked:
assertion failed: p.root.to_string_lossy().contains("IEML")
```

原因不是代码坏了，是**那条断言本身早就过期了**：

* 它写于"数据目录永远是 `…\IEML`"的年代；
* 从「新建/切换游戏根目录」（beta.46）起，玩家可以把根目录设成任意路径；
* beta.51 把入口做成"在启动器里选"之后，用户**第一次试**就换成了 `D:\测试目录`
  —— 于是这条断言开始把**一个合法的用户选择**判成 bug。

现在它只断言真正该成立的事：数据根目录是**绝对路径**，且 `instances/`、
`.minecraft/` 都挂在它下面。

★ 教训（值得单独记一笔）：**"永远成立"的断言会随着功能长大而变成假的** ——
它不会自己报错，只会在某天把一个正常用户打成"测试失败"。判据要写"这件事
在什么条件下成立"，而不是"现在这台机器上它长什么样"。

### 补记三：换到**空目录**要提前说"你的东西会看起来不见了"

同一个用户操作暴露的第二件事：换到 `D:\测试目录` 之后，启动器里
**一个实例都没有了**（它们仍在 `D:\IEML`，只是新根目录里没有 `instances.json`）。

原来的提示只在"目标里已经有数据"时说一句，反过来那半句（"旧目录里的东西
一个都没动"）埋在 toast 尾巴上，用户不会读。现在目标里**没有**游戏数据时
明确警告，而且 toast 用 warning 语气：

> ★ 这个目录里还没有游戏数据 —— 重启后版本列表会是空的
> （旧数据仍在原目录里，随时能换回来）

### 补记四：**同一块盘上的另一个目录**换不回去（我这版的 bug）

还是那次用户操作暴露的：他把根目录换成了 `D:\测试目录`。而我的列表是按**盘**
判断"正在用"的 —— 于是 **D: 那一行整行被标成「正在用」、按钮禁用**，
他**再也没有办法从列表里换回 `D:\IEML`**，只能去开系统文件夹对话框
（正是他要避开的那个）。

修法：把一件事拆成两件（`platform::VolumeInfo`）：

| 字段 | 意思 | 界面 |
|---|---|---|
| `is_current` | 建议的那个目录**就是**现在用的那个（精确到路径，大小写与末尾 `\` 不算差别） | 标「正在用」、按钮禁用 |
| `on_current_drive` | 现在用的目录**在这块盘上**（可能是别的目录） | 不做事（信息本身留着） |

★ 教训与补记二同源：**判据要写得比现状更细一层**。"这块盘是当前盘"当时看着
够用，是因为我脑子里想的只有"用户会选我们建议的那个目录"—— 而用户第一次试
就选了个自定义目录。

### 验证

```
cargo check（含新依赖）        → 通过
node tools/live/live-single-instance-check.mjs <exe>  → 四条判据全过
node tools/verify.mjs          → 全部 16 项通过
```

---

## 0.1.0-beta.51 — 2026-09-20（第六十三轮：数据目录在启动器里选 + 那个溢出的按钮）

用户一张截图（`数据目录` 那一行，`新建/切换…` 那颗按钮压在卡片边上）+ 两句：

> 「**先把这个修了，这个数据目录的这个按钮会溢出**；
>  **我希望数据目录是在启动器里选，不需要到资源管理器里找**」
>
> 补一句：「**新建肯定要打开资源管理器，但切换就不需要**」。

### 一、溢出：一颗只在**默认窗口宽度**下才犯的错

量出来的形状（`.field-row` 是 `200px minmax(0,1fr) auto` 的三列网格）：

| 窗口宽 | 值列宽 | 三个按钮要 | 结果 |
|---|---|---|---|
| 1280 | 248px | 229px | 塞得下 —— **所以一直没人发现** |
| **1180**（`tauri.conf.json` 的默认宽度） | **194px** | 229px | **溢出 34px**，压到卡片内边距甚至边框上 |

根因不是"按钮太多"，是 flex 项**不会缩到内容宽度以下**：容器只有 194px，
里面三个按钮最小 229px，于是整排溢出自己的格子，而 `.field-control` 的
`min-width: 0` 让它**连滚动条都不给**（用户看到的就是按钮骑在卡片边上）。

修法是一行：`.field-control { flex-wrap: wrap }` —— 放不下的按钮落到第二行，
**任何宽度下都留在自己的格子里**。没有去改按钮文案、也没有砍功能。

★ 这一条的教训：**"我这儿看着是对的"要连默认窗口尺寸一起量**。
默认宽度 1180 恰好落在"两栏布局还在、值列已经不够"的那一小段里
（1150 以下会退化成单列，反而不会溢出）。

### 二、数据目录改成**在启动器里选**

以前那个按钮**直接弹系统文件夹对话框** —— 想换个盘得自己在那个像资源管理器的框里翻。
现在它打开 `components/DataRootPicker.tsx`：

| 用户的分工 | 界面上是什么 |
|---|---|
| **切换** | 启动器里列的候选盘，**点一下就换，不打开资源管理器**：盘符 · 剩余/总容量 · 系统盘标记 · 会建到哪个目录 · 按钮 |
| **新建** | 「新建文件夹…」→ 系统对话框（要新建文件夹，那是它该干的事） |

* 候选**由后端给**（新命令 `list_data_volumes` → `platform::list_volumes`）：
  盘符、剩余空间、哪块是系统盘、以及**建议目录**（`<盘>\IEML`）。
  ★ 目录名来自 Rust 的 `DATA_DIR_NAME`，**前端不许自己拼** ——
  拼错的话"界面说的位置"和"文件真正落下的位置"就不是同一个地方，且一点报错都没有。
* 排序与自动选址同一套判据：非系统盘在前、按剩余空间从大到小（`pick_best_volume`）。
  ★ 系统盘**照样列出来**（只有一块盘的机器上它就是唯一选择），只是带个标记。
* 当前那块盘标「正在用」且按钮不可点；剩余不到 1 GB 的盘禁用并**写明理由**
  （"禁用必须给具体理由"）；不到 5 GB 给一枚「快满了」。
* 语义一个字没变：**只切换、不迁移**，旧目录一个字节都不动，**重启才生效**。
  换完设置页那一行会挂一枚「重启后生效 + 新路径」的角标 ——
  否则用户回来一看还是旧路径，只会以为"没换成"。

### 验证（桌面真机，CDP 读真实 DOM）

```
窗口内宽 1180（默认宽度）
  存储行：按钮 max右 1055 ≤ 卡片内右 1130（修前是 1152 > 1130），按钮分 2 行
弹窗：列出 3 块盘（D:\ 451/932 GB · E:\ 114/244 GB · C:\ 50/199 GB 系统盘）
  界面行数 == 后端 list_data_volumes 的 3 条，目标目录逐字相同（D:\IEML / E:\IEML / C:\IEML）
  恰好一块标「正在用」（D:\）；非系统盘排在系统盘前面
只读验证：全程没点过"用这个"，%APPDATA%\IEML\datadir.txt 仍是 D:\IEML（内容与时间戳都没动）
```

```
cargo test --lib platform::   → 24 项通过（含新增 2 条：
                                建议目录必须在盘内且以 DATA_DIR_NAME 结尾；
                                当前目录必须**恰好**命中一块盘）
pnpm exec tsc --noEmit        → 零错误
```

★ 这一轮还顺手把 `pnpm desktop:dev` 的一个坑记下来了：`tauri dev` 起的进程里
前端加载的是 **`http://tauri.localhost`（内嵌的 dist）**，不是 devUrl ——
所以改完源码在 dev 窗口里看不到（我因此白跑了一轮真机验证）。
要快速看真实前端，走「`pnpm exec vite build` → `tauri build --debug --no-bundle`
（约 30 秒）→ 直接跑 `target\debug\ieml.exe`」。

---

## 0.1.0-beta.50 — 2026-09-20（第六十二轮：下载页的版本与加载器筛选 —— 以前"搜什么"是界面替玩家定的）

用户一句：「**下载页下载资源我想要可以选择版本（不给推荐版本）同时能选择模组加载器，
这俩可以叠加，做到「选择资源来源」的左边吧**」。

### 以前是什么样

下载页那四个资源页签（Mod / 资源包 / 光影 / 数据包）里，"搜哪个版本、哪个加载器"
**完全由上面「装到」的那个实例决定** —— 界面上一个能改的地方都没有。
于是"我想看看 1.20.1 的 Forge 有什么 Mod"这个再正常不过的念头，
在这一页根本表达不出来：要么先造一个 1.20.1 + Forge 的实例，要么就别想看。

### 现在

`ResourceCenterBody` 多了一个 `gameFilters`（**只有下载页给**），
在「内容来源」**左边**多两个下拉：

| 下拉 | 默认 | 选项 |
|---|---|---|
| 游戏版本 | **不限版本** | 清单里的**正式版**（新到旧）+ 玩家自己装着的版本（快照也在内） |
| 模组加载器 | **不限加载器** | Forge / NeoForge / Fabric / Quilt |

* 加载器的名字取自 `BASE_LOADER_NAME`（**唯一一份**），不在界面上再抄一遍；
* **两个叠加**（版本 × 加载器，AND），一起交给后端当 facet；
* **"不给推荐版本" = 两个都默认「不限」，不预选、不塞一个给玩家**。
  空串表示"**不加这个条件**"（传 `undefined`），而不是传一个叫"不限"的值 ——
  后者会搜出一个空列表，而界面只会说"没有结果"（把"查不到"说成"没有"是老毛病）；
* 点开某个项目时，它的**版本列表用的是同一套筛选** ——
  否则会出现"列表是 1.20.1 的，点开却是别的版本"；
* 判"正式版"用的是与安装页**同一条**判据（上游 `release_type === 'release'`
  **且**版本号长得像最终版）：少任何一条，`26.2-rc-2` 这种预发布版就会混进来；
  快照不做成选项（900+ 项的下拉没法用，它真正的入口是「安装游戏」那一页）。

### 两个配套的小地方（都是"别让它说谎"）

* **未选时下拉里的值是次要色**（`.cs.is-unset`）——"现在没筛"要一眼看得出来，
  而不是只能靠读"不限"两个字；
* 页尾那行小字改成**如实复述当前筛选**（"没限定版本、没限定加载器"也要说）。
  以前那句写死的"只列适配当前加载器的版本"在缺省状态下**是假话** ——
  这轮它随筛选口径一起改了。

### 顺手补的一个坑：筛的和装的不是一回事

筛选器与「装到」的实例**可以不一致**（这正是这个功能的用处），
但不一致时玩家搜的是 1.20.1 + Forge，文件却落进 1.21.4 + Fabric 的 `mods/`，
提示还说"已装好"，游戏里根本不加载它 —— 正是本仓库最忌讳的那类"看不出来"的错。
所以两者不一致时，筛选器右边出现一枚警告：**与「装到」不一致**
（`title` 里写明两边到底是什么）。一致时**不出现**，一个字的噪音都不加。

### 没动的地方

* **整合包**那一页没动（用户说的是"下载资源"，那里是整合包自己的搜索）；
* **弹窗形态**（版本设置页的「社区资源」）**不给**这两个筛选器：
  那个弹窗的前提就是"装到这一个实例上"，再筛成别的版本只会装出一个不生效的文件；
* 加载器下拉在**资源包 / 光影 / 数据包**上是**禁用的**（后端那张表里
  `needs_loader_filter = false`）：理由挂在能收到鼠标的外层 `title` 上
  （禁用按钮自己不弹提示），页尾小字里也有同一句 —— "禁用必须给具体理由"。

### 验证

```
pnpm exec tsc --noEmit   → 零错误
pnpm test                → 129 项全过
```

★ tsc **中途拦了我一次**（TS2448）：第一版把筛选那一段写在了 `current` 之前，
而 `loaderForQuery` 要读 `current?.needs_loader_filter` —— 先读后声明就是 TDZ。
这不是新坑（beta.26 在 `DownloadPage` 踩过一次），所以那条注释直接写在代码里了。

真机（浏览器演示模式，`tools/live/shot.mjs` 一次性剧本，没入库）实测：

```
.res-bar 的子元素 = spacer | res-filters(不限版本 不限加载器) | seg(Modrinth/CurseForge)
   → 两个下拉确实在「内容来源」**左边**
版本下拉 10 项 = 不限版本 + 内置 9 个正式版（快照 24w45a 已被过滤掉）
加载器下拉 5 项 = 不限加载器 + Forge / NeoForge / Fabric / Quilt
选 1.20.1 + Forge 之后出现「与「装到」不一致」（装到的是 1.20.4 + fabric）
```

★ **演示模式看不到的两处**：页尾那行小字、加载器下拉的禁用状态 ——
它们都依赖后端给的资源种类表（`resource_kinds`），浏览器里没有后端。
这两处要在桌面版里看。

---

## 0.1.0-beta.49 — 2026-09-17（第六十一轮：Quilt 走同一张表 —— 我上一轮的理由是错的）

★ **补记一件我自己的疏忽**：这一轮的发布**第一次没成功**，而我没发现。
原因是我把发布命令的输出截成了 `Select-Object -Last 1`，**正好把失败信息截掉**，
只看到自检那行绿的。直到下一轮跑端到端验签才发现线上还停在 beta.48：

```
✓ HTTP 200  version=0.1.0-beta.48
✗ 线上版本 0.1.0-beta.48 ≠ 本地 0.1.0-beta.49
```

**教训：发布这种"要么成功要么什么都没发生"的动作，不许截断输出。**
而且**发布完必须验端点**，不能以"命令没报错"当结论 ——
`release:verify` 那条脚本（publish → verify-manifest → verify-endpoint）
本来就是把这三步串起来的，该用它，别手动分步跑再自己挑输出看。

顺带修了 `verify-endpoint.mjs` 的一个短板：本机 DNS 间歇性解析不了
`asset.cnb.cool`（清单 302 跳到那儿）时，它抛的是一屏 undici 堆栈 ——
看的人只会以为"发布炸了"。现在给一句人话 + "重跑一次再判断"，
但**退出码仍然是 1**（不能因为可能是网络问题就当通过）。

用户一句：「**Quilt 和 Fabric 支持的版本是重合的，所以你懂的**」。

上一轮我把 Quilt 排除在闸门之外，理由是「Quilt 有自己的支持范围，
拿 Fabric API 的表去卡它是另一类错」。**那个理由是错的** —— 实测：

| | 支持的 MC 版本 |
|---|---|
| Fabric 加载器 | 1.14 起 |
| **Quilt 加载器** | **1.14.4 起**（1.14 整版返回 404） |
| Fabric API | 1.14 起 |

**Quilt 的范围不是它自己的，是跟着 Fabric API 的** —— Quilt 上装 Mod 靠的是
**QFAPI（Quilted Fabric API）**，它的支持范围跟着 Fabric API 走。
所以「Fabric API 不支持这个版本」对 Quilt 同样成立。

### 改了什么

`getLoaderCapabilities` 里那道闸从「只针对 fabric」扩到「fabric 或 quilt」，
并且理由按加载器措辞 —— Quilt 的说法要讲清前置是 QFAPI：

```
Quilt 上的前置 API 是 QFAPI（Quilted Fabric API），它的支持范围跟着
Fabric API 走 —— 而 Fabric API 没有发布 1.7.10 版本，它从 1.14 起才支持正式版。
```

Rust 侧那条防漂断言也一起扩到 Quilt（`builtin_table_never_offers_fabric_family_
outside_the_api_list`）。

★ **上一轮那条测试被翻了过来**：它原来叫「这道闸只针对 Fabric，不牵连 Quilt」，
现在叫「Quilt 走同一张表」。**测试写成什么样，取决于我当时信的是什么** ——
这次是用户的领域知识纠正了我的臆断。Forge / NeoForge 仍然不受影响，
另有一条测试单独守它们。

### 顺带

`meta.quiltmc.org` 的 API 是 **`/v3`** 不是 `/v2`（我探针写错了一次，
返回 404 还以为是"Quilt 不支持"）。这一类"探针 URL 写错 → 得出错误结论"
本轮是第二次（上次是 mcimirror 漏了 `/v2`）—— **探针报"没有"时先怀疑探针**。

### 验证

```
node --test tests/fabric-api.test.mjs   → 10 项
cargo test --lib domain::loader_caps    → 17 项
pnpm exec tsc --noEmit                  → 零错误
```

---

## 0.1.0-beta.48 — 2026-09-17（第六十轮：不让选，而不是选完再拒绝）

用户截图 + 一句反问：「**与其这样，直接不让选不就好了**」。

截图里 1.7.10 的 Fabric **点得动、能选中**（蓝点选上了），选完下面才弹
「当前组合不可用」。上一轮那道闸**根本没生效在这一项上**。

### 为什么没生效

`InstallComposer` 里基础加载器的禁用条件是：

```ts
const confirmed = q?.status === 'ok' && q.versions.length > 0;
const disabled = o.value === '' ? false : !confirmed;
```

**它只看"在线清单有没有版本"，完全没读 `opt.available`。**
Fabric 加载器在 1.7.10 上确实有 253 个构建 → "清单非空 → 点得动"，
而能力表早就判它不可用了。

现在两者分开看：

```ts
const liveOk = q?.status === 'ok' && (q?.versions.length ?? 0) > 0;
const blockedByCapability = liveOk && opt != null && !opt.available;
const disabled = o.value === '' ? false : blockedByCapability || !liveOk;
```

★ 这条区分是有意义的，不能合并：
`liveOk` 说的是「**清单到没到**」（没到 → "查不到，可以重试"），
`available` 说的是「**我们判它能不能用**」（判不能用 → "这个版本没有它"）。
两者为假时给用户的话完全不同 —— 合成一个条件就会把"没查到"说成"没有"，
那正是 ADR-050 记着的那类假话。

### 顺带修掉截图里的第二个问题

理由文案里的 `**1.14**` **把星号原样显示出来了** —— 「当前组合不可用」面板与
`title` 提示都按**纯文本**渲染，不认 markdown。去掉星号，并加一条断言：
**理由里不许出现任何 markdown 记号**（`* _ \` #`），免得以后再犯。

### 测试

```
node --test tests/fabric-api.test.mjs   → 9 项（+1 条：理由里不许有 markdown）
cargo test --lib domain::loader_caps    → 17 项
pnpm exec tsc --noEmit                  → 零错误
```

---

## 0.1.0-beta.47 — 2026-09-17（第五十九轮：用 Fabric API 的支持范围精确限制 Fabric）

用户要求：「根据 Fabric API 支持版本来精确限制哪些版本有 Fabric 哪些没有」，
并给了 MC百科的 Fabric API 词条。

### 修的是一个真实错配：**加载器存在 ≠ Fabric API 存在**

以前"这个版本有没有 Fabric"只看 **Fabric 加载器**的在线清单
（`meta.fabricmc.net/v2/versions/loader/{mc}`）。这两件事不是一回事：

- Fabric **加载器**在很多版本上都有构建（包括快照）；
- Fabric **API** 才决定"装完能不能装 Mod"。

玩家装 Fabric 基本都是为了装依赖 Fabric API 的 Mod。所以加载器有、API 没有时，
勾上 Fabric 只会得到一个**装不了任何 Mod 的空壳**，而界面说它"可用"。

### 判据落在哪

`src/domain/fabric-api-versions.json` —— **一份表两边读**：
前端 `import`，Rust `include_str!`（`domain/loader_caps.rs`）。
不抄第二份数组：抄一份就会漂，而"支持范围"漂了之后的表现是
"某个版本莫名其妙不能装"。

来源 MC百科（JSON 里记了 `_source` 与读取日期）。页面正文原话：
「除支持 1.14+ 的正式版本外，Fabric API 也跟进最新快照版本开发」。

★ 核对过：**1.14 起的正式版一个不多一个不少，正好 48 个**
（1.14段5 + 1.15段3 + 1.16段6 + 1.17段2 + 1.18段3 + 1.19段5 + 1.20段7 + 1.21段12 + 26.x段5）。
所以这张表的精确含义就是「Fabric API 只支持 1.14 及以上的正式版」。

### 闸门规则（三态）

| 情况 | 结论 |
|---|---|
| 正式版且在表里 | Fabric 可用（版本号仍以在线清单为准） |
| 正式版但不在表里 | **不可用**，理由说清"1.14 起支持 + 低版本是 Legacy Fabric 那套移植 + 这个版本该用 Forge" |
| 快照 | **不走这道闸** —— 表里只有正式版，而页面写明 Fabric API 会跟进快照 |

★ 快照为什么必须放行：一律拒掉是**误伤**，而
"用户明明能装、我们不让"比"漏放一个"更糟。这条规则在代码、Rust 测试、
TS 测试三处都写明了，并且有一条专门的测试钉住它（`snapshot_is_recognized_and_exempted_by_design`）。

★ 闸门**只针对 Fabric**：Quilt / Forge / NeoForge 各有自己的支持范围，
拿 Fabric API 的表去卡它们是另一类错。有一条测试专门守这件事。

### 界面上保留版本号

被判不可用时，`versions` 里**仍然保留**在线拿到的加载器版本号 ——
用户看得见"加载器确实有构建"，再配上理由就能明白是
「**API 不支持**，不是没查到」。

### 测试

```
node --test tests/fabric-api.test.mjs        → 8 项
cargo test --lib domain::loader_caps         → 17 项（+6 条新断言）
```

其中两条是**防漂**的：静态能力表里凡是给了 Fabric 的正式版，
都必须在 API 支持表里 —— 以后有人往 `profile()` 里加一个 1.12.2 + Fabric，
测试会立刻变红，而不是等玩家装完发现一个 Mod 都装不了。

★ 写这两条时它们**当场就红了**：静态表给快照 `24w45a` 配了 Fabric。
那是对的（Fabric API 跟进快照），所以测试补上了快照例外 ——
并额外加一条测试把这个例外本身也钉住，免得它哪天悄悄变成"快照全跳过"。

---

## 0.1.0-beta.46 — 2026-09-17（第五十八轮：可以新建游戏根目录了）

### 新建 / 切换游戏根目录（用户要求，语义由用户明确）

用户原话：「**单独建一个根目录，源目录不删**」。

按这个语义实现 —— 这个动作**只切换、不迁移**：

- 新根目录是空的，游戏要重新装（该盘空间自己算）
- 旧根目录里的版本 / 存档 / Mod **一个字节都不动**，随时能切回去
- 界面文案必须让人看完就敢点：toast 里同时给出**新的**和**旧的**路径，
  并明说「旧目录里的东西一个都没动，想换回来重新选它就行」

★ 我上一轮把这件事拖着没做，理由是"换了之后旧数据怎么办没定"。用户这一句澄清
把那个问题消掉了 —— **不需要搬**。记一笔：拿不准的需求先问，别自己替用户
设想一个更复杂的版本然后卡住。

**做的地方**：

| 层 | 内容 |
|---|---|
| `platform.rs` | `validate_data_root`（**纯校验**）+ `set_data_root`（校验通过才写记录文件） |
| `commands_real.rs` | `set_data_root` 命令，返回新路径 / 旧路径 / 是否系统盘 / 是否要重启 |
| `bridge/tauri.ts` | `api.launcher.setDataRoot(path)` |
| 设置页 →「存储」 | 「新建/切换…」按钮，走系统文件夹选择框（`@tauri-apps/plugin-dialog`） |

**拒掉的几种情况**（每条都给具体原因，不返回一句"路径非法"）：
空路径、相对路径、与当前相同（含只差一个反斜杠）、
选在当前目录**里面**、当前目录选在**目标里面**、建不出来或写不进去。

**不拒绝但会提示**：目标在系统盘上。默认选址刻意躲开系统盘（数据能到 20 GB+），
但玩家可能只有一块盘 —— 这是提示不是错误，判断权在他。
另外目标里若已有游戏数据（`instances.json` / `.minecraft` / `versions`），
也会说明"会直接用那一份"。

**需要重启才生效**，并且**如实这么说**：`AppPaths` 是启动时解析一次放进
`AppState` 的，实例列表、缓存路径、日志路径全从它派生。运行中换掉它意味着
全部重建 —— 那是另一件事。所以命令只**记录**选择，不假装切成功了。

★ 校验逻辑拆成 `validate_data_root` 纯函数再测：`set_data_root` 成功时会写
真实的 `datadir.txt`，直接测它会让**跑一次单测就改掉开发机的数据目录记录**。
新增 4 条判据测试（空/相对、同一个、双向嵌套、可用临时目录）。

### 下载长尾：先加诊断，不改调度

用户报「下载引擎下载……最后一个文件，需要等待很长时间」，并强调
**这是所有版本下载的共性**。

先读代码：`download_batch` 里 `sort_large_first` 那段注释说明**长尾本来就被
"大文件优先"消掉过** —— 收尾应该只剩几个一眨眼就完的小文件。既然仍然卡，
剩下的可能只有三种，而它们**都会在日志里留下痕迹**：重试轮（退避 0.8→15s +
降并发到 1）、换源重下、单文件超时窗口。

所以这一轮**只补一样原本没记下来的东西：每一轮各花了多久**：

```
[IEML/download] 各轮耗时：第0轮 41.2s · 第1轮 16.8s（合计 58.0s，3598 个文件）
```

配合已有的 `第 N 轮补下 M 个失败文件` 与 `换 mcimirror 重试`，
"最后那段时间被谁吃掉"就能一眼看出来。

★ **刻意不动任何下载逻辑**：没有证据就改调度，很容易把已经对的地方改坏。
（这一轮我在埋点上还走过一次弯路 —— 想把每个文件的耗时也记下来，但那要
给 `run_round` 加参数、改动面比诊断本身还大，于是收回到"只记轮次耗时"。）

---

## 0.1.0-beta.45 — 2026-09-17（第五十七轮：整合包装不上的真因是一个字母）

用户报了七件事。这一轮修掉五件，其中一件的根因值得单独说。

### ★★ 整合包无法安装：`rename` 写成了同名字段

Rust 侧那条链路写得很完整（读清单 → 装本体与加载器 → 按清单下 Mod →
解压 overrides → 补 API 前置包），前端也完整（选包 → 面板 → 确认并开始安装），
所以**静态读代码读不出问题**。

于是加了一条**真实联网**的测试（`src-tauri/tests/live_modpack.rs`）走完整条链路，
第一次跑就红在这里：

```
整合包清单格式不对：missing field `version_id` at line 507 column 1
```

根因是**一个字母**：

```rust
#[serde(rename = "version_id")]     // ← 字段本来就叫 version_id
pub version_id: String,             //    这行 rename 是**空操作**
```

作者显然想写 `versionId`（Modrinth 整合包规范是 **camelCase**，与它自己的 API
的 snake_case 不一样），但写成了下划线版 —— 等于没改。serde 于是去找
`version_id`、真实清单里只有 `versionId`、直接报错。**所有整合包都装不上。**

同一族还有两处**不报错、但读到错数据**的：
`formatVersion` 拿不到 → `format_version` 静默变成 0；
`fileSize` 拿不到 → `file_size` 静默变成 0（"要下多少字节"永远是 0）。

修法是给两个结构体加 `#[serde(rename_all = "camelCase")]` —— **一次盖住所有字段**，
而不是再逐个手写 `rename`（逐个写正是当初漏掉的那个形态）。

★ **为什么单测没抓到**：原有的 mrpack 测试全都是**直接用 Rust 结构体字面量**
构造 `MrpackIndex` 的，从来没走过反序列化 —— serde 的字段名一条都没被验证过。
所以这一轮补了两条**按真实 JSON 形状**解析的测试（JSON 逐字取自真实 .mrpack），
以及那条真实联网的 `live_modpack`。

修完实测：

```
清单：MC 26.3  loader=fabric-loader 0.19.5
清单声明文件数：33 → 生成任务 33 个，跳过 0 个
实际下载 5 个文件：成功 5，失败 0
overrides 解压：51 个文件
```

### 整合包封面

- **关掉原生拖拽**：WebView 里 `<img>` 默认可拖拽，按住封面往外拖会把图拖到
  桌面去。`.pack-cover img` 与 `.res-cover` 都加了 `-webkit-user-drag: none`
  + `user-select: none`，JSX 侧同时加 `draggable={false}`
  （**两个都要** —— `draggable` 是硬开关，CSS 那条在某些路径下不生效）。
- 封面区高度原本靠 **CSS `:has(img)`**，那是没必要的版本依赖（要 Chromium 105+）。
  改成由 JSX 给 `data-has-img` 属性。**图加载失败时会退回"分量色条"** ——
  否则会留一个 96px 的空灰块。

### 任务中心：下载完自动收起

展开栏挡住主界面，下完之后还得手动点一下才收 —— 那一下是纯粹的收尾动作。

判据刻意写成**从有活跃任务变成没有**（而不是"当前没有活跃任务"），
否则用户主动打开面板查看历史任务时会被一把收掉。
**失败时坚决不收**：收起来等于把错误信息藏了。

### 版本列表：删掉「启动」按钮

原来每行有一个显眼的「启动」主按钮。列表是**管理版本**的地方（设置 / Mod / 日志
都在二级页），启动入口也在二级页。就地启动的路没堵死：「更多操作」菜单里仍有「启动」。

### 启动前自检 Java

以前是**先启动、失败了再解释**。而 `preview_launch` 早就能在**不启动游戏**的前提下
把同一条准备路径（解析 Java、拼 classpath、查文件）跑一遍，失败时给出同样的结构化
错误（`code` / `required_major`）—— 这个命令本来就是为"点之前就知道会发生什么"写的，
只是从没被接上。

现在**先自检、再启动**：Java 不对就原地告诉他该装哪个版本，连那次必然失败的启动
都不发生。自检失败就**不继续启动** —— `preview_launch` 与 `launch_minecraft` 调的是
同一个 `prepare_spec`，自检过不去的启动一定也过不去。

### 还没做的两件

- **可新建 / 选择游戏根目录路径**。机制是现成的（`platform::resolve_data_root`
  的优先级里就有 `datadir.txt` 记录文件，内容是一行路径），但**"换了之后旧数据
  怎么办"是个必须先定的问题**：直接切会让用户已有的版本"消失"（文件还在，
  只是看不见）。要做就得先决定搬不搬、怎么搬、失败怎么办 ——
  不能顺手做一个"切换后东西不见了"的按钮。
- **下载引擎卡在最后一个文件很久**。代码里看不出确定的病因：阶段只分
  「下载核心文件 / 下载资源文件」两步，大文件本来就优先调度
  （`large_files_are_scheduled_first`），没有明显的尾部串行点。
  需要用户补一点现象（卡在哪个阶段、百分比、卡多久、什么版本）才能定位。

### 验证

```
cargo test --lib modrinth   → 23 项通过（含 2 条新增的防复发断言）
live_modpack（真实联网）     → 3 项通过
pnpm exec tsc --noEmit      → 零错误
```

---

## 0.1.0-beta.44 — 2026-09-17（第五十六轮：客户端更新通道打通，玩家终于能收到更新）

这一轮的主题是**把"更新"这条路走完**。前几轮把发布端搭起来了（CNB 上的 `latest.json`、
签名、`tauri.conf.json` 里的端点和公钥），但**前端从来没有调用过 updater 插件** ——
东西全在，玩家永远收不到更新。这轮补上那条缺失的链路，并且**端到端验证**了它真的通。

### 加了什么

| 位置 | 内容 |
|---|---|
| `src/hooks/useLauncherUpdate.ts`（新） | 检查 / 下载 / 安装的全过程状态机 |
| `src/pages/SettingsPage.tsx` | 「关于」卡里新增一行「启动器更新」 |
| `package.json` | 依赖 `@tauri-apps/plugin-updater`；新增 `release:manifest` / `release:publish` / `release:verify` 脚本 |
| `tools/release/verify-manifest.mjs`（新） | 上传**前**自检：清单/签名/公钥三者自洽 |
| `tools/release/verify-endpoint.mjs`（新） | 上传**后**自检：匿名拉清单 → 下包 → 真验签 |
| `tools/release/check-exe-wiring.mjs`（新） | 确认更新能力**真的编进了 exe**（端点、公钥、插件、ACL 权限） |
| `tools/release/set-pubkey.mjs`（新） | 换公钥（原文替换，不重排 conf） |
| `tools/env/build-signed.ps1`（新） | 带签名的构建；**构建前**就校验公钥与私钥同源 |
| `tools/env/msvc-env.ps1`（新） | MSVC 环境抽成可 dot-source 的共享文件（原来只有一份、且无法复用） |
| `tools/gates/check-ps1-encoding.mjs`（新） | 验门禁第 16 项：含中文的 `.ps1` 必须有 UTF-8 BOM |

### 按钮为什么叫「检查启动器更新」

这个程序里有**两种**更新：启动器自己、以及游戏 Mod（`ModsPanel` 里那个）。
两者都叫"检查更新"会让人点错，所以文案上必须能分清。

### 修掉的三个真问题

1. **发布脚本会发错包。** 原来 `.sig` 和安装包都是"取 bundle 目录里第一个匹配"，
   而 `bundle/` 里攒着历次构建的产物 —— 干跑时它就挑中了 `beta.28`。
   改成**按当前版本号精确匹配**，一个都对不上才退回最新的并大声警告。

2. **确认上传时 URL 拼错了。** `fetch(`${API}${verify_url...}`)` 里那个三元表达式
   两个分支都是空字符串（半途改坏的），于是 CNB 返回绝对地址时拼成
   `api.cnb.coolhttps://…` → `ENOTFOUND api.cnb.coolhttps`。
   加了 `abs()` 统一补全，并给确认请求补上 JSON content-type（否则 406）。

3. **签名器挂死**（承接上一轮）根因确认：`--ci` 生成的无密码密钥会让
   `tauri signer sign` 在解码成功后挂住，还打印误导性的 `Signing without password.`。
   **带密码的密钥 + `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` 正常。**

### 换了一次发布密钥（不可逆，所以趁现在）

`beta.43` 用的是 `ieml2` 密钥对，密码是随手设的测试密码且已泄露。
**公钥是编进 exe 里的** —— 换密钥对之后，装了旧版本的机器会认为新版本的签名是伪造的，
永久失去自动更新能力。所以这件事只有"还没人安装"时能免费做。

`beta.43` 发布后约 20 分钟内没有任何分发，因此这轮：
- 生成 `ieml3` 密钥对，密码为 32 位随机串（存 `~/.ieml-release/PASSWORD.txt`，见该目录的备份说明）
- `tauri.conf.json` 的 pubkey 换成 `ieml3`
- **版本号升到 `beta.44`** —— 绝不能用同一个 `beta.43` 重发，
  否则世上会存在两个公钥不同的 `beta.43`，而装过第一版的人永远更新不了

### 顺带清掉的死代码

`pnpm typecheck` 长期有 3 条 `TS6133`（未使用变量），让验证门禁一直是红的：
- `ResourceBrowser.tsx` 的 `loaderLabel`
- `InstanceSetup.tsx` 的 `memoryReasoning` 导入、`autoSuggestion` 计算

后两个是当初删掉内存"依据"那一行时忘了跟着删的导入和 `useMemo`
（`InstanceSetup.tsx` 里那段注释正好记着这件事）。现在 `tsc --noEmit` 零错误。

### 新增一道门禁：PowerShell 脚本的编码

含中文的 `.ps1` 没有 UTF-8 BOM 时，PowerShell 5.1 按 GBK 解码、**直接解析失败**，
而报错指向一个无辜的 `}`（真凶是第一行的中文注释）。**这个坑本轮又踩了一次** ——
用工具重写 `cargo-manual-msvc.ps1` 时 BOM 被悄悄抹掉，文件看起来完全正常。

它极易复发，因为任何"以 UTF-8 无 BOM 重新落盘"的工具都会抹掉 BOM，diff 里也看不出来。
所以做成了会红的检查（`tools/gates/check-ps1-encoding.mjs`），而不是一句"记得加 BOM"。
纯 ASCII 的 `.ps1` 不报（没有可被误解码的字节）。

### ★ 一次真实的疏漏：构建成功了，但你双击到的还是旧版

首发 beta.44 之后用户反馈「开发端没更新，客户端也没有」—— 核对结果：

| 位置 | 版本 | 说明 |
|---|---|---|
| `src-tauri/target/release/ieml.exe` | **beta.44** | 构建成功 |
| 桌面 `IEML.exe` | beta.43 | **没跟着更新** |
| `%LOCALAPPDATA%\IEML\ieml.exe` | beta.43 | 从没在本机装过 |

**根因**：`pnpm desktop:build` 原本是 `tauri build && deploy-desktop.ps1`，
而我新写的 `build-signed.ps1` **只做了前半截**。
于是"构建成功"和"用户双击到的是新版"分了家 ——
**这正是 `deploy-desktop.ps1` 头部记着的那起事故（2026-09-13），
我以新脚本的形式让它复发了一次。**

已修：`build-signed.ps1` 末尾调用 `deploy-desktop.ps1`（失败就抛错），
并加 `-Install` 开关可顺带静默安装客户端。
**默认不装** —— 替换用户机器上正在用的客户端是有副作用的动作，
不该在"构建"里偷偷发生。

顺带把"release exe 与已安装 exe 差 3 字节"这件事**逐字节验证了**（以前只是记着）：

```
差异字节数: 3
  偏移 8220674  release="U"(55)  已安装="N"(4e)
  偏移 8220675  release="N"(4e)  已安装="S"(53)
  偏移 8220676  release="K"(4b)  已安装="S"(53)
上下文：BUNDLE_TYPE_VAR_UNK  →  BUNDLE_TYPE_VAR_NSS
```

就是 Tauri 的打包标记。（★ 偏移量已从记忆里的 7783306 变成 8220674 ——
二进制变了，**凭记忆写数字就会错**，所以这次真的跑了一遍比对。）

### 验证

```
node tools/verify.mjs                     → 全部 15 项通过（含 420 个 Rust 测试）
node tools/release/verify-manifest.mjs    → 16 项全绿
node tools/release/verify-endpoint.mjs    → 匿名取清单 ✓ 下包 ✓ Ed25519 验签 ✓
node tools/release/check-exe-wiring.mjs   → 端点/公钥/插件/ACL 权限都编进了 exe ✓
```

★ 最后那条查的是"配置写对了但没进二进制"这个经典失败形态 ——
构建、类型检查、打包**全都不会报**，只有玩家点按钮时才暴露。

★ 顺带纠正一个我自己写错的判据：`capabilities/default.json` 里写的是
`updater:default`，但 Tauri 会把它**展开成具体命令**再编进二进制，
形态是 `plugin:updater|check` / `plugin:updater|download_and_install`。
第一版查的是字面量 `updater:default`，找不到就误报"权限没编进去"。
（`createUpdaterArtifacts` 同理不该查 —— 它是纯构建期选项，运行时 conf 里会被剥掉。）

★ 门禁项数这一轮从 15 变 16（新增 PowerShell 编码检查），
所以上面 `verify.mjs` 的输出是加那一条之前跑的。

---

## 0.1.0-beta.43 — 2026-09-17（第五十五轮：帽子层要按 9/8 放大，这才叫"画了帽子"）

用户发来 **PCL 的对照图**："这是PCL的"。

### 真因（前两轮我都判断错了）

PCL 画出来的头发**凸出在头部轮廓之外** —— 左右和顶部都包出来一圈。
而我的渲染里头发全被框在 40×40 的方框内。

原因：**Minecraft 的帽子层比头大一圈**。头是 `8×8×8`，帽子层是 **`9×9×9`**
（每边外扩 0.5 像素）—— 所以它本来就应该**包住**头。我原来按"同样大小、同样位置"画，
那圈多出来的头发根本画不出来，看起来就是"没渲染"。

实测 PCL 那张图：**头发外框 / 脸 = 1.09**，与理论值 **9/8 = 1.125** 吻合
（差值来自抗锯齿）。

### 改了什么

头显示 40px 时的三个数（**一套，改一个另外两个都得跟着改**）：

| 项 | 值 | 怎么来的 |
|---|---|---|
| 帽子显示尺寸 | **45px** | 40 × 9/8 |
| 居中偏移 | **-2.5px** | -(45-40)/2 |
| 背景缩放 | **360px** | 64 × (45/8) |
| 背景坐标 | **-225px -45px** | 源(40,8) × 360/64 |

★ CSS 里 `inset: auto` 必须写在 `left/top` **之前** —— 否则它会把前面设的
`left/top` 一起重置成 auto（后者覆盖前者，这个顺序坑差点又踩一次）。

### ★ 流程上的修正

前两轮（"图层序反了"、"多背景语义不确定"）我都是**改代码猜**，两次都错。
这轮先用项目的 CDP 工具（`tools/live/shot.mjs`）**把真浏览器里的渲染截出来看**，
确认 9/8 的写法确实让头发凸出来之后**才**动 app。

**教训：看起来像渲染问题的时候，先截图，别改代码。**
（截图脚本：`tmp/hat-fix.html` + `tmp/shots4/`；对照页与截图都留着复现。）

### 验证

* CDP 截图对照：新写法头发凸出轮廓，与 PCL 一致（旧写法仍被框住）；
* `pnpm exec tsc --noEmit`：仍是那 3 个既有 TS6133，没有新增。

---
## 0.1.0-beta.42 — 2026-09-17（第五十四轮：账号头像的帽子层没画出来）

用户："**帽子没渲染**"。

### 先确认坐标没错

从真实皮肤里把两块抠出来验过：

* 头部正面 `(8,8)` 8×8 → **64/64 不透明像素**（是张脸）
* 帽子正面 `(40,8)` 8×8 → **28/64 不透明像素**（顶部与两侧的深色头发）

按 alpha 合成出来的效果 = 脸 + 头顶两侧的头发 —— 说明**坐标与数据都是对的**，
问题在渲染那一层。

### 改了什么

原来是**一个元素叠两层背景**：

```js
backgroundImage: `url(skin), url(skin)`,
backgroundPosition: '-200px -40px, -40px -40px',   // 图层0=帽子, 图层1=头
```

CSS 规范里多背景**第一层在最上面**，所以"帽子在前"这个写法理论上是对的。
但这条链上有三处**任何一处出错都会静默地只画出一层**，而且从代码上完全看不出来：
逗号分隔的解析、`background-*` 三个属性的图层配对、以及 React 把样式对象序列化成字符串。

★ **最像的失败模式是图层序被反过来** —— 头那层是 64/64 全不透明，
一旦它盖在帽子上面，帽子就被**完整地遮住**，症状正好是"帽子没渲染"。

改成**两个元素显式叠放**：

* 底下一层画头（`background-position: -40px -40px`）
* 上面一层画帽子（`-200px -40px`）
* **谁盖在谁上面由 DOM 顺序决定**，不再依赖 `background` 的多层语义；
  真出问题也能一眼看出是哪一层。

`background-size: 320px`（= 64 × 5）移到 CSS 类里，行内只留各自的 position。

### 如实说明

**我没能在本机复现这个现象**（渲染结果要看运行中的界面），
所以上面是"按最可能的失败模式消除歧义"，不是"找到了确凿根因"。
坐标与数据这两半是**实测确认过的**，剩下的一半靠这次结构改动排除。

### 验证

* `pnpm exec tsc --noEmit`：仍是那 3 个既有 TS6133，没有新增；
* `app.css` 行尾：CRLF 0 / 裸 CR 0。

---
## 0.1.0-beta.41 — 2026-09-17（第五十三轮：账号头像改成走 Mojang 官方，照 PCL 的做法）

用户："**PCL 的头像服务也是境外的？**" → "**按 PCL 的做吧**"。

### 先纠正一个前提

**PCL 根本不用第三方头像站。** 它走 Mojang 官方：拿 64×64 的**皮肤原图**，
**在本地裁头部**画出来 —— 所以它不依赖任何第三方服务。

实测（本机，用户真实账号）：

| 端点 | 结果 |
|---|---|
| `mc-heads.net/avatar/<uuid>/64` | **403 + 1084 B HTML**（`<img>` 只会静默失败） |
| `minotar.net/avatar/<uuid>/64` | 200 + 458 B（能用，但仍是第三方） |
| **`sessionserver.mojang.com/.../profile/<uuid>`** | **200 / 727 B / 0.9 s**，还带回玩家名与披风 |
| **`textures.minecraft.net/texture/<hash>`** | **200 / 3054 B / 真 PNG 64×64** |

→ **官方那条链在用户的网络环境里反而是通的**，而且比第三方更权威、不会哪天关站。

### 改了什么

* 新增 `auth::fetch_skin(uuid)` + 命令 `account_skin`：
  查 profile → base64 解 `textures` → 取出皮肤/披风 URL。
* 前端 `AccountPanel`：拿到皮肤后在**本地裁头部**（PCL 的做法）——
  CSS 多背景 + `background-position`，不引入任何图像处理依赖。
  64×64 皮肤里**头部在 (8,8) 的 8×8**、**帽子层在 (40,8) 的 8×8**；
  显示 40px = 8px × 5 ⇒ `background-size: 320px`、位置 `-40,-40` / `-200,-40`。
  ★ 帽子写在**第一层**（CSS 多背景里第一层在最上面，帽子本来就叠在头上）。
* `SkinInfo` 加 `#[serde(rename_all = "camelCase")]` —— 它是**直接透传**给前端的，
  按 ADR-036 透传结构必须 camelCase，否则前端读到的两个值是 `undefined`。

### 两个"静默失败"的坑（都已处理）

1. **Mojang 给的皮肤地址是 `http://`**，而界面 CSP 的 `img-src` **只允许 `https:`**
   → 地址看着完全正确，图就是不显示。已实测 `https://textures.minecraft.net/...`
   与 http 返回**同一张图、同样 3054 字节**，所以在后端 `force_https` 改写协议
   （比放宽 CSP 更对）。
2. **sessionserver 对不存在的账号回 `204 No Content`（不是 404）** ——
   直接 `.json()` 会得到一句看不懂的反序列化错误。已翻译成人话：
   「Mojang 查不到这个账号（xxx）—— 可能它不是正版账号，或 UUID 不对」。

### 验证

* 新增真机测试 `tests/live_account_skin.rs`（2 项）：
  ```
  玩家名 = Heshan001
  皮肤   = https://textures.minecraft.net/texture/7f4005c8…
  披风   = https://textures.minecraft.net/texture/569b7f2a…
  皮肤图 = 3054 字节，PNG 魔数正确
  ```
  ★ 断言里包含"**协议必须是 https**"与"**响应前 4 字节必须是 PNG 魔数**"——
  这两条正是上面两个静默失败的坑，只看"URL 拿到了"是测不出来的。
* Rust 全量 `cargo test --lib`：见下方结果。
* `pnpm exec tsc --noEmit`：仍是那 3 个既有 TS6133，没有新增。

---
## 0.1.0-beta.40 — 2026-09-17（第五十二轮：下拉遮挡真修好 + 头像换对端点）

用户："**Java 选择方式下拉还是被挡**；**登录头像加载不出来**"。

两条都是**上一轮没修对**，这轮找到真因。

### 下拉遮挡：量错了容器

上一轮加的是"打开时量剩余空间，不够就往上开"，思路对，但**量的对象错了**：
量的是 `window.innerHeight`（视口），而真正裁剪面板的是 `.content`
（二级页面的滚动容器，`overflow-y: auto`）。

**视口比滚动容器大** —— 容器上面有标题栏、下面还有别的东西，所以
"视口底部还有空间"跟"容器底部还有空间"是两回事。量出来说下面够，
实际早被容器裁没了。

★ 上一轮我还在注释里写「量视口是最保守也最不会出错的口径」——
**那句是错的**。规则是：**量哪个容器裁它，就得量哪个容器。**
现在先往上找**最近的可滚动祖先**（`overflow-y` 为 auto/scroll/hidden），
以它的边界为准；找不到才退回视口。

### 头像：端点是 403，返回的根本不是图片

`mc-heads.net/avatar/<uuid>/64` 返回 **403 + 1084 字节 HTML**。
`<img>` 遇到非图片响应是**静默失败**的 —— 界面上什么都不显示，
连 `onError` 都要等浏览器放弃解码才触发。

逐端点实测（用真实 UUID `edc5ea66…`）：

| 端点 | 结果 |
|---|---|
| `mc-heads.net/avatar/<uuid>/64` | **403 + 1084 B HTML** |
| `crafatar.com/avatars/…` | 422 |
| 两个国内站 | 连不上（000） |
| **`minotar.net/avatar/<uuid>/64`** | **200 + 458 B，PNG 魔数正确**（3/3 稳定） |

→ 换成 `minotar.net`。

★ **教训：挑图片服务不能只看"域名有没有名气"，要验证响应体到底是不是图片。**
403 也能带 1KB HTML 回来，而 `<img>` 不会因此报错。

### 验证

* `pnpm exec tsc --noEmit`：仍是那 3 个既有 TS6133，**没有新增**；
* `minotar.net` 连测 3 次：200 / 458 B / PNG，稳定。

---
## 0.1.0-beta.39 — 2026-09-17（第五十一轮：主页「管理这个版本」改成直达 + 更名）

用户（截图）："主页的**管理这个版本**改成『**管理此版本**』，
然后**直接转到这个版本的设置页**，而不是版本列表"。

`LaunchPage` 的版本卡里那颗链接原来 `go('versions')` —— 把人送去**版本列表**，
然后还得在列表里再找到这一行、再点进设置。而用户此刻看的就是这个版本的卡片，
点"管理"当然是"管理**这一个**"，不该让他去列表里再对号入座（卡片上已经有
版本名和图标，那一步是纯多余的）。

改成 `openVersion(target.id, 'setup')` —— 与卡片右边那颗「版本设置」按钮
**同一个落点**，两条路通向同一个地方。文案也改成「管理此版本」：
"这个/那个"在中文里带指代距离，"此"才是"我正看着的这一个"。

★ 顺带清掉一处**过期注释**：上面「其它版本」那块里写着
「`go('versions')` 在别处仍有 4 处调用」—— 本轮改完之后它**一处都不剩**了。
（`go` 本身仍有 `go('launch')`/`go('download')`/`go('settings')` 三处使用，
不会变成未使用的解构变量。）

### 验证

* `pnpm exec tsc --noEmit`：仍是那 3 个既有 TS6133，**没有新增**。

---
## 0.1.0-beta.38 — 2026-09-17（第五十轮：修掉两处自绘下拉的布局事故 + 账号头像）

用户（三张截图）：
「**java选择方式的下拉栏被遮挡了**，**版本隔离的下拉栏直接爆了**；
这个正版登录的这个对勾位置，**换成正版账号头像，头像不要做圆角**（不会就看 PCL）」。
另：「CFPA 这个先不做了」。

★ 前两条是**上一轮（beta.36）换 `CustomSelect` 时引入的**，属于回归 —— 本轮的账。

### 版本隔离：`Field` 外面又套了一层 `.field-row`

`Field` 组件**自己就渲染 `<div className="field-row">`**（label / control / source 三列），
而改动时在外面又包了一层同名的 wrapper → **grid 套 grid**：外层把内层当成"一个格子"，
内层那三列被压进 `1fr`，触发器直接扁成一个小方块。

原来能工作时，这行用的是**原生 `<select>` 直接当 grid 子元素**；
换成 `Field` 之后多了一层容器，布局就塌了。

修法：去掉外层 wrapper，`id="row-isolation"` 交给 `Field` 自己的 `rowId`
（这个 prop 本来就是为"从摘要跳过来"准备的），label / hint / 控件 / Chip 仍占三列。

### 选择方式：面板被滚动容器裁掉

`.content`（二级页面）带 `overflow-y: auto`，而 `.cs-menu` 是 `position: absolute` ——
**超出容器底部就被裁**，越靠页面底部的下拉越明显（Java 那一栏正好在最下面）。

这不是某一页的问题，是"滚动容器里的自绘下拉"的通病，所以修在组件里：
`CustomSelect` 打开时量一次视口剩余空间（`measureDrop`），
**下面不够且上面更宽裕就翻上去**（`data-drop="up"`，CSS 负责翻转后的位置）。
判据用视口而不是滚动容器 —— 容器可能只占视口一部分，视口是最保守的口径。

### 正版账号头像

`.acct-avatar` 原来放的是一个**绿色对勾**：它只说"登录成功了"，
对"是**哪个**账号"一个字都没说，而右边就写着玩家名 —— 两边对不上。
PCL 的做法就是账号行最左边放这个号的头像，照做。

* 头像地址用 `state.prefs.accountUuid` 拼（本来就有的 Minecraft UUID，不需要额外请求）；
* **`border-radius: 0`** 是用户明确要求的（父级 `.acct-avatar` 有圆角，必须显式覆盖）；
* `image-rendering: pixelated` —— 皮肤的头是 8×8 像素图，放大到 40px 用双线性插值会糊；
* ★ **拉不到就退回对勾**：头像服务在境外（见 `2026-09-16-network-diag.md`，
  这台机器访问境外站是时段性不通的），不能让它在网络抖动时留一个空方块。

### 验证

* `pnpm exec tsc --noEmit`：仍是那 3 个既有 TS6133，**没有新增**；
* `app.css` 行尾复核：CRLF 0、裸 CR 0（项目有混行尾踩坑史，每次改 CSS 都验）。

---
## 0.1.0-beta.37 — 2026-09-17（第四十九轮：EXE 体积砍掉 1.94 MB + 启动页换正式图标）

用户（从上个会话的排队队列里恢复出来的两条）：
「**IEML的EXE文件有点大，排查一下**」「**加载页的图标没改，用现在的icon**」（附图）。

### 体积：根因是两张**给 64px 显示位塞了 1189px** 的图标

拆开看，前端产物 2.66 MB 里有 **2.24 MB 是两张 PNG**（占 84%）：

| 资产 | 文件 | 尺寸 | 显示位最大 | 浪费 |
|---|---|---|---|---|
| `brand-icon.png` | 1,328,679 B | 1189×1189 | **64 px**（`index.html` 的 splash） | 18× |
| `version-icon.png` | 970,351 B | 1060×1094 | **72 px**（主页大卡） | 15× |

这两张会被 Tauri **整份内嵌进 exe**，所以多出来的像素直接变成下载体积。

**改法**：按「最大显示尺寸 × 4」（HiDPI 上限）等比缩 —— 品牌图标 → **256×256**、
版本图标 → **310×320**。原图备份在 `src/assets/_original/`，脚本 `tools/shrink-frontend-icons.ps1`。

★ **alpha 必须保住**：缩图最常见的坑是透明区变黑底。脚本用
`Format32bppArgb` + `CompositingMode.SourceCopy` + `Clear(Transparent)`，
改完**逐像素复核过四角 alpha = 0、中心 alpha = 255**，两张都是。

**实测**：

| | 改前 | 改后 | 降幅 |
|---|---|---|---|
| `ieml.exe` | 10,452,992 B（9.97 MB） | **8,425,472 B（8.04 MB）** | **−19.4%** |
| NSIS 安装包 | 5,269,764 B（5.03 MB） | **3,285,935 B（3.13 MB）** | **−37.8%** |
| 前端 `dist` | 2.66 MB | **0.73 MB** | **−73%** |

### 启动页图标：一直是设计初期的占位符

`AppShell.tsx` 的启动页画的是**内联的通用方块 SVG**（lucide 的 box 图标）——
那句「图标没改」说的就是它。同一文件标题栏（22×22）和 `index.html` 的 splash
早就在用 `brandIcon` 了，只有这一处漏了。

改成 `brandIcon` 之后，`app.css` 的 `.boot-mark` 也要跟着改：它原来是
「`--accent` 圆角色板 + 26px 白色描边 SVG」，而**品牌图标自带蓝色底与圆角**，
再叠一层会在图标边缘露出一圈色块 —— 那层背景已去掉，改为 48×48 的 `<img>`。

### 验证

* `pnpm exec tsc --noEmit`：仍是那 3 个既有 TS6133，**没有新增**；
* `node tools/verify.mjs`：**14/15**，唯一失败项是那 3 个既有 TS 错误。
  ★ 其中「exe 内嵌前端一致性」逐字节确认了新图标确实在 exe 里
  （`brand-icon-CpdX0Inc.png` 106,094 B、`version-icon-BoHZ1Lab.png` 166,656 B）；
* 部署后桌面 `IEML.exe` 8,425,472 B，与构建产物**逐字节一致**。

---

## 0.1.0-beta.36 — 2026-09-17（第四十八轮：捡回上一个会话没做完的 6 项 UI 改动）

> ★ 本轮的活**不是新提的** —— 上一个会话（`session-80ada539`）在响应最后一条用户消息的
> 过程中被中断（`turn/end` + `session/end-seed`），那批任务一项都没实施。
> 用户重启电脑后会话丢了，问"上一个会话你能访问到吗"。
> 恢复过程与会话记录的读法见 `.workbuddy/memory/2026-09-17-未完成任务恢复.md`
> 与 `tools/diag/read-dsh-session.mjs`。
>
> 用户原话（2026-09-16 22:15:16，配 7 张截图）：
> 「这里可以断言正常；**图二这里，原版不给装mod的选项，能装mod的版本，跳转mod下载页**；
> **图三，内存取整数即可，给也是直接给整数内存**；
> **图四图五，这个下拉栏也得改**；
> **主页这里，也不给原版mod管理的键**；
> **还是主页，这个全部版本的按钮是没必要的**；」

### 图四图五 —— 版本设置页的两处原生下拉换成自绘

用户（两张图）："**图四图五，这个下拉栏也得改**"。

两张图都是**展开状态的原生 `<select>`**（灰底、菜单由浏览器绘制），都在版本设置页
`InstanceSetup.tsx`：图四是「启动选项 → 版本隔离」，图五是「选择方式」。

这正是 **beta.32** 那节末尾记着的那笔账 —— 当时写着「还有 4 处原生下拉（本轮**没动**，
等用户发话）」，其中 2 处就是这一页。用户现在就是在发话。

### 改了什么

* **「选择方式」**（Java 卡）：`Select`（原生 `<select>`）→ **`CustomSelect`**（`.cs-*`）；
* **「版本隔离」**（启动选项卡）：原生 `<select id="iso">` → **`CustomSelect`**。

两处都改成设置页「下载源」同款的写法（`Field` 包 `CustomSelect`），**值与行为一点没变**：

* `onChange` 拿到的是**值本身**，不再是 `e.target.value` —— 这是这次唯一需要改的机械差异，
  类型上由 `value: string` 挡住（`updateConfig` 的字段仍是原来的联合类型，用 `as` 收窄）；
* `label` 与 `hint` 仍走 `Field`，只是里面的控件换了；
* 「版本隔离」行尾那个 Chip（自动 / 已覆盖）留在外面，**正好占住 `.field-row` 的第三列**；
* ★ 原来那个 `htmlFor="iso"` 一并去掉：自绘下拉是个 `<button>`，指针指不到 `<select>` 上，
  它的无障碍名称改由 `ariaLabel` 提供（`CustomSelect` 的注释里写了为什么要补这一项）。
  `id="row-isolation"` 留着 —— 它标的是**整行**，给摘要条"跳过去"用（`rowId`），与 `htmlFor` 是两件事。

`Select` 这个 import **没有删**：同一页「指定 Java」那一行仍在用它（`javaPath`）。两处换掉之后
`InstanceSetup.tsx` 里 `Select` 的使用点数从 2 降到 1，不是 0。

### 还剩 3 处原生下拉（grep 实测；本轮只改用户点名的 2 处）

| 在哪 | 是什么 | 本轮 |
|---|---|---|
| 安装页 `InstallComposer.tsx` | 「加载器版本」`<select id={bv-…}>` | 没动 |
| 安装页 `InstallComposer.tsx` | 「OptiFine 版本」`<select id="of-ver">`（各带 `<label>` + 一个 Chip） | 没动 |
| 版本设置页 `InstanceSetup.tsx` | 「指定 Java」的 `Select`（`javaMode === 'path'` 时才渲染） | **没动** —— 图四图五没点它，等发话 |

grep 口径（`<select` 在 `src/**/*.tsx` 里命中 3 处）：上面 `InstallComposer.tsx` 那 2 处，
加 `ui/index.tsx` 里 `Select`（原生下拉）**自身的实现** —— 那是组件定义，不是使用点。
所以**使用点**是：`InstallComposer.tsx` 2 处 + 版本设置页「指定 Java」1 处 = **3 处**。

★ 与 beta.32 那笔账的差额说明：那节写「还有 4 处」，数的是 `InstanceSetup.tsx` 2 处 +
`InstallComposer.tsx` 2 处，**没有把「指定 Java」这第 3 处数进去**（它只在选到
"使用指定的 Java"时才出现，所以当时扫页面没扫到）。这一轮改掉 2 处，账单从 4 变 3。

### 图二 / 图六 —— 「安装 Mod」按版本区分，且改成真的跳到下载页

用户（图二：实例卡片的 ⋯ 菜单，含 `打开设置 / 安装 Mod / 启动 / 重命名 / 创建副本 / 打开目录 / 删除`）：
「**原版不给装mod的选项，能装mod的版本，跳转mod下载页**」。
图六是主页实例卡片右侧那四个直达动作（`打开目录 / 日志 / Mod 管理 / 版本设置`），
用户：「**主页这里，也不给原版mod管理的键**」。

**① 原版不再显示这两个入口。** 判据沿用 `loader`：

* 版本列表 ⋯ 菜单：`inst.loader !== null` 才渲染；
* 概览页顶栏「安装 Mod」：同上；
* 主页「Mod 管理」：`target.loader` 才渲染。

之所以**藏**而不是置灰：原版实例根本没有 `mods/` 能加载，置灰会让人以为"差一点就能用"。
判据与 `ModsPanel.tsx:720/886/891` 那三处「纯原版不能加载 Mod」是同一个概念，
**没有另写一套**（ADR-020 的教训：同一件事两处判定，早晚不一致）。

**② 「安装 Mod」改为跳「下载页的 Mod 页签」，并把当前版本带过去。**

以前它走的是 `openVersion(id, 'mods')` / `setSubPage('mods')` —— 进的是
**实例的 Mod 管理**（"已经装了什么"），而用户要的是**下载页**（"有什么能装"）。
两者在启动器里是不同的东西，`DownloadPage` 才是带搜索与来源切换的那个。

**③ ★★ 顺带修掉一个会静默装错版本的 bug。**

"跳过去并默认选中这个版本"这件事，原来是 `goDownloadTab('mod')` 之后
**立刻派发 `ieml:download-target` 事件**。但 `DownloadPage` 的监听器是在它
**挂载之后**的 effect 里注册的 —— 调用方（版本列表 / 概览页 / Mod 管理）
发事件时它还没挂载，**事件当场被丢掉**。

后果不是报错，而是：下载页静默用"最近玩过的那个"版本，
**不是用户当前这个**，界面上完全看不出来 —— 用户会把 Mod 装到错的版本上。

> 这个坑 `goDownloadTab` 本身就是为了解决同类时序问题才被造出来的
> （见 `AppContext.tsx` 里它上方那段注释），但当时只搬了"页签"，
> 漏了"带哪个版本"。`ModsPanel` 那处的注释还写着"用事件比把状态提到全局更小、更局部"
> —— 那个判断是错的，代价就是这个 bug。

修法：把目标版本也放进 state（`store.ts` 的 `downloadTargetId`），
加一个 `goDownloadFor(tab, instanceId)`，**页签 + 目标版本 + 页面切换在同一次派发里定下来**，
不依赖任何事件时序。三处调用方统一改过去，`ieml:download-target` 事件与其监听器一并删除。

★ 下载页那个「装到」选择器改成写**同一个字段**（不是只写自己的局部 state）——
"跳过来"和"手动改选"只留**一处真相**，否则两个值会打架。

### 图七 —— 主页删掉「全部版本」

用户（图七就是"全部版本"四个字）：「**还是主页，这个全部版本的按钮是没必要的**」。

它是「其它版本」那一块的右上角跳转。删掉的理由：下面那排卡片本身就能点一下切启动目标、
点右边箭头直接进设置，"其它版本"这块就是**快捷入口**；
再放一个跳"版本列表"页的按钮，是把用户从主页推走，与这块的意图相反。

### 图三 —— 内存取整数

用户（图三：摘要行写着 `内存 3.599609375 GB`，右侧「自动」角标）：
「**内存取整数即可，给也是直接给整数内存**」。

**根因是铁证级的**：全仓库唯一**没取整**的内存显示点是
`InstanceSetup.tsx` 的 `const memoryGb = active.config.memoryMb / 1024;`
（其余 6 个显示点本来就带 `Math.round`）。而本机真实实例库里确实躺着
`memoryMb: 3686` —— `3686 / 1024 = 3.599609375`，与截图**逐位吻合**
（同库还有 `4710 → 4.599609375`、`4506 → 4.400390625`，同一个病）。

改动：

* `domain/memory.ts` 新增 `wholeGbFromMb(memoryMb)` 与 `wholeGb(gb)` ——
  MB→整数 GB 的转换**只留一份**；
* `autoMemory` 的**出口**从 `round1(...)` 改成 `wholeGb(...)`：新落库的 MB 恒为 1024 的整数倍；
* 滑块 `onChange` 落库值也收敛成整 GB（**档位曲线一个数都没动**）；
* 滑块手柄改由取整后的 `memoryGb` 反推，守住"手柄与数字必须一致"那条既有约束。

★ **`memoryMb` 的语义一个字节都没改**：内部仍是 MB，引擎仍按 MB 算内存，
取整只作用在"给人看的那一眼"和"用户给的那个数"。
★ 出口用 `Math.floor` 而不是 `Math.round`，是因为四阶段分配有一条被测试钉死的
"绝不超发"约束（`autoMemory(24,'modded',8,1.5).gb <= 1.5`），而 `Math.round(1.5) = 2` 正好会破掉它。

### 验证

* `pnpm exec tsc --noEmit`：3 个既有 TS6133 稳定复现
  （`ResourceBrowser.tsx` 的 `loaderLabel`、`InstanceSetup.tsx` 的 `memoryReasoning` / `autoSuggestion`）
  —— 与改前**逐字一致**，**本轮没有新增**。
  ★ 本轮真的踩到了那次"改完忘了删 import"：`VersionsPage` / `InstanceOverview` 的
  `EVT_OPEN_MOD_BROWSE`、`ModsPanel` 的 `goDownloadTab`、`DownloadPage` 的 `setTargetId`
  都因为改了调用方式而变成死引用 —— 逐个删干净后才回到基线。
* `node --test tests/domain.test.js`：**73 项全通过**（含内存那 10 条：
  含"绝不超发""可用只有 1 GB 不超 1 GB""滑块档位与显示值一致"）。

---

## 0.1.0-beta.35 — 2026-09-17（第四十七轮：正版登录报「密钥环操作失败」修掉）

用户（截图）：登录没有完成 ——
「**密钥环操作失败：Attribute 'password encoded as UTF-16' is longer than platform limit of 2560 chars**」。

### 根因

`store_account` 把**整个 `McAccount` 序列化成 JSON 当作一条凭据**写进系统密钥环。
而单条凭据存不下那么长：

* Windows 的 `CRED_MAX_CREDENTIAL_BLOB_SIZE` = **2560 字节**；
* `keyring` 把密码按 UTF-16 存，判据是
  `password.encode_utf16().count() * 2 > 2560`（`windows.rs:224`）；
* → **真实上限是 1280 个 UTF-16 码元**。

而正版账号的 JSON 实测 **2740 字符**（光 `access_token` 那个 JWT 就 ~1600 字符，
再加微软的 refresh token）—— 一存必炸。

★ **报错信息本身是误导的**：它写「platform limit of **2560 chars**」，
但 2560 是**字节数**不是字符数。按字面把分片定成 2560 会继续失败。

### 改了什么

`auth/mod.rs`：账号改为**分片存进密钥环**（每片 ≤1200 UTF-16 码元）：

* **不加新依赖**，也**不写明文文件** —— 数据仍然全部由系统凭据管理器加密保管；
* 第 0 片沿用**裸 uuid** 作凭据名 → **旧格式天然向后兼容**，不需要迁移；
* 按 **UTF-16 码元**切分，不是按字节或 `chars()`：emoji 在 UTF-16 里占
  2 个码元，按 `chars()` 切会少算一半、照样超限；
* ★★ `store_account` **必须先清旧分片再写新的**：刷新之后微软给的新
  refresh token 长短会变，上次 3 片这次 2 片的话，残留的第 3 片会被拼到
  新 JSON 尾巴上 → 读出来是「账号数据损坏」。这个坑只有连续存两次不同长度才会现形；
* `remove_account` 删**全部**分片（只删第 0 片会把 `uuid#1` 永远留在用户的
  凭据管理器里，既清不掉也看不见）；
* 加一道防御性检查：分片若仍超限，抛**中文可行动**的错误，
  而不是把 keyring 那句误导的英文原样丢给用户。

### 验证

* 单测 7 条（`auth::tests`）：真实尺寸必被分片 / 拼回逐字符一致 /
  **UTF-16 码元计数**（emoji 那条专门防"按 chars 切"）/ 空串仍产 1 片 /
  小账号仍是 1 片 / 第 0 片是裸 uuid / 分片常量对得上平台真实上限。
* ★ **真机集成测试** `tests/live_keyring_chunks.rs`（真的读写 Windows 凭据管理器）：
  ```
  大账号 JSON = 2740 字符（单条上限 1280，改造前必炸）
  ✓ 大账号存取成功（2740 字符）
  小账号 JSON = 290 字符（片数更少）
  ✓ 覆盖存更小的账号后读回正确（旧分片已清）
  ✓ 删除后读不到，清理完成
  ```
  第二条是关键：**连续存两种不同长度**，专门抓残留分片。
* 全量 `cargo test --lib`：**420 项全通过**。

---

## 0.1.0-beta.34 — 2026-09-16（第四十六轮：下载源改为「国内优先 + 启动实测延迟决定次序」）

用户：「**改一下 IEML 的定位吧，国内有的就用国内，没有国内就用国内最近的延迟低能连的节点**」。

看着像是"把所有源换成国内镜像"就够了。**逐项实测之后发现不能一刀切** ——
同一时刻，不同项目的快慢是**相反**的：

| 探测项 | 官方 | 国内镜像 |
|---|---|---|
| 原版版本清单 | **346 ms / 3-3** | 639 ms / 2-3 |
| 原版库文件 asm-9.5 | 1136 ms | **157 ms** |
| Fabric loader jar | 1363 ms | **187 ms** |
| Forge installer jar | 1004 ms / **1-3** | **196 ms / 3-3** |
| OptiFine 版本列表 | **0-3 全失败** | **51 ms / 3-3** |
| CurseForge `/v1/games` | 2432 ms / 1-3 | **430 ms / 3-3** |
| Modrinth CDN jar | 1-3 | 0-3（**两条路都不通**） |

所以顺序**不能写死**：无条件国内优先会在原版版本清单上真的变慢，
一律官方优先又会在库文件上慢 7 倍。**改成实测驱动。**

### 改了什么

* **新增 `src-tauri/src/net/probe.rs`** —— 启动期源延迟探测。
  每个源探 **3 个代表性端点取中位数**（只探一个会系统性偏向一边：
  只探版本清单偏向官方、只探库文件偏向国内）；量的是 **TTFB 而非总耗时**
  （总耗时受文件大小影响，270 KB 的 JSON 和 3 KB 的 JSON 比"谁快"是错的）。
  探测端点**只写官方地址**，镜像侧由 `mirror_url` 推导 ——
  这样镜像表写错了探测会立刻暴露，而不是等用户装游戏时才 404。
* **`source.rs`：三层叠加评分**（优先级从高到低）：
  ① 限流冷却（−5000，这是"现在别用它"，比任何长期倾向都紧急）；
  ② **国内优先 +25**（校准过：大到能压过"官方仅快 300 ms"，
  小到能被"国内慢 13 倍"翻盘）；
  ③ **实测 TTFB 加成**（0~60，三端点全失败 −120），结论 **10 分钟过期**。
* **`mirror.rs`：Fabric meta 改为改写** —— 推翻 2026-09-13 的旧结论。
  官方 4 次里 3 次超时；镜像 `/profile/json`、`/game`、`/installer` 全部
  200 / 0.3~0.5 s，两侧**互补**（官方挂 3/5、镜像挂 1/5）。
  确认镜像**不是过期副本**：两侧 loader 列表都是 **253 条**，版本集合完全相同。
* **`mirror.rs`：Modrinth / CurseForge 顺序反转为国内优先** ——
  旧注释记的「官方 230 KB/s vs 镜像 146 KB/s」已不成立（CF 实测国内 3-3/430ms、
  官方 1-3/2432ms）。官方仍是第二候选，没被丢掉。
* **`lib.rs`：`.setup()` 里 `spawn` 探测，绝不 `await`** ——
  最坏要 4 秒，拿它换启动时间是亏的；拿不到结果就沿用默认序。
* **`download_sources` 手工映射加三个 camelCase 字段**
  （`probeTtfbMs` / `probeOk` / `probedSecondsAgo`）——
  用户该看到的不只是"算出来的分"，还有"实测多少毫秒、几个端点通、多久前测的"。
* 新增 `tools/probe/` 五个探测脚本（源策略对比、限流假象复核、Adoptium 镜像三连）。

### ★ 没做到的那一半（诚实记录）

「**没有国内的就用最近的节点**」这条**没有落地**，因为实测发现
**Adoptium（Java 运行时）根本没有可用的国内镜像**：

| 镜像站 | 结果 |
|---|---|
| 清华 TUNA | **整站 403**（连 `/ubuntu/` 都是 403，与 Adoptium 无关） |
| NJU / BFSU / SJTU / ZJU / PKU / 阿里云 | 404，没有此镜像 |
| 中科大 USTC | 有目录，但**文件下载被 JS 反爬拦死**（返回 859 B 验证页而非文件） |
| CERNET 联合镜像 | `/Adoptium/` **302 跳回 TUNA**（同样 403） |
| BMCLAPI `/v1/products/java-runtime/` | **302 跳到 Cloudflare 后端**（`*.749333.xyz`），不是国内源 |

`ARCHITECTURE.md` 里那句「**有国内镜像可换**」**是错的**，已删除并附实测表
（`adoptium.rs` 的代码注释一直是对的，是文档写错了）。
Java 自动下载只能走官方，而官方 API 时段性不可达 —— 属于国际出口问题，本机修不了。

### 验证

* `tools/env/cargo.ps1 test --lib`：**413 项全通过**（含 9 条新断言：
  默认国内优先 / 国内三端点全挂时让位 / 国内慢 13 倍时让位 /
  国内仅慢 300 ms 时保持 / 探测过期失效 / 探测不污染传输统计 /
  加成单调有封顶 / 冷却压过策略 / Fabric 改写后确有两个候选）。
* ★ **发现的探测方法坑**：第一版探测**连打 3 次**同一个 BMCLAPI 地址，
  把「限流」误判成了「源不可靠」（NeoForge maven 连打 1/3、间隔 3 秒 3/3）。
  加间隔后结论反转 —— 复核脚本 `probe-ratelimit-artifact.mjs` 就是为这条留的。

---

## 0.1.0-beta.33 — 2026-09-16（第四十五轮：资源卡片右下角那行 id 删掉）

用户："每个 mod / 资源包 / 光影 / 数据包，资源卡片右下角是啥，大多都是乱码似的，那端小字不要"。

那行是 `hit.project_id`：Modrinth 给的是 base62 id（`AANobbMI` 这种），CurseForge 给的是
**数字 id** —— 对玩家没有一点意义，看着确实像乱码。（当初加它，是想让"卡片上能对上项目页"。）

* `ResourceBrowser.tsx`：删掉卡片脚部那行 `<span className="dim mono res-page">{hit.project_id}</span>`；
* `pages.css`：删掉 `.res-page` 规则（grep 过，没有第二个使用点）；
* ★ **`page_url` 没有丢** —— 它仍在"作者不允许第三方下载"的提示里出现（那种时候它才有用）。

### 验证

* `pnpm exec tsc --noEmit` 通过；
* **真机（桌面版 `IEML.exe` + WebView2 CDP，四个页签逐个点）**：
  `Mod 20 张 · 资源包 20 张 · 光影 20 张 · 数据包 20 张`（都是**真卡片**，骨架屏单独计数，
  第一版脚本把骨架也数进去了，那次结论不算），`.res-page` **四个页签全为 0**，
  每张卡片的脚部元素数 `[1,1,1,1,1]`（只剩那个"选择版本并安装"按钮）；
  截图里卡片右下角已是空白。
* 重建 + 部署：exe `10,434,048 B`、sha256 `A57A3D19…`，桌面 `IEML.exe` 逐字节一致；
  安装包 `IEML_0.1.0-beta.33_x64-setup.exe`。
* `node tools/verify.mjs`：**全部 15 项通过**。

---
## 0.1.0-beta.32 — 2026-09-16（第四十四轮：设置页的「下载源」也换成自绘下拉）

用户（两张图对照）："忘了，这里的下拉栏也要像图二这样" —— 图一是**原生 `<select>`**
展开的样子（灰底、菜单由浏览器画），图二是启动页「要启动的版本」那个自绘下拉。

* `SettingsPage` 的「下载源」：`Select`（原生 `<select>`）→ **`CustomSelect`**（`.cs-*`，
  与启动页 / 下载页同款）。标签与提示仍走 `Field`，**值与行为一点没变**
  —— 还是同一条 `ieml:prefs` 事件，只换了控件。
* 顺手把 `Select` 从 SettingsPage 的 import 去掉（那里没有第二处用它）。

### 为什么这一处会漏掉

`CustomSelect` 是 2026-09-15 加的（`ui/index.tsx` 里写明了原因："原生 `<select>` 的下拉菜单
是浏览器画的，CSS 改不了"），当时只替换了**启动页与下载页**两处 —— 设置页这处留在原生，
于是同一个界面里两种下拉长得不一样。这一轮补上。

### 还有 4 处原生下拉（本轮**没动**，等用户发话）

> ⚠️ **本节的这笔账已在 beta.36 改掉一半**：下面表里「`InstanceSetup.tsx` 2 处 `Select`」
> 就是用户后来发的**图四图五**，已在 0.1.0-beta.36 换成 `CustomSelect`。
> **现在还剩 3 处**（`InstallComposer.tsx` 的「加载器版本」「OptiFine 版本」，加版本设置页
> 「指定 Java」那一个）。
> ★ 注意：本节那个「4 处」**本身漏数了一处** —— 版本设置页「指定 Java」的 `Select`
> 只在选到"使用指定的 Java"时才渲染，当时扫页面没扫到它。所以 4 − 2 = **3**，不是 2。
> 本节原文保留不改（已发布过的记录不许改写）。

| 在哪 | 是什么 |
|---|---|
| 版本设置页 `InstanceSetup.tsx` | 2 处 `Select` |
| 安装页 `InstallComposer.tsx` | 「加载器版本」「OptiFine 版本」两处原生 `<select>`（各带 `<label>` + 一个 Chip） |

### 验证

* `pnpm exec tsc --noEmit` 通过；
* 浏览器演示模式（CDP 截图 + DOM）：`.cs-trigger` 从 0 变 1（文字 `BMCLAPI 镜像（推荐）`），
  点开后 `.cs-menu` 里两项 `BMCLAPI 镜像（推荐）[selected] | Mojang 官方源`；
* 真机（桌面版 `IEML.exe` + WebView2 CDP，一次性脚本没入库）：设置页里
  「下载源」那一行的控件是 `cs-trigger input`（文字 `BMCLAPI 镜像（推荐）`）、
  行内 `nativeSelectInRow: false`、**整页 `select` 数量 0**；
  点开后 `role=listbox`，两项 `BMCLAPI 镜像（推荐）[selected] | Mojang 官方源`；截图两张。
* 重建 + 部署：exe `10,434,048 B`、sha256 `EE241EAE…1F1717`，桌面 `IEML.exe` 逐字节一致；
  安装包 `IEML_0.1.0-beta.32_x64-setup.exe`。
* `node tools/verify.mjs`：**全部 15 项通过**（含 exe 内嵌前端一致性）。

---
## 0.1.0-beta.31 — 2026-09-16（第四十三轮：收起的分组标题**不再列出版本号**）

用户（发来版本列表的截图）："后面的这个详细版本就不用了，去掉即可"。

版本列表按世代分组，**收起**的世代标题行上曾经这样显示：

```text
> [icon] 26.1   26.1.2 · 26.1.1 · …                     3 个
                ^^^^^^^^^^^^^^^^^^^^^^^ 这一块去掉
```

那块是 **beta.21** 按用户当时那句话加的（"版本折叠的地方的效果不如直接到那里就
显示哪个的版本号"）。这一轮按新反馈删掉：收起状态只留 **世代号 + 个数**。

* `InstallComposer.tsx`：删掉 `folded ? <span className="ver-group-ids">…` 整块；
* `pages.css`：删掉 `.ver-group-ids` 规则（grep 过，没有别的使用点，不留死 CSS）；
* 原地留了一条注释，写明"用户看过后不要了，别再往回加" —— 这个位置已经被加回来过一次。

### 验证

* `pnpm exec tsc --noEmit` 通过；
* **真机（桌面版 `IEML.exe`，WebView2 + CDP，一次性脚本没入库）**：下载页里
  `groups: 19 · idsSpans: 0 · folded: 18`，每行可见文字只有「世代号 + N 个」
  （`26.2 → 1 个`、`26.1 → 3 个`、`1.21 → 12 个`…），
  判据 `anyDotSeparatedIds: false`（那种 `26.1.2 · 26.1.1` 的串在 19 行里一个都没有）；
  截图里 `26.1 · 3 个` 那一行就是用户截图里被去掉的那块位置。
* 重建 + 部署：exe `10,434,048 B`、sha256 `E0638189…78499`，桌面 `IEML.exe` 与它逐字节一致；
  安装包 `IEML_0.1.0-beta.31_x64-setup.exe`。
* `node tools/verify.mjs`：全部检查通过（项数见脚本自己打印的那行）。

---
## 0.1.0-beta.30 — 2026-09-16（第四十二轮：资源中心换成同款分页条）
用户："资源中心的同款翻页"（这条我欠了四轮）。

`ResourceCenterBody` 底部原来是「加载更多（第 N 页）」一个按钮：
只能往后走、**回不去**，而"已显示 40 / 18337"还要玩家自己做除法。
现在换成整合包那套 `.pager`：上一页 / 第 N / M 页 · 共 X 个 / 下一页。

★ 接口语义没变（offset + append），但**两个方向必须分开**（新增 `gotTo(n)`）：
往后跳 `append: true`（接着堆）、往回跳 `append: false`（清空重取）——
否则"上一页"会把第 1 页接在最后面。顺手删掉了没人再用的 `more()`。

### ⚠️ 这一条**没通过真机验收**（如实记）

* 源码在（`ResourceBrowser.tsx:677` 的 `<div className="pager">`）；
* 构建产物里也有（`dist/assets/index-rHOr58ad.js` 里能搜到 `pager-info`）；
* exe 重建并部署完成；
* **但真机探测四个页签全都是 `pager: false`** —— 界面上没有分页条，
  运行中的是**旧的一屏**。

源码与产物都对、运行时不符，最可能是**运行中的 WebView2 还在用缓存里的旧包**
（Tauri 的资源走 `http://tauri.localhost`，WebView2 会缓存）。
下一轮第一件事：用 CDP 读运行中页面实际加载的 JS 文件名，与 `dist/assets/` 对照 ——
不一样就清掉应用的 WebView2 缓存目录再验。

### 补记（2026-09-16，用户真机实测结清两条）

用户回报："Quilt 真机安装 与 两个真实例真机多开 我实测可以"。

* **Quilt 安装**：用户真机装好、并成功启动 —— 它走的是与 Fabric 同一条 profile
  合并路径（实现见 `0.1.0-beta.16`），此前只有"代码同路径"这个推断，
  没有"本机真的装过一个 Quilt 版本"的证据。README 里两处 ⚠️ 已改成 ✅。
* **多开实例**：用户真机**同时开两个游戏**、各自能单独停 —— `0.1.0-beta.6` 的
  `running: HashMap<实例id, 进程>` 至此才有"两个真 Minecraft 同时跑"的记录
  （在那之前只有真进程单元测试 + CDP 断言）。README 已知限制第 12 条已清。

★ 上面那条 ⚠️（资源中心分页条）**不在这次实测范围内，仍然没验** —— 计划照旧。

---
## 0.1.0-beta.29 — 2026-09-16（第四十一轮：**图标真的嵌进去了** —— 是我上一轮没清干净）

用户："哪里也没换" → 追问确认为"exe / 任务栏图标没重做进去（还是旧立方体）"。

### 真凶：上一轮我只删了 `target/release/build`，**残留的 `.obj` 里带着旧图标资源**

这一轮**删掉整个 `target/release`**（3.2 GB）重新构建，图标才真正进到 exe 里。

### ★★ 验证方式也必须换 —— 我前三次的"结论"都是错的

`System.Drawing.Icon.ExtractAssociatedIcon` **走的是 Windows 的 shell 图标缓存**，
不是读 exe 的资源段。我拿它测了三次，三次都说"还是旧图标"，于是：
先怀疑 `build.rs` 没重跑 → 改 build.rs；再怀疑资源没重生成 → 删 build/；
**每一步都在修一个不存在的问题。**

这一轮换成**直接读 exe 的资源段**（`LoadLibraryEx(...AS_DATAFILE)` +
`EnumResourceNames` + `FindResource`），结果：

```text
RT_GROUP_ICON 组数: 1
  group id=32512  size=90
exe 里 6 张 / icon.ico 里 6 张
=== 结论：6 张与 icon.ico 一致，0 张不同 ===
```

**6 张全部逐字节相同** —— 新图标确实在 exe 里。而 `ExtractAssociatedIcon`
至今仍返回旧图（连清两轮图标缓存 + 重启资源管理器都没变），所以那条路**不可信**，
以后验图标一律用资源段。

★ 教训（这一轮最贵的一条）：**用一个会骗人的工具验证三次，不等于验证了三次。**
第一次拿不到预期结果时就该换工具，而不是换假设去迎合工具。

### 仍未做

**资源中心的同款翻页**（mod / 资源包 / 光影 / 数据包）—— 连续四轮被别的事吃掉。
下一轮**只做它**。

---
## 0.1.0-beta.28 — 2026-09-16（第四十轮：**品牌标换成新图标**）

用户给了新的品牌标（蓝底圆角方块 + 白色 `IEML` 字标），并说"这个正式成为我们的品牌标，
包括左上角也要换"。

### 做了什么

* 用它重跑了 `pnpm exec tauri icon`：`.ico`（95 KB，多尺寸）/ `.icns` / 32·64·128·256
  PNG / Windows Store 与 iOS·Android 全套**都是新标**；
* 前端那份缩到 **128px**（源图 991 KB，界面里只显示 22px —— 直接把 991 KB 打进
  前端产物是浪费）；
* **左上角是你自己改的**（`AppShell.tsx` 里 `import brandIcon from '../assets/brand-icon.png'`
  + `<img>`）；`VersionIcon.tsx` 里也加了 `version-icon.png`。

### ★★ 我补的那一处：**图片导入编译不过**

你的两处 `import ... from '../assets/*.png'` 都报：

```text
error TS2307: Cannot find module '../assets/brand-icon.png' or its corresponding type declarations
```

代码本身没问题 —— **TypeScript 默认不认识 `.png` 这种导入**，需要一份模块声明告诉它
"`import` 进来的是个字符串 URL"。以前没报，是因为**之前没有任何地方 import 过图片**。

→ 新建 `src/vite-env.d.ts`（`/// <reference types="vite/client" />`），
Vite 那份类型里 png / jpg / svg / webp 全都有声明。加完 `tsc` 立刻转绿。

★ 这类错值得记一笔：**它只在"第一次引入某类资源"时出现**，
而且 `vite build` 不做类型检查 —— 所以只跑打包是发现不了的（打包能过、类型是红的）。

### 还差一条

**资源中心的同款翻页**（mod / 资源包 / 光影 / 数据包）仍然没做 ——
这一轮的时间全花在品牌标与编译错误上。它还是下一轮的第一件事。

---
## 0.1.0-beta.27 — 2026-09-16（第三十九轮：加载器的 title 提示也删掉）

用户："鼠标停在选项上时同一句仍在 title 里，这个也不要，大伙都知道是什么"。

上一轮我删了加载器选项的**可见副标题**，但把同一句留在了 `title`（鼠标悬停提示）
里，理由是"没丢信息"。用户说这个也不要 —— 对，那种说明（"老牌加载器，Mod 数量最多"）
是在教常识，摆哪儿都是噪音。

→ `title` 现在**只在不可用时给理由**（"为什么点不了"必须留着，那是真信息），
可点时一律不给。

★ 顺带清死代码：`BASE_LOADER_DESC` 这个常量（四行中文说明）已经没有任何使用点，
连同 import 一起删掉了 —— 留着它就是"下次有人以为它还在界面上"。
`loader-catalog` 测试 11 条全过。

---
## 0.1.0-beta.26 — 2026-09-16（第三十八轮：排队里的五条）

用户："正在排队的那些，开始"。

| 你说的 | 做了什么 | 真机验证 |
|---|---|---|
| 启动游戏按钮纯色不好看 | 竖向渐变（上浅下深，模拟受光）+ 1px 内高光 + 同色系外发光；hover 抬起 1px、active 压下去 | `background-image: linear-gradient(…)` + inset 白高光 ✅（原来是 `none`） |
| 图三那个副标题去掉 | 选中/不可用时那行说明删掉。**没丢信息**：鼠标停在选项上，同一句仍在 `title` 里 | `.b-desc` 数量 **0** ✅ |
| 没选版本也能下载、不知道下到哪 | 选择器后面直接写出**实例 + 目录**：「装到「X」的 `resourcepacks` 目录」；没有可用实例时说清"装不了"（而不是让人点完才发现） | 读到「装到「Minecraft 26.2」的 resourcepacks 目录」 ✅ |
| 记住玩家最后一次选的版本 | `localStorage` 记**实际生效的那个**（不是"手动点过的那个"——从 Mod 管理跳过来的目标也要记住），下次打开就是它 | 落盘 ✅ |
| 外观里加低性能损耗模式 | 一个开关，**只关装饰**：卡片的磨砂（几十层 `backdrop-filter` 是实打实的 GPU 开销）、标题区磨砂、背景三团光晕。功能与布局一点不动 | 开之前 `blur(24px)` / 光晕 `block` → 开之后 `none` / `none`，且写进 `localStorage` ✅ |

★ 低性能模式在 `main.tsx` 里**首屏之前**就挂类：放到 React 里再挂会"先磨砂闪一下再变平"，
而这个开关本来就是给弱机用的，多闪一帧更糟。存 localStorage 也是有意的 ——
它是**本机性能偏好**，换机器本来就该重新选（和"跟着人走"的偏好不同）。

### 还差一条（下一轮）

**给 mod / 资源包 / 光影 / 数据包 做同款翻页**：资源中心现在是「加载更多」按钮，
要换成整合包那套 `.pager`（上一页 / 第 N 页 / 下一页）。这一轮没做完就停手，
是因为 `ResourceCenterBody` 的翻页状态（`PAGE` / `hasMore` / `loadMore` 的 append 语义）
要一起改，属于"改一半会留下两套翻页"，不适合赶在收尾时动。

★ 这一轮我自己踩了一个坑：把 `useEffect` 写在 `const target = useMemo(...)` **之前**，
而那个 effect 里读了 `target` —— `tsc` 报 `Block-scoped variable used before its declaration`。
`vite build` **不做类型检查**，所以它照样能打包成功、运行到那一页才崩。
**"打包成功"不等于"能跑"**，这次是 tsc 拦下来的。

---
## 0.1.0-beta.25 — 2026-09-16（第三十七轮：**删掉游玩时间记录**）

用户："还有这个'从未启动'相关的记录时间的功能，删掉，这没有用"。

这条要连着上一轮看：用户先前说过"从未启动是纯属骗人，这个记录的功能根本没做或者没生效"
—— 我当时**把它实现了**（`game-exit` 的时候把 `lastPlayedAt` / `totalPlaySeconds`
写回实例）。现在用户明确说**不需要这个功能**，理由也站得住：启动器的本职是
"把游戏跑起来"，玩多久是游戏自己的事。

### 删了什么

| 位置 | 原来显示 |
|---|---|
| 启动页（身份卡） | 「累计 xx 小时 / 还没玩过」 |
| 启动页（状态行） | 「上次 3 天前 / 从未启动」 |
| 启动页（其它版本卡片） | 右下角那行「从未启动 / 3 天前」 |
| 版本列表（每行右侧） | 「今天 / 3 天前 / 从未启动」那一列 |
| 版本概览（现状表） | 「最近游玩」「累计时长」两格 |

### 一并做的

* **不再写入**：`game-exit` 时那段 `instances/update` 删掉了（那个写回是上一轮加的）；
* **字段保留**：`lastPlayedAt` / `totalPlaySeconds` 在类型和 `instances.json` 里**不动**
  —— 删字段会让老文件解析报错，而"留着不写也不显示"没有任何副作用；
* **删死代码**：`relativeTime`（三处副本）、`formatDuration`、`IconClock` 的 import，
  以及 `.ver-time` / `.lo-time` 两条再没人用的 CSS（pages.css −11 行）。

★ 唯一保留的时间显示是**"已运行 xx 分钟"** —— 那是**当下的状态**（游戏正在跑），
不是历史记录，删了它用户就不知道游戏跑了多久。

---
## 0.1.0-beta.24 — 2026-09-16（第三十六轮：标题区磨砂**羽化**）

用户："图一没有告诉你要做什么，这文字左侧的地方和左侧栏有割裂感，我建议做个羽化"。

磨砂（`backdrop-filter`）是个**矩形**：它从侧栏边界一刀切到右边缘，于是标题区
左边（和右边滚动条那侧）各有一条硬邦邦的竖线 —— 就是那个"割裂感"。

★ **不能直接给 `.page-head` 加 mask**：标题、按钮都在里面，mask 会把它们
**一起**羽化掉（字会变淡）。所以把磨砂**单独做成一层伪元素**（`::before`，
`z-index: -1`）—— 它管羽化，文字在上面一点不受影响。

三层边缘一起羽化（左右各 28px 渐隐 + 下缘 22% 渐隐），两张 mask 用
`mask-composite: intersect` 相乘。

真机读样式确认：`::before` 上 `content: ""` / `blur(16px) saturate(1.4)` /
`mask-image: linear-gradient(to right, transparent 0px, #000 …)` / `z-index: -1`。

---
## 0.1.0-beta.23 — 2026-09-16（第三十五轮：先修两条真问题，其余七条排队）

用户这批提了 9 条。这一轮**只做两条**（都是"不修就是错"的），其余七条下一轮，
理由写在最后。

### 一、"游戏关闭后，启动器仍然显示游戏在运行"（**第二次报**）—— 加一条不依赖事件的兜底

这条我上次查过三处（退出标记写了 / 事件带了 `instanceId` / 前端桥接原样传 payload），
**每一段单独看都对，但没有复现**，也就没法证明是哪一段丢的。

与其继续猜，这一轮加一条**不依赖事件**的兜底：

> 只要界面认为"有实例在跑"，就**每 4 秒**问一次后端的 `running_games`
> （它手里有子进程句柄，是唯一知道真相的地方），用结果覆盖本地表。

* 真关了窗口的用户，**4 秒内界面自己会修正**；
* 只在"有实例在跑"时轮询 —— 空转成本可以忽略；
* 判据没有第二份：`running_games` 内部用的就是 `is_still_running`（同一个函数）。

★ 这是**兜底**不是"修好了"：如果事件真的丢，日志里应该还留得下痕迹 ——
下次再出现，请把当时的 `D:\IEML\logs\*.exit.json` 时间戳告诉我，我按时间对。
（顺带说明：崩溃/退出**是**被后端正确检测到的，标记文件一直都在写。）

### 二、资源包 / 数据包**必须能选原版**（我上一轮一刀切，过度的）

上一轮我把原版实例从「装到」下拉里**全过滤掉了**。用户指出：资源包和数据包
原版本来就能用，这样一刀切等于"想给原版换个材质包都做不到"。

→ 改成**按种类判**：

| 种类 | 能不能装到原版 | 为什么 |
|---|---|---|
| Mod | ❌ | 纯原版不加载 `mods/`，装进去必然不生效 |
| 光影 | ❌ | 要有 Iris / OptiFine 这类前置，都靠加载器 |
| **资源包 / 数据包** | ✅ | **原版本来就能用** |

### 三、其余七条（下一轮，附我打算怎么做）

| 你说的 | 打算怎么做 |
|---|---|
| 给 mod/资源包/光影/数据包 做同款翻页 | `ResourceCenterBody` 现在只有"加载更多"，要换成和整合包一样的 `.pager`（上一轮那套） |
| 没选版本也能下载，不知道下到哪去了 | 两道：**没选目标就拦住并说清**；已有目标的要把"装到哪个实例的哪个目录"显示出来 |
| 记住玩家最后一次选的版本 | 把 `targetId` 落到 `prefs`（现在它是组件内 state，刷新就没了） |
| 外观里加"低性能损耗模式" | 一个开关：关掉 `backdrop-filter` 与氛围渐变（玻璃很吃 GPU，弱机上是真开销） |
| 图三那个副标题去掉 | 加载器选项下的说明行（`BASE_LOADER_DESC`）删掉 |
| 启动游戏按钮美化 | 现在纯色平涂；改成主色渐变 + 内高光 + 阴影 |

---

## 0.1.0-beta.22 — 2026-09-16（第三十四轮：世代标题用世代号 · **不吸顶**）

用户澄清了上一轮的两条（我上一轮都理解偏了）。

### 一、"在 26.2 就显示 26.2" —— 我上一轮做成了"列出组里的版本号"

用户原意：**分组的标题就用世代号**（26.2 组写 `26.2`、26.1 组写 `26.1`）。

根因在 `versionFamily()`：只有**现代版本**那一支写的是 `label: v`（整串版本号），
而 `key` 是世代 —— 于是标题取到了组里**第一行的完整 id**：
`26.2-rc-2`、`26.2-pre-6` 这种当了标题，看着像"这一组是 rc"，
其实它和 `26.2` 是**同一组**。

→ 修：`label: `${major}.${minor}`` —— 与其它分支（`1.21` / `1.7`）一致。
（上一轮我加的"收起时列出组里版本号"**保留**：它解决的是另一个问题
——收起后看不到里面有什么。）

### 二、"叠起来那个 bug 应该是没清除多余的" —— 用户判断是对的，我上一轮修错了方向

`.ver-group` 原来是 `position: sticky; top: 0; z-index: 1`：
**每个**分组标题都往 `top: 0` 上钉，滚动时后一个压在前一个上面 ——
两个标题叠在同一位置，看起来就是"一堆字糊在一起、没清除多余的"。

我上一轮的"加磨砂底"**是在遮症状** —— 底再厚，重叠还在（而且用户说那个底效果不好）。

→ 修：**去掉 `position: sticky`**（标题跟着内容滚，不跟别人抢位置），
底也随之删掉（`transparent`）。代价是滚动时看不到"当前在哪一组"，
但这个列表本来就有搜索框 —— 为那一点便利把标题叠成一团不划算。

★ 教训记一条：**「加东西遮住症状」和「去掉造成症状的东西」是两回事**。
上一轮我选了前者，因为它改动更小；但症状是结构性的（重叠），
遮只能让下一次更难看出问题在哪。

---

## 0.1.0-beta.21 — 2026-09-16（第三十三轮：六条 UI + 重做图标）

用户一次提了 6 条。

| # | 用户说的 | 做了什么 |
|---|---|---|
| 1 | 整合包底部的翻页太丑 | `.pager` 重做：**居中的一条细工具栏**（两个小按钮夹一句小字），上面留 `--space-5` 间距、加一条细分隔线、字用 tertiary 色、数字用等宽。原来它紧贴最后一行卡片、字又大又密 |
| 2 | **删除白色模式** | 整套浅色令牌删掉（`tokens.css` 少 69 行）、顶栏那个太阳/月亮按钮删掉、设置页「主题」整行删掉（**只有一个选项的选择器是假控件**）。`data-theme="light"` 现在匹配不到任何规则 → 自动落回深色，老 prefs 里有 `light` 也不会坏 |
| 3 | 下载页的 MOD 分区，选版本里不该出现原版 | 「装到」下拉过滤掉 `loader === null` 的实例 —— 纯原版不加载 `mods/`，装进去等于**装了个必然不生效的东西**，而它在列表里和别的版本长得一样 |
| 4 | 版本列表的「带加载器 / 纯原版」改名 | → 「**可装 Mod**」「**原版**」（说的是"能不能装 Mod"，而不是我们内部的实现术语） |
| 5 | 页面标题区的字左边和最右边有空缺 | 磨砂是个**盒子**：它住在 `.content` 里、左右各有 `--space-6` 内边距 → 两边各空出 24px，滚过去的内容在那两道缝里没被磨到。修法：**满宽**（负 margin 抵消父级内边距）+ 把内边距补回来，文字位置一点不变、磨砂铺满了 |
| 6 | 版本折叠不如直接显示版本号 | 收起时**把组里的版本号直接列出来**（最多 4 个，多了写「…等 N 个」）。收起本来是为了别铺满一屏，但只留一个世代号时用户还得点开才知道里面有什么 —— 等于用一次点击换一次点击 |

### 重新设计图标 + 全方位换一遍

原图标是 Tauri 的默认占位图。这一轮自己画了一个（`src-tauri/icons/icon-src.png`，
1024×1024，用 System.Drawing 画的，脚本留在 `%TEMP%` 没入库）：

* **等轴测立方体**，三个面用启动器的主色系（顶面亮蓝 `#8ab0ff` / 左面 `#4a76d2` / 右面 `#6054e0`）；
* 深色圆角底 + 左上角一团紫光（**与启动器界面的氛围一致**，不是一个孤立的 logo）；
* 亮描边：16px 下也能看出是个方块。

★ 第一版画出来**太扁**（顶面占了三分之二，像个砖头），调成真正的等轴测比例
（半宽 260 / 顶面半高 150 / 竖向棱 260）才像立方体。这条记下来：
**图标要按"最小尺寸能不能认出来"来画，不是按 1024 下好不好看。**

然后用 `pnpm exec tauri icon` 生成全套（Windows `.ico` 多尺寸 / macOS `.icns` /
iOS / Android / Store 图标），`bundle.icon` 上一轮已经列全了这些文件。

---

## 0.1.0-beta.20 — 2026-09-16（第三十二轮：页头去黑底 · 吸顶标题行叠字）

用户两条（配图）。

### 一、页头：去掉黑底，改成**纯磨砂**

上一轮我给它填了 `--glass`（深色下 `rgba(15,18,23,0.45)`）—— 在玻璃卡片旁边看
就是**一条黑带**，与周围割裂。用户要的是"没有割裂感还能作遮挡"。

→ 现在**完全不填色**，只靠 `backdrop-filter: blur(16px) saturate(140%)`：

* 遮挡照样有（滚过去的内容被磨掉，不会两段字叠在一起 —— 这条必须保住）；
* 没有色带，与底下的光晕连成一片；
* **分隔线也去掉了**（`border-bottom: none`）：磨砂本身已经形成边界，再加一条线就是又一道缝。

真机读样式：`bg: rgba(0,0,0,0)` / `blur(16px) saturate(1.4)` / `border: 0px`。

### 二、版本分组标题行**叠字** —— 是我上一轮改折叠时留下的

`.ver-group` 是 `position: sticky; top: 0`（吸顶），原来的底色是 `--bg-surface`。
上一轮我把它改成 `<button class="ver-group ver-group-btn">` 时顺手写了
`background: none` —— **把吸顶那一行的底给顶掉了**：

* 吸顶时它是透明的 → 下面的版本行从它底下**透上来**；
* 截图里那团糊掉的 `26.2` + `1 个` 就是两行叠在一起。

→ 修：`.ver-group-btn` 的底改成**磨砂玻璃**（`--glass` + `blur(12px)`）——
挡得住下面的内容，又不会在深色界面里显出一条生硬的色带。

真机读样式：`position: sticky / top: 0 / bg: rgba(15,18,23,0.45) / blur(12px)`。

★ 这两条是同一个教训的两面：**粘性/吸顶元素的底不是装饰，是功能** ——
去掉它，下面的内容就会穿上来。上一轮我为了"玻璃感"连续去掉了三处底色
（页头、侧栏、标题行），其中**标题行那一处是错的**（它要挡内容），
页头那一处只需要换一种挡法（模糊而不是填色）。

---

## 0.1.0-beta.19 — 2026-09-16（第三十一轮：用户自己做的**玻璃拟态改版** + 新图标）

用户这一轮自己动了界面（我读完之后补了三处会出问题的地方）。

### 一、用户做的

| 改动 | 内容 |
|---|---|
| **图标** | 跑了 `tauri icon`，整套新图标（`icon.png` 6.4 KB→229 KB、`icon.ico` 4.4 KB→83 KB，含 iOS/Android/Windows 全套） |
| **底光晕** | `body::before` 换成三团彩色（紫 12%/12%、绿 88%/88%、蓝 70%/15%） |
| **半透明面** | `--bg-surface / -raised / -overlay / -hover` 全改成 rgba（0.42~0.92） |
| **玻璃** | `.card` 加 `backdrop-filter: blur(24px) saturate(140%)`；顶栏 / 侧栏 / 页头**全透明** |
| **控件** | `.chip-btn` 改成玻璃药丸（半透明白 + 边框 + blur）；品牌 logo 从实心立方体改成**描边**立方体 |

### 二、我补的三处（都是"照原样会坏"的地方）

1. **页头透明 = 滚动的正文会从标题区透出来**。`.page-head` 是 `position: sticky`，
   内容会从它下面滚过 —— 透明之后两段字叠在一起糊成一片（这个坑这个文件里踩过一次，
   上一版注释写的"实底而不是渐变"就是从那儿来的）。
   → 修：底用 `--glass` + `backdrop-filter: blur(14px) saturate(140%)`
   （玻璃观感保留，滚过去的内容被磨砂掉）。
2. **浅色主题会重新长出一块彩色污渍**：新的三团光晕是**写死的颜色**，
   而我之前专门把它们抽成 `--ambient-a/b` 就是为了按主题取值
   （浅色下 0.18 的紫铺在白底上就是明显污渍）。
   → 修：抽成 `--ambient-violet/-green/-blue`（含 soft），深色给足、浅色收到约 1/4。
3. **`tauri.conf.json` 的 `bundle.icon` 只列了 `icons/icon.png`** —— 新生成的
   `icon.ico` 用不上，Windows 任务栏小尺寸（16/32px）只能从 PNG 现转、会糊。
   → 修：列上 32/128/128@2x PNG + `.icns` + `.ico`（Windows 用 ico，macOS 用 icns）。

★ 顺带记一个自己的错：我往 `tauri.conf.json` 里写了 `/* 注释 */` ——
**JSON 不支持注释**，打包当场失败（`failed to run custom build command`）。
删掉就好了，但这条值得记住：**改 JSON 配置时注释只能写在别处**。

---

## 0.1.0-beta.18 — 2026-09-15（第三十轮：封面溢出 + "选了才知道不兼容"）

用户两条（配两张图）。

### 一、封面**位置不对**：图溢出了整张卡片

上一轮我给整合包接上真封面，但 `.pack-cover` 本来是**一条 6px 高的色条**
（`data-weight` 决定颜色，表示整合包"分量"）。`<img>` 进去后**没有任何尺寸约束**，
就按原图大小（512×512 之类）铺开 —— 截图里那些糊满卡片的图就是这个。

修：封面区固定高度 + `overflow: hidden` + `img { width:100%; height:100%; object-fit:cover }`；
有真封面时（`:has(img)`）高度 96px、底色换成 surface（不再和"分量色条"打架）。

★ 诚实说明：这条**我没能在真机上验到**（探测脚本那几次都撞上骨架屏状态 ——
今晚镜像慢，14 秒还没出卡片）。CSS 这一版是照截图里"溢出"的形态改的，
要你重开后再看一眼确认。

### 二、"选了 Fabric 之后还能点，就不能黑了然后显示不兼容吗"

**这条是真 bug，且根因和上一轮那个死局同源**：

```ts
const removed = verdict.removed.find((r) => r.kind === a.kind);   // ← 只有"已勾上"的
disabled = !a.implemented ? true : !!removed ? true : …
```

`validateCombination` 是**遍历选中项**算的 —— 所以 `removed` 里**永远只有已经勾上的**。
没勾的高清修复因此一直"点得动"：用户点上去，它**才**变成不兼容、才被禁用、
安装按钮**才**被拦。用户说的"选了才知道不行"就是它。

修：`disabled` 改成**提前问一次** `addonCompatibility(mcVersion, base, a.kind, …, online)`
（**同一个判据**，只是不管选没选都问）。不兼容的**从一开始就是灰的**。

同时把理由也补上（灰掉不说为什么，用户只会以为功能坏了）：

* 短状态新增一档「**不兼容**」—— 以前所有灰掉的都显示「无」，
  于是"这个组合装不了"和"上游确实没有"看起来一模一样，而这两件事的下一步动作完全不同；
* 红角标「与当前加载器不兼容」现在对**没勾上但已判不兼容**的也显示。

真机验证（桌面版，选 1.21.11 + Fabric 后）：

```text
OptiFine    disabled=true  chip=与当前加载器不兼容  reason=不兼容
LiteLoader  disabled=true  chip=与当前加载器不兼容  reason=不兼容
```

---

## 0.1.0-beta.17 — 2026-09-15（第二十九轮：封面全加载不出来 = **CSP 拦掉了**）

用户："整合包，mod，光影，资源包封面全都加载不出来，我不懂，你得看看 PCL 怎么做的了"。

### 根因：不是 PCL 的做法问题，是**我们自己的 CSP 一行配置**

`tauri.conf.json` 里的 CSP 原文：

```text
img-src 'self' asset: http://asset.localhost data:
```

**没有 `https:`** —— 而封面图全部来自 `https://cdn.modrinth.com/...`。
于是浏览器直接拦掉每一个 `<img>`，封面组件里的 `onError` 把图换成**字母块**
（那是"图坏了不留破框"的兜底，本来是好设计）→ 用户看到的就是"封面全都加载不出来"。

★ 这一条我上一轮**差点又报成"已修"**：我给整合包接了真封面之后，
验证脚本写的是 `!!img`（元素在不在）—— **告出 20/20"有封面"**。
可 CSP 拦图时 `<img>` **照样在 DOM 里**，只是 `naturalWidth === 0`。
**元素存在 ≠ 图片加载出来了** —— 这是这一轮最该记住的一句。

### 修法

* `img-src` 加上 `https:`（一行）；
* **验证判据换成 `naturalWidth > 0`**，并且打出样本 src 证明图是从 CDN 来的。

真机实测（桌面版，Mod 页签）：`imgs: 20, loaded: 6, sample: https://cdn.modrinth.com/data/P7dR8mSH/icon.png`
—— **6 张真的加载出来了**，正好是首屏可见的那 6 张（其余是 `loading="lazy"`，
滚到才加载）。修之前这个数是 **0**。

### 顺带：最小窗口 + 侧栏文字不换行

用户："限制启动器最小窗口大小，保证左侧文字最长的也能一字排开"。

* `minWidth` 960 → **1040**（侧栏 196 + 内容区最小 ~840，谁都不挤谁）；
* 侧栏文字**一律不换行**：`.nav-item` 加 `white-space: nowrap`，
  `.side-recent / .side-recent-title / .side-link` 加 nowrap + 省略号。
  一换行整条变两行高、侧栏高度跟着跳，看起来像排版坏了。

---

## 0.1.0-beta.16 — 2026-09-15（第二十八轮：Quilt 的两条规则 + 三条待查）

用户这一批提了 8 条。**做完 2 条、明确不做 1 条、3 条要先取证、1 条确认可用。**

### 一、做完的

| 用户说的 | 做了什么 |
|---|---|
| "Quilt 完全不兼容，所以选了这个也得不让选高清修复" | `combination.ts` 里 Quilt 分支**直接返回不兼容**：Quilt 没有对应 OptiFabric 的桥接包（Fabric 那边是"有、但要自己下"，Quilt 是**根本没有**）。选 Quilt 时 OptiFine 那个选项会被禁用 |
| "不给 Quilt 装 API 了" | `api_for_base` 里 Quilt 不再返回任何 API 包（原来自动装 QFAPI）。测试改成 `quilt_gets_no_api_library`，**规则仍只有一处** |

### 二、明确不做（按用户要求）

* **资源中心"已装"标记** —— 用户说"明确不做"。README 的已知问题第 11 条据此标注为
  **按用户决定不做**（不是"还没做"）—— 这两件事在文档里必须分开写，
  否则下一个人会把它当成待办。

### 三、先说可用

* **同时开两个游戏是可以的** —— 多开实例（beta.6）实测通过，用户确认。

### 四、三条我**取证了但没修**（附证据，不做猜测性修改）

1. **Quilt 能装、打不开**。取证：`D:\IEML\logs\26.2-quilt-0.23.0-*.exit.json` =
   `{"exit_code":1,"played_seconds":1,"crashed":true}` —— **游戏 1 秒就退了**，
   所以"打不开"是真的（不是界面问题）。**为什么退**要从那次启动的日志看，
   这一轮没查。
2. **崩溃判据给出了误导性的原因**：上面那条的 `crash_reason` 是
   「当前 Java 版本过高，老版本 Forge 或 OptiFine 无法在此版本上运行」
   —— 而**那是一次纯 Quilt 启动，实例里既没有 Forge 也没有 OptiFine**。
   判据被日志里的字样带偏了：一条规则匹配到关键词就下了结论，没校验"这个组合里
   真的有它说的那个东西吗"。**这是一个真 bug**，但改判据要先确认 Quilt 失败的日志长什么样。
3. **"游戏崩溃了，启动器却还是显示在运行"**：证据显示后端**是**正常工作的
   （退出标记写了、事件也带了 `instanceId`、前端桥接把 payload 原样传下去），
   所以要么是那一瞬的竞态、要么界面停在别的实例上。**没有复现，不猜着改。**

### 五、还要你给一条信息

* **"下载栏会被挡住"**：截图里看到的是任务卡片（`Minecraft 26.2 + Quilt 加载器 完成`）
  压住了版本列表。请说一下被挡的是**底部那条"约下载 xx MB · 可占用 …"的摘要条**，
  还是别的 —— 我要知道具体是哪个元素，才能定层级而不是乱调 z-index。

* **"向下拉下去后一堆叠在一起"**：截图里那个世代标题行确实糊成一团
  （`26.2` 和 `1 个` 叠着）。这是我上一轮加折叠时**带出来的**（新加的
  `.ver-group-btn` 是按钮，里面的 flex 布局在滚动容器里没撑住），下一轮按真实 DOM 修。

---

## 0.1.0-beta.15 — 2026-09-15（第二十七轮：不兼容的附加组件会把人**卡死**）

用户（配图）："这怎么警告不让装了之后又不让点，不应该是直接不让点吗"。

### 那一屏为什么会卡死

截图里的状态是：`Fabric 1.21.11` + **已选中的 OptiFine**，红条写着"当前组合不可用"，
安装按钮被拦住 —— 而 OptiFine 那个选项**是灰的、点不掉**。于是：

* 用户取消不掉它（不兼容 → 按上一轮的规矩被 `disabled`）；
* 安装按钮又因为组合非法被拦；
* **界面上没有任何出路。**

### 两条规矩打架了，得一起满足

| 规矩 | 出处 |
|---|---|
| 不兼容的选项**不让选** | 用户上一轮："为什么不直接不让选来的实在" |
| 不兼容要**提示**，不要**悄悄替他取消** | 用户更早一轮："应提示玩家不兼容" |

两条都成立时的正确做法是**三者一起**：

1. **不让选**（不兼容的选项禁用）—— 保持；
2. **不让卡**：已经选中的**永远可以取消勾选**（`disabled = disabled && !selected`）；
3. **换加载器/版本导致不兼容时，把它去掉并说出来**：弹一条
   「已取消「高清修复」—— <原因>（换加载器/版本后它不再兼容）」。
   ★ 旧注释明确写了"不要自动移除"，但它针对的是**悄悄移除**（用户勾了却没了、
   只留一句原因）；**移除 + 明确告知**不违反那条规矩，而且它是这一屏唯一的出路。

★ 只在**选择本身变了**（基础加载器 / 加载器版本 / MC 版本）时才做第 3 条 ——
不跟着 `verdict` 跑；否则用户刚勾上、后台清单才回来判成不兼容时，
那一下也会被自动取消，又回到"勾了却没了"的老毛病。

---

## 0.1.0-beta.14 — 2026-09-15（第二十六轮：版本列表按世代**折叠**）

用户建议："做一个版本折叠功能"（配图是分组标题行 + 一个展开的组）。

清单有 900+ 个版本，全铺开就是一堵墙。规则三条（都写进代码注释了）：

| 规则 | 为什么 |
|---|---|
| 世代标题行可点，折叠/展开这一组 | 这是用户要的那个动作；标题行本身变成 `<button>`，带 `aria-expanded` |
| **默认只展开"含当前选中版本"的那一组**，其余收起 | 一眼看到"我在哪"，不用先滚过 20 个世代 |
| **搜索时全部展开** | 搜了却看不到结果是最气人的 |

实现细节：

* 折叠状态**只记用户手动点过的组**（`foldOverride`），没点过的按上面的规则算 ——
  这样"默认收起"能跟着选中版本走，清单刷新也不会把用户的展开状态冲掉。
* 收起时那一组的行**不渲染**（不是 CSS 藏起来）：900 个版本里渲染 200 个 DOM
  和渲染 20 个差别很大。
* 标题行加了旋转的小箭头（`prefers-reduced-motion` 下不动画）。

★ 这一轮同时**回归验证了上一轮的 key 修复**：把那六步又跑了一遍
（初始/快照/正式版/全部/正式版/快照），每一档的分组都对得上 ——
「正式版」两处都只有正式版，没有再出现新旧混排。

---

## 0.1.0-beta.13 — 2026-09-15（第二十五轮：筛选"卡住"的真凶 —— **React 的 key 撞了**）

用户给了一个**极准的复现**："点完快照后点正式版，不会显示正式版；点完全部后，
无论选哪个都显示全部"。

### 按这个复现跑一遍，真机数据立刻说明问题

| 点击 | 分段控件高亮 | 渲染出来的分组 |
|---|---|---|
| 初始 | 正式版 ✓ | `26.2(1)`、`26.1.2(3)`、`1.21(12)`… **对** |
| 快照 | 快照 ✓ | 只有快照 **对** |
| **正式版** | **正式版 ✓** | `26.2-rc-2(15)`、`26.1.2-rc-1(1)`、`26.2(1)`、`快照(1)`… **新旧混在一起** |
| 全部 | 全部 ✓ | 混合 |
| 正式版 / 快照 | 跟着变 ✓ | **永远是"全部"那份** |

**高亮一直在动，列表却冻住了** —— 这不是筛选逻辑的问题（`channel` 只有一个状态、
一处使用，`filtered` 的依赖数组也对），而是**渲染层**。

### 真凶：分组 key 用了**世代键**

```tsx
groupByFamily(filtered.slice(0, 200), (v) => v.id).map((g) => (
  <div key={g.fam.key} …>            // ← 这里
```

`g.fam.key` 是**世代**键，而 **`26.2-rc-2` 与 `26.2` 的世代键是同一个**（都是 `26.2`）。
于是换筛选前后，两次渲染的分组 **key 一一相同** → React 按 key 复用节点，
上一次的分组**没有被移除**，新的又插进来 → 就成了"新旧混在一起、之后再也不变"。

### 修法

* 列表容器：`key={channel|query}` —— 换筛选时**整列表重建**（几十个节点，肉眼无感）；
* 分组 key 带上渠道与首行 id：`${channel}|${g.fam.key}|${g.rows[0]?.id}`。

### 说明：上一轮（beta.11）我为什么以为修好了

上一轮我只验了**初始状态**（点「正式版」时它本来就是正式版，等于没切换），
所以"正式版那档是干净的"被当成了修复生效。**这一轮把六步全跑了一遍**才发现
问题在**切换**上 —— 教训：验证"能切换的组件"必须**真的切一遍**，
只验初始状态等于没验。

---

## 0.1.0-beta.12 — 2026-09-15（第二十四轮：首次启动把游戏语言设成中文）

用户："我希望在玩家首次启动游戏时，游戏语言默认是中文"。

### 为什么这是启动器该做的事

原版 Minecraft **永远**默认英文（它不跟随系统语言）。新玩家第一次进去看到的是
`Singleplayer / Multiplayer / Options`，很多人第一件事就是去翻语言菜单 ——
PCL 也是启动器替玩家把这一项写好。

### 两条硬规矩（先定规矩再写代码）

| 规矩 | 为什么 |
|---|---|
| ① **只在玩家还没选过语言时才写** —— `options.txt` 里已经有 `lang:` 就**一个字节都不动** | 玩家把语言改成英文/日文之后再启动，启动器把它改回去，**比"没帮他设"严重得多**。这条单独一条测试钉住（`existing_language_choice_is_never_overwritten`，逐字节比对） |
| ② **语言代码跟着版本走**：1.11 起是 `zh_cn`，1.11 之前是 `zh_CN` | 写错了游戏当无效值**静默回落到英文** —— 看起来就是"这功能没生效"。★ 本机正好同时有 1.7.10 和 1.12.2 两个实例，跨在这条线上 |

### 实现（`src-tauri/src/game/locale.rs`，新文件）

* `options.txt` 不存在 → 建它，内容就是 `lang:zh_cn` **一行**。
  ★ 只写一行是**安全**的：原版读这份文件时认识的键就用、缺的用默认值，
  进游戏后它自己会补全写回。不伪造一整份 —— 那等于把我们对默认值的猜测强加给玩家。
* 文件在但没有 `lang:` 行（别的程序建的）→ **只追加一行**（原有内容原样保留，
  末尾没换行时先补换行，否则会和上一行粘成 `fov:0.5lang:zh_cn` 整行作废）。
* 有 `lang:` 行 → `LeftAlone`，什么都不做。
* 写失败**不拦启动**（这是锦上添花），但会写进日志与 stderr ——
  否则"语言没变成中文"会变成一个查不到原因的现象。

### 测试（6 条，全过）

语言代码随版本变（1.7.10/1.8.9/1.10.2 → `zh_CN`；1.11/1.12.2/1.20.1/26.2 → `zh_cn`）、
首次建文件、老版本用老代码、**玩家选过就不动**、缺 lang 时追加、末尾无换行时补换行。

---

## 0.1.0-beta.11 — 2026-09-15（第二十三轮：正式版里混进 rc/pre —— 判据改成"两个条件都满足"）

用户第二次报同一个现象（并说"学学 PCL 怎么做的"）。上一轮我改了
`isSnapshotVersion` 的正则，**没用** —— 这一轮把链路一段段读穿了：

| 环节 | 实测结果 |
|---|---|
| 上游清单（缓存文件） | ✅ `26.2-rc-2 → type=snapshot` |
| Rust `fetch_version_manifest` | ✅ 原样透传（用 CDP 直接调命令读**原始行**确认：`26.2-rc-2 → "snapshot"`） |
| 前端桥接 | ✅ 整个 `ManifestSummary` 直接透传，没有二次映射 |
| 前端筛选逻辑 | ✅ 看着是对的（`release_type !== 'release'` 就排除） |
| **真机界面** | ❌ 还是混着 rc/pre —— 所以**某一处我还没找到** |

★ **没有再猜下去，而是把判据改成"不可能被上游字段打败"**（这也是 PCL 的做法：
它判断版本类型时同时看清单的 `type` **和版本号本身**）：

```ts
const finalRelease = r.release_type === 'release' && FINAL_RELEASE_RE.test(r.id);
```

`FINAL_RELEASE_RE = /^\d+\.\d+(\.\d+)?$/` —— 只有 `x.y` / `x.y.z` 才算最终版。
**两个都成立才放行**：上游字段（或缓存、或以后新增的某个来源）再出任何问题，
`26.2-rc-2` / `26.2-pre-6` / `24w14a` 这类 id 都永远进不了「正式版」那一档。

### 真机验证（桌面版读实际渲染的 DOM）

| 时刻 | 结果 |
|---|---|
| 初始（正式版） | 分组：`26.2 (1 个)`、`26.1.2 (3 个)`、`1.21 (12 个)`、`1.20 (7 个)`… **没有任何 rc/pre** |
| 点一次「正式版」之后 | 同上，完全一致 |

★ 如实说明：**"为什么只看 `release_type` 在真机上会漏"这一处我没找到确切原因**。
我找到的是"上游、桥接、筛选逻辑单独看都是对的，但合起来在真机上就是漏"——
所以改成双判据：它不依赖对那处未知原因的猜测，而且**有明确的、可验证的收敛效果**
（真机上已经验过）。这条不算"查清了"，只算"修好了"。

---

## 0.1.0-beta.10 — 2026-09-15（第二十二轮：「跳 1 帧」的根因：**把"还没读到"当成了"盘上没有"**）

用户把那一帧截图发来了（"你要的一帧"），一眼就看清了：版本列表里
`Fabric 26.2`、`Forge 1.12.2`、`Fabric 1.21.11` 三行都挂着 **「缺 Fabric」「缺 Forge」**——
而用户说得很准："但其实都是正常的"（那几个加载器确实装过、也跑起来过）。

### 根因

`refreshDisk()` 是**异步**的（拉一份清单，把每个 MC 版本在盘上装了哪些加载器填进
`diskLoaders`）。而 `diskConflictOf` 的判据是：

```ts
const base = onDisk.find((l) => l.loader_type === recorded.kind);
if (!base) return { short: `缺 ${loaderName(recorded.kind)}`, … };
```

**首帧 `diskLoaders` 还是 `{}`** —— 于是每一行都被判成"盘上没有任何 Fabric"，
角标亮起；等清单回来（几百毫秒后）再翻回去。这就是"跳 1 帧"。

★ 这条错这个仓库已经写过好几遍：**读不到 ≠ 没有**（`磁盘上没扫到` 与
`我们还没扫` 是两件事）。这次它换了个地方冒出来：**异步加载中的空值被当成了事实**。

### 修法

加一个显式的 `diskLoaded`：

* 读到过 → 正常判冲突（真的缺就标出来，这是有价值的信息）；
* **还没读到 → 不下任何结论**（不显示角标）；
* 读失败 → 仍然不下结论（`diskError` 那条"读不出来"的提示才是该出现的东西）；
* 浏览器演示模式没有磁盘可读 → 直接标记成"读过"，否则角标永远不出现也不消失。

### 真机验证（桌面版，两个时刻各拍一次）

| 时刻 | 结果 |
|---|---|
| 切到版本列表的**首帧** | 角标 **0 个** —— 不再闪「缺 X」 |
| 等清单读回来之后 | 角标 **0 个**，并且每行显示出真实信息「盘上 Fabric 0.19.5 Forge 65.1.3」 |

★ 第二行同时证明了两件事：磁盘读取**是成功的**（信息是真的），
以及用户截图里那些「缺 X」**确实是首帧那一瞬的假话**，不是检测错误。

---

## 0.1.0-beta.9 — 2026-09-15（第二十一轮：Mod 列表变快 · 添加入口改路 · 原版不做 Mod）

用户（配两张图）提了 4 条。

### 一、Mod 列表**加载慢**：根因是"每个 jar 都联网反查，且在此之前列表是空的"

`reloadMods` 原来是**一次** `modrinth.scanMods(...)`：读盘 + 对每个 jar 算
SHA1/指纹 + 逐个查在线库 —— 全部完成之前 `entries` 是空的。
装了几十个 Mod 的整合包就要等很久，观感是"白屏很久，然后一次性全出来"。

现在拆成**两段**：

| 段 | 做什么 | 用户看到什么 |
|---|---|---|
| ① 本地扫描（`backend.listMods`，毫秒级） | 只读文件名 / 大小 / 是否禁用 | 列表**立刻**出来（显示文件名） |
| ② 联网反查（后台） | SHA1 + 指纹 → Modrinth / CurseForge | 名称、版本、支持情况**补上** |

★ 没有造假：第一段显示的是**文件名**（不是编出来的名字），补全期间那一行明确写着
**「正在核对在线库…」**（新加的 `Spinner`），否则用户会以为"这就是它的名字"。

### 二、「添加 Mod」改成**跳下载页的 Mod 页签，并默认选中这个版本**

以前它弹一个 Modal —— 于是"浏览 Mod"在启动器里有**两个界面**（弹窗一个、下载页一个），
两处的搜索/翻页/装到哪个版本都要各维护一遍。现在只留下载页那一个入口。

"默认选中该版本"的实现：下载页的 `targetId` 是它自己的局部状态，
所以从 Mod 管理页**发一个事件**（`ieml:download-target`）告诉它选谁。
★ 这条很重要：下载页默认挑的是"最近玩过的那个"，很可能**不是**你手上这个 ——
于是 Mod 会被装到另一个版本上，而且一眼看不出来。

### 三、原版实例**不再显示「Mod 管理」**

（用户："我觉得原版不应开放 mod 列表功能"）

纯原版不会加载 `mods/` 里的任何东西 —— 把入口摆在那儿，用户放进去的 jar
会**无声无息地不生效**，而他以为装上了。侧栏那一格直接消失；
「概览」页里"纯原版不能加载 Mod，要装请先在设置里加加载器"那句仍然在。

### 四、概览页顶部内容离分隔线太近

`.page-head` 只有 `padding-bottom: space-3`，那条分隔线紧贴着第一行内容
（概览页上是工具栏那一排）。加 `margin-bottom: var(--space-4)`，
不动 sticky 的定位计算。

---

## 0.1.0-beta.8 — 2026-09-15（第二十轮：删掉 OptiFine 的安装提示）

用户（配图）："关于安装高清修复的提示，也不需要"。

那一屏原来有两块 OptiFine 专用提示，**都删了**：

| 删掉的 | 原来在哪 | 为什么删 |
|---|---|---|
| 「将用 OptiFine 自带的 Patcher 在原版 jar 上打补丁（会在临时目录里跑，不会动你的原版）」 | 「安装前请确认」那张黄条（来源是 `combination.ts` 的 `judged.note`） | 那是**实现细节**，不是用户需要"确认"的事。用户要的是"勾上 → 装 → 能玩"；"会在临时目录跑、不动原版"是我们**自己该保证**的事，写进确认框反而像在让他承担风险 |
| 「OptiFine 会在装完原版后自动安装 …… 选中的是最新版」 | 组合区下方的 info 条（`InstallComposer` 的 `cw-notes`） | 它讲的是"我们打算怎么装"，用户不需要在点安装前读这个 |

★ **没有删掉桥接包那条**（Fabric 1.14~1.20.4 上 OptiFine 必须配 OptiFabric，
缺一个游戏会崩）：因为那是**功能性前提**，不是提示。而且删掉的那两块与它并不重复 ——
`judged.note`（带着下载地址）仍然会出现在「安装前请确认」里，所以
**没丢任何可操作的信息**。
（原来那两块提示的作用是"压掉打架的文案"：不能一边红字说不兼容、一边蓝字说会自动装。
现在蓝字没了，那两处“压制条件”连同它们服务的变量一起删了 —— 不留死代码。）

---

## 0.1.0-beta.7 — 2026-09-15（第十九轮：按用户第 8 批反馈改）

用户一次提了 8 条。逐条记，**没做完的和没验的写在最后**。

### 一、做完的（都当真机验过）

| # | 用户说的 | 做了什么 | 怎么验的 |
|---|---|---|---|
| 1 | 文字默认不可被选取，除非报告类 | `body { user-select: none }`，再把**输入框 / 日志 / 崩溃报告 / `.mono`** 用 `user-select: text` 放开（默认禁止、例外放行，反过来一定漏） | 真机读 `getComputedStyle`：文本 `none` |
| 2 | 整合包页还是大长条、没封面、没翻页 | 三处一起改：① 骨架换成**卡片形状**（原来 `Skeleton rows={4} height={110}` = 四条通栏长条）；② **接上真封面**（Modrinth 的 `icon_url` 一直有，是这一页没接）；③ **加翻页**（原来写死 `limit: 20` 且没有 offset） | 真机：骨架 6 张卡片；加载完 **20 个整合包 20 个有封面**；翻页显示「第 1 / 917 页 · 共 18337 个」 |
| 3 | 正版登录有两个关闭键 | 模态框右上角已有 ✕，footer 里那个「关闭」是重复入口 → 删 | 真机数 `.modal` 里的关闭类按钮：**1 个** |
| 4 | 两条清理功能重复，合并；存储移到外观右侧；窗口尺寸移回原位 | ① `clean_unused` + `clean_caches` 合并成**一次 dry run → 一次确认 → 一次执行**（确认框里逐类列出各自多少、哪些不动）；② 「存储」卡移到「外观」卡右边（同一个 `.set-cols` 网格）；③ 「窗口尺寸」搬回「新版本的默认值」 | 真机：外观 \| 存储并排；清理类标签只剩 **1 条**；`窗口尺寸` 在「新版本的默认值」里 |
| 5 | （图三）那个栏图层不对，且点空白不消失 | ① 菜单从 `absolute` 改 **`fixed`** —— 它住在 `overflow: hidden` 的列表容器里，absolute 会被**裁掉**（看起来像层级问题，其实是剪裁），坐标由按钮的 `getBoundingClientRect()` 算；② 点空白 / 滚动 / Esc **三种都关**（fixed 菜单不跟着滚，会浮在错的位置） | 真机：`position: fixed`、`z-index: 80`；点空白后菜单**消失** |
| 6 | 启动器不该被右键呼出浏览器菜单（图四） | 拦掉 `contextmenu`，但**输入框与"已选中文字"放行**（粘贴是输入框唯一的鼠标入口）；顺手拦掉 F5 / Ctrl+R 的刷新（意外刷新会让前端状态全部重建） | 真机：body 上的 contextmenu `defaultPrevented = true` |

### 二、版本筛选那一条：**判据修对了，但我没能复现用户看到的现象**

用户："选择快照版或全部后，再切回正式版，居然还会显示快照版"。

查证过程（一步步记，因为结论和直觉不一样）：

1. **上游数据是对的** —— 读启动器自己缓存的清单（`cache/version_manifest_bmclapi.json`，914 条）：
   `26.2-rc-2 → type=snapshot`、`26.2-pre-6 → type=snapshot`。Rust 侧**原样透传** `type`。
2. **前端筛选逻辑也是对的** —— `channel === 'release'` 时 `r.release_type !== 'release'` 全部排除。
3. **但 `isSnapshotVersion` 这个判据本身是错的**：`SNAPSHOT_RE = /^\d{2}w\d{2}[a-z]$/`
   **只认周更快照**，`-rc` / `-pre` / `-alpha` 一律被当成"正式版"。
   演示路径（内置版本子集）就在用它 → 那条路径上**确实会出现用户描述的现象**。
4. **改成问反面**：正式版只有 `x.y` / `x.y.z` 两种形状，其它一律非正式
   （`/^\d+\.\d+(\.\d+)?$/`）。判据自测 10/10：`1.20.1 / 26.2 / 1.7.10 / 1.21.1` → 正式版；
   `26.2-rc-2 / 26.2-pre-6 / 1.21.2-pre1 / 24w14a / b1.7.3 / a1.2.6` → 非正式。
5. **没复现**：我写脚本在真机上依次点「快照 → 全部 → 正式版」，那个版本列表面板在我的
   自动化里没抓到（选择器没命中），所以**"桌面版上到底走哪条路"我没能证实**。
   如实写在这里：判据改了（这是确定的改进），但用户看到的那一次**是不是**由它引起，我不知道。

### 三、没做的

* **「有模组加载器的版本会跳 1 帧」**（用户说"但其实都是正常的"）：这一轮没查。
  它是**渲染次序**问题（大概率是 loader 信息在首帧之后才到位），需要单独抓一次
  首帧前后的截图对比才能定位，不想凭猜改。

## 0.1.0-beta.6 — 2026-09-15（第十八轮：多开实例）

用户："**做多开实例**"。上一轮我把它单独留出来了，理由是它动到**启动状态模型**，
而"做错就是启动不了游戏"。

### 一、改的是什么：从一个槽 → 一张表

"同时只能开一个"这件事原来被**写死在三个层面**，所以它不是界面改改就行：

| 层 | 原来 | 现在 |
|---|---|---|
| 后端 | `AppState.running: Mutex<Option<RunningGame>>`（一个槽） | `Mutex<HashMap<String, RunningGame>>`（按**实例 id** 索引） |
| 前端状态 | `running: {instanceId,…} \| null` | `running: Record<string, {startedAt, pid}>` |
| 判据 | 各处自己写 `running?.instanceId === x` | **`isInstanceRunning` 一处**（`runningInfo` / `runningCount` / `runningInstances` 同源） |
| 停止命令 | `stop_minecraft()` **不带参数**（只有一个能停） | `stop_minecraft(instance_id)` —— 不点名就等于"随机停一个" |

★ 用 **HashMap 而不是 Vec**：判据只写一遍 —— "在不在跑"是 `contains_key`、
"哪几个在跑"是遍历、"停哪个"是 `remove`。用 Vec 就得在每个调用点各写一遍查找。

★ **键是 `instance_id` 而不是 slug**：slug 是磁盘目录名（可改、可重复的显示名），
退出事件、前端实例表、日志文件名认的都是 id。前端 `LaunchRequest` 因此新增
`instance_id` 字段（`#[serde(default)]` 兼容旧前端）。

### 二、顺带修掉的两个真问题

1. **同一个实例重复启动的判据换成"这一个"**：原来拦的是"**有没有**游戏在跑"，
   现在是"**这一个**在不在跑" + `retain` 顺手清掉表里已经死掉的条目。
2. **界面重新加载后状态会丢**：前端的运行表是**攒**出来的，而它可能被重新加载 ——
   那一刻本地是空的、后端的子进程还活着，界面就会说"没有游戏在运行"，
   用户再点一次启动，**同一个存档被两个进程写**。新增命令 `running_games`
   （顺手 `retain` 掉死的），boot 时用它覆盖本地表。

### 三、事件与监听器的一个隐患（顺手治了）

`AppContext` 那个注册监听器的 effect 原来依赖 `[state.running]` —— 每次开关游戏
都**重新注册一遍全部监听器**，目的只是"让闭包里的 state 别太旧"。
多开之后 `running` 变得更频繁，这个治标办法更站不住了：
改成 `stateRef.current` + 依赖 `[]`，监听器只注册一次，读到的永远是最新 state。

### 四、界面

* 侧栏底部从"那一个"改成**一列**：每个在跑的版本各一行（名字 + 已运行多久 +
  自己的停止按钮），两个以上时先给一行"**N 个在运行**"。
* 启动页：多开时提示"现在有 N 个版本同时在跑 —— 内存各占各的，侧栏底部可以逐个停止"。
  ★ 这条的判据改过一次：第一版是"别的在跑而当前这个没在跑"，理论上更精确，
  但**我三次都没能把"把目标切到另一个版本"这个动作自动化验证出来** ——
  一个我证不了的提示，不如一个朴素但能证的（现在：表里超过一个就提示）。

### 五、验证（三层，都是实测）

| 层 | 手段 | 结果 |
|---|---|---|
| 状态模型 | 新增 `tests/multi-instance.test.mjs`（8 条，已挂进 `pnpm test`） | ✅ 两个能共存 / **停一个不影响另一个** / sync 以后端为准 / 删实例后不崩 / stop-all |
| 后端表语义 | `launch.rs` 新增 3 条**真进程**测试（`ping` 冒充两个游戏，不碰 Minecraft） | ✅ 两个同时在表里 / 停 a 后 b 还活着 / `retain` 只清死的那个 |
| 真机界面 | 新脚本（CDP，桌面版）：两个 `ieml:started` → 查 DOM → 点其中一个停止 | ✅ 两行各带停止按钮、标题"2 个在运行"、**停一个之后只剩另一个** |

★ 一条测试暴露了我自己的错：`stop()` 对"不肯优雅关闭"的进程会强杀并返回一条
**警告**（`Err` 里装的是"已强制结束、存档可能没保存"），调用方本来就是按
`warning` 用的。我第一版测试写了 `.expect()`，于是误判成失败 —— 改成断言
"进程真的没了"，并把这条契约写在测试注释里。

★ 还抓到一个**类名撞车**：新提示的类名第一版叫 `.launch-others`，
而"其它版本"那一块本来就叫这个 —— 于是那条 CSS 顺手改了别的区块。
是 CDP 查那个选择器时**读到了"其它版本"的卡片文本**才发现的。改名 `.launch-multi`。

### 六、没验的（如实写）

* **没有真的同时启动两个 Minecraft**：那要占两份内存（每个 4–6 GB）并往屏幕上
  放两个游戏窗口。所以"两个真游戏同时在跑"这一步是用户第一次双击时才算数；
  我验的是它下面三层（状态模型 / 表的语义 / 界面与事件）。
* 「还有 N 个在跑」那条提示的**第一版判据**没能自动验证，已按上面第四节的说明换掉。

---

## 0.1.0-beta.5 — 2026-09-15（第十七轮：按用户 7 张截图逐条改）

用户一次提了 20 条（配了 7 张图）。逐条记录，**没做完的写在最后一节**。

### 一、用户报的 bug（都有实证）

| # | 现象 | 真正的根因 | 修法 |
|---|---|---|---|
| 1 | **右上角「最大化」点了没反应**（最小化/关闭正常） | `toggleMaximize()` 调的是 `plugin:window\|toggle_maximize`，而 capabilities 里**没有 `allow-toggle-maximize`** —— 权限拒绝，而我把 Promise `void` 掉了 → **静默失败** | 补权限；并且窗口操作失败**弹 toast**，不许再静默 |
| 2 | **顶栏切主题时设置页不同步** | `useState(state.theme)` 只在挂载时取一次值 | `useEffect` 跟随 `state.theme`（只监听真实主题，不监听用户选择，避免两向绑定打架） |
| 3 | **「从未启动 / 累计时长 –」是骗人** | 实测确认：`lastPlayedAt` / `totalPlaySeconds` 在 Rust 侧**只有类型定义**、只有浏览器演示数据里有假值，**真机从来没被写过** | 在 `game-exit` 事件的收口处落账 —— 后端这一条路**两条路都推**（自己关掉/崩溃/被停止），所以只写一处就不会漏 |
| 4 | **滚动条上下两个三角** | 有两层原因，**第一层修完三角还在**（见下） | ① 关掉 `::-webkit-scrollbar-button`；② **删掉 `* { scrollbar-width: thin; scrollbar-color: … }`** —— 只要元素上设了标准属性，Chromium 就走标准渲染、`::-webkit-scrollbar-*` 全部失效。两处都改完才算修好 |

★ 第 4 条是**放大截图看出来的**：headless Chrome 用悬浮滚动条、而且我们的截图脚本
带 `--hide-scrollbars`，根本拍不到箭头，所以那条只能在桌面版（WebView2）里拍 ——
顺带记下一个坑：**WebView2 会复用上一次没有调试参数的浏览器进程**，
要抓 CDP 得先把旧的 `msedgewebview2` 一起杀掉。

★★ **这条修复值得单独记一笔：我第一次只改对了一半。**
第一次加了 `::-webkit-scrollbar-button{display:none}` 就重建部署，拍出来**三角还在** ——
因为 `app.css` 里那两行标准属性（`scrollbar-width/scrollbar-color`）把整个
`::-webkit-scrollbar` 体系顶掉了。**如果没有再看一眼截图，我就会带着"已修复"的结论交付。**
教训：修外观类问题，"改完了"和"看起来对了"是两件事，中间必须有一次**重新观察**。

### 二、用户要的改动（14 条）

* **资源中心加载骨架改成卡片形状**（图1+图2）：原来是 5 行等宽长条，在卡片网格里
  就是"一整块灰板"，加载完却是卡片 → 布局会跳。现在骨架与真卡片**同一套网格与结构**。
* **下载页去掉上下重复**（图7）：内层的 Mod/资源包/光影/数据包 四个页签与外层完全重复
  → 给 `ResourceCenterBody` 加 `hideKindTabs`；顺带删掉「管理这个版本的资源」那个绕路按钮。
* **下载页版本栏放大**：下拉高度 40px、字号提到 `--text-md`、宽度上限 420→520，
  版本号加粗（"很费眼，太小了"）。
* **设置页外观劈成两栏**（图5）：左栏主题 + 减少动效；右栏**窗口尺寸** ——
  它原来在「新版本的默认值」里，本身就是错位的（窗口多大属于外观）。窄窗口自动退化单栏。
* **删掉设置页的账号栏**、**删掉关于里的字体声明**（那两行说的字体我们早就不带了）。
* **版本设置页行距放宽**：`.stack-roomy`（只作用于这一页，设置页保持紧凑）。
* **Mod 管理删掉那句声明**（"不会自动更新 / 原地覆盖"那段）。
* **「与当前加载器不兼容」直接不让选**：以前 `verdict.removed` 只用来画个红角标、
  开关照旧点得动 → 现在把 `removed` 也算进 `disabled`；**理由照常显示**（不让选，但不瞒着）。
* **三个点菜单补齐图标**：重命名/创建副本/删除 都补上图标（打开目录/打开设置/安装 Mod/启动本来就有）。
* **删掉正版登录里的「应用 ID」声明块**（图4）。
* **左侧栏 218 → 196px**。

### 三、没做完的（如实写）

* **允许多开实例**：这是这批里唯一动到**启动状态模型**的。现状是后端
  `AppState.running: Mutex<Option<RunningGame>>`（单槽）、前端 `state.running` 单值，
  涉及后端 3 处 + 前端 10 处。**没有在这一轮硬塞** —— 它一旦做错就是"启动不了游戏"，
  而这一轮我已经改了 20 处；下一轮单独做，并且先补一条"两个实例同时在跑"的测试。
* **网络优化**：先量了。`tools/probe/probe-sources.mjs` 的实测：直连 Mojang / Modrinth /
  Adoptium / Fabric Meta 全部 `fetch failed`（这台机器的网络到不了），而 **BMCLAPI 通**
  （清单 269.8 kB / 3057ms）、Forge Maven 通（206.5 kB / 2609ms）、OptiFine 清单通
  （198.8 kB / 838ms）。也就是说：**现在真正决定速度的是"走没走镜像"，而不是连接数** ——
  引擎已经在做的（单连接为主 + 失败换源 + 429 退避 + 并发可调 + 同内容去重）覆盖了
  目前能确认的瓶颈。**没有凭感觉再改**；要再快得先做一轮"镜像测速预选"的测量，
  确认预选比现在的"失败才换"更快，再动手。

---

## 0.1.0-beta.4 — 2026-09-15（第十六轮：深色主题重做 · 自绘标题栏）

用户自己动手改了界面，我读完之后修了几处、又补了一件他明确要的事。

### 一、用户做的设计改动（6 个文件，+290/−112）

* **深色主题整体换了一套**：边框从"白透明"改成**实色灰**（`#262c36 / #303743 / #3c4450`），
  侧栏/顶栏比内容区**更深**（`0f1217` vs `12151a`），文字三级走实色不叠透明度。
  层次靠明度递进，不靠白色覆盖。
* **新增一组组件令牌**：`--card-bg / --card-inner / --btn-primary-* / --shadow-card /
  --scroll-thumb-hover`；卡片与主按钮改成「1px inset 顶光 + 纯平色」。
* **新增 `CustomSelect`**（`ui/index.tsx`）：替换原生 `<select>`（浏览器画的那个深色下确实丑），
  点外部关闭、箭头翻转、当前项高亮。
* 导航项左侧加 3px 高亮条、`.btn-lg` 放大到 52px、chip 改胶囊形、滚动条变细。
* 启动页：图标 56→72、四个 chip 收成一行、动作按钮 secondary→ghost、下拉换 `CustomSelect`。

### 二、我在这批改动里修掉的 5 处（都是实测出来的）

| # | 问题 | 怎么发现的 | 修法 |
|---|---|---|---|
| 1 | **类型检查红了** | `tsc`：`IconChevronDown` 已无人使用 | 删掉 import |
| 2 | **`DownloadPage.tsx` 被写进 BOM** | 前 3 字节 `EF BB BF`，其余文件都没有 | 去 BOM |
| 3 | **下拉菜单比按钮宽一倍** | 量的：外层 `.cs=880` / 按钮 `=420` / **菜单 `=880`** | `.res-target .cs{max-width:420px}` → 现在 420/420/420 |
| 4 | **换掉原生 select 时丢了无障碍** | 原 `aria-label="装到哪个版本"` 无处安放；Esc/方向键也没了（原生自带） | 加 `ariaLabel`、`role=listbox/option`、Esc/方向键/回车、键盘光标样式 |
| 5 | **浅色主题下氛围渐变变成一块蓝污渍** | 截图对比：右下角明显一坨蓝 | `body::before` 的写死 rgba 抽成 `--ambient-a/b`，浅色收到约 1/3 |

★ 第 3、5 条**只读 diff 看不出来**，是跑起来量尺寸、对比修前修后截图才发现的。

### 三、自绘标题栏（用户："这个白色的条，并进软件里，这个额外的在外面，很丑"）

`tauri.conf.json` 里 `decorations: false` + 顶栏自己承担原生标题栏的两件事：

* **拖动**：`data-tauri-drag-region`。Windows 上 Tauri 发的是
  `WM_NCLBUTTONDOWN + HTCAPTION`，所以**双击最大化、拖到边缘吸附都是原生白送的**——
  自己再实现一遍反而会打架（双击变"最大化又立刻还原"）。
* **窗口按钮**：新组件 `components/WindowControls.tsx`（最小化 / 最大化-还原 / 关闭），
  图标随 `isMaximized()` 变；**浏览器演示模式不渲染**（没有窗口可管，不摆点了没反应的按钮）。
* 权限：capabilities 里补 `core:window:allow-is-maximized`（其余三个早就在）。

**验证（真机，不是看配置）**：

| 检查 | 结果 |
|---|---|
| 顶栏 `data-tauri-drag-region` | ✅ 在，且顶栏矩形 `y=0` —— **webview 从窗口最顶端开始，上面没有原生条** |
| 三个窗口按钮 | ✅ 都在、都可见（x=1060/1098/1136，36×30） |
| 点最小化真的最小化吗 | ✅ 系统侧读 `IsIconic`：点击前 `False` → 点击后 `True` → 还原后 `False` |
| 还能拖边框改大小吗 | ✅ `WS_THICKFRAME` 仍在（这条本来担心会被 `decorations:false` 一起干掉） |

★ 新检查脚本 `tools/live/live-titlebar-check.mjs`（可重复跑）。
★ 里面记了一个**我自己的错误**：原以为"最小化后 `document.visibilityState` 会变 hidden"
—— WebView2 里**不会**，那个判据会让脚本谎报"点了没反应"。真正可靠的信号在系统侧
（`user32!IsIconic`），脚本证不了的事它就不假装证得了。

### 四、验证与交付

* `tsc` 绿、`vite build` 绿；`pnpm verify` 15 项全绿。
* exe 重建并部署；真机跑了一遍看效果（本节的结论都来自那次运行）。

---

## 0.1.0-beta.3 — 2026-09-15（第十五轮：瘦身 · 数据布局 · 账号提到顶栏）

用户这一轮提了 5 件事：仓库 50 GB 要深度清理、本体别那么大、用系统自带字体、
压缩启动器数据、游戏数据放进数据目录里的 `.minecraft`（像 PCL）、正版登录要显眼。

### 一、50 GB 的真相：**是构建缓存，不是你的数据**

先量后删（用户要"深度清理"，那就得先知道清的是什么）：

| 位置 | 清理前 | 是什么 |
|---|---|---|
| `src-tauri/target/debug/incremental` | **28.8 GB** | 增量编译缓存（纯缓存） |
| `src-tauri/target/debug/deps` | **16.9 GB** | 其中 **13.5 GB 是 `.pdb` 调试符号** |
| `src-tauri/target/debug/build` | 2.6 GB | 依赖的构建脚本产物 |
| `src-tauri/target/release` | 3.3 GB | 发布构建缓存（保留，否则每次打包都从头编译） |
| `node_modules` / `dist` / `src` / `docs` | 0.1 GB | 真正要留的东西 |
| **E:\IEML 合计** | **50.77 GB** | **交付物 exe 只有 12.6 MB** |

删除 `target/debug` 后：**50.77 GB → 3.36 GB（释放 47.4 GB）**。

★ 光删是不够的（下次编译又会长回来），所以同时改了 `Cargo.toml` 的 dev/test profile：
`debug = "line-tables-only"`（保留 panic 的行号，但不再生成十几 GB 的类型/变量调试信息）
+ `incremental = false`（不再写那 28.8 GB 的增量缓存）。两条都写了理由。

★ 顺手删掉 `src-tauri/` 下两个 Forge 安装器日志（`.jar.log`）——第十二轮清了仓库根，
这两个在 `src-tauri` 里漏了。

### 二、启动器数据 1.73 GB —— 大头是**游戏文件**，不是垃圾

实测量了 `D:\IEML`（数据目录）：

| 子目录 | 大小 | 性质 |
|---|---|---|
| `shared/assets` | 816 MB | 真正的游戏资源文件 |
| `shared/libraries` | 587 MB | 真正的库文件 |
| `shared/versions` | 269 MB | 已装版本本体 |
| `instances/` | 74 MB | 存档 / Mod / 配置（用户的） |
| `cache/` | 27 MB | **可再生**：Forge/OptiFine 安装器 + 元数据缓存 |
| `logs/` | ~0 MB | **可再生**：每次启动一份 |

所以能安全回收的只有后两类。新加一条命令 `clean_caches`（两段式：先算再删）：

* 清 `cache/`：安装器 + 元数据 JSON（要用时重新下载）
* 清 `logs/`：只留最近 **5** 份（崩溃分析读的是最近那份，不能全删）
* ★ **一个游戏文件都不碰** —— 与已有的 `clean_unused_files`（清"没有任何版本引用的
  库与资源"）判据不同，所以是**两条命令**，而不是合成一个"一键清 1.4 GB"的按钮
  （那 1.4 GB 是用户的游戏，删了就得重下）。

### 三、本体瘦身：**不再随程序附带字体**

上一轮按用户要求打包了 `HarmonyOS_Sans_SC_Medium.ttf` —— 一个字重 **8.2 MB**，
而本体一共才 8 MB 出头。用户这一轮说"应该用电脑自带且通用又好看的字体"，
于是改回**系统字体栈**：

```
system-ui → Segoe UI Variable Text → Segoe UI → Microsoft YaHei UI
→ PingFang SC(macOS) → Noto Sans CJK SC(Linux) → HarmonyOS Sans SC(装了就用)
```

* 删除 `src/assets/fonts/`（字体 + 许可），删掉 `@font-face`；
* 前端产物 **8.67 MB → 442 kB**（JS 370.8 + CSS 71.4 + html 0.6）；
* 「关于」页的声明**同步改掉**（原来写着"随程序附带 HarmonyOS Sans SC"——
  不删就是一句假话），并加一行解释"为什么带不起字体"。

### 四、游戏数据搬进 `.minecraft`（用户要求：像 PCL）

`<数据根>/shared/` → **`<数据根>/.minecraft/`**（`libraries` / `assets` / `versions`）。
全仓库 45 处引用都走 `AppPaths.shared` 这一个字段，所以换位置只改了一行
（字段名仍叫 `shared`：**"共享"是职责，`.minecraft` 是位置**）。

迁移用**同卷 `rename`**（原子、1.4 GB 瞬间完成），不是逐个复制；五种情况都写了测试：

| 情况 | 行为 |
|---|---|
| `shared/` 在、`.minecraft/` 不在 | 改名 |
| `shared/` 在、`.minecraft/` 是**空壳** | 删空壳再改名（`ensure()` 每次启动都会建它，不处理就永远搬不动） |
| `.minecraft/` 里有东西 | **一个字节都不动**（不猜那是残留还是用户自己的东西） |
| 两个都不在 | 什么都不做（全新安装） |
| 再跑一次 | 什么都不做（幂等） |

跨数据根搬家那条路（`migrate_data_root`）也跟着改了：老根的 `shared/` 落到新根的
`.minecraft/`，新根如果已是新布局也照搬。

### 五、正版登录提到**顶栏**

用户："正版登录界面应该放在显眼位置，而不是藏起来"。

* 新增 `components/AccountPanel.tsx`：把整条登录流程（设备码 → 自动轮询 →
  三种失败 → 离线模式切换）**收成一份实现**；
* 顶栏加一个账号按钮（状态点 + 当前身份 + 「正版 / 登录」标签），点开就是这个面板；
* 设置页那张卡改成同一个组件（**一份实现，两处使用**，ADR-051 的规矩）；
* 理由写在代码里：账号不是"设置"（改一次就不动），而是**状态**
  （我现在是谁、能不能进正版服务器），所以它该一直看得见。

### 六、真机验证时**抓到两个自己的 bug**（都修了，都写了回归测试）

**① 布局迁移与"跨根补齐"的**顺序**错了 —— 差点让用户以为版本丢了**

第一次真机验证（跑的是我自己刚构建的 exe）结果是错的：

```
启动前：D:\IEML\{shared, instances, cache, logs, …}      ← shared 里是真数据
启动后：D:\IEML\{.minecraft, shared, instances, …}       ← .minecraft 凭空出现
        .minecraft\assets = 6754 个文件 / 1079 MB        ← 而真数据还在 shared 里
        shared\assets     = 7958 个文件 / 816 MB
```

`.minecraft` 里那 6754 个文件的**修改时间还是 9/11 与 9/13** —— 说明它们不是新下载的，
是从别处**复制**过来的。查下来是 `%APPDATA%\IEML\shared`（几个月前留在 C 盘的旧数据，
6754 个文件 / 1079 MB）——**我把"跨数据根补齐"的循环放在了布局迁移前面**，
于是它按新映射把 C 盘的旧数据复制成了 `D:\IEML\.minecraft`；
随后布局迁移看到 `.minecraft` 已有内容，按它自己的规矩跳过（**那是对的**，
它不能去猜用户目录里是什么）→ 真数据留在 `shared`，而启动器会去读空荡荡的 `.minecraft`。

修法两条：
* `lib.rs` 里**把本根的布局迁移放到最前**（同卷 `rename`，用的就是真数据），
  跨根补齐排到它后面 —— 那段顺序要求写成了注释，免得以后被"整理代码"挪走；
* `migrate_data_root` 不再死映射 `shared → .minecraft`，改成**看目标根现在是什么布局**
  （目标还有 `shared/` 就放进 `shared/`），并加了回归测试
  `migration_respects_the_destination_layout`。

修完后真机再跑一次，这次是对的（`.minecraft` 里 7958 / 419 / 29 个文件，与迁移前**逐一相同**，
`shared/` 已消失）。

**② 数据目录里躺着一个假 client_id，会把**能用**的那把挤掉**

同一次验证的启动日志里露出：

```
[IEML/auth] 已载入正版登录用的 client_id（11111111…）
```

`ms_client_id.txt` 里是 `11111111-2222-3333-4444-555555555555` —— 一个手打的占位符 GUID。
而优先级是"文件 > 环境变量 > 内置"，于是它把**实测可用**的内置 id（Prism Launcher 公开 id）
挤掉了：用户点登录只会拿到 `AADSTS700016`，界面还写着"正在用你自己填的"。

修法：把"像不像占位符"抽成一个判据 `looks_like_placeholder`（全零 / 连号 / 同字符重复的 GUID），
**保存时拒绝**（以后写不进去）、**启动时忽略**（已经在里面的不再生效，回落到内置），
但**不删用户的文件**。4 条测试守着它，包括"真的 id 一个都不许误伤"。
★ 其中 `empty_is_not_a_placeholder` 这条**测试先抓出了我自己的 bug**：
原写法只过滤 `-`，于是 `"   "`（全空格）会命中"所有字符相同"被判成占位符。

### 七、验证

* `pnpm verify` 15 项全绿；Rust 单测 382 → **392**（新增 10 条：5 条布局迁移 +
  1 条跨根布局优先级 + 4 条 client_id 占位符判据）。
* 真机跑了两次（第一次抓到上面那个 bug，修完第二次通过），文件数与迁移前逐一相同。
* 仓库体积：**50.77 GB → 3.36 GB**；前端产物 8.67 MB → 442 kB；
  交付物 exe **13,171,200 B → 8,032,768 B**，安装包 2,911,960 B。

### 八、交付后补记：正版登录**走到哪一步了**（2026-09-15 用户实测）

用户在界面上点了「正版登录」，浏览器里**真的完成了授权**，微软页面写着：

> 大功告成！你现在已登录到 **Prism Launcher**。现在可以安全地关闭此窗口。

两件事必须分开说，不能混成一句"登录好了"：

| 半程 | 状态 | 证据 |
|---|---|---|
| **微软侧**（设备码 → 用户同意） | ✅ **实测通过** | 上面那句页面文案（用户提供） |
| **应用侧**（拿设备码换令牌 → XBL → XSTS → MC 账号） | ❌ **这次没完成** | `D:\IEML\prefs.json` 里 `accountUuid` 仍是 `null`、`offlineUsername` 还是 `Player`；凭据管理器里没有条目 |

原因不是代码：**那期间启动器没在跑**（本轮为了构建/部署我停过几次进程），
而设备码换令牌的那次请求是**由启动器发出的** —— 它不在，就没有人去领这个授权。

**所以这一轮又暴露了一个真实的坑，并且修了它**：设备码界面上原来只写
「完成后自动继续，不用回来点任何东西」，没告诉用户**别把启动器关掉**。
现在那里有一句明确的警告，且 `AccountPanel` 会一直在。

**另外用户问出了一个我原先没讲清的点**：授权页显示的名字属于 **client_id 所属的那个应用**
（这叫"一对一"）。内置的是 Prism Launcher 的公开 id，所以页面上写它的名字。
→ 界面新增「登录用的应用 ID」一行，如实显示**现在用的是哪一把、是谁的**，
并且**任何时候都能换成自己的**（以前只在"完全没有 id"时才显示输入框 ——
有了内置 id 反而没法换，那是个真实的功能缺口，一并修了）。
后端 `MsLoginStatus` 也补了 `source` 字段（`settings` / `env` / `builtin` / `none`），
让界面有据可依而不是猜。

> ★ **没有把"登录已跑通"写进功能表** —— 那是下一步的事：
> 保持启动器开着、再完整走一遍，`prefs.json` 里出现真账号，才算端到端。

### 九、内置 client_id 换成用户提供的那把（**未实测**）

用户给了 `32bde9cc-96cf-4f9d-ae6a-a83afadc0c7f`，并明确要求
**"不要验证，只换上"** —— 所以：

* `src-tauri/src/auth/mod.rs` 的 `BUILTIN_CLIENT_ID` 已换成它（只改这一个常量）；
* **没有**去打微软的接口验证（尊重"不要验证"这条要求）；
* 因此 CHANGELOG 与 README 里**都不写"它能用"** —— 只写"换上了、没验过"。
  上面那张 Prism id 的实测表保留着：它证明的是"哪一类 id 能用"，
  与"现在内置的是哪一把"是两件事。
* 真不认的时候，界面会把 `AADSTS700016` 翻成人话（"微软说这个应用 ID 不存在"），
  不会丢一句英文报错给用户。想自己确认随时可以跑
  `tools/probe/probe-ms-clientids.ps1`。

### 十、建了 git 仓库并推到 GitHub（私有，GPL-3.0）

用户要求"像 PCL 一样，创建并上传 GitHub 库"。

* **git 是便携版**（这台机器 PATH 里没有 git）：用
  `C:\Users\Administrator\AppData\Local\Programs\PortableGit\cmd\git.exe`；
  `gh` 已装且已登录（账号 `008heshan`）。
* 写了 `.gitignore`，判据只有一条 —— **能不能由仓库里的东西重新生成**：
  `src-tauri/target/`（本机实测 **3.2 GB**，光 `debug/` 就曾是 47 GB）、
  `node_modules/`、`dist/`、`*.log`、安装包，另外把
  `ms_client_id.txt` / `prefs.json` / `instances.json` 也挡在外面（那是数据目录里的东西）。
  → 提交 **243 个文件 / 5.03 MB**，云端树核验 **278 个 blob，没有 target / node_modules /
  dist / pdb / exe**。
* **协议选 GPL-3.0**（用户定的，与 PCL2 同一系）：LICENSE 用的是**本地现成的官方全文**
  （PortableGit 附带的 `COPYING3`，35,147 字节），不是凭记忆敲的。
* 仓库：`https://github.com/008heshan/IEML` —— **私有**。
* ★ **为什么私有，以及公开前必须先做什么**：源码里内置着 CurseForge API Key
  （实测仍有效，接口 200）。公开仓库会把它一起发出去。README 的「许可」一节写了
  转公开的三步（置空常量 → 处理历史里的旧 key → 过一遍 `.workbuddy/memory`）。
  **这一条是决策，不是提醒**：私有是这件事的当前状态，不是"以后再说"。

---

## 0.1.0-beta.2 — 2026-09-14（第十四轮：资源中心重做 · 字体 · 命名规范 · 首页与侧栏）

用户这一轮提了 20 条，逐条记在下面（**没做完的也写出来**，不含糊）。

### 一、资源中心：从"要你先搜"改成"列出来让你挑"

| 用户原话 | 改法 |
|---|---|
| 「添加 mod 什么的得给 mod 列出来然后让玩家选，而不是让玩家自己搜，玩家记不住」 | 打开即列热门（空关键词查询），卡片带封面 / 下载量 / 简介 / 分类 |
| 「居然没有翻页，就给这么一点，这太不对了」 | 以前写死 `limit: 20` 且没有 offset 入口 → 现在「加载更多」+ 底部 `已显示 N / M`，一页 20（Modrinth 接口上限） |
| 「这些应该给这些资源的不同版本……要选择资源版本进行安装，而不是靠我们来推荐来自动匹配」 | 点「选择版本并安装」**先展开该项目全部版本**（版本号 / MC / 加载器 / 日期 / 体积 / 正式或测试），玩家自己挑一个再装。以前是 `versions[0]` 直接装 |
| 「这些是有封面的，我们得提供，得显示」 | `icon_url`（Modrinth）/ logo（CurseForge）显示在卡片上；取不到时用首字母块兜底（不留破图） |
| 「下载mod等资源的列表怎么又大对勾啊」 | **根因是图标没有默认尺寸**：`ui/Icons.tsx` 的 SVG 不带 width/height 时按 100% 宽渲染，那一行的 `<IconCheck/>` 落在没有任何 svg 规则的地方 → 撑成一整块大对勾。现在 `base()` 给每个图标 `width="1em" height="1em"`（表现属性优先级低于 CSS，已有的尺寸规则不受影响），并把那个对勾换掉 |

### 二、下载页：六格一字排开

* 页签：**安装游戏 · 整合包 · Mod · 资源包 · 光影 · 数据包**（用户："资源包，光影，数据包也一字排开"）。
* 用户："下载页里的 mod 列表，应该是显示 mod，不要显示版本" —— 旧的第三格列的是
  "每个版本各装了什么 Mod"（**已有**的东西），整块删掉；后四格现在都是资源中心本身。
* 「其他地方的入口删除」：Mod 管理页右上角那个「资源包 / 光影 / 数据包」按钮删掉
  （那一页只留「添加 Mod」，走的还是同一个资源中心）。
* 装到哪个版本：资源页签顶部一个显式选择器，默认最近玩过的那个 —— **不替他决定，也不让他每次重选**。
* 用户："下载页这个地方其实下面还有大片空区，可以再往下扩展" —— 高度从
  `calc(100vh - 290px)`（猜出来的减法）改成真正的弹性链 `.page-fill → flex: 1`，
  窗口多大就多高。

### 三、层级与遮挡（用户："下载栏会被奇怪的一些 UI 遮住"）

* 根因：**toast 固定在右下角**，而下载页的主按钮就在右下角，安装过程连发几条提示就把它盖住。
  → toast 挪到**顶部居中**（页头下方一条窄带），永远不会压住主操作。
* 顺手把散落的 6 个 `z-index` 魔数（20/50/60/90/100/200）收成一组令牌：
  `--z-sticky / --z-dropdown / --z-modal / --z-toast / --z-boot`，新组件从令牌取值。

### 四、字体：HarmonyOS Sans SC（随程序附带）

* `HarmonyOS_Sans_SC_Medium.ttf`（8.2 MB）打进 `src/assets/fonts/`，`@font-face` 声明，
  `--font-sans` 首选项改成它；许可全文一并附带（`HarmonyOS_Sans_LICENSE.txt`）。
* 「关于」里**如实声明**：字体名、只有 Medium 一个字重、许可是 HarmonyOS Sans Fonts
  License Agreement；另加一行第三方组件说明。
* ★ **代价写在明处**：安装包体积会因为这个字体明显变大（前端产物从 0.36 MB 涨到 8.6 MB）。
  这是用户明确要的取舍，README 的体积指标同步改成实测值，不藏。

### 五、版本命名规范（用户给定）

`{mc_version}-{loader_type}-{loader_version}`，例：`1.20.1-forge-47.2.0`；纯原版就是 `1.20.1`。

* **显示名与目录名（slug）用同一套规范**。以前显示名是「Forge 1.20.1」、目录名是
  `forge-1201` —— 同一个 MC 版本换一个 Forge 构建，**两个名字都撞**，只能靠 "(2)" 区分，
  而且名字里看不出加载器版本（那恰恰是"能不能跑"的关键）。
* 只影响**新建**的版本；已有的不改名（存档、攻略、群里的说法都对着旧名字）。

### 六、MC 版本图标与分组

* 新增 `components/VersionIcon.tsx`：**自绘等距方块**，按版本世代配色
  （1.21+ 绿 / 1.16~1.20 蓝 / 1.13~1.15 靛 / 1.7~1.12 琥珀 / 快照 紫 / 愚人节 红）。
  「哪个版本算哪一代」只有这一处判据。
* 为什么不复刻游戏里的草方块：那是 Mojang 的美术资源，**不能随启动器分发**
  （README 许可一节写着"不含任何游戏资源文件"）。形状是自己画的几何，不复制官方美术。
* 版本清单按世代**分组显示**（每组标题 + 数量），解决用户说的"展开后看到版本们一堆堆在一起"。
* 版本列表页、启动页、下载页三处共用同一个图标组件。

### 七、主页面与侧栏（用户："主页面太简单……留着大片空区不要，简直浪费"）

* 启动页补三块**真数据**：选中版本的**身份卡**（图标 / MC / 加载器 / 内存 / Java / 累计时长
  + 打开目录 · 日志 · Mod 管理 · 版本设置 四个直达动作）、**其它版本**（点一下换启动目标）、
  **本机状态**（可用内存 / CPU / 探测到的 Java / 数据目录）。
* 侧栏下半部分不再空着：**最近玩过**（最近两个版本，点一下直接进）+ 两个直达动作
  （数据目录 / 关于与设置）。
* 仍然不做、并写明理由：新闻公告栏（没有真实内容，编一条就是假信息）、
  游戏封面大图（美术资源不能分发）、实例大网格（版本列表页已经在做）。

### 八、去掉的文案与入口

* 「不装加载器也能玩 —— 想加光影的话，OptiFine 会对原版 jar 打补丁安装」（用户要求删）。
* 设置页的 **CurseForge Key 输入框**（用户："玩家不会有用自己的 Key 这方面的需求"）。
  后端能力一个没删（`cf_set_key` 命令、`IEML_CURSEFORGE_KEY` 环境变量都还在），只是不再摆在界面上。
* 附加组件与加载器那两排**长告警压成短状态**（用户："简写一个'无'所有人就知道啥意思了，
  要是网络不佳一时半会儿查不到……就做个加载等待的动画，像 windows 开机的那个转圈圈"）：
  `正在查 → 转圈` · `没查到 → 「查不到」` · `确认没有 → 「无」`，整句原因仍在 `title` 上。
  ★ 三种状态**不许混**：把"没查到"写成"无"正是 ADR-050 记着的那类假话。
* 新增 `Spinner` 组件（缺口圆弧 + "在查什么"）：骨架屏表达的是"这里会有一行行内容"，
  而"正在查在线清单"是在**一行文字的位置等一个结论**，骨架屏会让人以为数据已经在了。

### 九、微软登录：用户指定的 client_id **实测不可用**（如实报告）

用户要求用 `00000000402b5328`。**先实测再动手**，新探针 `tools/probe/probe-ms-clientids.ps1`
打真实端点：

```
tenant=consumers  client_id=00000000402b5328
  HTTP 400  error=unauthorized_client
  AADSTS700016: Application with identifier '00000000402b5328' was not found ...
tenant=consumers  client_id=c36a9fb6-4f2a-41ff-90bd-ae7cc92031eb（Prism Launcher 公开 id）
  HTTP 200  ACCEPTED  user_code=…  uri=https://www.microsoft.com/link
tenant=consumers  client_id=<随机编造>   ← 对照组
  HTTP 400  同样的 AADSTS700016
```

**结论**：那个 id 在今天就是"没注册过"，和随手编一个的结果一模一样 ——
写进代码等于交付一个"点了必然失败"的登录按钮。所以**没有**照抄它；
"做法学 PCL"这一半照做了（PCL 的流程我们本来就是那么实现的：设备码 → 自动轮询 →
XBL → XSTS → MC 令牌，错误码逐条翻译），差的只是**一把能用的 id**。
→ 默认用 Prism Launcher 那把公开 id（同样是"玩家不用填、开箱即用"），
设置页仍可覆盖成自己的 id。**用户的指示没有被忽略，是被实测否掉了，证据留在这里。**

### 十二、补充做完的两条（同一轮内追加）

**① 「默认列出」这条改法**先证明再实现**（`tools/probe/probe-browse.mjs`）**

"打开就列出来"成立的前提是**空关键词真的会返回内容**。实测（走应用自己在用的镜像
`mod.mcimirror.top/modrinth/v2`，因为本机直连 `api.modrinth.com` **连接超时**）：

| 种类 | 空关键词结果 | `index=relevance` vs `downloads` | 第 2 页与第 1 页重复 |
|---|---|---|---|
| Mod | 17 381 条 | 总数与首个完全相同 | 0 条 ✓ |
| 整合包 | 7 289 条 | 同上 | 0 条 ✓ |
| 资源包 | 17 080 条 | 同上 | 0 条 ✓ |
| 光影 | 596 条 | 同上 | 0 条 ✓ |
| 数据包 | 61 条 | 同上 | 0 条 ✓ |

结论：**空 query 在两种排序下都返回内容、且首个就是最热门的那个**，所以"默认列出"不是空话；
翻页也是真的（第 2 页与第 1 页零重复）。关键词搜索（sodium / iris / terralith）不受影响。
—— 顺带解释了为什么后端那句写死的 `index=relevance` 不用改：实测两种排序在空 query 下等价。

**② 「版本设置页和 PCL 太像，适度调整」（用户第十条要求）—— 做了**

原来的这一页是一条**从头读到尾的长清单**：想改内存得先滚过隔离、窗口标题、服务器、Java。
改法用的是这一页**比 PCL 多的那个概念**（每项可以"跟随全局"或"已覆盖"）：

* **顶部加一条只读摘要**：内存 / Java / 隔离 / 窗口标题 —— 四项都是**真值**，
  而且**每一项都能点**，点了滚到对应的那一行并高亮一下（长清单里找一行不用再靠眼睛扫）。
  只读是刻意的：能改的地方仍然只有下面那些行（ADR-001 铁律①）。
* **顺序按"改动频率"重排**：内存 → Java → 启动选项 → 其他。
  打开这一页的人十次有八次是为了改内存或 Java，它们现在在最上面。

### 十三、这一轮**没做**的（不含糊）

* 「整体 UI 让启动器宛如半成品，美化」—— 只做了上面这些具体项，
  没有做一次全站视觉走查。
* 真机点击穿透（新建实例 / 真的点一次启动）仍未做，见「验证」一节。

---

## 0.1.0-beta.1 — 2026-09-14（第十三轮：界面重构 · 进阶公开测试）

用户要求：「重构 UI，使其现代化，正式化；版本正式进阶成 Beta 版；有些地方也要简化，
把某些实用功能放的显眼，而不是藏起来。有些地方文字太多，很繁杂。有些地方的界面太挤，
观感很乱。如果你不会改就参考 PCL」。

这一轮**只动界面**（外加版本号与文档），领域规则、后端命令、安装流程一行没改 ——
所以 Rust 382 项测试与 node 111 项全部保持原样通过。

### 一、怎么发现问题：先拍图，再改

不靠"看代码觉得挤"，而是用 `tools/live/shot.mjs` 把每个页面拍下来看。
拍出来的三处硬伤（都是**看得见**的，不是猜的）：

| 现象 | 证据 |
|---|---|
| 版本列表把同一批警告句反复说 | 一屏 4 行里，"缺加载器"的两句完整告警出现 **3 次**，每行占两行高度 |
| 概览页四张卡各带 2~3 行解释 | 半屏全是说明文字，按钮被挤到卡片最右侧；真正要点的东西反而最不显眼 |
| 设置页标签列只有 156px | 提示被折成 2~3 行，断行还落在词中间（"数据目 / 录：…"）；Java 的完整绝对路径独占一行 |

### 二、动作条：把"实用功能"从说明里抬出来

| 页面 | 以前 | 现在 |
|---|---|---|
| 概览 | 「检查」「一键补齐」两个按钮 + 四张解释卡 | **一条动作条**：打开目录 · 查看日志 · 检查并补齐文件 · 重命名 · 创建副本 |
| Mod 管理 | 「打开 mods 目录」埋在说明块第二段里 | 抬到右上角工具条，与「添加 Mod」并列 |
| 设置 → Java | 「下载 Java 21」在卡片**最底部**，前面压着一行长句 | 抬到卡片标题行右侧（`CardTitle` 新增 `actions`），与「重新扫描」并列 |
| 下载页 | 三个页签下面挂一条三行教学提示 | 删掉；演示模式提示改成页头右侧一枚 Chip |

★ 「检查」与「一键补齐」合成**一个**按钮：缺文件没有任何需要用户决策的地方，
先 verify、缺了才下载、补完再 verify 一次报实数。判据仍只有一处（`installer.verify` +
`installGame`），界面只是把两步串起来。

### 三、去文字：删掉的是解释，不是事实

| 删了什么 | 为什么确定它是多余的 |
|---|---|
| 概览页蓝色「概览页的定位」Note（3 行） | 它在解释"为什么这一页这么设计"——用户不需要知道设计过程 |
| 版本列表底部的「游戏文件是共享的」（2 行） | 概念解释；页头副标题已经写着「盘上已装 N 份游戏文件」 |
| Mod 管理两块 Note（"不会自动更新" 4 行 + "装 Mod 的两条路" 4 行） | 压成**一行**：留"不会自动更新"和"原地覆盖同名文件"（真话且会咬人），其余是废话 |
| 设置页「IEML 下载的 Java 由你掌控」Note | 下面每行都带删除按钮，不用再承诺一次 |
| 下载页「整合包和安装游戏的区别」+「这个包自带什么」两条 Note | 前者是概念对比，后者塞进了底栏那一行 |
| OptiFine 两条 5~6 行 Note | 各压到 2~3 行，**保留**"缺桥接包游戏会崩"这条硬事实 |
| 「当前配置无需额外 API 包」的空框 | 用户没问 API 包，"没有"这件事不值得占一块地方 |
| 日志页「怎么看日志」三行教学 | 收进**默认折叠**的 `<details>`：内容一个字没删，但正常看统计/崩溃分析的人一个像素都不被它占 |

**一条规则**（写进 ADR-054）：**一行状态用 Chip 或 `.field-hint` 说，不用 Note 块**。
Note 只留给"用户需要据此做决定"的事（缺前置包、组合不兼容、崩溃原因）。

### 四、去拥挤：把宽度和层级还回来

* `.field-row` 标签列 **156px → 200px**；`.card-title` 加 `.card-tail`/`.card-actions`；
  窄窗口（≤1150px）仍退化成单列。
* 列表行统一成一套原语（`.list` / `.list-row` / `.list-title` / `.list-sub` / `.list-side` / `.toolbar`），
  版本列表/Java 列表/Mod 一览共用容器 + 细分隔线，行高 `--row-min-h: 52px`。
* Java 路径只显示**尾部三段**（`…\jdk-21.0.2\bin\javaw.exe`），完整路径进 `title`；
  这一处以前一行就被路径吃掉。
* 模块标题里的说明从 2 行缩到 1 行（"四选一 · 互斥，装错会导致游戏无法启动" → "四选一"）。
* 加载器说明**只在选中或不可用**时显示（原来四个各带 1~2 行，右栏只剩字）。
* 实例设置页顶部那条"以下设置只作用于…"的 Note 改成一行小字（`.scope-line`）：
  它说的是**这一页的作用范围**，是标题的延伸，不需要用户据此做决定。
* 长提示一律瘦身（"同时下载的文件数量，设多少就是多少。网络慢时调高能更快吃满带宽" →
  "同时下载的文件数"；"留空则停在主菜单。例：mc.example.com:25565（全角标点会自动换成半角）" →
  "留空则停在主菜单 · 全角标点会自动改半角"）。
* 删掉 5 条死 CSS（`.ov-*` / `.api-empty` / `.java-path`）：界面换掉的东西，样式表里也不留。

### 五、顺手修掉的两处**文档假话**

1. `README.md` §「Mod 绝不自动更新」还写着"启动器只在启动时后台检查并打角标，
   且旧文件先移入回收站" —— 这两句在 dev.12 就被实机证伪了（没有启动时检查；
   `install_mod` 直接覆盖、`delete_mods` 直接删），当时的修正只进了代码注释和 ModsPanel 的
   界面文案，README 一直没跟上。**现在改成与代码一致的说法**。
2. 设置页「关于」卡片里重复的"数据目录：…"被折成"数据目 / 录：…"两行 ——
   存储卡片里已有这一项（还带「打开」按钮），删掉重复的那一份。

### 六、版本进阶：`0.1.0-dev.14` → `0.1.0-beta.1`

用户明确要求进阶。按 `docs/VERSIONING.md` §3.3 的三条判据**逐条对**：

| 判据 | 结果 |
|---|---|
| ① 功能表里没有 ❌ | ⚠️ **未完全满足**：功能表仍有一行 ❌ —— 「macOS / Linux 未实测」。**没有把它改成 ✅**，它留在表里 |
| ② `pnpm verify` 全绿 | ✅ 15 项全绿（本轮界面改完重跑） |
| ③ 「已知会挡住用户的事」清空 | ⚠️ 部分满足：Windows 上的阻断项已清空；剩下的 ⚠️ 是「只测过 Windows」「Quilt 未实测」「正版登录要自备 client_id」 |

**所以这一节要如实写清楚**：进阶是**用户的决定**，不是"三条全满足"的自然结果。
把 ❌ 留着、把没满足的那一条写在这里，比把它涂成 ✅ 再宣布 beta 诚实。

### 七、验证

* `pnpm verify` **15 项全绿**；Rust 单元测试 **382** 项、`node --test` **111** 项、e2e **59** 项全过。
* 界面改动用 CDP 截图逐页看过（`tools/live/shot.mjs`）：启动 / 版本列表 / 概览 / 实例设置 /
  Mod 管理 / 日志 / 下载（三格）/ 设置（四屏），共 20 余张。
* 交付物重建并部署（`pnpm desktop:build` = 构建 + 部署 + SHA256 逐字节证明），
  `exe 内嵌前端一致性` 变绿即证明**桌面那份 exe 里的界面就是现在这份 dist**。
* ★ **本轮没有做真机点击穿透验证**（没有新建实例、没有点一次"启动游戏"）——
  改动集中在 CSS 与 JSX，后端零改动（`commands_real.rs` 只删了两个未使用的 import）。

---

## 0.1.0-dev.14 — 2026-09-14（第十二轮：清仓、目录与命名规范化）

用户要求：「清理 IEML 文件夹的多余文件，优化代码，优化文件夹结构，规范命名」。
这一轮**不加功能、不改行为** —— 只动"东西放在哪、叫什么、有没有用"。
判据是"删掉之后 `pnpm verify` 还全绿吗"，改动全部可复核。

### 一、清理：删掉 96 MB 与 45 个文件

| 删了什么 | 多少 | 为什么确定它多余 |
|---|---|---|
| 仓库根的 40 个 `.build*.log` / `.forge*.log` / `.verify.log` 等 | 40 个文件 | 都是"把命令输出重定向到文件再读"留下的**中间产物**；结论早写进 CHANGELOG 与记忆里了 |
| `forge-1.20.1-47.2.0-installer.jar.log` | **1.0 MB** | Forge 安装器自己吐的日志，误落在仓库根 |
| `tmp/` | **91 MB / 511 个文件** | 截图、探针输出、一次性脚本的堆放处。`shot.mjs` 自己会 `mkdir -p`，删了不影响任何流程 |
| `logs/` | 3 个文件 | Minecraft 跑起来时把 `logs/latest.log` 写进了仓库（诊断时 cwd 用了仓库根）；**应用自己的日志在数据目录**，不在这 |
| `design/.keep` | 1 个 | 目录里已经有 `mockup.html`，占位文件没意义了 |
| `tools/extract-js.js` `fix-residue.js` `check-mockup.js` `fetch-pcl.js` `serve.js` | 5 个脚本 | 全都**硬编码 `C:/Users/Administrator/Desktop/IEML/...`** —— 那个路径早就不存在了（项目在 `E:\IEML`）。改都没法改，是死脚本 |
| `tools/probe-curseforge-{cdn,fix,fingerprint,mirror}.mjs` | 4 个探针 | 它们各自回答的一个问题**已经固化成测试**（见下），脚本只剩噪音 |

> **那 4 个探针的结论去了哪**（这是"删掉不等于丢掉"的关键）：
> · 指纹端点 = `POST /v1/fingerprints`、指纹用无符号 → `net::curseforge` 的模块注释 + 12 条向量表
> · `exactFingerprints[i] ↔ exactMatches[i]` 逐位对应 → `tests/live_curseforge.rs::batch_fingerprints_keep_the_position_mapping`
> · CDN 候选（mediafilez / mcimirror 才通） → `candidates_from_a_curseforge_url` 单元测试 + live 下载测试
> · 镜像带不带 key 都能用 → 由 `get_text_third_party_with_headers` 的兜底路径覆盖

### 二、目录结构：`tools/` 按**用途**分家

脚本从 54 个平铺在 `tools/` 改成五类，并加了 `tools/README.md` 索引（说清"什么时候该跑它"）：

```
tools/  verify.mjs · set-version.mjs · make-icons.mjs · gen-curseforge-fingerprint-cases.mjs
        gates/   静态门禁（进 verify）：子进程窗口 / 内嵌前端 / 打包产物 / exe 字符串
        env/     构建环境：cargo.ps1（唯一入口）· cargo-manual-msvc.ps1 · deploy-desktop.ps1
        live/    真机验证：live-*-check.mjs · shot.mjs
        probe/   打上游接口的探针
        diag/    一次性诊断与考古（find-orphans / natives-* / version-json-* …）
```

`pcl-download-reference.md`（PCL 下载引擎的研读资料）从仓库根搬进 `docs/` ——
它是**研究资料**，和另外四份研读文档是一类东西，放在根目录只会让人以为是项目说明。

### 三、规范命名

* **`xxx2.mjs` 这种后缀一律改掉**：它等同于"我不知道这两个有什么区别"。
  `check-natives.mjs` / `check-natives2.mjs` → `diag/natives-dir-and-jar.mjs` /
  `diag/natives-vs-library-path.mjs`；`inspect-natives{,2}.mjs` → `diag/version-json-natives.mjs` /
  `diag/version-json-lwjgl-natives.mjs`；`check-bundle.cjs` → `gates/bundle-is-prod.cjs`（名字说清判据）。
* `find-class-jar.mjs` → `diag/class-comes-from-which-jar.mjs`（原名把主客体说反了）。
* 脚本**自己的用法注释**里也写着旧路径 —— 一并改（不改就是新的"假标识"）。
* `CHANGELOG.md` **一个字的旧路径都没动**：那是历史记录，项目规矩是"发布过的不许改写"；
  新旧对照就看上面这张表。

### 四、代码优化（三处，都带证据）

1. **删掉 4 个真·死函数**（`#[allow(dead_code)]` 自己承认的）：
   `commands_real::{command_to_json, plan_to_json, mirror_helper}`、
   `download::manager_speed` —— 全仓库引用数 = 1（就是定义本身）。
2. **`lib_key` / `neoforge_belongs` 从 `#[allow(dead_code)]` 改成 `#[cfg(test)]`**：
   它们**只被测试用到**。以前那句 allow 等于说"它是死代码但别报"，
   读者分不清"真死了"还是"只在测试里活"；改成 `#[cfg(test)]` 之后
   它们不进 release 二进制，意图一眼可见。
3. **`tools/cargo.ps1` 变成跑 cargo 的唯一入口**（原来 package.json 里
   8 条命令走 `cargo-novcvars.ps1`、verify 走 `cargo.ps1`，调用方得记住哪个是哪个）：
   * 首选 `vcvars64.bat`；它不可用 → **自动**改用自己拼的 MSVC 环境，并**把这件事打印出来**
     （静默换环境会让"为什么这次编译行为不一样"变成谜）；
   * 判据用"%LIB% 有没有被设上"，不是"退出码非 0" —— 编译错误也会非 0，那种重试纯属浪费；
   * `cargo-manual-msvc.ps1` 的 MSVC/SDK **版本号不再写死**（原来硬编码 `14.44.35207`，
     换台机器就指不到），改成按目录名挑最新，找不到就说清去哪儿装/怎么指路。
   两条路都实测：正常路径 `cargo 1.98.1`；把 `IEML_VCVARS64` 指到不存在的路径即可验证回退分支。

### 五、顺手挖出来的两个环境坑（都写进 README 与记忆了）

* **PS 5.1 会把无 BOM 的 `.ps1` 按 ANSI(GB2312) 读**：中文注释变乱码，而乱码的字节对
  **会吃掉后面的引号/反引号** —— 于是报错指向**别处**。
  实测现场：它说"第 30 行的 `&&` 不是合法分隔符"，而真正的破坏在第 35 行的中文串。
  → `tools/env/*.ps1` 与 `tools/probe/probe-ms-devicecode.ps1` 现在一律存成 **UTF-8 with BOM**。
* **记忆里那条"vcvars64.bat 不可用"已经过时**：本轮实测它工作正常
  （`tools/env/cargo.ps1` 走 vcvars 跑完了整套 Rust 测试）。回退路径改成"保险"而不是"日常"。

### 六、验证

* `pnpm verify` **15 项全绿**（含新路径下的 Rust 测试、四道静态门禁、文档版本口径一致）。
* Rust 单元测试仍 **382** 项全过；`node --test` 111 项、e2e 59 项不变（本轮没动行为）。
* 仓库根现在只剩：`.npmrc` `CHANGELOG.md` `README.md` `index.html` `package.json`
  `pnpm-lock.yaml` `tsconfig.json` `vite.config.ts` + 9 个目录。

---

## 0.1.0-dev.13 — 2026-09-14（第十一轮：CurseForge 真的接上了）

用户给了一把 CurseForge API Key，要求"开箱即用"（内置）+ 四个种类的搜索/安装/查更新。
**先把接口打穿、再写代码** —— 这一轮几乎所有结论都是实测出来的，而且有几条
**推翻了猜测**（见下表）。

### 第一步：先证明上游是什么样（`tools/probe/probe-curseforge*.mjs`）

| 事实 | 怎么证明的 |
|---|---|
| key 有效：`GET /v1/games/432` → **200**；不带 key → **403**（README 那句"需要 Key"是对的） | 连通性探针 |
| `classId`：mod=**6**、resourcepack=**12**、shader=**6552**、datapack=**6945** | 每种各搜一次，看有没有结果 |
| `modLoaderType`：forge=**1**、fabric=**4**、quilt=**5**、neoforge=**6**；**liteloader=3 是 0 条**（1.12.2 上也是） | 逐个试 + 计数 |
| `/mods/{id}/files` **默认按 `fileDate` 倒序**（界面取 `[0]` 当最新版是安全的） | 读回 10 个文件比对时间戳 |
| 指纹端点是 **`POST /v1/fingerprints`**（`/v1/mods/fingerprints` → **404**），指纹传**无符号**值 | 试了 5 种写法 |
| `exactFingerprints[i]` 与 `exactMatches[i]` **逐位对应**（把查不到的指纹夹在中间也成立） | 专门设计了一次"夹假指纹"的实验 |
| 官方 CDN `edge.forgecdn.net` 本机**时好时坏**（200 / 连接失败 / 404）；`mediafilez.forgecdn.net` 与 `mod.mcimirror.top/files/…` **都稳定**；`media.forgecdn.net` 403、BMCLAPI 不代理 CF 文件 | 每个候选各试 2 次 |
| `api.curseforge.com` 本机**间歇性连接超时**（10s）；`mod.mcimirror.top/curseforge/v1/…` 稳定且**不带 key 也能用** | 探针第一次直接超时失败 |
| 作者可以关掉"允许第三方分发"：热门 50 个里就有 1 个（`allowModDistribution=false`，文件 `downloadUrl` 是 null） | 扫 50 条统计 |
| 文件的 `hashes` 里 `algo=1` 是 **SHA1**（所以 CF 的下载**也能真的校验**） | 读文件对象 |

**指纹算法是用接口自己当裁判验证的**：下载真实文件算指纹 →
`POST /v1/fingerprints` → 接口把那个文件**原样认了回来**。
（算错的表现是**静默**的：接口只回一个空的 exactMatches，于是"所有 Mod 都查不到更新"。）

### 做了什么

* **`net/curseforge.rs`（新）**：搜索 / 文件列表 / 指纹反查 / 下载候选 / key 管理。
  结果**映射成与 Modrinth 同一个响应形状**（`SearchResponse` / `SearchHit` /
  `ProjectVersion`），所以界面不需要为来源写第二套渲染（ADR-051 的规矩）。
  映射里哪些是"上游真给的"、哪些"上游没有"，逐条写在注释里
  （例：CF **不给单个文件的下载量**，如实留 0，不编数字；版本号用作者写的 `displayName`）。
* **key 的管理**（按用户选的方案）：**内置一把 + 设置页可覆盖 + 环境变量
  `IEML_CF_API_KEY`**。优先级写在 `api_key()` 一处；设置页**如实显示它从哪来**
  （"正在用内置的 key（随程序附带）" vs "你自己填的"），并且**只显示前缀**。
  「测试连接」是**真打一次接口** —— 403 与网络不通分开说（修法完全不同）。
* **界面**：资源中心多一个来源切换（Modrinth / CurseForge）+ 作者禁止分发的
  项目**提前标出来**并说明原因（而不是等用户点了安装才报"没有可下载文件"）。
* **查更新**：`check_mod_updates` 现在**同时收 SHA1 与指纹**（两个列表**按位置对应**，
  没有指纹的位置传空串、后端保位置）。先查 Modrinth，查不到再用指纹查 CurseForge ——
  这样一个只发布在 CurseForge 的 Mod 也能被认出"有更新"（OptiFabric 就是这种）。
  ★ 命中结果里的 SHA1 与本地 SHA1 **交叉验证**：对不上就不采信 ——
  万一上游的对应关系变了，最多"查不到更新"，**绝不会装错版本**。
* **下载多候选**：CF 的官方 CDN 在本机时好时坏，所以按 URL 推导出
  `mediafilez.forgecdn.net` 与 `mod.mcimirror.top/files/…` 两条备选
  （`candidates_from_url`，只对**已知的 CF 主机**推导 —— 不是 CF 地址就一条都不给）。
* **Mod 列表**：扫盘时顺带算指纹（同一个文件、两次不同的预处理，所以是两次计算），
  并且**只在反查命中时**才把它交给界面（`ModScanEntry.fingerprint`）——
  给一个查不到的指纹只会让界面多显示一个没用的字段。

### 测试

* **`tests/curseforge-fingerprint.cases.json`（12 条向量）**：Rust 与 JS 两侧读同一份表
  （`pnpm verify` 里有一条「CurseForge 指纹判据表」）。生成它的工具里写着这份
  可信度的来源 —— **被接口验证过**，不是"我觉得对"。
* **`src-tauri/tests/live_curseforge.rs`（4 条，`#[ignore]`）**：真机走完整条链 ——
  四个种类各搜一次、文件列表倒序 + 带 SHA1、**下载走我们自己的引擎**（多候选 + SHA1 校验）、
  自己算的指纹被接口认回来（含"夹着假指纹"的批量对应关系）。
  实跑通过（下载 2 566 436 字节，指纹 3227395021 被原样认回）。
* Rust 单元测试 369 → **382**；`pnpm verify` 新增一项（共 15 项）。

> ★ 这一轮**没有**做 CurseForge 整合包（CF zip 一键安装）—— 用户选的范围是
> "四个种类的搜索/安装/查更新"。CF 整合包要另读 `manifest.json` 并逐个解析
> projectID/fileID，是另一个量级的工作。
>
> ★ 交付：`tauri build` 出了 dev.13 的 exe 与 NSIS 安装程序，并部署到桌面
> （`tools/env/deploy-desktop.ps1` 已证明与构建产物逐字节一致）。
> **没有做的验证**：在界面上点一次"切到 CurseForge → 搜索 → 安装"
> （后端整条链有 4 条 live 测试证明，界面上那一层还没有证据）。

---

## 0.1.0-dev.12 — 2026-09-14（第十轮：P0 缺陷与"假承诺"清理）

这一轮**不加功能**，只修七组缺陷，并清掉文档与代码里对不上的说法。
判据一律回到"这句承诺，代码真的做到了吗"。

### P0-1 `${library_directory}` 不再从 classpath 反推

Forge 的 profile 里有 `-DlibraryDirectory=${library_directory}`，而它是靠
`classpath.first()` **往上数四层**推出来的：`libraries/<组>/<件>/<版>/<文件>.jar`
的祖先第 4 层"恰好"是 `libraries`。只要 classpath 的第一项不是标准四层布局
（调用方会把客户端 jar 也 push 进来）、或者 classpath 为空，推出来的就是
**别的目录**或**空串** —— 游戏只会报"找不到某个库"，而根因在启动器拼的变量上。

现在 `LaunchSpec` 多一个 `libraries_dir` 字段，由 `prepare_spec` 直接给
`shared/libraries`（它自己的路径，不需要猜）；`build_command` 在拿到空串时
**出声**。两条守门测试：把客户端 jar 放在 classpath 第一位（典型骗过 `nth(4)`
的输入）仍必须得到 `shared/libraries`；classpath 为空时也不许给出空串。

### P0-2 `.mrpack` 的 `files[].path` 现在真的过闸门

清单里的 `path` 是**整合包作者写的字符串**，我们直接 `game_dir.join(path)` 就落盘。
于是 `../../../../…/Startup/x.jar`（写到自启动目录）、`C:/Windows/System32/x.dll`、
`\\server\share\x` 这类路径都会被当真 —— Windows 上 `Path::join` 遇到绝对路径
**整个替换前缀**，写出去就是系统目录。

`extract_overrides` 一直有这道校验（zip crate 的 `enclosed_name()`），
但**按清单下载**那条路完全没有：同一份不可信输入，一条路拦、一条路不拦。
现在两条路共用 `modrinth::safe_relative_path`（反斜杠归一、拒绝 `..`/绝对路径/盘符），
被拒绝的路径**交回调用方 `emit` 出来**（静默丢文件等于让"装完少了东西"变成谜）。
新增 4 条测试（正常条目、穿越、绝对路径与盘符、空输入），以及"坏路径不进任务表但必须被报告"。

### P0-3 暂停：第一批也要能停，而且**不许假装装完了**

三处都错了：

1. `install()` 给**第一批**（客户端 jar + 几十个库，最大的一块）传的是写死的
   `pause: None` —— 用户在这段时间按暂停，界面显示"已暂停"、下载一路跑到底；
2. 下载引擎的 `paused` / `remaining` 被 `install()` **扔掉**：两个阶段被暂停后
   函数照样往下走，最后返回 `Ok(装好了)` —— 前端写"完成 100%"，盘上一半文件都没有；
3. `install_version` 在 `install()` 返回后**无条件**跑 Forge/NeoForge 安装器，
   于是"暂停"换来一次几 MB 下载 + 几分钟打补丁。

现在：第一批接上同一个暂停令牌；`InstalledVersion` 多出 `paused` / `remaining_files` /
`paused_stage` 三个字段并一路上报到界面（`InstalledSummary`、`ModpackInstallResult`、
`bridge/tauri.ts` 同名同义）；暂停时**立刻返回**，不写"完成"、不建实例、不跑加载器安装器
（那一步是外部 `java -jar`，没有暂停的概念，所以改为**开始前**看令牌、已暂停就不启动）。
任务中心也不再一点按钮就写「已暂停」—— 按下令牌只是"请求"，真正的结论由
`paused: true` 回来才算（`markPausedConfirmed`）。
两条 Rust 测试钉住"暂停必须如实上报、且没按暂停时不许报暂停"。

### P0-4 206 偏移不符：**重发一个不带 Range 的请求**

老代码遇到"服务端回的 `Content-Range` 偏移不是我们要的那个"时，
只是丢掉响应体、把目标文件截成 0，然后**照样把这一轮的 body 写进去** ——
而那份 body 是从**服务端给的偏移**开始的内容。落盘的文件缺开头、SHA1 对不上，
用户看到的是"某个文件反复校验失败"。

现在三种"对不上"（偏移不符 / 没有 `Content-Range` / 服务端文件大小变了）
统一处理：**丢掉这份响应、清掉本地残留、重发不带 `Range` 的请求**要整份，
并且在同一轮调用里下完（不再白烧一次重试预算）。HTTP 416 同样如此。

### P0-5 长度校验移到**读完响应体之后**

服务端宣告的大小与任务记录不符时，老代码在**拿到响应头**就判"服务端文件变了"。
而那个"不符"最常见的真实原因是**限流页**：BMCLAPI 对一批库回过 146 字节
（任务记录 286 KB ~ 947 KB）。于是 `looks_throttled` 那条判据**永远没机会执行**，
`note_throttled` 从没被调用过 —— 源管理器不知道被限流，不降并发、不冷却，
下一个文件照样被搪塞。

现在顺序反过来：先读完响应体，再看**实际到手多少字节**：
① 像限流页 → 告诉源管理器 + 报错；② 否则长度与任务记录不符 → 报错并说清差多少
（没有 SHA1 的任务以前会漏过这一关，半份文件会被当成成功落位）。
`note_throttled` 也改为写在**传进来的**源管理器上（不再取全局单例），
于是测试能用一个独立实例断言"真的冷却了、真的降了并发"。

新增 7 条测试：起一个本地假 HTTP 服务器按脚本回响应，断言"第二次请求**不带 Range**"、
"落盘内容逐字节等于整份"、"限流页被认出来且源被冷却"、"短包被拒"、"长度正好放行"、
"大小未知时不乱判"、"正常续传仍走追加"。全部不联网。

### P0-6 崩溃判据：**一条**判据，而不是两条互相打架的

两条路径各判各的：

* `watch_game_exit`（游戏自己退出）**只看退出码** —— 而 native 库加载失败时
  JVM 的退出码是 **0**，于是报"正常退出"，用户看到"游戏启动了然后关了"，没有任何解释；
* `stop_minecraft`（点停止按钮）**只看日志规则** —— 而离线启动的日志里**必然**有
  `401 Unauthorized`，于是点一下「停止游戏」就弹「游戏异常退出 · 登录状态已失效」。
  那是**我们自己用离线身份启动**造成的现象，被报成了故障。

现在只有一份判据 `domain::crash::judge_crash(退出码, 是否用户停止, 是否离线, 时长, 日志)`：

* 用户主动停止 → 不算崩溃（但我们动手前会先看进程是否已经死了 —— 已经死了就是
  "游戏自己退出"，按正常判据走）；
* "崩没崩"只看**退出码**与**游戏/JVM 自己写下的崩溃声明**（`The game crashed whilst` /
  `crash-reports/crash-` / `A fatal error has been detected by the JVM` /
  `Exception in thread "main"` / Fabric 的 `FormattedException`）—— 不再拿那 30 条
  "原因规则"当判据（一局两小时正常退出也可能命中 `FileNotFoundException`）；
* 离线身份传进去，账号类命中进 `benign_matches`：**照实列出来、并解释"它不是原因"**，
  但不参与结论（前端 `CrashModal` 多一块"日志里有这几行，但它们不是崩溃原因"）。

顺带删掉 `commands.rs` 里的 `launch_game` / `stop_game` —— 它们是真实路径的第二份实现、
`invoke_handler` 里**根本没登记**（前端永远调不到），而 `stop_game` 返回
`exit_code: Some(0)`：一个**编出来的数字**（它走 taskkill，没读过子进程退出码）。

### P0-7 Java 规则：两侧同一份判据，界面不再抠散文

三件事：

1. **Rust 侧补上 `forge_version` / `fabric_version`**，并把 PCL `ModJava.vb` 里
   按加载器版本分段的规则全部移植过来（Forge 34.0.0~36.2.25 最高 Java 8 /
   36.2.26+ 最高 23 / 37.0.0~37.0.79 最高 16 / 45.0.21~45.0.65 最高 19 /
   45.0.66~47.4.8 最高 21；NeoForge 20.2.62-beta 之前也最高 21；
   Fabric Loader < 0.17.0 不兼容 Java 25）。以前这些**只有前端会生效** ——
   于是界面说"需要 Java 17"、启动却报"需要 Java 25"。
   同时修掉一处边界漂移：`MC_LEGACY_MAX8` 原来是 `<= 1.11.2`（PCL 与前端都是 `<= 1.5.2`），
   1.6~1.11 这一段本来**没有任何基线约束**。
2. **一份判据表，两侧共用**：`tests/java-rules.cases.json`（26 条，全部来自 PCL 源码），
   TS 侧 `tests/java-rules.test.mjs` 与 Rust 侧 `domain::java::java_rules_table_matches_rust_engine`
   各跑各自的引擎、比对同一个期望区间字符串。写这条表时它当场抓出了 3 处我对期望值的误判 ——
   这正是它存在的意义。
   区间格式化也统一了：**无界的一侧用圆括号**（`[17, )`），以前打印成 `[17, ]`
   （"没设上限"被读成"上限是空的"）。
3. **界面不再从报错文案里抠数字**：`launch_minecraft` / `preview_launch` 的错误类型
   改成结构化的 `LaunchError { code, message, required_major, required_range }`，
   启动页按 `code === 'java-missing'` + `required_major` 走（原来那句
   `/Java (\d+)/.exec(msg)` 已删除）。顺手删掉启动页里写死的
   "1.20.5 及以上需要 Java 21…"（那是第二套规则，而且对 26.2 必然说错 —— 真相是 Java 25）。
   前端的字段映射也收成一处 `toEngineInput`（实例概览 / 实例设置 / 安装配置器共用），
   三个调用点现在都带上加载器的种类**与版本**。

### 文档：把说过的话改回事实

* README 里三处「**暂停 = 取消**」全部删掉（引擎早已有真正的暂停语义，
  这几行还写着"`pauseTask` 仍抛暂不支持"）；
* **改写分片叙事**：分片下载**默认是关的**（实测 BMCLAPI 上 8 路 Range 只快 16%、
  会招 429、还带一串只在分片路径上存在的失败，见 `chunking_enabled()`），
  而 README 一直在标题行写着"分片并行 + 段级断点续传"、还举"39 MB 客户端 jar 走分片"
  当实测证据。现在如实写成"默认单连接，`IEML_CHUNKED=1` 才分片"；
* 回填过期数字：README 的「当前版本」落后 **8 个版本**（写着 dev.3，代码是 dev.11），
  测试项数写的是 273/140/58/47（实际 369 / 111 / 59）；
* **加一条会红的检查**：`tools/set-version.mjs` 现在把 README 也纳入版本号落点，
  并新增 `--docs` 模式（README 与 CHANGELOG 最新一节必须逐字相同）；
  `pnpm verify` 里多一项「文档版本口径一致」。改版本号忘了改文档 = 红。
### 测试

* Rust 单元测试 340 → **369**（launch_args 2、modrinth 4、installer 2、download 7、crash 9、java 5…）
* 前端 `node --test` 新增 `tests/java-rules.test.mjs`（5 项），合计 **111 项**
* `pnpm verify` 新增两项：Java 规则判据表、文档版本口径一致
  （**总项数看它自己打印的那一行** —— 这一轮修的正是"文档里写死的数字悄悄过期"，
  所以这里不再抄一个数）

> ★ 这一轮**重新构建了 exe**（`tauri build` → `src-tauri/target/release/ieml.exe`，
> 7 909 888 B，与 NSIS 安装程序 `IEML_0.1.0-dev.12_x64-setup.exe` 一起产出），
> `pnpm verify` 里的「exe 内嵌前端一致性」是绿的 —— 也就是说**交付物与源码一致**。
> 但**没有做真机启动验证**（没有点一次"启动游戏"看它进不进渲染循环）：
> 改动集中在下载/暂停/崩溃判据/Java 规则，这些都有单元测试或判据表守着，
> 而"装到桌面点一下能起来"这件事这一轮没验。
> 要部署到桌面：`powershell -NoProfile -ExecutionPolicy Bypass -File tools\deploy-desktop.ps1`
---

## 0.1.0-dev.11 — 2026-09-14（第九轮）

### ★★★ 更正 dev.10：**OptiFabric 一直都在**，是我查错了平台

dev.10 我写下了这句话，并据此把「Fabric + 高清修复」在 **1.16 ~ 1.20.4**
整段判成不兼容：

> 也就是说 **1.16 ~ 1.20.4 这一段没有任何桥接包** —— OptiFabric 的主要作者
> 在 1.16 前后停止维护，那个项目现在在 Modrinth 上连项目页都没有。

**这句话是错的。** 用户拿 MC百科的数据纠正了我，两个证据都硬：

| 来源 | 事实 |
|---|---|
| Modrinth `/v2/project/optifabric` | **404**（这个是真的 —— 但只说明"它不在 Modrinth"） |
| `api.cfwidget.com/minecraft/mc-mods/optifabric` | 项目 **322385**，**75 个文件**、总下载 **10,056,111** 次，最高 `optifabric-1.14.3.jar`（MC **1.19.3**，2024-01-12） |
| MC百科 class/1703「支持MC版本」 | Fabric **1.14 ~ 1.20.4** 全列（Chocohead 1.16~1.20 / modmuss50 1.14~1.16），CurseForge Project ID **322385** |

#### 错误的根子：把"某个平台上没有"当成了"这东西不存在"

我的"证据表"三行全是 **Modrinth** 的查询结果 —— 而 OptiFabric
**从来就不在 Modrinth 发布**。查错了平台，还把缺席当成了铁证。

所以现在的规矩（见 ADR-046）：
**凡是"上游有没有"的结论，必须落在它自己发布所在的那个平台上。**

> ★ 顺带说明：`legacy-optifabric`（Modrinth，最高 1.14.4、标 legacy-fabric/ornithe）
> 和 `optifabric-origins`（Modrinth，只有 1.14.4 / 1.15.2）这两行**本身没错**，
> 错的是从"这两个 fork 不覆盖 1.16+"推出"1.16+ 没有桥接包"——
> 主项目根本不在被查的那个站上。

### 修正后的规则（两半都要对）

| MC 版本 | 结论 | 界面必须说 |
|---|---|---|
| 1.14 ~ 1.15.2 | 有桥接包（**OptiFabric Origins**，只有 1.14.4 / 1.15.2 两个版本） | 上游建议用 Origins + 地址 |
| 1.16.1 ~ **1.20.4** | 有桥接包（**OptiFabric**，CurseForge 322385） | 「**要你自己下载**」+ 地址 |
| **≥ 1.20.5** | **真的没有** → 判不兼容 | 换 Forge / 用 Iris + Sodium |
| ≤ 1.13 | 真的没有（那一支是 Legacy Fabric） | 换支持的版本或 Forge |

### ★ 顺手纠正的第二件假承诺：桥接包**永远不会自动装**

`bridgeFile()` 这个下载接口**只存在于 `src/bridge/web.ts`（浏览器演示模式）**，
`SourceAdapter` 里没有它，生产用的 `src/bridge/tauri.ts` 更没有。
所以老代码返回 `manual: false` 时界面写的「（会自动装）」是**一句可验证的假话**。

现在一律 `manual: true` + 真实下载地址，并且加了一条测试钉住它
（`bridge_is_always_manual_because_nothing_downloads_it`）。
哪天真做了自动下载，先把 `bridgeFile` 加进生产适配器再来改这条断言。

### ★ 顺手纠正的第三处：两个判据打架（实测出来的）

`InstallComposer` 的 OptiFine 开关是**在线清单**说了算的
（`ofVersionsKnown`），而 `validateCombination` 走的是**静态表**的 `exists`
（内置表只有 10 个版本）。于是 `1.16.1 + Fabric + OptiFine` 出现：

* 开关**点得动**（在线清单里 1.16.1 有 22 个 OptiFine 发布，实拉核对）；
* 勾上之后却报「OptiFine 的自动安装 IEML 还没有做」—— 因为静态表里没有 1.16.1。

两处修：

1. `OnlineLoaderVersions` 多一个 `addons` 字段，把 OptiFine 的在线清单
   **同时**喂给能力表和组合校验（三态语义与 `bases` 一致）；
2. Rust 侧新增 `loader_caps::addon_exists_on()` —— 基于一张**实拉核对过的**
   OptiFine 覆盖表（BMCLAPI `/optifine/versionList`，实拉 **497 条**，
   逐版本计数抄在注释里），没有实测数据的版本返回 `None`（"不知道"），
   **绝不**当成"没有"。

### 新增：`src/domain/bridge-range.ts`（+ Rust 同名镜像）

桥接区间**只留一份实现**。以前区间判断在 TS 和 Rust 各写一遍，
Rust 那份还用 `mc_version.split('.').get(1)` 取次版本号 ——
对 `24w45a` / `26.2` 算出垃圾值（Java 要求那边踩过同一个坑，这是第二次）。
现在两边都读同一份数据，各自的测试逐条钉住 MC百科表里的 **25 个版本**。

### 测试

* `tests/domain.test.js` 71 → **73**（新增"25 个版本逐个核对"、"1.20.4 合法但要手动下"；
  原「1.16+ 被拒」那条改成 1.20.5+）；
* `tests/e2e-check.mjs` 56 → **59**；
* Rust 331 → **340**（新增 `bridge_range` 6 条 + `combination` 4 条）；
* `tools/live-optifine-incompat-check.mjs` **重写**：现在两个版本段都验 ——
  1.20.1（有桥接包，必须说"要你自己下"+地址，且**不许**说会自动装、安装按钮不被拦）
  与 1.20.6（真不兼容，必须拦住 + 两条替代）。

### 顺手清掉的重复实现

`combination.ts` 里原来有**两条**规则判同一个上界：开头的
`if (compareVersion(mcVersion, '1.20.4') > 0) return 不兼容`
与 `bridgeFor` 内部的上界。第一条先返回，把「缺桥接包 OptiFabric」这条
更有用的信息盖掉了。现在只剩 `bridge-range.ts` 一份。

---

## 0.1.0-dev.10 — 2026-09-14（第八轮）

> ⚠️ **本节的结论已被 dev.11 更正**：文中所说
> 「1.16 ~ 1.20.4 这一段没有任何桥接包」**是错的** —— 那是查 Modrinth
> 得到的结论，而 OptiFabric 从来不在 Modrinth 发布（CurseForge 项目 322385）。
> 本节保留原样，因为"错在哪、怎么错的"本身是最该记住的东西。

### ★★★ 修：Fabric + 高清修复「不兼容」时**必须提示玩家**（用户要求）

用户原话：「当我们选择 Fabic 时如果有版本的 Fabic 与高清修复不兼容，
应提示玩家不兼容」。一查就查出**两层**问题。

#### 第一层：那段版本根本没有桥接包，而我们在承诺"会自动装"

OptiFabric 是 Fabric 上挂 OptiFine 的桥接包。查 Modrinth 的真实结果：

| 项目 | 支持的 MC | 加载器 |
|---|---|---|
| `optifabric`（原 slug） | **404 —— 已下架** | — |
| `legacy-optifabric` | b1.7 ~ **1.14.4** | `legacy-fabric` / `ornithe`（**不是 fabric**） |
| `optifabric-origins` | **只有 1.14.4 / 1.15.2** | fabric |

也就是说 **1.16 ~ 1.20.4 这一段没有任何桥接包** —— OptiFabric 的主要作者
在 1.16 前后停止维护，那个项目现在在 Modrinth 上连项目页都没有
（原 slug 与三个已知 project id 全部 404，都实测过）。

而老代码对这一整段返回 `{ kind: 'optifabric', manual: false }`
—— 等于告诉用户「会自动装」，而**这个包不存在**。
更糟的是 `buildInstallPlan`（唯一会读桥接包的地方）**只有浏览器演示模式在用**，
生产安装流程一行都没碰过它 —— 所以"会自动装"从来没有发生过。

#### 第二层：即使拦下了，也是**悄悄拦**

`validateCombination` 把不兼容的附件从 `addons` 里移出后就**放行**了
（`valid: true`），而 `InstallComposer` 里还有一个 `useEffect` 会
`setAddons(prev => prev.filter(...))` —— **界面直接把用户的勾去掉了**。
用户勾了高清修复、点安装、装完了，而高清修复根本不在里面。

用户的要求恰恰相反：**提示不兼容**。所以：

* 不兼容的附件现在**同时**进 `removed`（界面标红说明）和 `errors`
  （`valid = false` → 安装按钮被拦住）；
* 删掉那个"自动移出勾选"的 `useEffect` —— **去掉那个勾必须由用户自己决定**；
* 附件选项上多一个红色角标「与当前加载器不兼容」；
* 拒绝理由里带上**怎么改**：「取消勾选「OptiFine」就能按现在的组合安装」。

#### 理由必须可行动

提示里说清三件事：缺的是哪个包（OptiFabric）、**为什么**没有
（1.16 起停止维护 / 原项目已下架）、以及两条**真正可行**的替代：

```text
· 换 Forge 作基座 —— Forge 上 OptiFine 是官方支持的；
· 或留在 Fabric，用 Iris + Sodium（光影与性能优化的现代替代，不需要 OptiFine）。
```

#### 顺手修掉一处自相矛盾的文案

真机验证时抓到：界面上**同时**出现「不兼容」和「OptiFine 会在装完原版后
自动安装」两条 —— 一条红一条蓝，互相打架。
现在后者加了条件，两条文案互斥。

#### 真机验证

`tools/live-optifine-incompat-check.mjs`（**8 条断言全绿**）：
在真实界面上选 1.20.1 + Fabric、勾上高清修复，断言出现「不兼容」、
点名 OptiFabric、说清为什么、给出两条替代、**安装按钮真的被禁用**
（`disabled === true`）、且不再同时说"会自动安装"。

> ★ 第一次跑这条测试时，安装按钮那条断言**空过了** ——
> 我的选择器 `^安装$` 没匹配到实际按钮「安装 Fabric 1.20.1」，
> 于是 `installDisabled === null` 被当成"通过"。
> 改成"找不到就算失败"之后才真的验到。
> **断言写宽一点，就等于没写。**

#### 测试断言的方向整体反过来

`tests/e2e-check.mjs` 与 `tests/domain.test.js` 里原来断言
「Fabric 1.20.4 + OptiFine 合法、会自动装 OptiFabric」——
那是在承诺一个不存在的包。现在断言**被拒**，并逐条验理由的内容。
（e2e 从 49 条涨到 56 条。）

---

## 0.1.0-dev.9 — 2026-09-14（第七轮）

### ★★★ 新增：**社区资源子系统**（Mod / 资源包 / 光影 / 数据包）

目标里最后一项。照 PCL 的 `Modules/Resource/*` 做的 ——
那边是六个文件约 2300 行（`ResourceProject` / `ResourceSearcher` /
`ResourceVersion` / `LocalResourceLoaders` / `LocalResourceFile`），
配五个页面（Mod / 整合包 / 资源包 / 光影 / 数据包）：

> **它们不是五个功能，是同一套抽象的五种实例。**

我们这边原来只有 Mod 一种，`resourcepack` / `datapack` / `shader`
在源码里**一处都没有**。

* **`domain/resources.rs`** —— 全部内容就是"那张表"。每种资源只描述四件事：
  ① 到 Modrinth 上怎么查；② 装到实例的哪个子目录；③ 认哪些扩展名；
  ④ 相不相信"必须搭配某个加载器"。
  于是搜索 / 筛选 / 安装 / 扫本地这四件事都只写一遍，加一种资源只需加一行。
* **前端只有一个 `ResourceBrowser`** —— 四种资源共用一套界面，
  选项卡由后端那张表生成（没有 `if (kind === 'shader')` 之类的分支）。
* **入口**：「Mod 管理」页右上角「资源包 / 光影 / 数据包」。

#### 实测纠正了两个想当然的地方

| 我以为 | 实测 | 结果 |
|---|---|---|
| Modrinth 有 `datapack` 这个 `project_type` | 查过去返回的是 **`mod`** | 改用 `mod` + `categories:datapack`（**单数**，`datapacks` 是 0 条） |
| 资源包/光影也该按加载器筛 | 那会把结果砍到几乎没有，而用户会以为"没有这个资源" | 只有 Mod 筛加载器 |

#### ★ 真机测试抓到我自己写的半成品

`extra_category()` 我**定义了却忘了在搜索里用它** —— 于是数据包页搜出来的是
普通 Mod。真机测试一眼抓到（「datapack」0 条，因为下游的二次过滤把普通 Mod
全滤掉了）。**规则定义了却没人用，是这类项目里最难发现的一类半成品** ——
它不报错、不崩、看起来还挺正常。

修法：新增 `modrinth::search_with_facets`，`extra_category` 真的进 facets
（单独一层 —— Modrinth 的外层是 AND、内层是 OR）。

#### 装到哪必须只有一份描述

`platform::instance_resource_dir(slug, kind)` 与 `instance_mods_dir` 同源，
全部挂在 `instance_game_dir` 下面。这是唯一会让"装好了但游戏读不到"出错的点
（这个 bug 修过一次：曾经把 Mod 下到 `instances/{slug}/mods`，
而游戏只读 `game/mods`）。另外安装时**文件名要过判据**
（扩展名 + 不许是临时/禁用文件），因为它来自**外部**。

真机验证：`tools/live-resource-check.mjs` —— **11 条断言全绿**
（四种资源的目录正确、只有 Mod 筛加载器、四种都搜得到东西、
数据包结果每条都带 datapack 分类、界面上四个选项卡、弹窗写明装到哪个目录）。

### ★★ 修：`check-frontend-embedded.mjs` 第五次"检查方法错了"

前几版靠"自己用 brotli q9 压一遍再去 exe 里搜那串字节"定位压缩流。
小文件行得通，**大文件不行**：

```text
index-DYO6pDdS.js   368508 B   q9=false  解压比对=false  解出=0 B
```

实测：我自己压出来的流与 exe 里那份**只有前 10 个字节相同**。
原因是 Rust 与 Node 的 brotli 在**输入分块**上不同
（`BrotliCompress` 内部按固定大小缓冲读 `Cursor`），文件越大分块点越多，
两条流越早分岔。（之前 356 KB 那版碰巧前 662 字节相同，
才让"搜前缀"这条路看起来可行。）

**结论：不能假设"我压出来的字节 == 它压出来的字节"。**

改成**按内容定位**：从每个偏移试着解压，看解出来的东西对不对
（不报错 + 长度对 + 逐字节相同）。压缩流没有 magic number，
只能用内容判断 —— 但这比"字节流相同"**更强**：它证明的是**内容**相同。

同时把判据分清了"证明不足"与"冗余证明"：Vite 产出的文件名
（`index-DYO6pDdS.js`）**中间那段就是内容哈希**，所以对这类文件
"key 命中"**等价于**内容相同；再逐字节比对一遍是冗余的（且要 3 分钟）。
现在大文件靠哈希证明，中等文件仍然逐字节验（76 秒 → 12 项全绿）。

### 本轮验证

`pnpm verify` **12 项全绿** · Rust **331 通过** · 前端 **116 通过** ·
e2e **49 通过** · 资源子系统真机 **11 条** · 真机 UI **14 条** ·
桌面 exe SHA256 一致。

---

## 0.1.0-dev.8 — 2026-09-14（第六轮）

### ★★★ 新增：`domain/validate.rs` + `domain/validate.ts` —— 校验判据收敛成可组合规则

**起因是这个仓库已经因为"同一个判据写两遍、有一处忘了改"栽过两次**，
两次都是我修的：

| 重复的判据 | 后果 |
|---|---|
| `net.minecraftforge:forge` 的前缀匹配写在两个函数里 | 第一次只修了一处，界面照旧显示 `Forge 7.0.1`（真值 47.2.0） |
| 「需要哪个 Java」手写在三个文件里 | `26.2` 的 major 被当成 26 → 落到 `return 8`，显示「需要 Java 8」 |

两次形态完全一样。这个模块提供的是"判据只有一份"的**承载形式**：
规则是**数据**，不是散落的 `if`。

**照着 PCL 的 `ModValidate.vb`（424 行）做的**，包括它那三条很容易写漏的语义：

1. **规则是三态的**：`skip`（中断并直接通过）/ 通过 / 原因。
   `skip` 是为了 `optional` —— "这个字段可以不填，不填的话**后面所有规则都不适用**"。
   ★ 我在 Rust 侧第一版把 `Optional` 的两个方向**全写反了**：
   没填时继续跑后面的规则（把"没填"判成错），填了反而直接通过（把非法值放行）。
   两个方向都错，测试一眼抓出来。
2. **第一条不通过的规则决定结论**（不攒错误）—— 用户一次只该看到**一个**要改的地方。
3. **文案由规则自己带着**，不是调用点拼的 —— 换个地方用同一条规则文案自动一致。

7 条规则（PCL 那 14 个类里真正需要的）：`Optional` / `NotEmpty` / `NotBlank` /
`Matches` / `IntRange` / `LenRange` / `SafeName` / `Custom`。
`SafeName` 逐条抄了 PCL `ValidateFolderName` 的判据（空格开头结尾、点结尾、
`..~数字`、非法字符、`CON`/`NUL`/`COM1..9` 等保留名）。

**自定义谓词用"名字 + 注册表"**（第一版把函数指针放进 enum，导致
`Rule` 彻底不能序列化 —— `#[serde(skip)]` 更是让序列化**直接报错**）。
现在 `Rule` 是纯数据能进 JSON，没注册的名字**报错**而不是静默通过。

**真机上收敛掉的三处重复**：

| 原来 | 现在 |
|---|---|
| 三个页面各写一遍重命名（`prompt` → `trim` → `rename` → 自己弹 toast） | 全走 `renameInstance`，校验 + **重名检查** + 文案只在一处 |
| 离线玩家名**完全没校验**（中文/空格直接写进 prefs → 传给 `--username`） | 走 `offlineUsernameRules()` |

`tests/validate-rules.test.mjs`（14 条）不仅验语义，还**验两边参数逐条一致**
（`max: 64` / `max: 48` / `1..65535` / 同一个正则）——
像服务器地址那张表一样，改一边不改另一边就直接红。已接进 `pnpm verify`。

### ★★★ 新增：**真正的暂停**（不再等于取消）

原来 `pauseTask` 就是 `cancelTask`，注释写着"引擎没有暂停原语"。
对用户来说两者**不是一回事**：

| | 取消 | 暂停 |
|---|---|---|
| 用户意图 | 不要了 | 等会儿接着下 |
| 界面该说 | 已取消 | **已暂停**（进度留在原地） |
| 后续 | 要重新发起 | 点「继续」就接着下 |

混成一个之后，那个"暂停"按钮点下去告诉用户"已取消"——
而它其实什么都没丢，用户于是不敢再点。

**实现**（`download::PauseToken`）：**不再开始新的下载，让在跑的收尾**，
把"还剩哪些"记进 `DownloadOutcome.remaining`（保持原顺序）交回调用方。
已下的 `.part` 分片全部保留。

* 检查点放在"取到并发许可之后、spawn 之前"—— 暂停时**不会留下半启动的任务**；
* 为什么不去中断正在跑的 HTTP：资源阶段几千个小文件，一个通常几百毫秒就完了，
  中断只会丢掉已收到的数据。PCL 的 `LoaderTask` 也是这个语义
  （`TriggerThreadInterrupt` + 不启动下一步，而不是杀连接）；
* 暂停**立刻返回**，不等重试轮 —— 用户按的是"暂停"，不是"再试几轮"；
* 新增 `pause_install` / `resume_install` 两个命令；`InstallOptions` 也带上
  `pause`，于是**核心文件阶段与资源阶段都能暂停**，整合包安装同样可以。

三条测试：`pause_stops_starting_new_tasks_and_returns_the_rest`（8 个任务全暂停
→ `remaining` 恰好是全部 8 个且顺序不变 → **把 remaining 原样喂回去能跑完 8/8**）、
`without_pause_nothing_is_reported_as_paused`（没按暂停就不许报 `paused`）、
`pause_token_toggles`。

### 本轮验证

`pnpm verify` **12 项全绿**（新增「输入校验规则」）· Rust **325 通过** ·
前端 **69 + 14 + 11 + 8 + 14 = 116 通过** · e2e **49 通过** ·
真机 UI **14 条** · 桌面 exe SHA256 一致。

### 还没做

**社区资源子系统（资源包 / 光影 / 数据包）** —— 目标里唯一还没动的一项。

---

## 0.1.0-dev.7 — 2026-09-14（第五轮）

> 用户给了六件事，其中一件是"看看 PCL 是怎么进行微软登录的"。
> 那件**直接挖出一个真故障**（见第一条）。

### ★ 修：`26.2` 显示"需要 Java 8" → 最终显示 **25**

（承接上面第一条：三份副本删掉之后只剩 21，**仍然是错的** ——
因为规则引擎里那条「Mojang 在版本 JSON 里声明」需要的数据前端拿不到。）

* **第二层根因**：`resolveJavaRequirement` 有一条约束是
  「版本 JSON 的 `javaVersion.majorVersion`」（26.2 写的是 **25**），
  而那个数只有版本 JSON 里有 —— 前端只能算到基线规则给的 21。
  也就是"修好了显示 8，但显示成了 21"。
* **修法**：新增 `domain/java-requirement.ts`（**纯的**：缓存 + 唯一计算入口）
  与 `hooks/useJavaRequirement.ts`（调后端取数 + 触发重渲染）。
  * 用 `metadata.version()` 把 `java_major` 取回来 ——
    **这条命令一直在返回 `java_major`，但此前全仓库没有任何地方调用过它**；
  * 结果缓存进内存 + localStorage（`javaVersion` 发布时定死，无过期问题）；
  * 拉不到**不写缓存** —— 写个 0 进去等于把"没查到"永久记成"没有"。
* ★ 按项目铁律，`domain/` 不许有 I/O，所以 React hook 单独放 `hooks/` ——
  `node --test` 于是能直接跑领域层（不需要 DOM / Tauri）。
* 真机验证：`tools/live-java-display-check.mjs` 读**真实 DOM**，
  并轮询到后端那次取数回来为止，读到 **Java 25**
  （与版本 JSON 里声明的 25 一致）。
  * ★ 这条测试第一版**报过假失败**（读到 21）：它只等了 1.5 秒，
    而那次后端调用实测要约 1.6 秒 —— 又是一次"检查方法错了"。

### ★ 修：正版令牌过期后**静默退回离线** —— "第二天变成离线 Player"

* **用户报的**：看 PCL 怎么登录时对照出来的 —— PCL 在启动前会判令牌过期并
  **静默续期**（`ModLaunch.vb` 557-582），我们**完全没有这一层**。
* **根因**（`commands_real.rs::prepare_spec`）：
  ```rust
  auth::load_account(uuid).map(|a| a.to_launch_account())
      .unwrap_or_else(|_| Account::offline(&req.username))
  ```
  **任何**失败都悄悄变成离线账号，而 `is_expired()` 从来没在启动路径上被调用过。
  于是正版 access token 一过期（约 24 小时）：
    * 进不了正版服务器（服务端报验证失败）；
    * 皮肤没了；
    * 而界面上一切正常，**一个字都没说**。
* **修法（照 PCL 的三段式）**：
  ① 没过期 → 直接复用（不打网络）；
  ② **过期 → 先用 `refresh_token` 静默续期**
     （`login.live.com/oauth20_token.srf`，PCL 第 929 行）；
  ③ 续不上 → **如实说出来**（`LaunchSpec.notice` → 界面 toast），
     而不是静默降级；失败原因区分"要重新登录"与"读不到凭据"。
* 新增 `LaunchStarted.notice` / `LaunchPreview.notice` / `LaunchSpec.notice`，
  界面在启动成功的同时弹一条 warning。
* 完整对照见 **`docs/MS-LOGIN-PCL-STUDY.md`**（逐行引用 PCL 行号，已核对）。

### ★★★ 修：`26.2` 显示"需要 Java 8" —— 判据被抄了三份，全都算错

* **用户报的**：「我的 26.2 给我显示要 java8，虽然游戏能打开，但这毕竟不对」。
* **根因**：`1.20.5+ → Java 21 / 1.17+ → Java 17 / 其余 → Java 8` 这条老逻辑
  被**抄了三份**：
  ```
  src/pages/InstanceOverview.tsx   函数的本地副本
  src/pages/LaunchPage.tsx         函数的本地副本
  src/bridge/tauri.ts              函数的本地副本
  ```
  三份都只取 `mcVersion.split('.')[0]` 当 major —— 于是 `26.2` 的 major 是
  **26 而不是 1**，所有 `major === 1 && …` 分支全不成立，落到 `return 8`。
* **启动没坏的原因**：Rust 侧以版本 JSON 的 `javaVersion` 为准 ——
  所以用户看到的是"游戏能打开，但显示不对"。
* **修法**：
  * 三份副本**全部删掉**，统一走 `domain/java.ts::displayJavaMajor`
    （= 同一个 13 条约束求交集的规则引擎，与 Rust 的 `domain::java` 同一套）；
  * ★ 把 `LaunchRequest.java_major` 这个字段**从接口上整个删掉** ——
    前端不再传任何一个数，**错的兜底值不该存在于接口上**；
  * Rust 侧 `find_java_by_requirement` 也不再需要"前端建议值"。
* 回归测试三条（`tests/domain.test.js`）：
  * `26.2 显示 Java 25，不是 8（两位数主版本号）`
  * 26.2 + Forge 必须 ≥ 25
  * **`源码里不许再有第二份「需要哪个 Java」的手写判定`** ——
    源码里再出现 `function neededJavaMajor` 或 `minor >= 17` 就直接红。

### ★★ 改：下载页去掉「已装 xxx 加载器」提示（用户要求）

* 原话：「下载页的版本的模组加载器已装xxx那个提示不要了」。
* 删掉的**不只是文案**，还有它背后的 `detect_installed_loaders` IPC 调用
  和整个 `installed` state —— 留着一段没人显示的代码就是半成品，
  而且它每次切版本都要读一遍盘。
* 触发理由（记在代码里）：这一页的职责是**装新的**，不是汇报旧状态；
  「盘上有」≠「有版本在用」，在这个位置怎么措辞都容易被读成"可以直接玩"。
  「盘上有什么、有没有在用」现在只在两个对的地方说：
  「版本列表」页，以及版本清单每一行的副标题。

### ★★ 改：Fabric API 不自己选就走**默认推荐版**（用户要求）

* 原话：「Fabric 的 API 也是有版本的，如果玩家不自己选，那就走默认推荐」。
* 原来直接 `versions.first()` —— 拿 Modrinth **返回顺序**的第一个，那是碰运气
  （Fabric API 大量发 beta，列表里 release / beta 混着）。
* 新增 `modrinth::pick_default_version`：**正式版 > beta > alpha**，
  同档内取**最新发布**的。
* ★★ **第二级的判据被真机推翻过一次**：第一版按"下载量最高"，
  真机跑出来（`tests/live_api_pick.rs`）：
  ```text
  fabric-api @ 1.20.1：列表第一条 0.92.12+1.20.1（release，412967 次下载）
                       ★ 挑中的   0.92.2+1.20.1（release，9745980 次下载）
  ```
  下载量最大的是 **0.92.2**，比 0.92.12 **更旧** —— 老版本挂得久，累计下载自然更高。
  **按累计下载量挑 = 永远挑到最老的那个。** 改成按发布时间降序后，
  真机 4 个组合全部挑中"最新正式版"。
* 挑中的不是正式版时**如实说明**（"Modrinth 上这个组合没有正式版，
  这是现有版本里最稳的一档"）。
* 回归测试：5 条纯函数测试 + 1 条真机测试（`live_api_pick.rs`），
  其中夹具**故意把下载量设成与"新旧"反相关** —— 一旦有人改回按下载量挑，立刻红。

### ★ 文档：`docs/MS-LOGIN-PCL-STUDY.md`

PCL2 微软登录的逐行研读：六步流程、**缓存层（我们缺的那一层）**、
错误翻译表（设备码轮询 / XSTS 五个码 / 第 4 步 HTTP / 第 6 步 404）、
以及"值得学但还没做的"五条。**引用的每个行号都回查过**。

### ★★ 修：下载「实际文件数和标注的不一致」（用户报的第 4 条）

用户原话：「下载引擎的一个普遍问题，版本下载实际文件和我们标注的文件数量
不一致，虽然这不影响任何功能，但是既然不对，就得改」。

* **根因**：分母是**调用方自己拼出来的**。资源阶段报进度时手里只有
  `finished_files` / `skipped_files` / `failed` 三个数，于是写了
  ```rust
  total_files: outcome.finished_files + outcome.failed.len()
  ```
  这个式子**不等于计划里的任务总数**：
  * 漏了 `skipped_files`（已经下过、这次跳过的）；
  * 失败的文件在重试轮里可能被下成功 → 它进了 `finished` 而不进 `failed`，
    两个数加起来仍然不对；
  * 于是界面上「已完成 / 总数」的分母与计划里标注的文件数**对不上**。
* **修法**：
  * `DownloadOutcome` 新增 **`processed_files`**（成功 + 跳过 + 最终失败），
    与进度事件的分子**同一个判据**（`processed_files` 计数器）——
    调用方不需要、也不允许自己拼分母；
  * 资源阶段的 `total_files` 改用**计划数**（`assets_count`）；
  * 虚拟资源那一段的 `done + failed` 加了说明：它就是**这一个阶段**的总数
    （同步跑完才返回，没有中间进度），不能拿去当整个安装的分母。
* 新增确定性测试 `every_progress_event_uses_the_plan_total_as_denominator`，
  钉住四条：
  ① **每一条**事件的分母都等于计划数（不能中途变）；
  ② 分子单调不减（进度条不许倒退）；
  ③ 最后一条的分子**追平分母**；
  ④ 批次结论的 `processed_files` 与进度事件的分子同源。

> 这条与上一轮那个「2591/2596」是**同一类病**：分子/分母各算各的。
> 上一轮修的是"分子漏了失败与本地复制"，这一轮修的是"分母是拼出来的"。

---

## 0.1.0-dev.6 — 2026-09-14（第四轮）

### ★★★ 修：「早已删掉的加载器版本，下载页还显示已装」

用户原话：「版本列表删除有模组加载器的版本之后，下载列表的对应版本有模组
加载器的版本，**还显示已装**」。

* **根因**：两张表读的是**两个不同的东西** ——
  * 「版本列表」= `instances.json`（用户建的版本）
  * 「下载」页　= 直接扫 `shared/versions/`（盘上有什么）

  删实例只删 `instances/{slug}/`（存档 / Mod / 配置），
  **共享的游戏文件**（`shared/versions/`、`libraries/`）是**故意留着**的 ——
  多个实例可能共用同一份，删了会把别人的游戏弄坏。于是下载页照旧扫到那个
  加载器版本目录，继续显示"已装"。

  **两句话都对，但合在一起就是界面在骗人**：「已装」让人以为可以用，
  而它其实没有任何版本在用。

* **修法**：把「盘上有」与「有版本在用」**分开说**（不是二选一，是两件事）。
  * 后端新增 `ManifestRow.in_use` 与 `InstalledLoader.in_use`
    （`domain::loader_trace::annotate_usage`），来源是 `instances.json`；
  * 判据按**加载器种类**而不是精确版本号 —— 实例记录里的版本与我们实际装出来的
    目录名不一定逐字相同（实测记录写 `47.2.0`、盘上是 `1.20.1-forge-47.4.23`），
    按精确版本匹配会把**真在用**的判成"没人用"，那种误报会让用户去删一份
    正在用的游戏；
  * 界面分三种写法：

    | 情况 | 写法 |
    |---|---|
    | 有实例在用 | `已装 Forge 65.1.3` |
    | 盘上有、没实例在用 | `盘上有 Forge 47.2.0（暂无版本在用）` |
    | 都没有 | 不写 |

  * 「模组加载器」面板里同一份诚实：闲置时写
    「盘上已经有这些加载器，但**暂时没有版本在用**」（配"安装时会复用，
    不用重新下载"），而不是"已经装了"。
* 真机验证：`tools/live-inuse-check.mjs` 直接比对**两个数据源** ——
  后端给的 `in_use` 与磁盘上真实的 `instances.json` 必须一致，
  且凡是 `in_use === false` 的行界面上不许出现"已装"。
  实测本机 7 行带标记的版本全部对得上（`1.12.2`/`26.2` 在用，其余 5 个标闲置）。

> ★ 这条测试**不依赖**用户当前碰巧有没有闲置版本（那样的测试会时绿时红）：
> 一个闲置版本都没有时它会**明确打印"这次没验到核心场景"**，
> 而不是给一个绿得毫无内容的结论。

### ★★★ 修：`net.minecraftforge:forge` 的前缀匹配把 `forgespi` 的版本当成了 Forge 版本

修上面那个 bug 时，真机输出里冒出一行可疑的东西：

```text
1.20.1   2023-06-12 · 盘上有 Forge 7.0.1（暂无版本在用）
```

`7.0.1` 不是 Forge 的版本 —— 本机装的是 **47.2.0**。

* **根因**：判据是 `name.starts_with("net.minecraftforge:forge")`。
  看起来对，其实是个**前缀陷阱** —— `net.minecraftforge` 这个组底下还有一堆
  **别的** artifact，名字都以 `forge` 开头：

  ```text
  net.minecraftforge:forgespi:7.0.1            ← 一个 SPI 库，不是 Forge 本体
  net.minecraftforge:forge-transformers:…
  net.minecraftforge:forge:1.20.1-47.2.0       ← 这个才是本体
  ```

  `1.20.1-forge-47.2.0` 的版本 JSON 里**没有** `:forge:` 那一条
  （老版本 Forge 不写它，只有 `fmlloader` / `fmlearlydisplay` / `forgespi`），
  于是循环先撞上 `forgespi` → 把 SPI 的版本号当成了 Forge 版本。
  而 `26.2-forge-65.1.3` 恰好有 `:forge:` 那一条，**显示正确** ——
  同一个功能两个版本一个对一个错，是最容易漏掉的那种。
* **修法**：判据要求 **artifact 段完全相等**（`group:artifact:` 带结尾冒号，
  把边界钉死）。只判 `contains` 又会把别的组里的同名 artifact 捞进来。
  取不到本体时返回 `unknown`，由调用方用目录名兜底 —— 于是显示回正确的 `47.2.0`。
* ★★ **这个 bug 在两个地方各有一份判据**（`loader_versions_in_json` 与
  `version_of_flavor`）。**第一次只修了一处，界面照旧显示 7.0.1** ——
  是重新真机跑了一遍才发现。教训：**同一个判据写在两个地方，
  就一定会有一处忘记改。**
* 回归测试：`forge_prefix_does_not_match_forgespi`（纯函数）+
  `full_detection_does_not_take_forgespi_as_the_forge_version`（完整检测链）+
  `live-inuse-check.mjs` 里那条"显示的 Forge 版本号必须与磁盘目录一致"。

### ★ 同一次真机跑出来的另一件事：实例记录与磁盘目录不一致

`D:\IEML\instances\forge-1122\` 有目录（一个空的 `game/` + `natives/`），
而 `instances.json` 里**没有**对应记录。这是上次会话修复
`instances.json` 被清空时留下的**孤儿目录** —— 记录补回来了，
但这一份当时不在里面。

这一轮**没有动它**：它不影响任何功能（没有记录就不会出现在任何列表里），
而"替用户猜哪些目录该删"是危险动作。已记录在此，供你决定。

---

## 0.1.0-dev.5 — 2026-09-14（第三轮）

> 用户的原话是：「你至少得验证一切都没问题后，才可以交付于我」。
> 所以这一节的第一条不是"加了什么"，而是**验证过程中抓到的真故障**。

### ★★★ 修：「界面上写着已安装，点启动却报缺 35 个库」

**这条是测试自己抓到的 —— 而且它挖出了一个一直存在的假绿。**

* **假绿是怎么来的**：`commands_real.rs` 里那条"真实版本不缺库"的测试
  把数据目录写死成 `%APPDATA%\IEML\shared`。数据目录搬去 `D:\IEML` 之后，
  这条测试就变成**每次都跳过**（目录不存在 → 直接 `return`），
  绿得毫无意义。
* 改成 `AppPaths::resolve()` 之后**它立刻变红**，报出：
  ```text
  ★ 真实版本 1.20.1 报了 35 个库缺失：
    com.github.oshi:oshi-core:6.2.2、com.google.code.gson:gson:2.10、
    com.google.guava:guava:31.1-jre、com.ibm.icu:icu4j:71.1 …
  ```
* **现场**：`versions/1.20.1/` 里躺着 23 MB 的客户端 jar 和 62 KB 的版本描述，
  43 个库里 **35 个根本不在盘上**（`libraries/com/google/code/gson/gson/2.10/`
  这个目录压根没建）。
* **后果**：界面上这个版本是"已安装"（有 json、有 jar），
  点启动却只会得到一句
  「请回到「下载」页重新安装一次」——
  **我们把该自己干的活推给了用户**：他凭什么知道"已安装"是假的？
  "重新安装"还藏在版本列表的下拉菜单里。
* **修法：启动前自愈**（`net::installer::repair_missing`）
  * 启动前扫盘 → 缺什么补什么 → **补完重新扫一遍**（真相以磁盘为准，
    不以"下载器说补好了"为准）→ 只有补不齐才拦下并如实说明；
  * 与 `install()` **共用同一个 `plan_download_tasks`**，
    所以"该有哪些文件"这份判据只有一份，不会两侧漂移；
  * **幂等**：文件齐了一个网络请求都不发。
* 实测：1.20.1 缺 43 个 → **补回 43 个 / 失败 0** →
  重扫 classpath 从 8 项涨到 43 项、缺失 0 个；再跑一次"文件齐全，不需要补"。
* 回归测试：`tests/live_repair_missing.rs`
  （确定性夹具：把真实版本 JSON 复制到临时共享目录、**故意删掉 3 个库**，
  硬链接指回真文件，绝不碰用户的真实目录）。

### ★★★ 修：Forge 的本地生成 client jar 被当成"缺库"，永远补不上

* **现场**：`26.1.2-forge-64.1.3` 被启动检查拦下，报"缺 1 个库文件"，
  而 `26.2-forge-65.1.3` 同一个位置有文件。
* **根因**：Forge 56+ 的版本 JSON 里有这么一条（真实内容）：
  ```json
  { "name": "net.minecraftforge:forge:26.1.2-64.1.3:client",
    "downloads": { "artifact": {
        "path": "net/minecraftforge/forge/26.1.2-64.1.3/forge-26.1.2-64.1.3-client.jar",
        "url":  "",                       ← 空！
        "size": 77124153 } } }
  ```
  这个 77 MB 的 jar **不是下载来的** —— 它是 Forge 安装器的 processor
  拿原版 jar 打补丁**本地生成**的，所以没有 URL。
  而 `Library::download_url()` 在 url 为空时会**拿 maven 坐标编一个地址**
  （`libraries.minecraft.net/net/minecraftforge/forge/…-client.jar`）——
  那个地址不存在（实测全 404）。于是：
  * 启动检查把它算成"缺失的库" → **装好的 Forge 版本永远被拦在启动之前**；
  * 自愈一次次去下那个 404 → 永远补不上；
  * 用户重装几次都消不掉。
* **修法**：
  1. `download_url()`：**自带 `downloads` 但 `url` 为空 → 返回 `None`**
     （"这份元数据自带下载信息，那就以它为准；`url` 空 = 没有远程地址，
     不要替它编一个"）；
  2. `scan_classpath` 把这个信息分成两类：
     * `missing` —— 编得出地址、盘上没有 → **下载能补**（自愈干这个）；
     * `missing_generated` —— 本地产物（`is_forge_generated_client`）→
       **下载补不了，只能重装加载器**；
  3. 启动报错分开写：后者明确说"到「下载」页重新安装一次 Forge"，
     而不是让用户去点一个修不好的"补全文件"。
* **★ 顺手抓到我自己写的一个顺序 bug**：第一版把"编不出地址就判缺失"
  放在了**看盘之前**，于是连装得好好的 `26.2-forge-65.1.3`
  （client jar 75.52 MB 就在盘上）也被报成缺失。
  是 `live_repair_missing.rs` 把两个 Forge 版本**一起列出来**才看出来。
  正确顺序：**先看盘 → 有就照常进 classpath；没有才报缺。**
* **实测修复**：`tests/live_forge_processor.rs` 对着真实安装流程重跑一次
  Forge 安装器 → `26.1.2-forge-64.1.3` 的 client jar **生成了，
  77,124,153 字节**（与元数据声明的 size 一字不差）。
* 现在本机 **14 个已装版本全部 classpath 完整、缺失 0**。

### ★★★ 修：LiteLoader 的挂载点写死了，Forge + LiteLoader 会丢 Forge 的全部库

* **现场**：界面的组合规则写着「LiteLoader 必须有 Forge 作基座」，
  而 `net::liteloader` 装出来的版本 JSON 里 `inheritsFrom` **写死成 `mc_version`**。
* **后果**：用户选了 Forge + LiteLoader，装出来的却是"挂在原版上的 LiteLoader"——
  Forge 的库**一个都不进 classpath**。安装时毫无提示，只在启动时缺库。
* **修法**：`build_version_json` 增加 `inherits_from` 参数；
  新增 `liteloader_mount_point()` 从**基座加载器**推导挂载点
  （Forge → 盘上那个 `{mc}-forge-{ver}` 目录；否则挂原版）；
  基座目录不存在或没有版本描述 → **报错，绝不偷偷退化**。
  `install()` 里**先定挂载点、再下载** —— 挂载点错了下载再成功也起不来，
  而那种症状（缺库/找不到主类）看起来和"下载坏了"差不多。
* 前端 `installLiteLoader(mcVersion, baseLoaderKind)` 把选中的基座传下去。
* 回归测试：`liteloader_mounts_on_forge_when_base_is_forge`、
  `liteloader_refuses_a_missing_mount_point`、
  `liteloader_refuses_a_mount_without_json`、
  `net::liteloader::tests::mounts_on_forge_when_asked`。

### ★ 修：一批编译不过的集成测试（测试根本没在跑）

`cargo check --all-targets` 一直是红的，意味着**这些测试从来没跑起来过**：

| 测试 | 问题 |
|---|---|
| `natives_layout.rs` / `launch_smoke.rs` / `live_launch.rs` / `live_launch_java.rs` | `LaunchSpec` 新增 `window_title` / `join_server` 后没同步 |
| `fabric_install.rs` | `metadata::fabric_loaders` 加了 `Source` 参数后没同步；还把数据目录写死成 `%APPDATA%\IEML` |

> 教训与上一轮同一条：**测试不跑，等于没有**。
> 编译不过的测试不会报错，它只是"安静地不存在"。

### ★ 修：另外 7 个测试把数据目录写死成 `%APPDATA%\IEML`

同一个假绿模式的**剩余部分**。数据目录搬到 `D:\IEML` 之后，写死的那条路径下
什么都没有，于是：

| 测试 | 写死的后果 |
|---|---|
| `launch_smoke.rs` / `forge_install.rs` | `data_root()` 指向空目录 |
| `install_progress.rs` | 找不到版本 JSON |
| `diag_clientjar.rs` / `fresh_install.rs` / `live_loader_speed.rs` / `live_loaders.rs` | `init_cache()` 指向**不存在的缓存** → 每次都是冷启动，"重复下载 / 热缓存速度"这类问题**永远测不出来**，而测试照样是绿的 |

全部改成 `AppPaths::resolve()`（`root` / `shared` / `cache` 都从它取）。

> 现在 `cargo check --all-targets` **零 error**，只剩两个"测试里的工具函数没被用"
> 的无害 warning。`pnpm verify` 里那条 Rust 检查跑的是 `--lib`，
> **不会**暴露集成测试的编译错误 —— 所以这次是手工跑 `--all-targets` 才发现的。
> 这类"检查范围没盖到"的问题，和假绿是同一种病。

### 其它

* `library_download_url` 的判据改为**共用**（下载侧与启动检查同一份实现），
  不再各写一份然后漂移。
* 新增 `tests/live_forge_processor.rs`：真机跑 Forge 安装器，
  断言 processor 的产物真的出现。
* 新增 `tests/live_repair_missing.rs`：自愈的确定性验证 + 全量真实版本体检。
* 新增 `domain::loader_trace::is_forge_generated_client()`。
* `tools/check-frontend-embedded.mjs` **接进 `pnpm verify`**。
* `tools/live-ui-check.mjs` 加「关于」卡片断言，版本号期望值从
  `package.json` 读（不写死）。
* README「已知限制」按实际代码逐条核对重写（三条已清、六条仍在、一条新增），
  并补上"真机验证"怎么跑。

### ★ 修：版本号维护在四个文件里，界面上却一个地方都不显示

* **现场**：`src/domain/version-info.ts` 的 `APP_VERSION` 由
  `tools/set-version.mjs` 认真同步、`pnpm verify` 里还有一条"四处必须一致"
  的检查、`docs/VERSIONING.md` 明说这个值是"关于页显示用"——
  但**前端从来没有 import 过这个文件**，它是死代码。
  用户报 bug 时第一个会问的就是"我这是哪个版本"。
* **修法**：设置页加「关于」卡片。
  * **后端版本**（编译期 `env!("CARGO_PKG_VERSION")`）—— 从 `app_info` 读出来，
    一路接到 `state.backendVersion`；
  * **前端版本**（`APP_VERSION`）—— 两者并排显示，
    **不一致就说明改了版本号没重新构建**（这是最容易发生又最难发现的一种漂移）；
  * 阶段（内部开发版 / 小范围测试版 …）直接用 `STAGE_LABEL`，
    不在页面里再写一份映射。
* 真机断言（`tools/live-ui-check.mjs`）：读到的是 `0.1.0-dev.5`，
  且与 `package.json` 一致。

### ★ 修：`check-frontend-embedded.mjs` 自己误报了四次

这个脚本的作用是"证明 release exe 里的前端就是当前 dist"。
它**四次**报出错误的结论，每次都是**检查方法错了**，不是被测对象错了：

| 第几次 | 方法 | 为什么错 |
|---|---|---|
| ① | 在 exe 里搜 `createRoot` 字符串 | Tauri 把前端 **brotli 压缩**后内嵌，原文不在二进制里 |
| ② | 文件名 + **原始字节数**命中 | 压缩后长度与原始长度不同 |
| ③ | `zlib.brotliCompressSync` **默认参数**（q11） | Tauri 用的是 **q9**（`tauri-codegen::compression_settings`，release 9 / debug 2）。改成 q9 后 5/7 立刻对上 |
| ④ | 用 Node 的 zlib **流**收集解压结果 | Node 的 zlib 流是**异步**的，同步收集永远拿到 0 字节 |

剩下 2 个文件对不上是**两个不同的原因**，混在一起看就像"构建坏了"：

* `index.html`：Tauri **故意改它** —— `map_core_assets` 往 HTML 里注入
  CSP nonce 与 script hash 再嵌。所以 exe 里那一份和 dist 里那一份
  **本来就不该相同**。
* 主 JS bundle：真的是同一份内容，只是 Rust 与 Node 的 brotli 编码器
  输出的字节流略有不同（同输入、同窗口、同质量，分块细节不同）。
  **精确字节比对在这条路上走不通** —— 改成"定位压缩流 → 解压 → 逐字节比对"，
  实测 **356665 / 356665 字节完全相同**。

现在用两个层次的判据：
  **A** 资源 key（Vite 的内容哈希文件名）必须在 —— 与压缩参数无关，最硬；
  **B** 内容比对 —— q9 字节流命中，或解压后逐字节相同。

并把这条检查**接进 `pnpm verify`**（有 release exe 时才跑）。

### ★★★ 修：进度永远到不了 100%（`2591 / 2596`）—— **真根因是两个，不是一个**

上一轮我以为修好了（把"失败"也算进分子），这一轮写了一条**确定性的**测试，
才发现分子**多算**和**少算**同时存在：

```
进度事件：[(1,3), (2,3), (3,3), (3,3), (4,3), (5,3) … (12,3)]
```

3 个任务，进度涨到 **12/3**（分子是分母的 4 倍）。所以之前那个"修法"
只是**碰巧**把少算的部分补上了 —— 而大盘上只有 5 个文件失败，
多算的几个正好盖住少算的几个，看起来像"正常"。

**两个真根因**：

| # | 问题 | 后果 |
|---|---|---|
| ① | `processed_files` 在 `match` **外面**无条件 +1 —— **失败的任务也被算了**，而它下一轮还会被重试 | 同一个文件被数好几次，分子**多算** |
| ② | 去重产生的**本地复制（`CopyJob`）从来没被算进分子**，却在分母里 | 只要一批里有重复内容的文件，进度**永远差那么多** |

②正是用户那张截图的成因：1.20.1 的资源里有 23 个"同一 hash 被两个名字引用"
的条目，分母含副本、分子不含 —— 于是差几个，永远到不了头。

**修法**：
* `processed_files` 只在**终结**时 +1：成功 / 跳过当场算，
  重试到最后的失败在批次末尾按 `pending.len()` 补算；
* 本地复制每做完一个也 +1（并补发一条进度事件）；
* 重试的进度事件从"轮次**开始前**"挪到"轮次**结束后**"——
  事件里的 `failed_files` 必须是**本轮结果**，不是上一轮残值。

**两条测试钉住它**（都用保留域 `.invalid` 造必然失败，不依赖网络运气）：
* `progress_reaches_total_even_when_files_fail` —— 3 个必失败任务 → 最后必须是 `3/3`
* `progress_counts_local_copies_too` —— 同 SHA1 的两个任务（1 网络 + 1 副本）→ 最后必须是 `2/2`

> 教训：**"看起来修好了"和"真的修好了"之间的距离，只有一条确定性测试能量出来。**
> 上一轮的修法在大盘上"有效"，是因为两个错误互相抵消 —— 那种巧合
> 比纯粹的 bug 更危险，因为它会让人以为已经没事了。

### ★★ 修：正版登录的「完成后自动继续」是**假的** —— 要用户点第二次

* **现场**：界面上写着「完成后<b>自动继续</b>，不用回来点任何东西」，
  而实际流程是：拿到设备码 → 用户去浏览器输码 → **必须回来点一次
  「我已在浏览器完成」**才会开始轮询。
  用户于是盯着那个设备码等，什么都不会发生 —— 看起来就是"登录坏了"。
* **修法**：拿到设备码就**立刻开始轮询**（`useEffect` 接管），
  用户只需要去浏览器里输码。界面上显示"正在等你在浏览器里完成登录"，
  失败的原因**留在页面上**（带「重新开始」）而不是弹一个会自动消失的 toast ——
  失败原因（设备码过期 / 拒绝授权 / 没买游戏 / 网络）是用户下一步做决定的依据。
* 真机断言（`tools/live-ms-login-check.mjs` 第 ⑥ 段）：
  点**一下**就拿到真实设备码（如 `KJX65MJD`）、页面进入等待态、
  **「我已在浏览器完成」按钮已不存在**。

### ★★ 正版登录的请求形状**对着真实端点验过**

* 登录的最后一步必须**人在浏览器里输一次设备码**，所以测试跑不完。
  但"请求形状对不对"完全可以验：拿一个**真实注册过的公开 client_id**
  去打微软的端点，看它是**接受**（200 + 真设备码）还是**判成格式错误**
  （400 + `invalid_request`）。
* 这是能抓到本仓库已经犯过的两个真 bug 的那条检查：
  ① 写死的 `00000000402b5328` 早被微软删了（`AADSTS700016`）；
  ② 刷新令牌用错端点（应是 `login.live.com/oauth20_token.srf`，PCL 第 929 行）。
* 实测（`tests/live_ms_protocol.rs` + `tools/probe-ms-devicecode.ps1`）：

  | 检查 | 结果 |
  |---|---|
  | (A) devicecode 端点接受 `client_id + scope` | **HTTP 200**，真设备码（`user_code` 8 位、`expires_in` 900、`interval` 5） |
  | (B) token 端点接受 `client_id + grant_type + device_code + scope` | **`authorization_pending`**（"看懂了，用户还没同意"）—— **不是** `invalid_request` |
  | (C) **对照组**：故意少字段 | **`invalid_request`**（`AADSTS900144`）→ 所以 (B) 的通过是有意义的 |

  > ★ (C) 是关键。没有对照组的话，一个"对什么都点头"的端点也会让 (B) 变绿。
* 这一步还纠了我自己的一个**误判**：我以为"轮询时少了 `scope` 是个 bug"，
  但两个权威实现并不一致 —— **PCL 带 scope**（`MyMsgLogin.xaml.vb` 111-114），
  **HMCL 不带**（`OAuth.java`）。真机验证说明两种都能过，
  所以这不是 bug。**证据推翻了推测**，注释按实测改了。
* ★ 探针用的 id 是 Prism Launcher 的**公开应用 id**（其 `CMakeLists.txt`，GPL-3.0），
  **只用于验证协议形状，不进入我们的构建产物**；仓库里不硬编码任何第三方 id。
  没有 `IEML_MS_PROBE_CLIENT_ID` 时测试**跳过并说明原因**，不假装通过。

### ★ 修：`live-ms-login-check.mjs` 看错了目录（又一次"检查方法错了"）

* 它把 client_id 文件路径写死成 `%APPDATA%\IEML\ms_client_id.txt`，
  而数据目录是 `D:\IEML`。文件**写成功了**，断言却报「没写盘」。
  同一个"写死旧数据目录"的病，这轮在 Rust 测试里修了 7 个，这里是第 8 个。
* 现在先读 `%APPDATA%\IEML\datadir.txt`，读不到才退回老布局。
* 同一脚本里还有一条**假通过**：设备码是在整页文字里捞
  `/\b[A-Z0-9]{8}\b/`，结果抓到的是「当前应用 ID：11111111…」里那段打码前缀。
  改成从设备码**自己的元素**（`.dc-code`）里读。
* 还有一条**假红**：断言找的是「缺一个微软应用 ID」，而文案早就改好了
  （现在说清"为什么"和"三个办法"）。改成查行为：
  缺 client_id + 三个办法 + 申请入口。

  > 断言要盯**行为**，不要盯**某一句具体的话** —— 文案会改，行为不会。

### ★ 实测证据（这一轮的"验证一切都没问题"）

| 验证 | 结果 |
|---|---|
| `pnpm verify` | **11 项全绿** |
| Rust 领域测试 | **297 通过 / 0 失败** |
| 前端单元测试 | **99 通过 / 0 失败** |
| 端到端规则校验 | **49 通过 / 0 失败** |
| 子进程窗口抑制审计 | 25 处全部抑制 / 0 遗漏 |
| 本机 14 个已装版本 | **classpath 全部完整、缺失 0** |
| 微软登录请求形状（真端点） | (A) 200 + 真设备码 · (B) `authorization_pending` · (C) 对照被拒 |
| 微软登录界面链路（真机 CDP） | **13 条断言全绿**（含"点一下就自动开始轮询"） |
| Forge processor 真机 | `26.1.2-forge-64.1.3` 的 client jar 重新生成成功（77,124,153 字节） |
| 启动前自愈真机 | 1.20.1 缺 43 个 → **补回 43 个 / 失败 0** |
| 真机 UI（WebView2 CDP） | **13 条断言全绿** |
| 桌面版部署 | exe 与构建产物 **SHA256 一致** |

---

## 0.1.0-dev.4 — 2026-09-14（第二轮）

### ★★★ 修：`merge_versions` 丢库 —— **每一个带加载器的版本都起不来**

这是本轮最严重的一条，而且它解释了为什么装了加载器反而更糟。

* **现场**：OptiFine / Forge / Fabric 版本启动，崩在
  ```text
  Caused by: java.lang.NoClassDefFoundError: org/lwjgl/BufferUtils
  ```
  而原版 1.16.5 的版本 JSON 里**明明列着** lwjgl 3.2.2。
* **根因**：`merge_versions` 按 `lib_key`（**`group:artifact`，不含版本**）去重，
  而 1.16.5 的 JSON 里 lwjgl 有**两代**：
  * `org.lwjgl:lwjgl:3.2.1` —— rules 只允许 osx（Windows 上会被过滤）
  * `org.lwjgl:lwjgl:3.2.2` —— rules 允许 windows（**真正要用的那个**）

  合并**不看 rules**，只看键 → 先遇到的 `3.2.1` 赢、`3.2.2` 被丢 →
  classpath 上是 3.2.1，而它里面没有 `BufferUtils`。
* **同一个粗键的第二个后果**：`natives: {}` 与
  `natives: {windows:"natives-windows"}` 是**两条**（一条进 classpath、
  一条要解压 dll），也被当成重复丢掉 → 实测 natives 从 **8 个变 0 个** →
  游戏死在 `UnsatisfiedLinkError`。
* **为什么只有合并时才会撞上**：不合并时两条都留着，`scan_classpath`
  按各自 rules 过滤，自然选对。所以这条 bug 只在
  OptiFine / Forge / Fabric / Quilt 这些 `inheritsFrom` 的版本上出现。
* **修法**：去重键改成**库的身份** `lib_identity()` = 完整坐标（含版本与
  classifier）+ natives 平台映射。分工变成：
  **合并负责别丢东西，过滤（`scan_classpath`）负责选对东西。**
* 回归测试：`tests/merge_keeps_libraries.rs`（合成用例 + 本机真实版本）

### ★★★ 修：Forge 26.2「还没打开就崩溃」

* **现场**（用户原话「它还没打开就报错崩溃了」），日志只有三行：
  ```text
  Error: Could not create the Java Virtual Machine.
  Error: A fatal exception has occurred. Program will exit.
  Unrecognized VM option 'UseCompactObjectHeaders'
  ```
  游戏**一行自己的日志都没写** —— JVM 在启动前就拒绝了参数。
* **根因**：`26.2` 的版本 JSON 写着 `"javaVersion": {"majorVersion": 25}`
  （Mojang 明确要求 Java 25），而 `26.2-forge-65.1.3` 的 profile 带
  `-XX:+UseCompactObjectHeaders`（**Java 24 才有**）。
  我们的规则表只有一条 `>= 1.20.5 → Java 21，区间 [21,24)`，
  **从来没读过 JSON 里那个字段** —— 于是选到 Java 21，JVM 直接拒绝。
* **修法（照 PCL 的 `GetJavaRequirement`，`ModJava.vb` 128-265 行）**：
  * Java 要求从"第一条命中的规则"改成**多条约束求交集**。
    老写法把下限和上限混在一个区间里，后写的规则**无法收紧前面写下的上限** ——
    那正是 `[21,24)` 挡住 Java 25 的原因。
    （`docs` 里那条"26.2 → Java 21"在 `docs/DECISIONS.md` 引用的
    `LAUNCHER_SOURCE_STUDY` 就是设计稿时代的产物，实测被推翻。）
  * **读 `javaVersion.majorVersion`**（PCL 第 173-185 行，且只在该值 >= 22 时采信）。
  * `find_java` 改成**按区间**挑，取区间内**最高**可用版本。
  * 启动参数最前面加 `-XX:+IgnoreUnrecognizedVMOptions` 作为保险 ——
    这类崩溃代价特别大（游戏一行日志都没有），多一道网值得。
* **真机验证**（`tests/live_forge_launch_args.rs`）：用真实版本 JSON 收集
  `-XX:` 参数，让 Java 25 真的解析一遍 → 退出码 0；**对照组**用 Java 21 →
  退出码 1、报的正是 `Unrecognized VM option 'UseCompactObjectHeaders'`。
  对照组必须失败，这条测试才算测到东西。

### 新增：OptiFine 自动安装（**实装**，不再是空头支票）

* 上一轮把 `addon_install_implemented(OptiFine)` 改成 `false`（诚实地说"没做"），
  这一轮**真的做出来了**。
* 算法来源：PCL `ModDownloadLib.vb` 的 `McDownloadOptiFineLoader`（473-631）与
  `McDownloadOptiFineInstall`（350-467）。关键事实逐条对应写在
  `src-tauri/src/net/optifine.rs` 的文件头。
* 两个实现细节是**真跑才发现的**，值得记下来：
  * **`--add-exports` 只能给 Java 9+**（PCL 第 381 行有同样的判断）。
    1.16.5 的 G8 安装器是 **Java 8** 编译的，Java 8 不认识这个选项 →
    `Unrecognized option: --add-exports` → 安装器一行都没跑。
  * **环境变量 `appdata` 必须改掉**（PCL 393-397）。只给 `-Duser.home` 时，
    安装器第一句输出是 `Dir libraries: C:\Users\...\AppData\Roaming\.minecraft\libraries`
    —— 它去**用户真实的** `.minecraft` 里找版本，于是什么都不装，
    而且退出码看着还正常。
  * 装出来的版本 id 是 `<mc>-OptiFine_<补丁>`（`1.16.5-OptiFine_HD_U_G8`），
    **不是**文件名去掉 `.jar`。我第一版按文件名拼，于是"装完了却找不到产物"
    —— 产物好好地在盘上。现在改成**读盘确认**（`locate_produced_version`），
    不拿"我们的理解"去当"实际发生了什么"。
* **真机验证**（`tests/live_optifine.rs` + `live_optifine_launch.rs`）：
  真的下载 7 MB 安装器 → 跑 Patcher → 产出 patched jar + 版本 JSON +
  库 → **真的启动，活过 30 秒**，日志里 OptiFine 的
  `ConnectedTextures` / `BetterGrass` 都在正常工作。

### 修：Fabric 前置包「不自动安装」

* 后端**是好的** —— 真机验证 26.2 能装到 `fabric-api-0.160.0+26.2.jar`（2.5 MB）。
* 真正的问题是**只有"创建实例"那一次机会**：老实例、创建时网络抖动装失败的实例，
  事后**没有任何地方能补**。
* 新增 `check_api_library` 命令 + Mod 管理页的常驻横幅：
  缺前置包就显示「一键补装」，而不是让用户等到装 Mod 崩了才发现。

### 顺带发现（待修）

* `versions/1.20.1/`、`1.21.1/`、`26.1.1/`、`rd-132211` **只有 jar、没有版本 JSON**
  —— 这种目录既起不来也没法打补丁。已列入下一轮。
* 合并后 `1.20.1-forge-47.2.0` 报 7 个库缺失 —— 已列入下一轮。

> 教训（本轮记了三次同一条）：**测试报红时，先确认测试本身在测什么。**
> OptiFine 那次最典型：我手写了一个"简化版合并"来拼测试命令，
> 结果它自己把 20 个游戏参数丢成 2 个 —— 于是 OptiFine 报 NullPointerException，
> 我一度以为是安装装坏了。用**生产代码**（`merge_versions` / `eval_args`）
> 而不是手写等价物，是唯一可靠的做法。

---

## 0.1.0-dev.3 — 2026-09-14

内部开发版。本轮的全部改动都由**用户报的真实问题**驱动，逐条记下来。

### 修：启动器找不到本机的 Java（用户报"它根本不知道去哪里找 java"）

* **根因**：`scan_java()` 只看四个地方 —— `JAVA_HOME` / `PATH` /
  `Program Files\*` / 自己下载的 `data/java`。
  而用户的 Java 21 与 Java 8 都是 **Minecraft 官方启动器**下的，躺在
  `%APPDATA%\.minecraft\runtime\java-runtime-delta`（`JAVA_VERSION="21.0.7"`）
  与 `…\jre-legacy`（`1.8.0_51`）—— **从来没被扫过**。
  于是启动器眼里本机"只有 Java 25"。
* 现在扫 **8 类来源**：IEML 下载的、用户手选的、Mojang 官方运行时、
  Windows 注册表、其它启动器缓存（PCL2 的 `config.json` / HMCL 的 `java.txt`）、
  `JAVA_HOME`/`PATH`、常见安装根目录、盘符浅扫描（`C:\jdk25\jdk-25.0.3+9` 这种）。
* 扫描改成**先零成本收集候选、再去并行探测**（最多 8 路）。以前是边发现边
  `java -version`、串行跑，一次扫描能把界面卡住几秒。
* `scan_java` 命令改成 `async` + `spawn_blocking`，不再占住事件循环。
* `JavaRuntime` 的 `source` 现在如实区分
  `downloaded` / `manual` / `mojang` / `registry` / `launcher` / `system` / `scan`。

### 修：找不到要求的 Java 时会**偷偷换一个**（1.12.2 崩溃的帮凶）

* **根因**：`find_java` 的最后一步是
  `if let Some(rt) = scanned.first() { return Ok(rt.path) }` ——
  "找不到 Java 8？那就随便拿一个吧"。1.12.2 于是被本机最新的 Java 25 拉起来。
* 现在分三步：① 精确命中主版本 → 用它；② 别的主版本**确实落在该版本的
  兼容区间内** → 用它并记日志；③ 都不满足 → **拒绝启动**，
  并把"本机有哪些 Java、该装哪个、去哪装"一次说清。

### 修：1.12.2 打不开、崩溃（natives 目录是空的）

* **现象**：日志只有一句话
  `java.lang.UnsatisfiedLinkError: no lwjgl64 in java.library.path:
   …\instances\vanilla-1122\natives`，而那个目录**空的**。
* **根因**：老格式（1.13 及以前）的 natives **坐标里看不出来** ——
  `org.lwjgl.lwjgl:lwjgl-platform:2.9.4-nightly-20150209` 没有任何
  `natives-` 字样（natives 声明在 `natives` 字段 + `downloads.classifiers` 上）。
  `is_native_library` 只认坐标里的 `natives-` classifier，于是：
  * 那个 **22 字节的占位 jar** 被塞进了 classpath；
  * 真正装着 `lwjgl64.dll` 的 `…-natives-windows.jar`（613 KB）
    **没有**被登记为"要解压的东西"。
* 修法：
  * 新的**唯一判据** `metadata::is_native_lib(&Library)` —— 坐标与 `natives`
    字段**一起问**（任一个单独都不够：`jinput-platform` 只有字段、
    `lwjgl:3.3.1:natives-windows` 只有坐标）。旧的 `is_native_library` 改名成
    `name_looks_native` 并标明"这只是坐标那一半"，逼调用方用完整版。
  * `legacy_natives_relative()`：老格式真正要下的那一个文件（classifiers 里的）。
  * 下载计划的**地址、sha1、size 全部取 classifiers 那一条** ——
    以前拿 22 字节 artifact 的哈希去校验 613 KB 的 natives jar，必然失败。
  * `legacy_natives_classifier()` 不再"随便拿一个平台"兜底
    （以前在 Windows 上会把 `java-objc-bridge` 解析成 `natives-osx`，
    然后去下一个 macOS 的包，报"库缺失"，用户重装一百次也修不好）。
  * `scan_classpath` 去重：1.12.2 里 `lwjgl-platform` 有 2.9.4 与 2.9.2 两条，
    以前两条都会进 classpath / natives 列表，两份冲突的 dll 会互相覆盖。
* **新增启动前硬闸门**：`count_native_binaries()` 数 natives 目录里的
  dll/so/dylib，**一个都没有就拒绝启动**并说清怎么修。
  以前是"解压完就往下走"、不看结果，把一个注定崩溃的进程拉起来，
  让崩溃现场离真正的原因隔了整整一层 JVM。

### 修：OptiFine 的"幽灵状态"（显示有、点了不让选）

* **根因一（前端）**：`InstallComposer.loadLoaderQueries` 的**缓存命中分支**
  只写了 `setQueries(...)` 就 `return`，**一句都没提 OptiFine** ——
  于是 `optifine` 状态还留着**上一个版本**的那一份。
  复现步骤正好就是用户描述的：切到有高清修复的版本 → 切回没有的版本，
  界面上那个开关显示着旧版本的结论。
  修法：抽出唯一的 `optifineStateOf(rec)`，两条路径都走它；
  而且缓存里没有 OptiFine 记录时**不拿旧结论充数**，老实重新查。
* **根因二（前后端一致）**：`addon_install_implemented(OptiFine)` 一直返回
  `true`，注释还写着"`install_version` 里有 Patcher 分支"——**那句话不成立**。
  全仓库搜 `optifine` 只有三件事：拉清单、认磁盘、算兼容性，
  **没有任何一处真的下载 OptiFine / 跑 Patcher / 写版本 JSON**。
  `README` 的功能表从第一天起就写着"未实现"，是这张表在说谎。
  于是界面上那个开关**一半能点一半不能点**：在线清单查到了就能点（但装了没用），
  查不到就灰掉。用户看到的"显示有、实际上不让选"就是这个。
  修法：前后端一起改成 `false`，界面统一显示「上游有 · 但我们还没做」，
  并保留"换个启动器装好、IEML 能启动它"这条出路。
* **顺带修一个措辞顺序 bug**：`addon_compatibility` 原来把"我们还没做"放在
  **最前面**，于是 `1.20.4 + NeoForge + OptiFine` 会显示
  「我们还没做 OptiFine」而不是「NeoForge 与 OptiFine 不兼容」——
  前者暗示"换个加载器就能装"，用户会照做然后白忙一场。
  现在**真实的不兼容一律先说**（Rust 与 TS 两侧同一条铁律）。
  Fabric/Quilt 分支原来是 `return {ok:true}`，**绕过**了 implemented 闸门 ——
  也一并修掉。
* **理由顺序的第三条规则**（`loader_caps`）：置灰理由里
  **"我们没做"要排在"清单没查到"前面**。这两句对用户的含义不同 ——
  前者是"等我们或换启动器"（不可解决），后者是"重试一下就有了"（可解决）。
  先说后者，用户会一直点「重新查询」，而真正的原因永远看不到。
  这条是**真机界面检查**发现的（见下面的 `tools/live-ui-check.mjs`）。

### 修：Mod 安装"没有功能键"（用户报"我怎么装 mod 嘛"）

* 查下来**功能是有的**（Mod 管理页右上角「添加 Mod」→ 搜 Modrinth → 安装），
  但它只存在于「版本列表 → 双击版本 → Mod 管理」这条三层深的路上。
  缺的不是功能，是**入口**。
* 新增三处入口：
  * 「概览」页顶栏 →「安装 Mod」（点了直接切到 Mod 页并弹出搜索框）
  * 「概览」页的 Mod 事实行 →「装 / 管」两个动作（"装"弹搜索框，"管"只看列表）
  * 「版本列表」每一行的 ⋯ 菜单 →「安装 Mod」
* 顺带修一个**导航契约**问题：`nav/open-instance` 会把 `subPage` 归零成
  `overview`，所以"先切页签再打开版本"会被覆盖、"先打开再切"依赖两次
  dispatch 的先后语义（隐式契约，改一次就悄悄坏）。
  新增一次性的 `nav/open-instance-sub` action 把意图装进**一次** dispatch。
* 新增 `src/state/events.ts`：跨页面的事件名从此只写一次
  （以前发送方和接收方各写一遍字符串，拼错一个字母就静默失效，
  界面上表现为"点了按钮什么都没发生"，还没有任何报错）。

### 改：数据目录默认避开系统盘

* 规则见 `platform::resolve_data_root`，优先级：
  ① 环境变量 `IEML_DATA_DIR` → ② exe 旁边的 `ieml-portable.txt`（便携模式）
  → ③ 选址记录 `%APPDATA%\IEML\datadir.txt` → ④ 老位置（**仅当它不在系统盘**）
  → ⑤ 空闲空间最大的非系统盘（本机 → `D:\IEML`）→ ⑥ 单盘机器老实回老位置。
* 为什么值得做：一个装了十几个整合包的启动器，`libraries` + `assets` +
  实例很容易超过 20 GB，而系统盘通常最小、最满、也最不该被写满。
* 老用户**一次性搬家**：启动时把老位置的数据**复制**到新位置
  （只复制、绝不删源 —— 搬家中途断电用户不能同时失去两边），
  成功后才写记录。可重复执行（已存在的文件跳过），半截的 `.part` 不搬。

### 改：版本号终于有规则了

* 新增 [`docs/VERSIONING.md`](docs/VERSIONING.md)：阶段表
  （`dev` < `alpha` < `beta` < `rc` < 正式）、什么时候动哪一位、
  **进入每个阶段的判定条件**、以及本项目自己的版本历史。
* 新增 `tools/set-version.mjs`：一条命令把版本号写进四处
  （`package.json` / `Cargo.toml` / `tauri.conf.json` / `version-info.ts`），
  `--check` 校验一致性（已接进 `pnpm verify`）。
* 以前三个文件里各写一份 `0.2.0` —— 而 `0.2.0` 按规则读作
  "第二个公开稳定版"，与本项目的实际阶段完全不符。**版本号一旦开始说谎，
  后面所有"这个 bug 是哪个版本引入的"都无从谈起。**
* 本轮把三处统一到 `0.1.0-dev.3`（前两轮的 `dev.1` / `dev.2` 为回溯补记）。
* 新增 `src/domain/version-info.ts`：浏览器开发模式（`pnpm dev`）没有 Rust
  进程，拿不到 `env!("CARGO_PKG_VERSION")`，所以前端要有一份自己的。

### 修：**丢用户数据**（本轮最严重的一条，我自己引入的）

* **现象**：`instances.json` 变成 `{"instances":[],"active_id":null}` ——
  用户建的版本从界面上全部消失（游戏文件还在，但记录没了）。
* **两端合起来造成的**：
  * **前端**（`AppContext`）：`boot` 用 `Promise.all([machineInfo,
    loadInstances, scanJava, loadPrefs])`，**任何一条失败**就整体进 catch →
    `boot/fail` → 实例列表留在 `[]`。
    而"实例变更时落盘"那个 effect 只判 `state.ready` ——
    `boot/fail` **也把 ready 置真**，于是 250ms 后把**空列表写回了磁盘**。
    现场证据：`instances.json` 的修改时间 = 启动后 1 秒。
  * **后端**（`platform::migrate_data_root`）：只按"目标存在就跳过"处理，
    于是被写空的那份**永远修不回来**，哪怕老位置的文件好好地躺在那里。
* **修法**：
  * 前端加 `instancesLoadedRef`：**没成功读到过，就绝不允许写**
    （与偏好那边的 `prefsLoadedRef` 同一个道理 —— 实例这边一直漏着，
    而实例里装的是用户的游戏，更贵）。
  * 启动的四条请求改成 `Promise.allSettled` **各自独立**：
    只有"机器信息"失败才算真的启动失败；实例/Java/偏好失败各自兜底并
    **把失败说出来**（不能假装"一个版本都没有"—— 那正是丢数据那个 bug 的外观）。
  * 迁移改成**补齐**语义：目标缺什么补什么，**目标已有的一字节都不动**；
    目标的 `instances.json` 是空列表而源里有人 → 用源的（能自愈）。
    仍然只复制、绝不删源。
* **回归测试**（`platform.rs` 里 5 条）：
  目标已有的文件不被覆盖 / 被写空的实例列表能补回来 /
  目标健康的列表不被源替换 / 目标为空时整份搬过去 / 半截下载残留不搬。
* **数据已恢复**：用 `src-tauri/tests/repair_instances.rs` 跑了真实修复，
  三个版本记录（Minecraft 26.2 / Fabric 26.2 / Minecraft 1.12.2）全部补回，
  实例目录也都在。修复后**再启动一次，确认没有被写空**。

> 教训：**"读失败"与"确实是空的"必须长得不一样。**
> 把前者当成后者，用户的数据就会在没人注意的时候消失 ——
> 而且现场只留下一个"正常"的空列表。

### 修：安装 Forge 会弹出一个"啥也没有的 cmd 黑框"

* **现象**（用户原话）：「安装 Forge 调出来个啥也没有的 cmd 是何意味」。
* **根因**：Windows 上，**GUI 子系统进程**（Tauri 的 `ieml.exe` 没有控制台）
  启动**控制台子系统程序**（`java.exe`）时，系统必须新建一个控制台；
  不传 `CREATE_NO_WINDOW` 就会**弹出那个窗口**。
  Forge 安装器的输出被启动器接走了（要判断成功失败、给用户看进度），
  所以窗口里什么都没有，只剩一个空壳挂在屏幕上。
* **为什么必须用命令行**：Forge 官方只提供 installer jar 这一个无人值守接口
  （`java -jar forge-installer.jar --installClient <dir>`），没有 API、没有库。
  所以"用命令行跑"是对的 —— 错的是**让那个窗口露出来**。
  静默地跑、把结果如实报告，这才叫"自动安装"。
* **为什么之前只修了一半**：`CREATE_NO_WINDOW` 只加在了
  `std::process::Command` 那几处，而唯一真的会弹给用户看的地方
  —— 安装 Forge —— 恰好是唯一用 `tokio::process::Command` 的地方。
  全仓库审计：**4 处加了标志，13 处没加**。
* **修法**：
  * 新增统一入口 `platform::hide_console`（std）与
    `platform::hide_console_async`（tokio）—— 那个魔数 `0x0800_0000`
    从此只出现一次，各处手写抄错一位就是一个新黑框且不会报错。
  * **17 处创建子进程的位置全部走统一入口**（含 6 个测试文件）。
  * 新增 `tools/audit-spawn-windows.mjs`：逐处审计"有没有抑制窗口"，
    已接进 `pnpm verify`（第 10 项检查，当前 17/17 通过）。
  * 新增 `src-tauri/tests/live_console_window.rs` 三条测试：
    统一入口确实写了标志（不是空函数）、没有地方绕过它手写魔数、
    链式调用没被破坏。
* **关于验证的边界（诚实说明）**：这个黑框**在 `cargo test` 里复现不出来** ——
  测试宿主自带控制台，"控制台进程启动控制台子进程"会继承父控制台、不新建窗口，
  于是抑不抑制都量不到差别（实测对照组也是 0）。
  换成 GUI 子系统的 notepad 当被试更不对：`CREATE_NO_WINDOW` 管的是
  **控制台**窗口，notepad 本来就没有控制台。
  **要真复现需要一个没有控制台的父进程，也就是 ieml.exe 自己。**
  所以这里给的是：静态审计（逐处）+ 标志确实写入了 + 请人工双击装一次 Forge 确认。

### 新增：真机 UI 检查（`tools/live-ui-check.mjs`）

用户这轮报的问题**全是界面层**的（"根本没这个功能键"、"显示有但点了不让选"），
单元测试证明不了"用户点得到那个按钮"。所以加了一个真机检查：

```
node tools/live-ui-check.mjs
```

它用 `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--remote-debugging-port=9333`
启动桌面版（WebView2 自带 CDP，不用装任何东西），从 `/json/list` 拿页面，
再用 `Runtime.evaluate` 在真实界面里跑断言：

* 界面真的渲染了（不是白屏、不是卡在「正在准备…」）
* 版本列表能点进一个版本
* **概览页上有「安装 Mod」这个功能键**
* 点下去**真的弹出** Mod 搜索框（不是"点了没反应"）
* **OptiFine 开关置灰**，且理由说清是"我们还没做"、不含"不兼容"

本机实测 **9/9 通过**（2026-09-14）。它还顺手确认了一件事：
概览页的 Java 一行显示 **「需要 Java 8 · 已就绪」** ——
也就是说 Java 探测真的找到了官方启动器下的 Java 8，
这正是本轮第一条修复要的效果。

> ★ 这个脚本自己也踩了两次"检查方法错了、却以为是代码错了"：
>   ① 在 exe 里搜前端源码的字面量来判断"前端有没有嵌进去" ——
>      Tauri 2 把资源**压缩**后内嵌，原文根本不在二进制里（见
>      `tools/check-frontend-embedded.mjs` 里的说明）；
>   ② 用中文字面量正则断言界面文案 —— 中文在管道里按 GBK 传，
>      正则变成不匹配任何东西的模式。改成写 **Unicode 码点**就稳了。
>   两次的教训是同一条：**测试报红时，先确认测试本身在测什么。**
>
> 这条教训在"控制台黑框"那次又出现了一遍（见上面那一节：
> 对照组量到 0，不是代码错，是实验环境根本复现不了）——
> 三次都是同一个毛病，所以单独记在这里：**量不到东西的测量，
> 它的"0"不是证据；要先把对照组成立起来。**

### 结论

`java -version` 级别的验证：1.12.2 在 **Java 8** 与 **Java 25** 上
（natives 正确解压后）都能进到渲染循环 —— 所以 1.12.2 崩溃的根因**不是**
Java 版本，而是 natives。两件事都修了。

---

## 0.1.0-dev.2 — 2026-09-13（回溯补记）

真实下载引擎（分片续传 / 换源 / 429 退避 / 校验自愈）、真实启动、
Fabric / Quilt / Forge / NeoForge 安装、Modrinth 搜索安装、
崩溃分析与脱敏、整合包清单解析、Mod 扫描与启停。

> 这一轮的 `package.json` 写的是 `0.2.0`，与阶段不符 —— 见 `VERSIONING.md` 第一节。

## 0.1.0-dev.1 — 2026-09-11（回溯补记）

第一份能跑的骨架：领域规则层 + React 界面 + 浏览器开发模式。
