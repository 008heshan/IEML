# IEML 版本号规则（SemVer + 阶段后缀）

> 这份文件是**唯一的版本号依据**。改版本号之前先读这里。
> 用户给的原始表格在本文最后一节，原样保留。

## 一、为什么要写这份文件

以前版本号是**手填**的，而且**三个地方各填一份**：

| 文件 | 字段 | 本轮之前的值 |
|---|---|---|
| `package.json` | `version` | `0.2.0` |
| `src-tauri/Cargo.toml` | `[package] version` | `0.2.0` |
| `src-tauri/tauri.conf.json` | `version` | `0.2.0` |
| 桌面快捷方式 | 文件名 | `IEML 启动器.exe`（名字里没有版本） |

三个 0.2.0 看起来是一致的，但它和"这个启动器实际上处于什么阶段"毫无关系 ——
`0.2.0` 按下面的规则读作"**第二个公开稳定版**"，而这个项目连第一次公开测试都没到。
版本号一旦开始说谎，后面所有"这个 bug 是哪个版本引入的"都无从谈起。

所以规则固定下来，并且**用一个脚本同步**（`tools/set-version.mjs`），
不允许再手填三份。

## 二、阶段表（用户给定，原样保留）

| 阶段 | 版本号示例 | 含义 | 谁用 |
|---|---|---|---|
| 内部开发 | `0.1.0-dev.1` | 代码没写完，随时会崩 | 只有你 |
| 小范围测试 | `0.1.0-alpha.1` | 核心功能能跑，但缺功能、有 bug | 熟人、内测群 |
| 公开测试 | `0.1.0-beta.1` | 功能基本完整，找 bug | 所有愿意试的人 |
| 发布候选 | `0.1.0-rc.1` | 只修 bug，不加功能 | 等正式版的人 |
| 正式发布 | `0.1.0` | 第一个公开稳定版 | 所有人 |
| 核心稳定 | `1.0.0` | API 稳定，插件系统就绪 | 生态开发者 |

## 三、怎么用（可执行的规则）

### 3.1 形状

```
<主>.<次>.<修订>[-<阶段>.<序号>]
```

* **有后缀** = 不是稳定版。`0.1.0-dev.3`、`0.1.0-beta.2` 都读作"不是给人用的正式版"。
* **没后缀** = 正式发布。`0.1.0` 是第一个公开稳定版，`1.0.0` 是 API 稳定版。
* 阶段顺序（低 → 高）：`dev` < `alpha` < `beta` < `rc` < 正式。

### 3.2 什么时候动哪一位

| 改动性质 | 动哪一位 | 例子 |
|---|---|---|
| 修 bug、改文案、补测试 | 阶段序号 +1 | `0.1.0-dev.2` → `0.1.0-dev.3` |
| 加功能（同一个目标版本内） | 阶段序号 +1，**并把序号重置**（如果刚跨阶段） | `0.1.0-alpha.4` → `0.1.0-beta.1` |
| 进下一个阶段 | 换阶段名，序号回 1 | `0.1.0-dev.9` → `0.1.0-alpha.1` |
| 正式发布 | 去掉后缀 | `0.1.0-rc.3` → `0.1.0` |
| 破坏性改动（存档/配置格式不兼容） | **主版本** +1 | `0.1.0` → `0.2.0` |
| 加功能但向后兼容 | **次版本** +1 | `0.1.0` → `0.2.0`（0.x 阶段次版本当主版本用） |

> **0.x 阶段的约定**：主版本为 0 时，任何东西都可能变，
> 所以"破坏性改动"走**次版本**（`0.1.0` → `0.2.0`），修订位只修 bug。
> 这与 SemVer 官方对 0.y.z 的规定一致。

### 3.3 阶段是怎么判定的（不要凭感觉）

| 阶段 | 进入条件（**全部**满足才能进） |
|---|---|
| `dev` | 默认状态。只要还有"已知会挡住用户的事"没解决，就在这里 |
| `alpha` | ① 能从零装一个版本并真跑起来；② 崩溃能给出原因；③ 没有"点了报成功但什么都没发生"的功能 |
| `beta` | ① 功能表里没有 ❌；② `pnpm verify` 全绿；③ README 的「已知会挡住用户的三件事」清空 |
| `rc` | 只修 bug 不再加功能；连续 N 个 rc 没出新的 P0 |
| 正式 | 用户能不看文档自己装好并玩上 |

**当前判定（2026-09-14 第十三轮，`0.1.0-beta.1`）**：

进阶到 `beta` 是**用户明确要求的决定**（原话：「版本正式进阶成Beta版」）。
按 §3.3 的三条判据**逐条对**，两条满足、一条**没有完全满足** —— 照实记在这里：

| 判据 | 结果 | 依据 |
|---|---|---|
| ① 功能表里没有 ❌ | ⚠️ **未完全满足** | README 的「功能状态」表仍有一行 ❌：「macOS / Linux 未实测」。**没有把它涂成 ✅** —— 涂掉就等于用版本号骗人 |
| ② `pnpm verify` 全绿 | ✅ | 15 项全绿（界面重构后重跑，见 CHANGELOG 第十三轮） |
| ③ 「已知会挡住用户的事」清空 | ⚠️ 部分满足 | Windows 上的阻断项已清空；留下的 ⚠️ 是「只测过 Windows」「Quilt 已实现未实测」「正版登录要自备微软 client_id」 |

`alpha` 的三条仍然成立（dev.5 之后没有退化）：

| 条件 | 现状 |
|---|---|
| ① 能从零装一个版本并真跑起来 | ✅ 原版 / Forge / Fabric / Quilt / OptiFine / LiteLoader 都跑通过 |
| ② 崩溃能给出原因 | ✅ `domain::crash` 分类 + 启动前自检（缺文件会**先自愈**，补不齐才报） |
| ③ 没有"点了报成功但什么都没发生"的功能 | ✅ OptiFine / LiteLoader / Fabric API 都真的装了（`ADDON_INSTALL_IMPLEMENTED` 有测试守着） |

**★ 写在最前面免得被误读**：`beta` 在这里的含义是「**功能基本完整，找 bug**」，
不是「三条判据全满足」。判据表留着是为了让下一步（`rc`）有可对照的基线 ——
要进 `rc`，上面那两个 ⚠️ 得先清掉，而且不能再加功能。

历史记录：`dev.1` ~ `dev.14` 期间一直停在 `dev`，因为每轮都还有"已知会挡住用户的事"没解决；
`dev.12` 之前这份文件写着"下一步应该进 alpha" —— 实际上 `alpha` 的三条在 dev.5 就满足了，
那句话在这一轮一并更正（**旧结论错了就改，不改就等于继续误导**）。

### 3.4 改了版本号要动哪些地方

**只跑一条命令**（它会把所有位置改成同一个值，并打印改了什么）：

```bash
node tools/set-version.mjs 0.1.0-dev.13
node tools/set-version.mjs --check    # 只校验一致性（pnpm verify 里跑）
node tools/set-version.mjs --docs     # 只校验文档口径（README / CHANGELOG）
```

它负责**改写**这些位置：

1. `package.json` → `version`
2. `src-tauri/Cargo.toml` → `[package] version`（**必须**，`app_info` 命令读的是它）
3. `src-tauri/tauri.conf.json` → `version`（Tauri 打包器读这个，决定安装包名字）
4. `src/domain/version-info.ts` → 前端的 `APP_VERSION`（关于页显示用）
5. `README.md` → 开头的「当前版本」与安装包文件名示例

> ★ 第 5 条是 2026-09-14（dev.12）补上的。在那之前 README 不在表里，
> 于是它漂移了**八个版本**都没人发现（代码是 dev.11，README 还写着 dev.3）。
> 教训：**说"当前版本"的地方就是版本号的落点**，漏一个就等于没有同步。

它**不写** `CHANGELOG.md`：那一节的正文是"这个版本改了什么"，只能人写。
但 `--check` / `--docs` 会要求 **CHANGELOG 最新一节的标题等于当前版本** ——
忘了加一节，`pnpm verify` 会红（这一项叫「文档版本口径一致」）。

跑完必须重新构建，否则 exe 里还是旧版本号。

### 3.5 为什么 `app_info` 读的是 Cargo 的版本

`src-tauri/src/commands.rs`：

```rust
version: env!("CARGO_PKG_VERSION").into(),
```

`env!` 是**编译期**展开的 —— 改了 `Cargo.toml` 不重新编译，界面上的版本号不会变。
所以"改了版本号"和"重新打包"必须成对出现（`pnpm desktop:build` 会做这件事）。

## 四、本项目的版本历史

| 版本 | 日期 | 说明 |
|---|---|---|
| `0.1.0-beta.26` | 2026-09-16 | 第三十八轮：**排队里的五条** —— ①启动按钮改渐变 + 内高光 + 外发光（原来平涂，真机读到 `linear-gradient(...)`）；②删掉加载器副标题（`.b-desc` 归零，信息仍在 `title` 里）；③「装到」后面直接写出**实例 + 目录**（"装到「X」的 resourcepacks 目录"），没实例时说清装不了；④`localStorage` 记住**实际生效的**下载目标；⑤外观新增**低性能损耗模式**：关卡片磨砂 / 标题磨砂 / 氛围光晕（真机 `blur(24px)`→`none`、光晕 `block`→`none`），首屏前挂类避免闪一帧。★ 自己踩的坑：`useEffect` 写在 `const target = useMemo` 之前读 `target` → tsc 报 TDZ；**`vite build` 不做类型检查，打包成功 ≠ 能跑**。**未做完**：资源中心的同款翻页（要连 `PAGE`/`hasMore`/`loadMore` 的 append 语义一起改） |
| `0.1.0-beta.25` | 2026-09-16 | 第三十七轮：**删掉游玩时间记录**（用户："记录时间的功能，删掉，这没有用"）—— 连着上一轮看：先前用户说"从未启动是骗人"，我把它**实现了**（game-exit 时写回 `lastPlayedAt`/`totalPlaySeconds`），现在用户明确不要。删掉五处显示（启动页身份卡"累计 xx/还没玩过"、状态行"上次/从未启动"、其它版本卡片的时间、版本列表右侧那一列、概览"最近游玩/累计时长"两格）+ **不再写入** + 删死代码（`relativeTime` 三处副本、`formatDuration`、`IconClock` import、`.ver-time`/`.lo-time` 两条死 CSS）。字段本身保留（删字段会让老 instances.json 解析报错）。★ 唯一保留的是"**已运行 xx 分钟**" —— 那是当下状态，不是历史记录 |
| `0.1.0-beta.24` | 2026-09-16 | 第三十六轮：**标题区磨砂羽化**（用户："这文字左侧的地方和左侧栏有割裂感，我建议做个羽化"）—— 磨砂是矩形，从侧栏边界一刀切到右边缘，两边各一条硬竖线。★ **不能直接给 `.page-head` 加 mask**（标题/按钮在里面，会一起被羽化掉）→ 把磨砂**单独做成 `::before` 伪元素**（`z-index:-1`），左右各 28px 渐隐 + 下缘 22% 渐隐，两张 mask 用 `mask-composite: intersect`。真机读样式确认 blur/mask/z 都挂在伪元素上。其余七条（同款翻页 / 没选版本也能下载 / 记住上次选的版本 / 低性能损耗模式 / 删加载器副标题 / 启动按钮美化）排队 |
| `0.1.0-beta.23` | 2026-09-16 | 第三十五轮：**先修两条真问题**（用户这批 9 条）—— ①"游戏关闭后启动器仍显示在运行"（**第二次报**）：上次查过三处（退出标记写了 / 事件带 instanceId / 桥接原样传 payload）每段都对但**没复现**，所以这轮加一条**不依赖事件**的兜底：只要界面认为有实例在跑，就**每 4 秒**问一次 `running_games`（后端有子进程句柄，是唯一知道真相的地方）并覆盖本地表 → 真关了 4 秒内自愈；只在有实例在跑时轮询；判据只有一份（`is_still_running`）。★ 这是兜底不是"修好了"。②**资源包 / 数据包必须能选原版**（我上一轮一刀切过滤原版，过度了）：按种类判 —— Mod ❌（纯原版不加载 mods/）、光影 ❌（要有 Iris/OptiFine 前置）、资源包/数据包 ✅（原版本来就能用）。其余七条（同款翻页 / 没选版本也能下载 / 记住上次选的版本 / 低性能损耗模式 / 删加载器副标题 / 启动按钮美化）排队下一轮 |
| `0.1.0-beta.22` | 2026-09-16 | 第三十四轮：**世代标题用世代号 · 不吸顶**（用户澄清上一轮两条，我上一轮都做偏了）—— ①`versionFamily()` 里只有"现代版本"那一支写 `label: v`（整串），于是分组标题取到组里第一行的 id，`26.2-rc-2`/`26.2-pre-6` 当了标题（其实和 `26.2` 同组）→ 改成 `label: 世代号`，与其它分支一致；②"叠起来"的根因是 `.ver-group` 的 `position: sticky; top: 0` —— **每个**标题都往 top:0 钉，滚动时互相压住，看着就是"没清除多余的"；我上一轮加的磨砂底是**遮症状**（用户也说那个底效果不好）→ 去掉 sticky，底随之删掉。★ 教训：**加东西遮住症状 ≠ 去掉造成症状的东西** |
| `0.1.0-beta.21` | 2026-09-16 | 第三十三轮：**六条 UI + 重做图标** —— ①整合包翻页重做成居中的细工具栏（原来紧贴卡片、字大又密）；②**删除白色模式**（用户要求）：浅色令牌整套删掉（tokens.css −69 行）、顶栏主题按钮删掉、设置页「主题」整行删掉（只有一个选项的选择器是假控件），`data-theme="light"` 匹配不到规则 → 自动落回深色；③下载页 Mod 的「装到」下拉**过滤掉原版实例**（纯原版不加载 mods/，装进去必然不生效）；④版本列表筛选改名「可装 Mod」「原版」；⑤页头**满宽**（磨砂盒子在 `.content` 里左右各空 24px → 负 margin 抵消内边距，文字位置不变）；⑥折叠时**直接列出组里的版本号**（最多 4 个 + 省略）。**图标重做**：自己画的等轴测立方体（1024，System.Drawing，三个面用主色系 + 深色圆角底 + 紫光），`tauri icon` 生成全套；★ 第一版太扁（像砖头），按真正的等轴测比例重画 —— 图标要按"最小尺寸能不能认出来"画 |
| `0.1.0-beta.20` | 2026-09-16 | 第三十二轮：**页头去黑底 · 吸顶标题行叠字** —— ①页头（用户："去掉黑底，做一个磨砂玻璃的底，没有割裂感还能作遮挡"）：上一轮填的 `--glass` 在玻璃卡片旁就是一条黑带 → 改成**完全不填色**只留 `backdrop-filter: blur(16px)`，分隔线也去掉（磨砂本身已是边界）；②**版本分组标题行叠字**（用户配图）：`.ver-group` 是 `position: sticky`、原本靠 `--bg-surface` 挡内容，而我上一轮把它改成按钮时写了 `background: none` 把底顶掉了 → 吸顶时透明、下面的行透上来 → 改成磨砂玻璃底（`--glass` + `blur(12px)`）。真机读样式确认：页头 `rgba(0,0,0,0)/blur(16px)/0px`，标题行 `sticky/rgba(15,18,23,0.45)/blur(12px)`。★ 教训：**粘性/吸顶元素的底不是装饰，是功能** |
| `0.1.0-beta.19` | 2026-09-16 | 第三十一轮：**用户自己做的玻璃拟态改版 + 新图标**（我读完补三处）—— 用户：跑 `tauri icon` 生成整套图标、`body::before` 换三团彩色光晕、`--bg-*` 全改半透明、`.card` 加 `backdrop-filter: blur(24px) saturate(140%)`、顶栏/侧栏/页头全透明、`.chip-btn` 玻璃药丸、logo 改描边。我补：①页头是 sticky，透明后滚动正文会从标题区透出来（这个坑踩过一次）→ 底用 `--glass` + `backdrop-filter: blur(14px)`；②三团光晕是写死颜色 → 抽成 `--ambient-violet/-green/-blue`，深色给足、浅色收到约 1/4（不然浅色主题重新长出彩色污渍）；③`bundle.icon` 只列了 `icon.png` → 补上 32/128/128@2x PNG + `.icns` + `.ico`（Windows 小尺寸不再糊）。★ 自己踩的坑：往 `tauri.conf.json` 写 `/* */` 注释 —— **JSON 不支持注释**，打包当场失败 |
| `0.1.0-beta.18` | 2026-09-15 | 第三十轮：**封面溢出 + "选了才知道不兼容"** —— ①整合包封面：`.pack-cover` 本来是一条 6px 色条，`<img>` 进去后没有尺寸约束就按原图铺开、糊满整张卡片 → 改成固定高度 + `overflow:hidden` + `object-fit:cover`，有真封面时（`:has(img)`）高度 96px；★ 这条**没能在真机验到**（探测几次都撞上骨架屏，今晚镜像慢）。②**"选了 Fabric 之后还能点"**（真 bug，与上一轮的死局同源）：`disabled` 只看 `verdict.removed`，而那是**遍历选中项**算的 → 没勾的永远点得动，勾上才知道不行 → 改成**提前问** `addonCompatibility`（同一判据，不管选没选）；同时短状态新增「不兼容」一档（以前所有灰掉的都显示「无」，与"上游确实没有"混在一起），红角标也对没勾上的显示。真机验证：`OptiFine disabled=true chip=与当前加载器不兼容 reason=不兼容` |
| `0.1.0-beta.17` | 2026-09-15 | 第二十九轮：**封面全加载不出来 = CSP 拦掉了**（用户："整合包/mod/光影/资源包封面全都加载不出来"）—— 根因是 `tauri.conf.json` 的 `img-src 'self' asset: http://asset.localhost data:` **没有 `https:`**，而封面全在 `https://cdn.modrinth.com/...` → 每个 img 被拦、`onError` 换成字母块。★ 上一轮我把这条**差点又报成"已修"**：验证写的是 `!!img`（元素在不在），可 CSP 拦图时 `<img>` 照样在 DOM 里，只是 `naturalWidth === 0` —— **元素存在 ≠ 图片加载出来了**。修法：`img-src` 加 `https:`；验证判据换成 `naturalWidth > 0` 并打印样本 src。真机实测 Mod 页签 `imgs:20 / loaded:6 / sample: https://cdn.modrinth.com/data/P7dR8mSH/icon.png`（其余是懒加载），修前是 0。顺带：`minWidth` 960→1040、侧栏文字一律不换行（用户要求） |
| `0.1.0-beta.16` | 2026-09-15 | 第二十八轮：**Quilt 的两条规则**（用户 8 条里的 2 条）—— ①`combination.ts` 里 Quilt 分支**直接判不兼容**（Quilt 没有对应 OptiFabric 的桥接包：Fabric 是"有但要自己下"，Quilt 是**根本没有**），选 Quilt 时 OptiFine 被禁用；②`api_for_base` 不再给 Quilt 返回 API 包（原来自动装 QFAPI），测试改为 `quilt_gets_no_api_library`，规则仍只有一处。**明确不做**：资源中心"已装"标记（用户要求）。**确认可用**：同时开两个游戏（beta.6 多开）。**取证但未修**：Quilt 打不开（退出标记 exit_code=1 / played_seconds=1 / crashed=true → 真退）、崩溃原因误导（纯 Quilt 启动却报"老版本 Forge 或 OptiFine"，判据被日志字样带偏，是真 bug 但改判据要先看那次日志）、"崩溃后仍显示运行"（后端三处证据都正常，未复现） |
| `0.1.0-beta.15` | 2026-09-15 | 第二十七轮：**不兼容的附加组件会把人卡死**（用户配图："这怎么警告不让装了之后又不让点"）—— 状态是 `Fabric 1.21.11` + 已选中的 OptiFine：红条说组合不可用、安装按钮被拦，而 OptiFine **是灰的、点不掉** → 界面上没有出路。两条规矩打架：上一轮"不兼容不让选"（禁用）撞上更早一轮"不兼容要提示、别悄悄取消"。三者一起才对：①不让选（保持）；②**已选中的永远能取消勾选**（`disabled = disabled && !selected`）；③**换加载器/版本导致不兼容时去掉它并弹提示说原因**（旧注释禁的是"悄悄移除"，移除+告知不违反，且这是那一屏唯一的出路）。第③条只在选择本身变了时触发，不跟 `verdict` 跑 |
| `0.1.0-beta.14` | 2026-09-15 | 第二十六轮：**版本列表按世代折叠**（用户建议："做一个版本折叠功能"）—— 清单 900+ 个版本铺开是一堵墙。规则三条：①世代标题行可点（`<button>` + `aria-expanded`）；②**默认只展开含当前选中版本的那一组**，其余收起；③**搜索时全部展开**（搜了看不到结果最气人）。实现：折叠状态只记用户手动点过的组（`foldOverride`），没点过的按规则算；收起时那些行**不渲染**（不是 CSS 藏）。标题行加旋转小箭头（reduced-motion 下不动画）。★ 同时回归验证了上一轮的 key 修复：六步重跑，每档分组都对 |
| `0.1.0-beta.13` | 2026-09-15 | 第二十五轮：**筛选"卡住"的真凶 —— React 的 key 撞了**（用户给了极准的复现：快照→正式版不显示正式版；全部→之后选谁都显示全部）。真机数据：**高亮一直在动、列表冻住** → 不是筛选逻辑（`channel` 只有一个状态、`filtered` 依赖也对），是渲染层。真凶：分组 key 用了 `g.fam.key`（**世代**键），而 **`26.2-rc-2` 与 `26.2` 的世代键相同** → 换筛选前后分组 key 一一相同，React 复用节点、上一次的分组没被移除。修法：列表容器 `key={channel\|query}` 整体重建 + 分组 key 带上渠道与首行 id。六步全跑验证通过。★ 上一轮（beta.11）我"以为修好了"是因为**只验了初始状态**（点正式版时它本来就是正式版 = 没切换）—— 教训：验证能切换的组件必须真的切一遍 |
| `0.1.0-beta.12` | 2026-09-15 | 第二十四轮：**首次启动把游戏语言设成中文**（用户要求）—— 原版永远默认英文（不跟随系统语言），PCL 也是启动器替玩家写好这一项。新文件 `src-tauri/src/game/locale.rs` + 启动路径调用。两条硬规矩：①**只在玩家还没选过语言时才写**（`options.txt` 里已有 `lang:` → 逐字节不动，单有一条测试钉住）；②**语言代码跟着版本走**（1.11 起 `zh_cn`，之前 `zh_CN` —— 写错会静默回落英文；本机 1.7.10 与 1.12.2 正好跨这条线）。首次只写 `lang:` 一行（原版会自己补全，不伪造整份）；缺 lang 行时只追加一行（含末尾无换行的处理）；写失败不拦启动但会进日志。**6 条测试全过** |
| `0.1.0-beta.11` | 2026-09-15 | 第二十三轮：**正式版里混进 rc/pre**（用户第二次报，说"学学 PCL 怎么做的"）—— 上一轮改的 `isSnapshotVersion` 正则没用；这一轮把链路逐段读穿：上游缓存 `26.2-rc-2 → type=snapshot` ✅、Rust 原样透传 ✅（用 CDP 直接调命令读原始行）、前端桥接无二次映射 ✅、筛选逻辑看着对 ✅，**可真机上就是漏** —— 那一处**没找到确切原因**。于是不再猜，把判据改成"不可能被上游字段打败"（PCL 也是同时看清单 type 和版本号本身）：`finalRelease = release_type === 'release' && /^\d+\.\d+(\.\d+)?$/.test(id)`，两个都成立才放行。真机验证：点「正式版」后分组为 `26.2(1)`、`26.1.2(3)`、`1.21(12)`…**没有任何 rc/pre**。如实记：这条**不算"查清了"，只算"修好了"** |
| `0.1.0-beta.10` | 2026-09-15 | 第二十二轮：**「跳 1 帧」的真实根因**（用户发来那一帧截图，一眼看清）—— 版本列表里带加载器的行会闪一下「缺 Fabric」「缺 Forge」。根因：`refreshDisk()` 是异步的，**首帧 `diskLoaders` 还是 `{}`**，而 `diskConflictOf` 把"**还没读到**"当成"**盘上没有**"（`if (!base) return 缺 X`）→ 每行先被判成缺加载器，清单回来再翻回去。★ 又一条"读不到 ≠ 没有"，这次是**异步加载中的空值被当成了事实**。修法：加显式 `diskLoaded` —— 读到过才判冲突，没读到/读失败都不下结论，演示模式直接标记成"读过"。真机两时刻各拍一次：**首帧角标 0 个**、读完后角标 0 个且显示真实信息「盘上 Fabric 0.19.5 Forge 65.1.3」（同时证明磁盘读取是成功的、截图里那些「缺 X」确实是首帧的假话） |
| `0.1.0-beta.9` | 2026-09-15 | 第二十一轮：**Mod 列表变快 · 添加入口改路 · 原版不做 Mod** —— ① `reloadMods` 从"一次联网反查、完成前列表是空的"改成**两段式**：本地扫描（毫秒级，显示文件名）先出列表，联网反查在后台补名称/版本，期间显示「正在核对在线库…」（不造假：第一段就是文件名）；②「添加 Mod」不再弹 Modal，改为**跳下载页的 Mod 页签并默认选中当前版本**（事件 `ieml:download-target`）—— 以前同一个"浏览 Mod"有两个界面，而下载页默认挑"最近玩过的那个"很可能是别的版本；③**原版实例不再显示「Mod 管理」**（纯原版不加载 mods/，入口只会让 jar 无声失效）；④ `.page-head` 加 `margin-bottom` —— 概览页顶部内容离分隔线太近 |
| `0.1.0-beta.8` | 2026-09-15 | 第二十轮：**删掉 OptiFine 的安装提示**（用户："关于安装高清修复的提示，也不需要"）—— ①「安装前请确认」里那句"将用 OptiFine 自带的 Patcher 在原版 jar 上打补丁（会在临时目录里跑，不会动你的原版）"（来源 `combination.ts` 的 `judged.note`）删掉：那是实现细节，不是用户要确认的事；②组合区下方「OptiFine 会在装完原版后自动安装」那条 info 删掉；③随之失效的两个变量（`removedOf` / `needManualBridge`）与它们的注释一起删，不留死代码。**没删**桥接包那条（Fabric 1.14~1.20.4 缺 OptiFabric 会崩）—— 那是功能性前提，且 `judged.note` 仍带着下载地址出现在「安装前请确认」里，没丢可操作信息 |
| `0.1.0-beta.7` | 2026-09-15 | 第十九轮：**按用户第 8 批反馈改（8 条）** —— ①文字默认不可选（`user-select:none` + 输入框/日志/报告放行）；②整合包页三处补齐：**卡片骨架**（原来是四条通栏长条）、**真封面**（Modrinth 的 `icon_url` 一直有、这一页没接）、**翻页**（原来写死 `limit:20` 无 offset）→ 真机 20/20 有封面、「第 1 / 917 页 · 共 18337 个」；③账号弹窗去掉重复的关闭键；④两条清理（`clean_unused` + `clean_caches`）**合并成一次 dry run → 一次确认 → 一次执行**，「存储」卡移到「外观」右侧，「窗口尺寸」搬回「新版本的默认值」；⑤版本行「更多操作」菜单从 `absolute` 改 **`fixed`**（原来被 `overflow:hidden` 的列表容器**裁掉**，看着像层级问题）+ 点空白/滚动/Esc 都会关；⑥拦掉浏览器右键菜单（输入框与已选文字放行）并拦 F5/Ctrl+R。**版本筛选**：`isSnapshotVersion` 的判据原来是"只认周更快照"的正则（`-rc`/`-pre` 被判成正式版）→ 改成问反面（正式版只有 `x.y`/`x.y.z`），判据自测 10/10；**但用户看到的现象我没能复现**（上游 `type` 与前端筛选逻辑都查过是对的），如实记在 CHANGELOG。**未做**：加载器版本"跳 1 帧"没查 |
| `0.1.0-beta.6` | 2026-09-15 | 第十八轮：**多开实例** —— "同时只能开一个"原来写死在**三个层面**（后端 `Mutex<Option<RunningGame>>` 单槽 / 前端 `running` 单值 / 各处自己写 `running?.instanceId === x`），现在统一成**按实例 id 索引的表** + 判据只有一处（`isInstanceRunning` / `runningInfo` / `runningCount` / `runningInstances`）；停止命令 `stop_minecraft(instance_id)` 必须**点名**（不点名 = 随机停一个）；新增 `running_games` 命令 + boot 时对表（界面重新加载后不再谎报"没有游戏在运行"、不会让同一存档被两个进程写）；侧栏底部列出**每一个**在跑的实例、各自能停；`AppContext` 监听器改用 `stateRef`（依赖 `[]`）。验证：8 条状态模型测试 + 3 条**真进程** Rust 测试 + 桌面版 CDP 真机断言。**未验**：没有真的同时启动两个 Minecraft |
| `0.1.0-beta.5` | 2026-09-15 | 第十七轮：**按用户 7 张截图逐条改（20 条）** —— 修 4 个真 bug：①「最大化」点了没反应（capabilities 缺 `allow-toggle-maximize`，而我把 Promise `void` 掉了 = **静默失败**，现在失败会弹 toast）；②顶栏切主题时设置页不同步（`useState` 只取一次初值）；③「从未启动 / 累计时长 –」是骗人（`lastPlayedAt/totalPlaySeconds` 在 Rust 侧只有类型定义、真机从没写过 → 在 `game-exit` 收口处落账）；④滚动条两端的小三角（**两层原因**：`::-webkit-scrollbar-button` 没关 + `*{scrollbar-width/color}` 让 Chromium 退回标准渲染；第一次只改对一半，靠重新截图才发现）。改 14 处 UI：资源中心加载骨架改成**卡片形状**、下载页去掉上下重复的页签与「管理这个版本的资源」、版本栏放大、设置页外观**劈成两栏**（窗口尺寸从「新版本默认值」搬进来）、删设置页账号栏与关于里的字体声明、版本设置页行距放宽、Mod 管理删掉那句声明、**「与当前加载器不兼容」直接不让选**（理由照常显示）、三个点菜单补齐图标、删登录面板里的应用 ID 声明块、左栏 218→196px。**未完成**：允许多开实例（动到启动状态模型，单独一轮做）与网络优化（量过了：决定速度的是走没走镜像，没有可确认的改动项） |
| `0.1.0-beta.4` | 2026-09-15 | 第十六轮：**深色主题重做 + 自绘标题栏**（用户自己改的界面 + 我修 5 处 + 补标题栏）—— 用户把深色主题换成「实色边框 + 明度分层 + 组件令牌」那一套，并新增 `CustomSelect` 替换原生 `<select>`；我修掉类型检查红（无用 import）、`DownloadPage.tsx` 的 BOM、**下拉菜单比按钮宽一倍**（量到 880 vs 420）、换掉原生 select 时丢掉的无障碍（aria-label / Esc / 方向键）、**浅色主题下氛围渐变变成一块蓝污渍**；`decorations:false` + `data-tauri-drag-region` + `WindowControls` 把 Windows 那条白色原生标题栏并进软件（真机验证：顶栏 y=0、三个按钮可见可点、点最小化 `IsIconic` 真的变 True、`WS_THICKFRAME` 仍在所以还能拖大小） |
| `0.1.0-beta.3` | 2026-09-15 | 第十五轮：**瘦身 · 数据布局 · 账号提顶栏**（ADR-056）—— 删掉 47.4 GB 构建缓存（`target/debug`，仓库 50.77 → 3.36 GB）并改 dev/test profile 免得长回来；**去掉随程序附带的字体，改用系统字体栈**（前端 8.67 MB → 442 kB，exe 13.17 → 8.03 MB）；游戏数据 `shared/` → **`.minecraft/`**（同卷 rename + 迁移测试；真机验证抓到"跨根补齐排在布局迁移前面 → 把 C 盘旧数据复制成假的新布局"这个 bug 并修好）；新增 `clean_caches`（安装器 / 元数据 / 旧日志，**不碰游戏文件**）；正版登录提到**顶栏**（`AccountPanel` 一份实现）+ 拦截占位符 client_id（本机那个 `1111…` 假 GUID 会把可用 id 挤掉） |
| `0.1.0-beta.2` | 2026-09-14 | 第十四轮：**资源中心重做**（ADR-055）—— 列出 / 翻页 / 封面 / **玩家自选资源版本**（先用 `tools/probe/probe-browse.mjs` 实测"空关键词真的返回内容"，五类全过、翻页零重复）；下载页六格一字排开（Mod / 资源包 / 光影 / 数据包）；修掉"大对勾"（图标没有默认尺寸）与"下载栏被 toast 遮住"（层级收成令牌）；**随程序附带 HarmonyOS Sans SC Medium** 并在「关于」声明；版本命名规范 `{mc}-{loader}-{version}`（显示名与目录名同一套）；MC 版本自绘世代方块图标 + 分组；启动页与侧栏补真数据；版本设置页加只读摘要条并按改动频率重排；删掉多余提示与 CurseForge Key 输入框。**用户指定的微软 client_id 实测不可用（AADSTS700016）**，改用可用的公开 id 并留下证据 |
| `0.1.0-beta.1` | 2026-09-14 | 第十三轮：**界面重构 + 进阶公开测试**（ADR-054）—— 动作条把实用功能抬到显眼处（概览 / Mod 管理 / 设置→Java / 下载页），一行 Chip 取代 Note 块，设置页标签列 156→200px，列表行统一成一套原语，Java 路径只显示尾段；删 5 条死 CSS；顺手修掉 README 里两句 dev.12 就已被证伪却一直留着的话。**进阶是用户的决定**，判据①（功能表无 ❌）**未完全满足**，如实记在 §3.3 |
| `0.1.0-dev.14` | 2026-09-14 | 第十二轮：**清仓与规范化**（ADR-053）—— 删掉 40 个根日志 + `tmp/` 91 MB + 误落进仓库的游戏 `logs/` + 9 个死脚本；`tools/` 按用途分 `gates/env/live/probe/diag` 并加索引；`xxx2` 式名字全部改掉；删掉 4 个真死函数、两个"自认死代码"改成 `#[cfg(test)]`；`cargo.ps1` 成为唯一入口（vcvars 不可用时自动回退并**说出来**）；顺手挖出 PS 5.1 的 `.ps1` BOM 坑 |
| `0.1.0-dev.13` | 2026-09-14 | 第十一轮：**接入 CurseForge**（ADR-052）—— 资源中心多一个来源，四个种类都能搜/装/查更新；key 内置一把、设置页可覆盖且**如实说明它从哪来**；指纹算法用**接口自己当裁判**验证过（12 条跨语言向量）；查更新用 SHA1 交叉验证兜住"装错版本"；CF 下载按 URL 推导多候选（官方 CDN 在本机时好时坏） |
| `0.1.0-dev.12` | 2026-09-14 | 第十轮：**七组 P0 收敛**（`library_directory` 不再反推、`.mrpack` 路径过闸门、暂停两批都生效且如实上报、206 偏移不符重发整份请求、长度校验移到读完响应体之后、崩溃判据只剩一份且排除离线必然的 401、Java 规则两侧收敛 + 跨语言判据表 + 结构化 `required_major`）；清掉文档里的假承诺与过期数字，并把"文档版本口径"变成会红的检查（ADR-051） |
| `0.1.0-dev.11` | 2026-09-14 | 第九轮：**更正 dev.10 的误判** —— OptiFabric 一直在 CurseForge（项目 322385，75 文件 / 1005 万下载），1.14~1.20.4 **有**桥接包（要玩家自己下），1.20.5+ 才是真不兼容；桥接区间收敛成 `bridge-range` 一份实现 + MC百科 25 个版本逐条钉住；顺手修好"开关点得动、勾上却说不兼容"（在线清单与静态表两个判据打架） |
| `0.1.0-dev.10` | 2026-09-14 | 第八轮：Fabric + 高清修复不兼容时如实提示玩家（不再"静默去掉勾选然后照装"，安装按钮被拦住并给出两条可行替代）— ⚠️ **其中的版本区间判错了，见 dev.11** |
| `0.1.0-dev.9` | 2026-09-14 | 第七轮：**社区资源子系统**（Mod / 资源包 / 光影 / 数据包 共用一套抽象，照 PCL `Resource\*`）、`exe 内嵌前端一致性` 第五次"检查方法错了"修好（大文件按内容定位而非字节流比对） |
| `0.1.0-dev.8` | 2026-09-14 | 第六轮：**校验判据收敛成可组合规则**（照 PCL `ModValidate` 的三态语义，前后端各一份且逐条对齐）、**真正的暂停**（不再等于取消，照 PCL `LoaderTask` 的语义） |
| `0.1.0-dev.7` | 2026-09-14 | 第五轮：`26.2` 显示「需要 Java 8」修到 25（判据三份副本 → 一份 + 读版本 JSON 声明）、正版令牌过期不再静默退回离线（照 PCL 的缓存层）、下载文件数分母改成计划数、Fabric API 默认走推荐版（新版优先，**不是**下载量最高）、下载页去掉「已装」提示、`docs/MS-LOGIN-PCL-STUDY.md` |
| `0.1.0-dev.6` | 2026-09-14 | 第四轮：**「盘上有」与「有版本在用」分开说**（删掉实例后下载页还显示"已装"）、`net.minecraftforge:forge` 前缀匹配把 `forgespi` 的版本当成 Forge 版本 |
| `0.1.0-dev.5` | 2026-09-14 | 第三轮：启动前自愈（缺文件自动补）、Forge 本地生成 client jar 的误报、LiteLoader 挂载点、**正版登录改成自动轮询**且请求形状对着真实端点验过、7 个写死旧数据目录的测试修好（它们此前只会假绿）、版本号真的显示出来 |
| `0.1.0-dev.4` | 2026-09-14 | 第二轮：`merge_versions` 丢库修复（所有带加载器的版本都起不来）、Forge 26.2 的 Java 25 要求、控制台窗口全量静默、正版登录诊断链、下载页 Mod 列表、进度计数 |
| `0.1.0-dev.3` | 2026-09-14 | **本轮**：Java 探测重写（扫 8 类来源）、禁止 Java 跨大版本兜底、1.12.2 natives 修复、OptiFine 假承诺纠正、Mod 安装入口、数据目录避开系统盘 |
| `0.1.0-dev.2` | 2026-09-13 | 真实下载引擎、真实启动、加载器安装、Modrinth、整合包解析 |
| `0.1.0-dev.1` | 2026-09-11 | 第一份能跑的骨架：领域规则 + 浏览器开发模式 |
 —— "同时只能开一个"原来写死在**三个层面**（后端 `Mutex<Option<RunningGame>>` 单槽 / 前端 `running` 单值 / 各处自己写 `running?.instanceId === x`），现在统一成**按实例 id 索引的表** + 判据只有一处（`isInstanceRunning` / `runningInfo` / `runningCount` / `runningInstances`）；停止命令 `stop_minecraft(instance_id)` 必须**点名**（不点名 = 随机停一个）；新增 `running_games` 命令 + boot 时对表（界面重新加载后不再谎报"没有游戏在运行"、不会让同一存档被两个进程写）；侧栏底部列出**每一个**在跑的实例、各自能停；`AppContext` 监听器改用 `stateRef`（依赖 `[]`，不再每次开关游戏重注册一遍）。验证：8 条状态模型测试 + 3 条**真进程** Rust 测试 + 桌面版 CDP 真机断言（两行各带停止按钮、停一个只影响那一个）。**未验**：没有真的同时启动两个 Minecraft（两份内存 + 两个游戏窗口） |

> 规则：**每完成一轮对话就加一行**，不要事后补。`dev.1` / `dev.2` 是回溯补记的
> （当时的 `package.json` 写的是 `0.2.0`，与阶段不符，见第一节）。
> ★ `dev.12` 这一行与 `CHANGELOG.md` 最新一节的标题由
> `node tools/set-version.mjs --docs` 检查（`pnpm verify` 里会跑）。
